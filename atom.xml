<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>YitaiCloud</title>
  
  <subtitle>以太云</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://tisonkong.github.io/github.io/"/>
  <updated>2019-01-27T00:12:31.354Z</updated>
  <id>https://tisonkong.github.io/github.io/</id>
  
  <author>
    <name>Tison</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Redis性能优化和高级用法</title>
    <link href="https://tisonkong.github.io/github.io/2019/01/27/Redis%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E5%92%8C%E9%AB%98%E7%BA%A7%E7%94%A8%E6%B3%95/"/>
    <id>https://tisonkong.github.io/github.io/2019/01/27/Redis性能优化和高级用法/</id>
    <published>2019-01-27T00:10:39.884Z</published>
    <updated>2019-01-27T00:12:31.354Z</updated>
    
    <content type="html"><![CDATA[<p>同关系型数据库一样，Redis也有自己的性能问题需要优化，在生产上可能会遭遇慢查询，弱事务，原子性等场景，这里总结几类Redis的常见问题并给出对应的解决方案。<br><a id="more"></a></p><h3 id="一、慢查询优化"><a href="#一、慢查询优化" class="headerlink" title="一、慢查询优化"></a>一、慢查询优化</h3><hr><p>从客户端发出的一个请求可以分为下图中4个步骤（<code>发送命令－〉命令排队－〉命令执行－〉返回结果</code>），由于Redis是单线程模型，那么很有可能一两个较慢的查询阻塞了多个业务的查询效率，这个时候需要对耗时较长的慢查询进行定位分析，首先需要设定Redis的日志，有两个关键参数可以记录第三个步骤执行命令的耗时记录。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-aff84bd7a51a4625.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Redis查询"></p><p>关于 Redis 慢查询的配置有两个，分别是 <code>slowlog-log-slower-than</code> 和 <code>slowlog-max-len</code>。 </p><ul><li>1.<code>slowlog-log-slower-than</code>，用来控制慢查询的阈值，所有执行时间超过该值的命令都会被记录下来。该值的单位为微秒，默认值为 <code>10000</code>(10毫秒)，如果设置为 <code>0</code>，那么所有的记录都会被记录下来，如果设置为小于 <code>0</code> 的值，那么对于任何命令都不会记录，即关闭了慢查询。可以通过在配置文件中设置，或者用 <code>config set</code> 命令来设置：<figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">config set slowlog-log-slower-than 10000</span><br></pre></td></tr></table></figure></li></ul><p>使用config set完后，若想将配置持久化保存到redis.conf，要执行<code>config rewrite</code></p><ul><li>2.<code>slowlog-max-len</code>，用来设置存储慢查询记录列表的大小，默认值为 <code>128</code>，当该列表满了时，如果有新的记录进来，那么 Redis 会把队最旧的记录清理掉，然后存储新的记录。在生产环境我们可以适当调大，比如调成 <code>1000</code>，这样就可以缓冲更多的记录。</li></ul><p>慢查询配置好，那么在定位问题的时候可以采用<code>slowlog get</code>获取信息。运行Demo如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1:6379&gt; slowlog get</span><br><span class="line"> 1) 1) (integer) 456</span><br><span class="line">    2) (integer) 1531632044</span><br><span class="line">    3) (integer) 3</span><br><span class="line">    4) 1) &quot;get&quot;</span><br><span class="line">       2) &quot;m&quot;</span><br><span class="line">    5) &quot;127.0.0.1:50106&quot;</span><br><span class="line">    6) &quot;&quot;</span><br><span class="line"> 2) 1) (integer) 455</span><br><span class="line">    2) (integer) 1531632037</span><br><span class="line">    3) (integer) 14</span><br><span class="line">    4) 1) &quot;keys&quot;</span><br><span class="line">       2) &quot;*&quot;</span><br><span class="line">    5) &quot;127.0.0.1:50106&quot;</span><br><span class="line">    6) &quot;</span><br></pre></td></tr></table></figure></p><p>分别对应着：<br>1) 慢查询记录 id<br>2) 发起命令的时间戳<br>3) 命令耗时，单位为微秒<br>4) 该条记录的命令及参数<br>5) 客户端网络套接字（ip: port）</p><p><strong>单个慢查询的操作定位出来以后，一般可以按照拆解的思路，化大为小，尽量减少对其他业务请求的影响。</strong></p><blockquote><p>source：<a href="https://blog.csdn.net/qianghaohao/article/details/81052461" target="_blank" rel="noopener">Redis 慢查询分析</a></p></blockquote><h3 id="二、Pipeline（管道）"><a href="#二、Pipeline（管道）" class="headerlink" title="二、Pipeline（管道）"></a>二、Pipeline（管道）</h3><hr><p>上面已提到过Redis执行命令分为了4个步骤：<code>发送命令－〉命令排队－〉命令执行－〉返回结果</code>，这个过程称为<code>Round trip time</code>(简称RTT, 往返时间)，<code>mget mset</code>有效节约了RTT，但大部分命令（如hgetall，并没有mhgetall）不支持批量操作，需要消耗N次RTT 。有时我们会在短时间内发送大量互不依赖的命令(例如：后执行的命令不需要使用前面返回的结果)这个时候需要Pipeline来解决这个问题。</p><p>使用Pipeline在对Redis批量读写的时候，性能上有非常大的提升。因为管道可以在一次tcp的请求中同时发送多条命令，并且将响应结果一次性的返回给客户端。对于既定数量的命令请求，Redis管道通过减少客户端和服务器端的通信次数，来达到减少通信传输中往返时间的目的，提高效率。</p><p>Pipeline示例代码，用法比较简单：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line">Pipeline pipelined = jedis.pipelined();</span><br><span class="line">Pipeline p = jedis.pipelined();</span><br><span class="line">/* 插入多条数据 */</span><br><span class="line">for(Integer i = 0; i &lt; 100000; i++) &#123;</span><br><span class="line">      p.set(i.toString(), i.toString());</span><br><span class="line">&#125;</span><br><span class="line">             </span><br><span class="line">Response&lt;String&gt; response = p.get(&quot;999&quot;);</span><br><span class="line">/**</span><br><span class="line"> * 执行报异常：redis.clients.jedis.exceptions.JedisDataException: </span><br><span class="line"> * Please close pipeline or multi block before calling this method.</span><br><span class="line"> * System.out.println(response.get()); </span><br><span class="line">**/</span><br><span class="line">p.sync();</span><br></pre></td></tr></table></figure></p><ul><li>和Redis的事务类似，管道能完成的操作也能够被更加灵活的Redis脚本实现，但是脚本的可读性不强、可维护性差，<strong>如果批量处理的命令之间不存在依赖关系时，优先使用管道</strong>；反之，则只能使用脚本了。</li><li>用Pipeline方式，Redis在处理完所有命令前先缓存起所有命令的处理结果，打包的命令越多，缓存消耗内存也越多，所以并不是打包的命令越多越好，<strong>正确的做法是可以将大量命令的拆分多个小的Pipeline命令完成</strong>。</li></ul><p><strong><em>注意：由于pipeline的原理是收集需执行的命令，到最后才一次性执行。所以无法在中途立即查得数据的结果（需待pipelining完毕后才能查得结果），这样会使得无法立即查得数据进行条件判断。</em></strong></p><blockquote><p>source：<a href="https://www.cnblogs.com/xiaoxiongcanguan/p/9954254.html" target="_blank" rel="noopener">redis管道</a></p></blockquote><h3 id="三、Redis事务和Lua脚本"><a href="#三、Redis事务和Lua脚本" class="headerlink" title="三、Redis事务和Lua脚本"></a>三、Redis事务和Lua脚本</h3><hr><p>Redis 事务可以一次执行多个命令，它的用法也比较简答，将一组需要一起执行的命令放到<code>multi</code>和<code>exec</code>两个命令之间，其中<code>multi</code>代表事务开始，<code>exec</code>代表事务结束。但是<strong>Redis的事务并不保证原子性</strong>，或者说是一种弱事务机制，Redis的事务可以理解为一个打包的批量执行脚本，但批量指令并非原子化的操作，中间某条指令的失败不会导致前面已做指令的回滚，也不会造成后续的指令不做。如果需要实现原子操作，<strong>Redis可以结合Lua脚本来保证原子性</strong>。</p><p>本人所在项目的红包模块在Redis上有很不错的应用，利用Redis的高性能缓存实现高并发秒抢，利用Lua实现原子操作，这部分的思路，可以在<a href="https://www.jianshu.com/p/b58ed2fe6976?utm_campaign=maleskine&amp;utm_content=note&amp;utm_medium=seo_notes&amp;utm_source=recommendation" target="_blank" rel="noopener">【利用Redis和Lua的原子性实现抢红包功能】</a>有进行参考，而具体的源码实现可以参照 <a href="https://github.com/ZhuBaker/redis-lua" target="_blank" rel="noopener">【Redis+Lua的红包源码】</a>来实现。</p><p>把其中的lua脚本和注释贴出来FYI：<br><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line">/**</span><br><span class="line">*      <span class="comment">-- 函数：尝试获得红包，如果成功，则返回json字符串，如果不成功，则返回空</span></span><br><span class="line">*      <span class="comment">-- 参数：红包队列名， 已消费的队列名，去重的Map名，用户ID</span></span><br><span class="line">*      <span class="comment">-- 返回值：nil 或者 json字符串，包含用户ID：userId，红包ID：id，红包金额：money</span></span><br><span class="line">*</span><br><span class="line">*      <span class="comment">-- 如果用户已抢过红包，则返回nil</span></span><br><span class="line">*      <span class="keyword">if</span> redis.call(<span class="string">'hexists'</span>, KEYS[<span class="number">3</span>], KEYS[<span class="number">4</span>]) ~= <span class="number">0</span> <span class="keyword">then</span></span><br><span class="line">*        <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">*      <span class="keyword">else</span></span><br><span class="line">*        <span class="comment">-- 先取出一个小红包</span></span><br><span class="line">*        <span class="keyword">local</span> hongBao = redis.call(<span class="string">'rpop'</span>, KEYS[<span class="number">1</span>]);</span><br><span class="line">*        <span class="keyword">if</span> hongBao <span class="keyword">then</span></span><br><span class="line">*          <span class="keyword">local</span> x = cjson.decode(hongBao);</span><br><span class="line">*          <span class="comment">-- 加入用户ID信息</span></span><br><span class="line">*          x[<span class="string">'userId'</span>] = KEYS[<span class="number">4</span>];</span><br><span class="line">*          <span class="keyword">local</span> re = cjson.encode(x);</span><br><span class="line">*          <span class="comment">-- 把用户ID放到去重的set里</span></span><br><span class="line">*          redis.call(<span class="string">'hset'</span>, KEYS[<span class="number">3</span>], KEYS[<span class="number">4</span>], KEYS[<span class="number">4</span>]);</span><br><span class="line">*          <span class="comment">-- 把红包放到已消费队列里</span></span><br><span class="line">*          redis.call(<span class="string">'lpush'</span>, KEYS[<span class="number">2</span>], re);</span><br><span class="line">*          <span class="keyword">return</span> re;</span><br><span class="line">*        <span class="keyword">end</span></span><br><span class="line">*      <span class="keyword">end</span></span><br><span class="line">*      <span class="keyword">return</span> <span class="literal">nil</span></span><br><span class="line">*/</span><br></pre></td></tr></table></figure></p><p>而在Java里加载脚本的方法也比较easy：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"> <span class="comment">//线程模拟抢红包</span></span><br><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"BEGIN "</span> + <span class="keyword">new</span> SimpleDateFormat(<span class="string">"HH:mm:ss"</span>).format(<span class="keyword">new</span> Date()));</span><br><span class="line">    Jedis jedis = RedisJava.getResource();</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i = beginNum ; i &lt;= endNum; i++)&#123;</span><br><span class="line">        <span class="keyword">try</span>&#123;</span><br><span class="line">            List&lt;String&gt; userInfo = <span class="keyword">new</span> ArrayList&lt;&gt;(<span class="number">3</span>);</span><br><span class="line">            userInfo.add(<span class="string">""</span>+i);</span><br><span class="line">            userInfo.add(<span class="string">"name"</span> + i);</span><br><span class="line">            userInfo.add(<span class="string">"http://xxx"</span> + i);</span><br><span class="line">            <span class="comment">// Lua脚本执行</span></span><br><span class="line">            Object eval = jedis.evalsha(hashsha, <span class="number">3</span>, userInfo.toArray(<span class="keyword">new</span> String[]&#123;&#125;));</span><br><span class="line">            System.out.println(eval);</span><br><span class="line">        &#125;<span class="keyword">catch</span> (Exception e)&#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    jedis.close();</span><br><span class="line">    System.out.println(<span class="string">"END "</span> + <span class="keyword">new</span> SimpleDateFormat(<span class="string">"HH:mm:ss"</span>).format(<span class="keyword">new</span> Date()));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;同关系型数据库一样，Redis也有自己的性能问题需要优化，在生产上可能会遭遇慢查询，弱事务，原子性等场景，这里总结几类Redis的常见问题并给出对应的解决方案。&lt;br&gt;
    
    </summary>
    
      <category term="Redis" scheme="https://tisonkong.github.io/github.io/categories/Redis/"/>
    
    
      <category term="Redis" scheme="https://tisonkong.github.io/github.io/tags/Redis/"/>
    
      <category term="性能优化" scheme="https://tisonkong.github.io/github.io/tags/%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96/"/>
    
  </entry>
  
  <entry>
    <title>地球物理勘探与互联网开发（非正式）</title>
    <link href="https://tisonkong.github.io/github.io/2019/01/26/%E5%9C%B0%E7%90%83%E7%89%A9%E7%90%86%E5%8B%98%E6%8E%A2%E4%B8%8E%E4%BA%92%E8%81%94%E7%BD%91%E5%BC%80%E5%8F%91%EF%BC%88%E9%9D%9E%E6%AD%A3%E5%BC%8F%EF%BC%89/"/>
    <id>https://tisonkong.github.io/github.io/2019/01/26/地球物理勘探与互联网开发（非正式）/</id>
    <published>2019-01-26T15:21:33.147Z</published>
    <updated>2019-01-26T15:30:20.849Z</updated>
    
    <content type="html"><![CDATA[<h3 id="一、引言"><a href="#一、引言" class="headerlink" title="一、引言"></a>一、引言</h3><hr><p>刚开始我是学动物科学的，大学入学之初被调剂在第一产业阵营中，因无法与各种萌宠水产和谐相处，人心思变之下跳入第二产业的石油工业圈，这才入了石油物探的贼船，初入时的美好憧憬被14年腰斩的油价破灭，<a id="more"></a>同学调侃我这是49年加入国军、08年高位冲入A股之行径，现在想想当时凭着一腔热血想为郭嘉献石油，郭嘉却连一张上车的站票都给不到，瞅一眼今年三桶油的招聘，激烈如斯，想想自己当初就算再读个博可能也还是拿不到一张坐票，便也释怀了。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-f34b4ad60c5c09a9.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="中石化2019年招聘公示"></p><p>好在不论出身的互联网给了个容身之所，16年再次转型进入第三产业的IT圈子做起了Java开发，在哪里都是搬砖不若来气候宜人的深圳，于是逃离了帝都的雾霾一路向南，在这边老老实实干起了“增删查改”的代码民工，如果软件有质量，我想这一公斤的代码里，有99%是CRUD的汗水，还有1%的算法，这也催生了我继续转型的动力，毕竟一直CURD的工作不得house，开局逆势一路逆风毫无翻盘点，开始更多的思考本质一点的，有挑战一点的问题。横跨多个产业专业，虽然被理数暴虐无完肤，但是不同学科的思维方式碰撞也能诞生一些有趣的结论，这篇文章仅仅站在一个普众角度来对比对比两个学科的异同和发展趋向。</p><p>严肃正文开始。</p><h3 id="一、差异"><a href="#一、差异" class="headerlink" title="一、差异"></a>一、差异</h3><hr><h4 id="1、研究对象的差异"><a href="#1、研究对象的差异" class="headerlink" title="1、研究对象的差异"></a>1、研究对象的差异</h4><p>从产业角度来看，地球物理勘探属于第二产业，想做的事情还是把方圆几百公里和地下几千米的结构形态搞清楚，探讨的还是认识自然、改造自然的一个工业属性任务，古时候因为没有地震勘探一说，但是对于地下情况的了解又有着直接的需求，所以诞生了许多风水学家和摸金校尉。“寻龙分金看缠山，一重缠是一重关，关门如有八重险，不出阴阳八卦形”——古人把地表地形和五行八卦结合起来，以此推断地下地貌，把地下勘探变成了一门玄学。直到步入近现代历史，探求地下情况的方法开始以地球物理勘探为正室，从两眼摸瞎逐步走向了一步步推导公式作图，其余风水、五行路子移入旁门左道。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-a006e66f23e9d589.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p><p>互联网更多是属于第三产业，虽然没有直接去改造自然，但是对人类社会的变革起了巨大作用，其奠基学科主要以计算机科学、网络通信和软件工程等为基础，互联网想要做的就是把原本一个个孤立的节点纳入到因特网中，完成数字化、信息化的任务，互联网发展的核心任务就是要建立连接，PC时代连接了组织，移动互联网时代连接了个人，物联网时代连接万物。当然这种连接太多导致的麻烦也随之而来，比如要解决存储和计算的问题便产生了大数据学科，要解决权利过度中心化和信息安全问题便产生了区块链技术，要解决传输带宽和速度限制便产生了5G技术等等。</p><h4 id="2、数据获取的差异"><a href="#2、数据获取的差异" class="headerlink" title="2、数据获取的差异"></a>2、数据获取的差异</h4><p>大家知道要打探消息有两种方式，要么就是别人主动告诉你，要么就是自己去问别人，类比到物探上，要去问地下有什么？长什么样？也是有两种方式，一种就是无法预测的地震，老天爷来一次地动山摇的通知，全世界的地震台都收到消息，这个能级的信息量很庞大，通常地壳与地球内部的信息获取的就要靠此。但是物探工程师们想要知道某一个地区范围的地下情况，就只能人工地震来获取数据了，这个信息获取成本较高昂且不可逆，排上几公里的检波器，一炮打完再换个地方接着炸，耗费几十几百人力不说，还得风餐露宿几个月才能把一个工区的数据采集完。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-b63a3f54911e28e3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="地震勘探"></p><p>一个炮打出来无数个雷克子波，子波射线在地下无尽的黑暗的世界里漂流，直到遇见了不一样的介质层，便可以换个角度继续漂流，有的子波转了几次方向，有幸携带着这些宝贵的介质层信息返回到了地面，告诉检波器自己的心跳震动，画出了一串漂亮的波形图便结束自己的使命。更多的子波要么在传输的过程中能量消耗殆尽、中道崩殂，要么就是转向了更深的地下被茫茫黑暗所吞噬。<br><img src="https://upload-images.jianshu.io/upload_images/8926909-5d7b498fbbd4ba0d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="射线追踪"></p><p>地震数据的获取十分辛苦，成本代价都很高，源于自然对象的深奥和未知。反过来看互联网，信息传递则十分方便便捷，一次数据的交互不外乎一端到另一端的TCP/HTTP的请求，一段文字、一张图片、一段语音在公网上都是一串串<code>0-1</code>数字在流动，数据的传递几乎是零成本的、数据的损耗和失真是可忽略不计的。</p><h4 id="3、进化的差异"><a href="#3、进化的差异" class="headerlink" title="3、进化的差异"></a>3、进化的差异</h4><p>地球物理勘探的一大核心问题便是在对于地震数据的处理上，如何高效保真的处理这些原始数据换原成地下一个真实图像，本质上是一个数学物理问题，一个根据已知条件$A、B$求解未知数$ x $的过程：<br>$$ Ax=B $$<br>这么多年来，物探处理的进步主要依赖于物理及算法的进步，算法的核心在求解波动方程上：<br>$$<br>{ \partial^2 u \over \partial t^2 } - a^2 \Delta \mu = 0<br>$$<br>过去受限于计算力处理能力和求解算法的限制，用简化模型去成像一个二维图像，可以大致勾勒一个结构图，误差几百米都可以接受，随着摩尔定律的发力，计算机处理性能的大幅提升，求波动方程可以从积分解到微分解，从二维扩展到三维四维，从声波到全波形，反问题的求解如同比特币挖矿中的哈希碰撞一样用计算机蛮力的去穷尽一个最优解，唯一担心的就是时间不够用，一个大工区三维成像的跑一个月才能出结果，一个物探项目做几年都不成问题，但是要是换做生命周期较短的互联网项目来说，黄花菜都凉了。</p><p>算法的进步难度之大，在于其基础学科属性，几百年来基础学科的推动都是举步维艰，联想到许多基础工业的发展，尤其是核心建立在数理化生的产业从来都是高端玩家，头部企业做大做强坐着吃肉，剩下的人吃土。互联网的发展能如此迅速，我想有部分在基础设施的廉价普及以及底层技术的开源，应用层的开发没有太高太大的技术壁垒，无论用全世界最好的PHP语言还是高贵的C++，业务应用层的开发者们都能满足产品同学的需求，堆出来了一个个的生活全覆盖的应用app。</p><h4 id="4、发展阶段的差异"><a href="#4、发展阶段的差异" class="headerlink" title="4、发展阶段的差异"></a>4、发展阶段的差异</h4><p>国内物探的发展有了近百余年的历史，物探作为石油工业圈的先锋产业，也随着石油行业的盛衰经历了多轮高低潮，油价高时，勘探开发热情较高，引发了石油行业的黄金就业时代，彼时流传着“德州的油工、加州的码工”两大高薪专业传说，而今德州油工传说破灭，而硅谷码农则一枝独秀。两个专业发展的差异是由两者的历史阶段和学科内因导致的。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-ab4e8daf65bf25bb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="150年原油价格史"></p><p>地球物理勘探起于第二次工业革命之初，此次工业革命期经历了内燃机的爆发式增长，全面拉动了石油能源的需求，油价高时近130美元一桶，历史后来多次腰斩和大幅抬升，这并不是产业发展的自身原因，而是石油关乎能源安全捆绑了太多的政治和经济利益，不过行业百余年发展到如今物探似乎真正进入中老年期，工业革命完成了第三轮，交接棒开始递往了第四次工业革命，其作为老工业的基础行业退出历史中心舞台已是明显趋势，这种传统行业和新兴行业的势力博弈正在米国深刻上演（特朗普所代表的传统行业与硅谷新产业势力呈现出巨大撕裂，曾经新产能的石油行业由先锋者变成了如今既得利益的保守捍卫者）。</p><p>切入石油产业链的物探经历过了黄金机遇期，如今也随着油价的颓势也显得势弱，与此相反的是互联网行业，因为切入了第三次工业革命的信息产业链，以摩尔定律为商业周期，实现了多次浪潮，成就了多批<code>BAT/TMD/PKQ</code>(拼多多、快手、趣头条)独角兽们的造富神话，互联网经历了早期的跌宕起伏，建立起更完善的<strong><code>ABCD</code>(AI-BlockChain-Cloud-Data)</strong>基础设施，随着5G的进一步普及，以及IoT基建的搭建，未来的想象空间会更加巨大，尽管寒冬来袭，但是从大周期来看此时的互联网所处的还是一个处于蓄力成长的青年期。</p><h3 id="二、相同"><a href="#二、相同" class="headerlink" title="二、相同"></a>二、相同</h3><hr><h4 id="1、进化过程一致"><a href="#1、进化过程一致" class="headerlink" title="1、进化过程一致"></a>1、进化过程一致</h4><p>物探处理中最终成果之一就是要输出贴近地下真实情况的成像结果，而互联网开发的中的重要成果则是要输出一个用户满意、市场认可的应用，这二者隶属于不同领域，一个需要对物理世界做去伪存真（自然科学命题），而后一个是需要迎合社会大众的审美（社会学命题），当然如何黏住用户、提升体验是产品经理需要考虑的事情，而开发人员更多的是考虑应用的高可用性和稳定性，那么这也是一个偏工程的命题了。二者在工程方面的进化轨迹有着高度一致。</p><p>物理勘探中的要输出可靠的结果核心是在速度，有时候地震勘探也被称为速度勘探，是因为$Ax=B $方程中，未知数通常就是$x$，速度分析和建模的发展就经历了从单速度体、层速度体到网格速度体的工业改良过程。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-5393a4665fa63370.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="单速度体的时距曲线，地下是单纯而美好的同性同向介质，一个一元二次方程就能描述"></p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-e681678aaae38101.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="层速度模型，纵向上的精度在提高但仍然希望速度在横向上的变化小"></p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-746598283410bed7.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="网格速度模型，网格尺寸一般为几十米，纵横向都适应，精度大幅提高"></p><p>互联网开发中服务端的架构也遵循了一个类似的发展轨迹：从最初的单体应用、SOA服务化、微服务和时下时髦起来的服务网格化（ServiceMesh）。援引一张Apache Dubbo架构演进图，在PC互联网时代，用户量不大，数据量有限且业务范围不广的背景下，程序员们在单体应用里自由的飞翔着，不需要担心太多，不要怕数据和服务撑爆、也不用考虑线程安全，引入一下分层解耦都觉得绰绰有余。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-f343f6bdc2bcefe0.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="单体时代过渡到服务分层，再升级到面向服务化的过程"></p><p>但是步入移动互联网时代以后，世界就不那么美好了，业务上线动辄日增几十万用户，每天数据库要新增上亿的数据量把DBA吓坏了，业务火爆忘了限流就把后台服务器搞垮了我们开发又要背锅了，搞一个秒杀活动配置几十台服务器的集群把运维同学愁哭了，再怎么分层解耦都解决不了这种粗粒度的层级架构缺陷，微服务+容器技术也就应运而生，开发同学把巨无霸应用拆成了一个个微粒度服务，运维同学搭建容器环境把一个个微服务轻松复制几十上百个，DevOps一体化联手应对业务的爆发增长，公司老板又可以美滋滋谈日增日活，讲PPT拉融资。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-70b01c41ca2981e9.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="微服务化和容器化"></p><p>技术有利弊，微服务规模的增长会带来很多的复杂性，如服务发现、负载均衡、故障恢复、监控,甚至A/B测试、访问控制等，服务网格化（ServiceMesh）的趋势虽在暗流涌动，采用代理来剥离依赖与控制，但现有的服务端架构并未完全准备好进入IoT万物互联和智能时代，未来的设备数量是人数的几倍几十倍，连接数与存储数也是现有基础的翻番，互联网技术的发展面临的还是一个严峻的挑战。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-54bd464669856dbf.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Dubbo提出的ServiceMesh构想"></p><h4 id="2、解决思路一致"><a href="#2、解决思路一致" class="headerlink" title="2、解决思路一致"></a>2、解决思路一致</h4><ul><li><strong>迭代与完善</strong>。建立在理工基础上的学科在发展的历程上都是非常的相似，先是将现实中的工程问题抽象成一个个简单的模型，然后再不断的将模型完善，加入更多的参数和变量，把控好问题的边界，那么模型或应用就能按照设定的流程求得一个合理的结果，物探中这个过程会叫反问题的求解而机器学习中叫目标函数优化，<strong>发展的过程看起来就像是完善模型/应用、输出更好结果的过程</strong>。</li></ul><p><img src="https://upload-images.jianshu.io/upload_images/8926909-4f20a625e69423b4.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="迭代流程"></p><ul><li><strong>抽象化的思维</strong>。在物探中要去探地下，地下本都是岩石或孔隙流体，但是物探家和地质家门就会把地下抽象成一个个的物理模型（速度模型、属性模型、地质模型），物理世界未曾发生过改变，但是抽象的模型却在思维层面经历了不断的熔炼和提取。计算机同样也是如此，为了方便开发者们更好的完成现实实体与数据结构的映射，有了面向对象的思想，把现实世界的对象抽象成为信息世界里的对象，同时，计算机学科还很擅长用虚拟化的思维来解决难题，抽象成一个虚拟层出来，比如Java虚拟机来屏蔽开发者对机器底层的晦涩操作，比如Hash虚拟层来映射一致性哈希机器节点分配不均的问题。</li></ul><h4 id="3、基础重复工作一致"><a href="#3、基础重复工作一致" class="headerlink" title="3、基础重复工作一致"></a>3、基础重复工作一致</h4><p>任何行业都有底层的辛苦劳作者，摩天大厦非一人之功，背后一定有许多辛苦勤劳的人员在付出着血汗，他们虽然是廉价的劳动力但是却是项目和工程进步的推动者和奠基者。就像修筑的长城，除了秦始被载入史册皇当时付出劳动血汗的那些修筑者也不应被遗忘。</p><p>物探工作的基础重复工作非常之多，无论是采集时候的放炮人员，填埋器材人员，还是处理时候的初至拾取和点速度谱人员，这些基础工作重复而不可避免却也是整个输出结果的关键。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-24618bc9c8f9997f.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="速度谱拾取，点击能量团拉平地震道集"></p><p>互联网行业现在AI看起来冉冉上升，但是行业的推动和发展都需要海量的特征数据和行为数据，这些原始数据的获得就是需要一大批“AI数据民工”来完成，比如工程师想让 AI 准确识别出足球，最好办法不是用代码来描述足球，而是直接找来很多张带有足球的图片，用 AI 能读取的方式把图片、视频里的足球“标注”出来，扔进 AI 模型训练，之后它“自然而然”就获得了识别足球的能力。和小孩不同的是，机器需要在不同场景、不同角度下反复学习，这时需要标注大量用于训练机器学习模型的数据，让机器越来越像人。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-5ae098ebb9e240cb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="一个让人奔溃的“数据标注”工作，数据标注得越准确、数量越多，最终训练出来的模型效果就越好。"></p><p>人类变成投喂机器的流水线工人，亚马逊AMT也顺势成为AI时代的富士康。在国内，人工智能创业公司、BAT同样采用了目前数据标注行业的主流模式——“外包”。BAT、商汤、旷视、云从科技这类大的人工智能公司，一年在数据上的外包支出高达数千万元。科技网站TechRepublic的一篇文章描述称：这些分布在全球各地的50万名工人们，昼夜交替、时常待命，对数据进行手工输入、分类，区分出下一张照片中是否有“狗”，语句中的“bass”到底是低音还是鲈鱼。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-96505a98676f562d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p><h3 id="二、互补融合"><a href="#二、互补融合" class="headerlink" title="二、互补融合"></a>二、互补融合</h3><hr><p>虽然两个产业分在上下游两端，上面还是总结了二者之间的异同，在过去，工业的上游打下坚实的产业链基础哺育了下游的发展壮大，在不远的未来，相信互联网相关技术的发展也能反补工业上游，一线城市所打造的智慧城市概念也可以迁移到油田的建设上，打造“智慧油田”，具体到物探层面，倒是可以首线用AI去解放基础重复的辛劳工作。</p><h3 id="三、个人发展窥探"><a href="#三、个人发展窥探" class="headerlink" title="三、个人发展窥探"></a>三、个人发展窥探</h3><hr><p>最后讲讲职业发展，意淫和想象不能当饭吃，不同的行业有不一样的打怪升级路线，各行各业也都有自己的阶层，如何突破个人阶层才是硬道理。过去石油物探是一个<strong>蓝海+增量市场</strong>，全国各地到处是等待开发的勘探区，于茫茫的地表之上“分金定穴”，勘探到一个亿吨级的大油/气田很快就能走向人生巅峰，响应郭嘉号召为祖国找石油、献石油不是一句空话，但是任何增量市场都会有红利消失殆尽的一天，蓝海变红海，增量变存量是一个必然的过程，相应的阶层之间的跨越和流动越来越难，这也是我身边许多物探同学感觉到求职困难和个人发展容易遭遇瓶颈的时代背景，<strong>行业给个人施展的空间少了</strong>。</p><p>这个和顾子明先生写《汉武帝与抗美援朝》的思路一致，汉武帝北征的根本在于国内人口和矛盾聚集，为了缓和矛盾选择了扩张之路，以北征获得了大量土地和增量蛋糕，把国内的存量博弈变为增量博弈，国内大量的改革都能够得以在增量中推进，同时军功也开启了底层向上流通的渠道，使得整个汉朝的体制朝气蓬勃。而如今这几十年，邓爷爷为什么改革开放成功背后的原因也大抵如此，那些跟着国家队出手、赌对国运的人，又有谁曾被亏待过。<strong>细看最近这几年的郭嘉政策，多次在十九大报告提到互联网、AI和大数据新动能，开启一个新增量市场的意图十分明显！</strong></p><p>个人也是需要利用这样的时代机遇去寻找增量市场和空间，互联网的发展经过了二十年已经过了两代重要时期，第一波的PC互联网红利诞生了BAT，第二波移动互联网诞生了TMD和PKQ，每一波机遇期都释放出了足够大的红利和增量市场，为每个细分领域的独角兽发展提供了充分的土壤，可以预见的是第三波红利期的到来也在不远的几年，这个机遇应该是长期性的趋势而非短暂性的风口，这个增量蛋糕也将是数倍于第一次和第二次的总和，足以抹平郭嘉在改革发展过程的阵痛，也会为许多人提供新财富和跨越自身阶层的机会。每次机遇期的开启都是以一次金融危机和寒冬为篇章，千禧年互联网泡沫的终结，开启了后面的PC互联网时代，08年金融危机后开启了全面的移动互联网浪潮和美股十年牛市。2018年又结束了一个循环周期，<strong>Winter is comming，普通人要做的就是活下去，提前储备布局等候春天的到来！</strong></p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-c2e3f667da3f4d9f.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;一、引言&quot;&gt;&lt;a href=&quot;#一、引言&quot; class=&quot;headerlink&quot; title=&quot;一、引言&quot;&gt;&lt;/a&gt;一、引言&lt;/h3&gt;&lt;hr&gt;
&lt;p&gt;刚开始我是学动物科学的，大学入学之初被调剂在第一产业阵营中，因无法与各种萌宠水产和谐相处，人心思变之下跳入第二产业的石油工业圈，这才入了石油物探的贼船，初入时的美好憧憬被14年腰斩的油价破灭，
    
    </summary>
    
      <category term="随笔" scheme="https://tisonkong.github.io/github.io/categories/%E9%9A%8F%E7%AC%94/"/>
    
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="地球物理勘探" scheme="https://tisonkong.github.io/github.io/tags/%E5%9C%B0%E7%90%83%E7%89%A9%E7%90%86%E5%8B%98%E6%8E%A2/"/>
    
      <category term="互联网" scheme="https://tisonkong.github.io/github.io/tags/%E4%BA%92%E8%81%94%E7%BD%91/"/>
    
  </entry>
  
  <entry>
    <title>网络编程之IO、NIO和Netty</title>
    <link href="https://tisonkong.github.io/github.io/2018/12/28/java%20%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B%E4%B9%8B%E2%80%94%E2%80%94IO%E3%80%81NIO%E5%92%8CNetty/"/>
    <id>https://tisonkong.github.io/github.io/2018/12/28/java 网络编程之——IO、NIO和Netty/</id>
    <published>2018-12-28T13:18:46.626Z</published>
    <updated>2018-12-28T13:27:23.878Z</updated>
    
    <content type="html"><![CDATA[<h3 id="一、基本概述"><a href="#一、基本概述" class="headerlink" title="一、基本概述"></a>一、基本概述</h3><p>IO 一直是编程学习中的核心部分，在这里所说的IO 不仅仅是对文件的操作，还常常应用在网络编程中，比如 Socket 通信、协议服务器等，都是典型的 IO 操作目标，伴随着海量数据增长和分布式系统的发展，IO 的扩展能力则显得十分重要，Java语言提供了强大的IO机制，基于不同的抽象模型和交互方式，总结之可归纳为三种：</p><blockquote><ul><li>传统的 <code>java.io</code> 包，基于流模型实现的BIO</li><li>升级的 <code>java.nio</code> 包，构建多路复用的、同步非阻塞 NIO </li><li>改造的NIO2，引入了异步非阻塞的AIO</li></ul></blockquote><a id="more"></a><p>这里面需要区分和澄清几个关键的概念：</p><ul><li><p>区分<strong>同步</strong>或<strong>异步</strong>（<code>synchronous/asynchronous</code>）。简单来说，同步是一种可靠的有序运行机制，当我们进行同步操作时，后续的任务是等待当前调用返回，才会进行下一步；而异步则相反，其他任务不需要等待当前调用返回，通常依靠事件、回调等机制来实现任务间次序关系。</p></li><li><p>区分<strong>阻塞</strong>与<strong>非阻塞</strong>（<code>blocking/non-blocking</code>）。在进行阻塞操作时，当前线程会处于阻塞状态，无法从事其他任务，只有当条件就绪才能继续，比如 ServerSocket 新连接建立完毕，或数据读取、写入操作完成；而非阻塞则是不管 IO 操作是否结束，直接返回，相应操作在后台继续处理。</p></li></ul><p><strong>首先介绍一下IO的基础API设计和概念模型：</strong></p><ul><li><ol><li>IO 不仅仅是对文件的操作，网络编程中，比如 <code>Socket</code> 通信，都是典型的 IO 操作目标。输入流、输出流（<code>InputStream / OutputStream</code>）是用于读取或写入字节的，例如操作图片文件。</li></ol></li><li><ol><li><code>Reader/Writer</code> 则是用于操作字符，增加了字符编解码等功能，适用于类似从文件中读取或者写入文本信息。本质上计算机操作的都是字节，不管是网络通信还是文件读取，<code>Reader/Writer</code> 相当于构建了应用逻辑和原始数据之间的桥梁。<code>BufferedOutputStream</code> 等带缓冲区的实现，可以避免频繁的磁盘读写，进而提高 IO 处理效率。这种设计利用了缓冲区，将批量数据进行一次操作，但在使用中千万别忘了 flush。</li></ol></li><li><ol><li>很多 IO 工具类都实现了 <code>Closeable</code> 接口，因为需要进行资源的释放，需要利用try-with-resources、 try-finally 等机制保证 <code>FileInputStream</code> 被明确关闭，进而相应文件描述符也会失效，否则将导致资源无法被释放。利用专栏前面的内容提到的 <code>Cleaner</code> 或 <code>finalize</code> 机制作为资源释放的最后把关，也是必要的。</li></ol></li></ul><blockquote><p>IO的具体介绍请戳：<a href="https://www.jianshu.com/p/48f2fb0b79b5" target="_blank" rel="noopener">《java基础之IO流（IO篇）》</a></p></blockquote><p><strong>接下来说NIO：</strong></p><ul><li><ol><li><code>Buffer</code>，高效的数据容器，除了布尔类型，所有原始数据类型都有相应的 <code>Buffer</code> 实现。</li></ol></li><li><ol><li><code>Channel</code>，Channel 是操作系统底层的一种抽象，数据总是从通道读取到缓冲区中，或者从缓冲区写入到通道中，可以通过Socket 获取 Channel，反之亦然。</li></ol></li><li><ol><li><code>Selector</code>，是 NIO 实现多路复用的基础，它提供了一种高效的机制，可以检测到注册在，<code>Selector</code> 上的多个 <code>Channel</code> 中，是否有 <code>Channel</code> 处于就绪状态，进而实现了单线程对多Channel 的高效管理。</li></ol></li></ul><p>在 Java 7 中，NIO 有了进一步的改进，也就是 NIO 2，引入了异步非阻塞 IO 方式，也有很多人叫它 AIO（<code>Asynchronous IO</code>）。异步 IO 操作基于事件和回调机制，可以简单理解为，应用操作直接返回，而不会阻塞在那里，当后台处理完成，操作系统会通知相应线程进行后续工作。</p><blockquote><p>NIO的具体介绍请戳：<a href="https://www.jianshu.com/p/f8454c6be204" target="_blank" rel="noopener">《java进阶之NIO》</a></p></blockquote><h3 id="二、网络编程应用"><a href="#二、网络编程应用" class="headerlink" title="二、网络编程应用"></a>二、网络编程应用</h3><h4 id="1-基于BIO实现"><a href="#1-基于BIO实现" class="headerlink" title="1. 基于BIO实现"></a>1. 基于BIO实现</h4><p>IO和NIO具体的应用在哪里，可结合服务器-客户端（B/S）模式来设计一个极简版的web后台，该后台仅仅能够同时服务多个客户端请求即可。这里有三种思路，首先使用<code>java.io</code> 和 <code>java.net</code> 中的同步、阻塞式 API，可以简单实现。</p><p>服务端的实现：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * IO服务端实现</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IOServer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">        ServerSocket serverSocket = <span class="keyword">new</span> ServerSocket(<span class="number">8000</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// (1) 接收新连接线程</span></span><br><span class="line">        <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// (1) 阻塞方法获取新的连接</span></span><br><span class="line">                    Socket socket = serverSocket.accept();</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// (2) 每一个新的连接都创建一个线程，负责读取数据</span></span><br><span class="line">                    <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            <span class="keyword">int</span> len;</span><br><span class="line">                            <span class="keyword">byte</span>[] data = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">                            InputStream inputStream = socket.getInputStream();</span><br><span class="line">                            <span class="comment">// (3) 按字节流方式读取数据</span></span><br><span class="line">                            <span class="keyword">while</span> ((len = inputStream.read(data)) != -<span class="number">1</span>) &#123;</span><br><span class="line">                                System.out.println(<span class="keyword">new</span> String(data, <span class="number">0</span>, len));</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;).start();</span><br><span class="line"></span><br><span class="line">                &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>客户端的实现：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * IO客户端</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IOClient</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Executor executor = Executors.newFixedThreadPool(<span class="number">8</span>);</span><br><span class="line">        Thread thread = <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Socket socket = <span class="keyword">new</span> Socket(<span class="string">"127.0.0.1"</span>, <span class="number">8000</span>);</span><br><span class="line">                <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        socket.getOutputStream().write((<span class="keyword">new</span> Date() + <span class="string">": hello world"</span>).getBytes());</span><br><span class="line">                        Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        executor.execute(thread);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>BIO模型实现要点是：</p><ul><li>服务器端启动 <code>ServerSocket</code>，端口 <code>8000</code> 表示自动绑定一个空闲端口。</li><li>调用 <code>accept</code> 方法，<strong>阻塞等待客户端连接</strong>。</li><li>利用 <code>Socket</code> 模拟了一个简单的客户端，只进行连接、读取、打印。</li><li>当连接建立后，启动一个单独线程负责回复客户端请求。</li></ul><p>这样，一个简单的 Socket 服务器就被实现出来了。但是我们知道 Java 语言目前的线程实现是比较重量级的，启动或者销毁一个线程是有明显开销的，每个线程都有单独的线程栈等结构，需要占用非常明显的内存，所以，每一个 Client 启动一个线程似乎都有些浪费。可以稍微修正一下引入线程池解决线程开销和切换的问题：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">serverSocket = <span class="keyword">new</span> ServerSocket(<span class="number">8000</span>);</span><br><span class="line">executor = Executors.newFixedThreadPool(<span class="number">8</span>);</span><br><span class="line"><span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">       Socket socket = serverSocket.accept();</span><br><span class="line">       ... 开启一个IO线程 ...</span><br><span class="line">       executor.execute(ioThread);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这样做似乎好了很多，通过一个固定大小的线程池，来负责管理工作线程，避免频繁创建、销毁线程的开销，这是我们构建并发服务的典型方式。这种工作方式，可以参考下图来理解。</p><p><img src="https://upload-images.jianshu.io/upload_images/8926909-c1435a8ceb30e2a8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="BIO交互模式"></p><p>如果连接数并不是非常多，只有最多几百个连接的普通应用，这种模式往往可以工作的很好。但是，如果连接数量急剧上升，这种实现方式就无法很好地工作了，因为线程上下文切换开销会在高并发时变得很明显，这是同步阻塞方式的低扩展性劣势。</p><h4 id="2-基于NIO实现"><a href="#2-基于NIO实现" class="headerlink" title="2. 基于NIO实现"></a>2. 基于NIO实现</h4><p>NIO 引入的多路复用机制，提供了另外一种思路：</p><ul><li>线程资源不受限：<code>NIO</code>编程模型新来一个连接不再创建一个新的线程，把这条连接直接绑定到某个固定的线程，然后这条连接所有的读写都由该线程来负责，把这么多<code>while</code>死循环变成一个死循环，这个死循环由一个线程控制，一条连接来了，不创建一个<code>while</code>死循环去监听是否有数据可读,，直接把这条连接注册到<code>Selector</code>上，然后通过检查<code>Selector</code>批量监测出有数据可读的连接进而读取数据。</li><li>线程切换效率提高：线程数量大大降低，线程切换效率因此也大幅度提高。</li><li>数据读写是以字节流为单位效率不高：<code>NIO</code>维护一个缓冲区每次从这个缓冲区里面读取一块的数据，数据读写不再以字节为单位，而是以字节块为单位。<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NIOServer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * serverSelector负责轮询是否有新的连接,clientSelector负责轮询连接是否有数据可读.</span></span><br><span class="line"><span class="comment">     * 服务端监测到新的连接不再创建一个新的线程,而是直接将新连接绑定到clientSelector上,这样不用IO模型中1w个while循环在死等</span></span><br><span class="line"><span class="comment">     * clientSelector被一个while死循环包裹,如果在某一时刻有多条连接有数据可读通过 clientSelector.select(1)方法轮询出来进而批量处理</span></span><br><span class="line"><span class="comment">     * 数据的读写以内存块为单位</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> args</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> IOException</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        Selector serverSelector = Selector.open();</span><br><span class="line">        Selector clientSelector = Selector.open();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                ServerSocketChannel serverSocketChannel = ServerSocketChannel.open();</span><br><span class="line">                serverSocketChannel.socket().bind(<span class="keyword">new</span> InetSocketAddress(<span class="number">8000</span>));</span><br><span class="line">                serverSocketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">                serverSocketChannel.register(serverSelector, SelectionKey.OP_ACCEPT);</span><br><span class="line"></span><br><span class="line">                <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                    <span class="comment">// 轮询监测是否有新的连接</span></span><br><span class="line">                    <span class="keyword">if</span> (serverSelector.select(<span class="number">1</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        Set&lt;SelectionKey&gt; selectionKeys = serverSelector.selectedKeys();</span><br><span class="line">                        Iterator&lt;SelectionKey&gt; keyIterator = selectionKeys.iterator();</span><br><span class="line">                        <span class="keyword">while</span> (keyIterator.hasNext()) &#123;</span><br><span class="line">                            SelectionKey selectionKey = keyIterator.next();</span><br><span class="line">                            <span class="keyword">if</span> (selectionKey.isAcceptable()) &#123;</span><br><span class="line">                                <span class="keyword">try</span> &#123;</span><br><span class="line">                                    <span class="comment">//(1)每来一个新连接不需要创建一个线程而是直接注册到clientSelector</span></span><br><span class="line">                                    SocketChannel socketChannel = ((ServerSocketChannel) selectionKey.channel()).accept();</span><br><span class="line">                                    socketChannel.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">                                    socketChannel.register(clientSelector, SelectionKey.OP_READ);</span><br><span class="line">                                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                                    keyIterator.remove();</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">new</span> Thread(() -&gt; &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">                    <span class="comment">// (2)批量轮询是否有哪些连接有数据可读</span></span><br><span class="line">                    <span class="keyword">if</span> (clientSelector.select(<span class="number">1</span>) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                        Set&lt;SelectionKey&gt; selectionKeys = serverSelector.selectedKeys();</span><br><span class="line">                        Iterator&lt;SelectionKey&gt; keyIterator = selectionKeys.iterator();</span><br><span class="line">                        <span class="keyword">while</span> (keyIterator.hasNext()) &#123;</span><br><span class="line">                            SelectionKey selectionKey = keyIterator.next();</span><br><span class="line">                            <span class="keyword">if</span> (selectionKey.isReadable()) &#123;</span><br><span class="line">                                <span class="keyword">try</span> &#123;</span><br><span class="line">                                    SocketChannel socketChannel = (SocketChannel) selectionKey.channel();</span><br><span class="line">                                    ByteBuffer byteBuffer = ByteBuffer.allocate(<span class="number">1024</span>);</span><br><span class="line">                                    <span class="comment">//(3)读取数据以块为单位批量读取</span></span><br><span class="line">                                    socketChannel.read(byteBuffer);</span><br><span class="line">                                    byteBuffer.flip();</span><br><span class="line">                                    System.out.println(Charset.defaultCharset().newDecoder().decode(byteBuffer)</span><br><span class="line">                                            .toString());</span><br><span class="line">                                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                                    keyIterator.remove();</span><br><span class="line">                                    selectionKey.interestOps(SelectionKey.OP_READ);</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><p>首先，通过 <code>Selector.open()</code> 创建一个 <code>Selector</code>，作为类似调度员的角色。然后，创建一个<code>ServerSocketChannel</code>，并且向 <code>Selector</code> 注册，通过指定 <code>SelectionKey.OP_ACCEPT</code>，告诉调度员，它关注的是新的连接请求。注意，为什么要配置非阻塞模式呢？这是因为阻塞模式下，注册操作是不允许的，会抛出 <code>IllegalBlockingModeException</code> 异常。<code>Selector</code> 阻塞在 <code>select</code> 操作，当有 <code>Channel</code> 发生接入请求，就会被唤醒。</p><p><strong><code>IO</code> 都是同步阻塞模式，所以需要多线程以实现多任务处理。而<code>NIO</code> 则是利用了单线程轮询事件的机制，通过高效地定位就绪的 <code>Channel</code>，来决定做什么，仅仅 <code>select</code> 阶段是阻塞的，可以有效避免大量客户端连接时，频繁线程切换带来的问题，应用的扩展能力有了非常大的提高</strong>。下面这张图对这种实现思路进行了形象地说明。<br><img src="https://upload-images.jianshu.io/upload_images/8926909-f919c016030f2148.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="NIO多路复用"></p><p>在 Java 7 引入的 NIO 2 中，又增添了一种额外的异步 IO 模式，利用事件和回调，处理<code>Accept、Read</code> 等操作。 AIO 实现看起来是类似这样子：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">AsynchronousServerSocketChannel serverSock =        </span><br><span class="line">      AsynchronousServerSocketChannel.open().bind(sockAddr);</span><br><span class="line">serverSock.accept(serverSock, <span class="keyword">new</span> CompletionHandler&lt;&gt;() &#123; </span><br><span class="line">    <span class="comment">//为异步操作指定 CompletionHandler 回调函数</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">completed</span><span class="params">(AsynchronousSocketChannel sockChannel, AsynchronousServerSocketChannel serverSock)</span> </span>&#123;</span><br><span class="line">        serverSock.accept(serverSock, <span class="keyword">this</span>);</span><br><span class="line">        <span class="comment">// 另外一个 write（sock，CompletionHandler&#123;&#125;）</span></span><br><span class="line">        sayHelloWorld(sockChannel, Charset.defaultCharset().encode</span><br><span class="line">                (<span class="string">"Hello World!"</span>));</span><br><span class="line">    &#125;</span><br><span class="line">  <span class="comment">// 省略其他路径处理方法...</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></p><h4 id="3-基于Netty实现"><a href="#3-基于Netty实现" class="headerlink" title="3.基于Netty实现"></a>3.基于Netty实现</h4><p>JDK 的 NIO 编程需要了解很多的概念，编程复杂，对 NIO 入门非常不友好，编程模型不友好，ByteBuffer 的 Api 难以掌握，而且JDK 的 NIO 底层由 epoll 实现，该实现饱受诟病的空轮询 bug 会导致 cpu 飙升 100%。而Netty 封装了 JDK 的 NIO，API开箱即用，对开发友好，用netty来实现将比NIO方是简化了很多：</p><p>Netty实现服务端：必须要指定三类属性，分别是<strong>线程模型、IO模型、连接读写处理逻辑</strong>。Netty服务端启动流程：1. 创建引导类，2. 指定线程模型、IO模型、连接读写处理逻辑，3. 绑定端口。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NettyServer</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> BEGIN_PORT = <span class="number">8000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//指定线程模型</span></span><br><span class="line">        NioEventLoopGroup boosGroup = <span class="keyword">new</span> NioEventLoopGroup();</span><br><span class="line">        NioEventLoopGroup workerGroup = <span class="keyword">new</span> NioEventLoopGroup();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//引导类，负责服务端的启动</span></span><br><span class="line">        <span class="keyword">final</span> ServerBootstrap serverBootstrap = <span class="keyword">new</span> ServerBootstrap();</span><br><span class="line">        <span class="comment">//</span></span><br><span class="line">        <span class="keyword">final</span> AttributeKey&lt;Object&gt; clientKey = AttributeKey.newInstance(<span class="string">"clientKey"</span>);</span><br><span class="line">        serverBootstrap</span><br><span class="line">                .group(boosGroup, workerGroup)         <span class="comment">//配置线程组，初始化引导类的线程模型</span></span><br><span class="line">                .channel(NioServerSocketChannel.class) <span class="comment">//指定IO模型</span></span><br><span class="line">                .attr(AttributeKey.newInstance(<span class="string">"serverName"</span>), <span class="string">"nettyServer"</span>)  <span class="comment">//自定义属性</span></span><br><span class="line">                .childAttr(clientKey, <span class="string">"clientValue"</span>)</span><br><span class="line">                .option(ChannelOption.SO_BACKLOG, <span class="number">1024</span>)         <span class="comment">//表示系统用于临时存放已完成三次握手的请求的队列的最大长度，如果连接建立频繁，服务器处理创建新连接较慢，可以适当调大这个参数</span></span><br><span class="line">                .childOption(ChannelOption.SO_KEEPALIVE, <span class="keyword">true</span>)  <span class="comment">//开启TCP底层心跳机制</span></span><br><span class="line">                .childOption(ChannelOption.TCP_NODELAY, <span class="keyword">true</span>)   <span class="comment">//开Nagle算法,要求高实时性，有数据发送时就马上发送，就关闭，如果需要减少发送次数减少网络交互，就开启。</span></span><br><span class="line">                .childHandler(<span class="keyword">new</span> ChannelInitializer&lt;NioSocketChannel&gt;() &#123;</span><br><span class="line">                    <span class="meta">@Override</span></span><br><span class="line">                    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">initChannel</span><span class="params">(NioSocketChannel ch)</span> </span>&#123;</span><br><span class="line">                        System.out.println(ch.attr(clientKey).get());</span><br><span class="line">                        ch.pipeline().addLast(<span class="keyword">new</span> FirstServerHandler());</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">//异步方法，调用之后立即返回</span></span><br><span class="line">        bind(serverBootstrap, BEGIN_PORT);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//自动banding递增端口</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">bind</span><span class="params">(<span class="keyword">final</span> ServerBootstrap serverBootstrap, <span class="keyword">final</span> <span class="keyword">int</span> port)</span> </span>&#123;</span><br><span class="line">        serverBootstrap.bind(port).addListener(future -&gt; &#123;</span><br><span class="line">            <span class="keyword">if</span> (future.isSuccess()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"端口["</span> + port + <span class="string">"]绑定成功!"</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                System.err.println(<span class="string">"端口["</span> + port + <span class="string">"]绑定失败!"</span>);</span><br><span class="line">                bind(serverBootstrap, port + <span class="number">1</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>服务端Handler处理类：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FirstServerHandler</span> <span class="keyword">extends</span> <span class="title">ChannelInboundHandlerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</span><br><span class="line">        ByteBuf byteBuf = (ByteBuf) msg;</span><br><span class="line"></span><br><span class="line">        System.out.println(<span class="keyword">new</span> Date() + <span class="string">": 服务端读到数据 -&gt; "</span> + byteBuf.toString(Charset.forName(<span class="string">"utf-8"</span>)));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 回复数据到客户端</span></span><br><span class="line">        System.out.println(<span class="keyword">new</span> Date() + <span class="string">": 服务端写出数据"</span>);</span><br><span class="line">        ByteBuf out = getByteBuf(ctx);</span><br><span class="line">        out.capacity();</span><br><span class="line">        ctx.channel().writeAndFlush(out);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> ByteBuf <span class="title">getByteBuf</span><span class="params">(ChannelHandlerContext ctx)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] bytes = <span class="string">"Hello,Clinet!"</span>.getBytes(Charset.forName(<span class="string">"utf-8"</span>));</span><br><span class="line"></span><br><span class="line">        ByteBuf buffer = ctx.alloc().buffer();</span><br><span class="line"></span><br><span class="line">        buffer.writeBytes(bytes);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> buffer;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>客户端启动流程：要启动Netty客户端，必须要指定三类属性，分别是线程模型、IO模型、连接读写处理逻辑。Netty客户端启动流程：1. 创建引导类，2. 指定线程模型、IO模型、连接读写处理逻辑，3. 建立连接。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">NettyClient</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAX_RETRY = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String HOST = <span class="string">"127.0.0.1"</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> PORT = <span class="number">8000</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        NioEventLoopGroup workerGroup = <span class="keyword">new</span> NioEventLoopGroup();</span><br><span class="line"></span><br><span class="line">        Bootstrap bootstrap = <span class="keyword">new</span> Bootstrap();</span><br><span class="line">        bootstrap</span><br><span class="line">                <span class="comment">// 1.指定线程模型</span></span><br><span class="line">                .group(workerGroup)</span><br><span class="line">                <span class="comment">// 2.指定 IO 类型为 NIO</span></span><br><span class="line">                .channel(NioSocketChannel.class)</span><br><span class="line">                <span class="comment">// 绑定自定义属性到 channel</span></span><br><span class="line">                .attr(AttributeKey.newInstance(<span class="string">"clientName"</span>), <span class="string">"nettyClient"</span>)</span><br><span class="line">                <span class="comment">// 设置TCP底层属性</span></span><br><span class="line">                .option(ChannelOption.CONNECT_TIMEOUT_MILLIS, <span class="number">5000</span>)</span><br><span class="line">                .option(ChannelOption.SO_KEEPALIVE, <span class="keyword">true</span>)</span><br><span class="line">                .option(ChannelOption.TCP_NODELAY, <span class="keyword">true</span>)</span><br><span class="line">                <span class="comment">// 3.IO 处理逻辑</span></span><br><span class="line">                .handler(<span class="keyword">new</span> ChannelInitializer&lt;SocketChannel&gt;() &#123;</span><br><span class="line">                    <span class="meta">@Override</span></span><br><span class="line">                    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initChannel</span><span class="params">(SocketChannel ch)</span> </span>&#123;</span><br><span class="line">                        ch.pipeline().addLast(<span class="keyword">new</span> FirstClientHandler());</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4.建立连接</span></span><br><span class="line">        connect(bootstrap, HOST, PORT, MAX_RETRY);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">connect</span><span class="params">(Bootstrap bootstrap, String host, <span class="keyword">int</span> port, <span class="keyword">int</span> retry)</span> </span>&#123;</span><br><span class="line">        bootstrap.connect(host, port).addListener(future -&gt; &#123;</span><br><span class="line">            <span class="keyword">if</span> (future.isSuccess()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"连接成功!"</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (retry == <span class="number">0</span>) &#123;</span><br><span class="line">                System.err.println(<span class="string">"重试次数已用完，放弃连接！"</span>);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 第几次重连</span></span><br><span class="line">                <span class="keyword">int</span> order = (MAX_RETRY - retry) + <span class="number">1</span>;</span><br><span class="line">                <span class="comment">// 本次重连的间隔</span></span><br><span class="line">                <span class="keyword">int</span> delay = <span class="number">1</span> &lt;&lt; order;</span><br><span class="line">                System.err.println(<span class="keyword">new</span> Date() + <span class="string">": 连接失败，第"</span> + order + <span class="string">"次重连……"</span>);</span><br><span class="line">                bootstrap.config().group().schedule(() -&gt; connect(bootstrap, host, port, retry - <span class="number">1</span>), delay, TimeUnit.SECONDS);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>客户端Handler类：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FirstClientHandler</span> <span class="keyword">extends</span> <span class="title">ChannelInboundHandlerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelActive</span><span class="params">(ChannelHandlerContext ctx)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="keyword">new</span> Date() + <span class="string">": 客户端写出数据"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 1.获取数据</span></span><br><span class="line">        ByteBuf buffer = getByteBuf(ctx);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2.写数据</span></span><br><span class="line">        ctx.channel().writeAndFlush(buffer);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> ByteBuf <span class="title">getByteBuf</span><span class="params">(ChannelHandlerContext ctx)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] bytes = <span class="string">"Hello,Server!"</span>.getBytes(Charset.forName(<span class="string">"utf-8"</span>));</span><br><span class="line">        ByteBuf buffer = ctx.alloc().buffer();</span><br><span class="line">        buffer.writeBytes(bytes);</span><br><span class="line">        <span class="keyword">return</span> buffer;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">channelRead</span><span class="params">(ChannelHandlerContext ctx, Object msg)</span> </span>&#123;</span><br><span class="line">        ByteBuf byteBuf = (ByteBuf) msg;</span><br><span class="line">        System.out.println(<span class="keyword">new</span> Date() + <span class="string">": 客户端读到数据 -&gt; "</span> + byteBuf.toString(Charset.forName(<span class="string">"utf-8"</span>)));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>Netty采用<code>Reactor</code>线程模型。这里面主要有三种<code>Reactor</code>线程模型。分别是单线程模式、主从<code>Reactor</code>模式、多<code>Reactor</code>线程模式。其都可以通过初始和<code>EventLoopGroup</code>进行设置。其主要区别在于，单<code>Reactor</code>模式就是一个线程，既进程处理连接，也处理IO。类似于我们传统的OIO编程。主从<code>Reactor</code>模式，其实就是将监听连接和处理IO的分开在不同的线程完成。最后，主从<code>Reactor</code>线程模型，为了解决多<code>Reactor</code>模型下单一线程性能不足的问题。改为了一组线程池进行处理。官方默认的是采用这种主从<code>Reactor</code>模型。其线程数默认为CPU内核的2倍。</p><hr><p><strong>读完可以思考：</strong></p><ul><li>基础 API 功能与设计， InputStream/OutputStream 和 Reader/Writer 的关系和区别。</li><li>NIO、NIO 2 的基本组成。</li><li>给定场景，分别用不同模型实现，分析 BIO、NIO 等模式的设计和实现原理。</li><li>NIO 提供的高性能数据操作方式是基于什么原理，如何使用？</li><li>从开发者的角度来看， NIO 自身实现存在哪些问题？有什么改进的想法吗？</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;一、基本概述&quot;&gt;&lt;a href=&quot;#一、基本概述&quot; class=&quot;headerlink&quot; title=&quot;一、基本概述&quot;&gt;&lt;/a&gt;一、基本概述&lt;/h3&gt;&lt;p&gt;IO 一直是编程学习中的核心部分，在这里所说的IO 不仅仅是对文件的操作，还常常应用在网络编程中，比如 Socket 通信、协议服务器等，都是典型的 IO 操作目标，伴随着海量数据增长和分布式系统的发展，IO 的扩展能力则显得十分重要，Java语言提供了强大的IO机制，基于不同的抽象模型和交互方式，总结之可归纳为三种：&lt;/p&gt;
&lt;blockquote&gt;
&lt;ul&gt;
&lt;li&gt;传统的 &lt;code&gt;java.io&lt;/code&gt; 包，基于流模型实现的BIO&lt;/li&gt;
&lt;li&gt;升级的 &lt;code&gt;java.nio&lt;/code&gt; 包，构建多路复用的、同步非阻塞 NIO &lt;/li&gt;
&lt;li&gt;改造的NIO2，引入了异步非阻塞的AIO&lt;/li&gt;
&lt;/ul&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="网络编程" scheme="https://tisonkong.github.io/github.io/categories/%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="NIO" scheme="https://tisonkong.github.io/github.io/tags/NIO/"/>
    
      <category term="Netty" scheme="https://tisonkong.github.io/github.io/tags/Netty/"/>
    
  </entry>
  
  <entry>
    <title>NIO的基础总结</title>
    <link href="https://tisonkong.github.io/github.io/2018/10/12/java%E8%BF%9B%E9%98%B6%E4%B9%8BNIO/"/>
    <id>https://tisonkong.github.io/github.io/2018/10/12/java进阶之NIO/</id>
    <published>2018-10-12T13:37:50.101Z</published>
    <updated>2018-10-12T13:40:13.087Z</updated>
    
    <content type="html"><![CDATA[<p>Java NIO(New IO)是一个可以替代标准Java IO API的IO API（从Java 1.4开始)，Java NIO提供了与标准IO不同的IO工作方式。<br><a id="more"></a><br><strong>Java NIO: Channels and Buffers（通道和缓冲区）</strong><br>标准的IO基于字节流和字符流进行操作的，而NIO是基于通道（Channel）和缓冲区（Buffer）进行操作，数据总是从通道读取到缓冲区中，或者从缓冲区写入到通道中。</p><p><strong>Java NIO: Non-blocking IO（非阻塞IO）</strong><br>Java NIO可以让你非阻塞的使用IO，例如：当线程从通道读取数据到缓冲区时，线程还是可以进行其他事情。当数据被写入到缓冲区时，线程可以继续处理它。从缓冲区写入通道也类似。</p><p><strong>Java NIO: Selectors（选择器）</strong><br>Java NIO引入了选择器的概念，选择器用于监听多个通道的事件（比如：连接打开，数据到达）。因此，单个的线程可以监听多个数据通道。</p><p>Java NIO 由以下几个核心部分组成：</p><ul><li>Channels</li><li>Buffers</li><li>Selectors</li></ul><p>基本上，所有的 IO 在NIO 中都从一个Channel 开始。Channel 有点象流。 数据可以从Channel读到Buffer中，也可以从Buffer 写到Channel中。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-fb3310e57d93894e.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Channel和Buffer的读写"></p><p>Channel通道涵盖了UDP 和 TCP 网络IO，以及文件IO<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">FileChannel</span><br><span class="line">DatagramChannel</span><br><span class="line">SocketChannel</span><br><span class="line">ServerSocketChannel</span><br></pre></td></tr></table></figure></p><p>Buffer实现，覆盖了能通过IO发送的基本数据类型：<code>byte, short, int, long, float, double</code> 和 <code>char</code>。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">ByteBuffer</span><br><span class="line">CharBuffer</span><br><span class="line">DoubleBuffer</span><br><span class="line">FloatBuffer</span><br><span class="line">IntBuffer</span><br><span class="line">LongBuffer</span><br><span class="line">ShortBuffer</span><br></pre></td></tr></table></figure></p><p>Selector允许单线程处理多个 Channel。如果你的应用打开了多个连接（通道），但每个连接的流量都很低，使用Selector就会很方便。例如，在一个聊天服务器中。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-5e50515713aa3509.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="单线程使用Selector处理多Channel"></p><h4 id="一、NIO的使用"><a href="#一、NIO的使用" class="headerlink" title="一、NIO的使用"></a>一、NIO的使用</h4><h5 id="1-Channel"><a href="#1-Channel" class="headerlink" title="1.Channel"></a>1.Channel</h5><p><code>FileChannel</code> 从文件中读写数据。<br><code>DatagramChannel</code>能通过UDP读写网络中的数据。<br><code>SocketChannel</code> 能通过TCP读写网络中的数据。<br><code>ServerSocketChannel</code>可以监听新进来的TCP连接，像Web服务器那样。对每一个新进来的连接都会创建一个<code>SocketChannel</code>。</p><p>使用<code>FileChannel</code>读取数据到Buffer中的示例：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">RandomAccessFile aFile = <span class="keyword">new</span> RandomAccessFile(<span class="string">"data/nio-data.txt"</span>, <span class="string">"rw"</span>);</span><br><span class="line">FileChannel inChannel = aFile.getChannel();</span><br><span class="line"></span><br><span class="line">ByteBuffer buf = ByteBuffer.allocate(<span class="number">48</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> bytesRead = inChannel.read(buf);</span><br><span class="line"><span class="keyword">while</span> (bytesRead != -<span class="number">1</span>) &#123;</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">"Read "</span> + bytesRead);</span><br><span class="line">    buf.flip();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(buf.hasRemaining())&#123;</span><br><span class="line">        System.out.print((<span class="keyword">char</span>) buf.get());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    buf.clear();</span><br><span class="line">    bytesRead = inChannel.read(buf);</span><br><span class="line">&#125;</span><br><span class="line">aFile.close();</span><br></pre></td></tr></table></figure></p><h5 id="2-Buffer的使用"><a href="#2-Buffer的使用" class="headerlink" title="2.Buffer的使用"></a>2.Buffer的使用</h5><p>使用Buffer一般遵循下面几个步骤：</p><ul><li>分配空间（<code>ByteBuffer buf = ByteBuffer.allocate(1024);</code> 还有一种allocateDirector）</li><li>写入数据到Buffer(<code>int bytesRead = fileChannel.read(buf);</code>)</li><li>调用<code>filp()</code>方法（ <code>buf.flip();</code>）</li><li>从Buffer中读取数据（<code>System.out.print((char)buf.get());</code>）</li><li>调用<code>clear()</code>方法或者<code>compact()</code>方法</li></ul><p>Buffer顾名思义：缓冲区，实际上是一个容器，一个连续数组。Channel提供从文件、网络读取数据的渠道，但是读写的数据都必须经过Buffer。如下图：<br><img src="http://upload-images.jianshu.io/upload_images/8926909-ac43d8ab02212cbf.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="channel和buffer的读写"></p><p>向Buffer中写数据：</p><ul><li>从Channel写到Buffer (<code>fileChannel.read(buf)</code>)</li><li>通过Buffer的<code>put()</code>方法 （<code>buf.put(…)</code>）</li></ul><p>从Buffer中读取数据：</p><ul><li>从Buffer读取到Channel (<code>channel.write(buf)</code>)</li><li>使用<code>get()</code>方法从Buffer中读取数据 （<code>buf.get()</code>）</li></ul><p>可以把Buffer简单地理解为一组基本数据类型的元素列表，它通过几个变量来保存这个数据的当前位置状态：<code>capacity, position, limit, mark</code></p><h4 id="3-Selector"><a href="#3-Selector" class="headerlink" title="3.Selector"></a>3.Selector</h4><p>Selector的创建：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Selector selector = Selector.open();</span><br></pre></td></tr></table></figure></p><p>为了将Channel和Selector配合使用，必须将Channel注册到Selector上，通过SelectableChannel.register()方法来实现：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ssc= ServerSocketChannel.open();</span><br><span class="line">ssc.socket().bind(<span class="keyword">new</span> InetSocketAddress(PORT));</span><br><span class="line">ssc.configureBlocking(<span class="keyword">false</span>);</span><br><span class="line">ssc.register(selector, SelectionKey.OP_ACCEPT);</span><br></pre></td></tr></table></figure></p><blockquote><p><a href="http://ifeve.com/java-nio-all/" target="_blank" rel="noopener">《Java NIO 系列教程》</a><br><a href="http://www.importnew.com/19816.html" target="_blank" rel="noopener">《攻破JAVA NIO技术壁垒》</a></p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Java NIO(New IO)是一个可以替代标准Java IO API的IO API（从Java 1.4开始)，Java NIO提供了与标准IO不同的IO工作方式。&lt;br&gt;
    
    </summary>
    
      <category term="网络" scheme="https://tisonkong.github.io/github.io/categories/%E7%BD%91%E7%BB%9C/"/>
    
    
      <category term="IO" scheme="https://tisonkong.github.io/github.io/tags/IO/"/>
    
      <category term="NIO" scheme="https://tisonkong.github.io/github.io/tags/NIO/"/>
    
      <category term="并发" scheme="https://tisonkong.github.io/github.io/tags/%E5%B9%B6%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>简单说说HTTP协议</title>
    <link href="https://tisonkong.github.io/github.io/2018/10/12/java%20web%E5%9F%BA%E7%A1%80%E7%AF%87%E2%80%94%E2%80%94Http%E5%8D%8F%E8%AE%AE/"/>
    <id>https://tisonkong.github.io/github.io/2018/10/12/java web基础篇——Http协议/</id>
    <published>2018-10-12T13:11:46.435Z</published>
    <updated>2018-10-12T13:14:49.202Z</updated>
    
    <content type="html"><![CDATA[<h4 id="一、Http协议"><a href="#一、Http协议" class="headerlink" title="一、Http协议"></a>一、Http协议</h4><p>HTTP协议（HyperText Transfer Protocol，超文本传输协议）是用于从3W服务器传输超文本到本地浏览器的传送协议。它可以使浏览器更加高效，使网络传输减少。它不仅保证计算机正确快速地传输超文本文档，还确定传输文档中的哪一部分，以及哪部分内容首先显示(如文本先于图形)等。<br><a id="more"></a><br>HTTP是一个应用层协议，基于请求与响应模式的、无状态的、应用层的协议，常基于TCP的连接方式。HTTP使用统一资源标识符（Uniform Resource Identifiers, URI）来传输数据和建立连接。URL是一种特殊类型的URI，包含了用于查找某个资源的足够的信息URL，全称是<code>UniformResourceLocator</code>, 中文叫统一资源定位符,是互联网上用来标识某一处资源的地址。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-55c27c7d4394a5a0.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="请求响应模型"><br>HTTP协议定义Web客户端如何从Web服务器请求Web页面，以及服务器如何把Web页面传送给客户端。HTTP协议采用了请求/响应模型。客户端向服务器发送一个请求报文，请求报文包含请求的方法、URL、协议版本、请求头部和请求数据。服务器以一个状态行作为响应，响应的内容包括协议的版本、成功或者错误代码、服务器信息、响应头部和响应数据。以下是 HTTP 请求/响应的步骤：</p><h6 id="1、客户端连接到Web服务器"><a href="#1、客户端连接到Web服务器" class="headerlink" title="1、客户端连接到Web服务器"></a>1、客户端连接到Web服务器</h6><p>一个HTTP客户端，通常是浏览器，与Web服务器的HTTP端口（默认为80）建立一个TCP套接字连接。例如，<a href="https://www.jianshu.com/u/abcf0e8f851a" target="_blank" rel="noopener">https://www.jianshu.com/yitaicloud</a>。</p><h6 id="2、发送HTTP请求"><a href="#2、发送HTTP请求" class="headerlink" title="2、发送HTTP请求"></a>2、发送HTTP请求</h6><p>通过TCP套接字，客户端向Web服务器发送一个文本的请求报文，一个请求报文由请求行、请求头部、空行和请求数据4部分组成。</p><h6 id="3、服务器接受请求并返回HTTP响应"><a href="#3、服务器接受请求并返回HTTP响应" class="headerlink" title="3、服务器接受请求并返回HTTP响应"></a>3、服务器接受请求并返回HTTP响应</h6><p>Web服务器解析请求，定位请求资源。服务器将资源复本写到TCP套接字，由客户端读取。一个响应由状态行、响应头部、空行和响应数据4部分组成。</p><h6 id="4、释放连接TCP连接"><a href="#4、释放连接TCP连接" class="headerlink" title="4、释放连接TCP连接"></a>4、释放连接<a href="http://www.jianshu.com/p/ef892323e68f" target="_blank" rel="noopener">TCP连接</a></h6><p>若connection 模式为close，则服务器主动关闭<a href="http://www.jianshu.com/p/ef892323e68f" target="_blank" rel="noopener">TCP连接</a>，客户端被动关闭连接，释放<a href="http://www.jianshu.com/p/ef892323e68f" target="_blank" rel="noopener">TCP连接</a>;若connection 模式为keepalive，则该连接会保持一段时间，在该时间内可以继续接收请求;</p><h6 id="5、客户端浏览器解析HTML内容"><a href="#5、客户端浏览器解析HTML内容" class="headerlink" title="5、客户端浏览器解析HTML内容"></a>5、客户端浏览器解析HTML内容</h6><p>客户端浏览器首先解析状态行，查看表明请求是否成功的状态代码。然后解析每一个响应头，响应头告知以下为若干字节的HTML文档和文档的字符集。客户端浏览器读取响应数据HTML，根据HTML的语法对其进行格式化，并在浏览器窗口中显示。</p><p>例如：在浏览器地址栏键入URL，按下回车之后会经历以下流程：<br>1、浏览器向 DNS 服务器请求解析该 URL 中的域名所对应的 IP 地址;<br>2、解析出 IP 地址后，根据该 IP 地址和默认端口 80，和服务器建立<a href="http://www.jianshu.com/p/ef892323e68f" target="_blank" rel="noopener">TCP连接</a>;<br>3、浏览器发出读取文件(URL 中域名后面部分对应的文件)的HTTP 请求，该请求报文作为 <a href="http://www.jianshu.com/p/ef892323e68f" target="_blank" rel="noopener">TCP 三次握手</a>的第三个报文的数据发送给服务器;<br>4、服务器对浏览器请求作出响应，并把对应的 html 文本发送给浏览器;<br>5、释放 <a href="http://www.jianshu.com/p/ef892323e68f" target="_blank" rel="noopener">TCP连接</a>;<br>6、浏览器将该 html 文本并显示内容;</p><blockquote><p><a href="http://www.cnblogs.com/ranyonsue/p/5984001.html" target="_blank" rel="noopener">《关于HTTP协议，一篇就够了》</a></p></blockquote><p><a href="http://blog.csdn.net/zhuolou1208/article/details/78106420" target="_blank" rel="noopener">如果TCP有点生疏了，请戳下这里</a></p><p><img src="http://upload-images.jianshu.io/upload_images/8926909-9f9e6a0cb61243cb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Http协议在TCP/IP协议栈中位置"></p><h5 id="1-常用的HTTP方法有哪些？"><a href="#1-常用的HTTP方法有哪些？" class="headerlink" title="1.常用的HTTP方法有哪些？"></a>1.常用的HTTP方法有哪些？</h5><p>GET： 用于请求访问已经被URI（统一资源标识符）识别的资源，可以通过URL传参给服务器。<br>POST：用于传输信息给服务器，主要功能与GET方法类似，但一般推荐使用POST方式。<br>PUT： 传输文件，报文主体中包含文件内容，保存到对应URI位置。<br>DELETE：删除文件，与PUT方法相反，删除对应URI位置的文件。<br>HEAD： 获得报文首部，与GET方法类似，只是不返回报文主体，一般用于验证URI是否有效。<br>OPTIONS：查询相应URI支持的HTTP方法。</p><h5 id="2-GET方法与POST方法的区别"><a href="#2-GET方法与POST方法的区别" class="headerlink" title="2.GET方法与POST方法的区别"></a>2.GET方法与POST方法的区别</h5><p><strong>区别一：</strong><br>get重点在从服务器上获取资源，post重点在向服务器发送数据；<br><strong>区别二：</strong></p><ul><li>get传输数据是通过URL请求，以field（字段）= value的形式，置于URL后，并用”?”连接，多个请求数据间用”&amp;”连接，如<a href="http://127.0.0.1/Test/login.action?name=admin&amp;password=admin，这个过程用户是可见的；" target="_blank" rel="noopener">http://127.0.0.1/Test/login.action?name=admin&amp;password=admin，这个过程用户是可见的；</a></li><li>post传输数据通过Http的post机制，将字段与对应值封存在请求实体中发送给服务器，这个过程对用户是不可见的；</li></ul><p><strong>区别三：</strong><br>Get传输的数据量小，因为受URL长度限制，但效率较高；<br>Post可以传输大量数据，所以上传文件时只能用Post方式；<br><strong>区别四：</strong><br>get是不安全的，因为URL是可见的，可能会泄露私密信息，如密码等(页面可以被缓存，或者获得历史访问记录)；<br>post较get安全性较高；<br><strong>区别五：</strong><br>get方式只能支持ASCII字符，向服务器传的中文字符可能会乱码。<br>post支持标准字符集，可以正确传递中文字符。</p><h5 id="3-HTTP请求报文与响应报文格式"><a href="#3-HTTP请求报文与响应报文格式" class="headerlink" title="3.HTTP请求报文与响应报文格式"></a>3.HTTP请求报文与响应报文格式</h5><p>请求报文包含三部分：<br>a、请求行：包含请求方法、URI、HTTP版本信息<br>b、请求首部字段<br>c、请求内容实体<br>响应报文包含三部分：<br>a、状态行：包含HTTP版本、状态码、状态码的原因短语<br>b、响应首部字段<br>c、响应内容实体</p><blockquote><p><strong>返回的状态</strong><br>1xx：指示信息–表示请求已接收，继续处理<br>2xx：成功–表示请求已被成功接收、理解、接受<br>3xx：重定向–要完成请求必须进行更进一步的操作<br>4xx：客户端错误–请求有语法错误或请求无法实现<br>5xx：服务器端错误–服务器未能实现合法的请求</p><p><strong>200：请求被正常处理</strong><br>204：请求被受理但没有资源可以返回<br>206：客户端只是请求资源的一部分，服务器只对请求的部分资源执行GET方法，相应报文中通过Content-Range指定范围的资源。<br>301：永久性重定向<br><strong>302：临时重定向</strong><br>303：与302状态码有相似功能，只是它希望客户端在请求一个URI的时候，能通过GET方法重定向到另一个URI上<br>304：发送附带条件的请求时，条件不满足时返回，与重定向无关<br>307：临时重定向，与302类似，只是强制要求使用POST方法<br>400：请求报文语法有误，服务器无法识别<br>401：请求需要认证<br>403：请求的对应资源禁止被访问<br><strong>404：服务器无法找到对应资源</strong><br><strong>500：服务器内部错误</strong><br>503：服务器正忙</p></blockquote><h5 id="4-常见HTTP首部字段"><a href="#4-常见HTTP首部字段" class="headerlink" title="4.常见HTTP首部字段"></a>4.常见HTTP首部字段</h5><p><img src="http://upload-images.jianshu.io/upload_images/8926909-b5c977f5065f41a1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Http请求截图"></p><p>a、通用首部字段（请求报文与响应报文都会使用的首部字段）<br>Date：创建报文时间<br>Connection：连接的管理<br>Cache-Control：缓存的控制<br>Transfer-Encoding：报文主体的传输编码方式</p><p>b、请求首部字段（请求报文会使用的首部字段）<br>Host：请求资源所在服务器<br>Accept：可处理的媒体类型<br>Accept-Charset：可接收的字符集<br>Accept-Encoding：可接受的内容编码<br>Accept-Language：可接受的自然语言</p><p>c、响应首部字段（响应报文会使用的首部字段）<br>Accept-Ranges：可接受的字节范围<br>Location：令客户端重新定向到的URI<br>Server：HTTP服务器的安装信息</p><p>d、实体首部字段（请求报文与响应报文的的实体部分使用的首部字段）<br>Allow：资源可支持的HTTP方法<br>Content-Type：实体主类的类型<br>Content-Encoding：实体主体适用的编码方式<br>Content-Language：实体主体的自然语言<br>Content-Length：实体主体的的字节数<br>Content-Range：实体主体的位置范围，一般用于发出部分请求时使用</p><h5 id="5-HTTP1-1版本新特性"><a href="#5-HTTP1-1版本新特性" class="headerlink" title="5.HTTP1.1版本新特性"></a>5.HTTP1.1版本新特性</h5><p>先上答案，然后再后面详细解释：<br>a、默认持久连接，节省通信量，只要客户端服务端任意一端没有明确提出断开TCP连接，就一直保持连接，可以发送多次HTTP请求<br>b、管线化，客户端可以同时发出多个HTTP请求，而不用一个个等待响应<br>c、断点续传原理</p><p>HTTP 1.1支持持久连接，在一个TCP连接上可以传送多个HTTP请求和响应，减少了建立和关闭连接的消耗和延迟。一个包含有许多图像的网页文件的多个请求和应答可以在一个连接中传输，但每个单独的网页文件的请求和应答仍然需要使用各自的连接。HTTP 1.1还允许客户端不用等待上一次请求结果返回，就可以发出下一次请求，但服务器端必须按照接收到客户端请求的先后顺序依次回送响应结果，以保证客户端能够区分出每次请求的响应内容，这样也显著地减少了整个下载过程所需要的时间。基于HTTP 1.1协议的客户机与服务器的信息交换过程。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-cfd0517c7223404b.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Http1.1工作"></p><blockquote><p><a href="http://blog.csdn.net/elifefly/article/details/3964766/" target="_blank" rel="noopener">【HTTP 1.1与HTTP 1.0的比较HTTP 1.1与HTTP 1.0的比较】</a></p></blockquote><h5 id="6-HTTP的缺点与HTTPS"><a href="#6-HTTP的缺点与HTTPS" class="headerlink" title="6.HTTP的缺点与HTTPS"></a>6.HTTP的缺点与HTTPS</h5><p>a、通信使用明文不加密，内容可能被窃听<br>b、不验证通信方身份，可能遭到伪装<br>c、无法验证报文完整性，可能被篡改<br>HTTPS就是HTTP加上加密处理（一般是SSL安全通信线路）+认证+完整性保护</p><h5 id="7-HTTP优化"><a href="#7-HTTP优化" class="headerlink" title="7.HTTP优化"></a>7.HTTP优化</h5><p>利用负载均衡优化和加速HTTP应用<br>利用HTTP Cache来优化网站</p><p>补充一点<strong>Cache</strong>的细节：<br>缓存是一个到处都存在的用空间换时间的例子。通过使用多余的空间，我们能够获取更快的速度。用户在浏览网站的时候，浏览器能够在本地保存网站中的图片或者其他文件的副本，这样用户再次访问该网站的时候，浏览器就不用再下载全部的文件，减少了下载量意味着提高了页面加载的速度。<br>缓存非常有用，但是也带来了一定的缺陷。当我们的网站发生了更新的时候，比如说Logo换了，浏览器本地仍保存着旧版本的Logo，那么浏览器如何来确定使用本地文件还是使用服务器上的新文件？下面来介绍几种判断的方法。</p><ul><li>Caching Method 1：Last-Modified<br>服务器为了通知浏览器当前文件的版本，会发送一个上次修改时间的标签，例如：<br>Last-modified: Fri, 16 Mar 2007 04:00:25 GMT</li><li>Caching Method 2: ETag<br>通常情况下，通过修改时间来比较文件是可行的。但是在一些特殊情况，例如服务器的时钟发生了错误，服务器时钟进行修改，夏时制DST到来后服务器时间没有及时更新，这些都会引起通过修改时间比较文件版本的问题。<br>ETag可以用来解决这种问题。ETag是一个文件的唯一标志符。就像一个哈希或者指纹，每个文件都有一个单独的标志，只要这个文件发生了改变，这个标志就会发生变化。如同 Last-modified 一样，ETag 解决了文件版本比较的问题。只不过 ETag 的级别比 Last-Modified 高一些。</li><li>Caching Method 3：Expires<br>缓存一个文件，并且与服务器确认版本的方式非常好，但是仍有一个缺点，我们必须连接服务器。每次使用前都进行一次比较，这种方法很安全，但还不是最好的。我们可以使用 Expiration Date 来减少这种请求。</li><li>Caching Method 4：Max-age<br>Expires的方法很好，但是我们每次都得算一个精确的时间。max-age 标签可以让我们更加容易的处理过期时间。我们只需要说，这份资料你只能用一个星期就可以了。<blockquote><p><a href="http://www.cnblogs.com/cocowool/archive/2011/08/22/2149929.html" target="_blank" rel="noopener">【利用HTTP Cache来优化网站】</a></p></blockquote></li></ul><h5 id="8-跨域问题"><a href="#8-跨域问题" class="headerlink" title="8.跨域问题"></a>8.跨域问题</h5><p>浏览器的同源策略会导致跨域，这里同源策略又分为以下两种</p><ul><li>DOM同源策略：禁止对不同源页面DOM进行操作。这里主要场景是iframe跨域的情况，不同域名的iframe是限制互相访问的。</li><li>XmlHttpRequest同源策略：禁止使用XHR对象向不同源的服务器地址发起HTTP请求。</li></ul><p>只要协议、域名、端口有任何一个不同，都被当作是不同的域，之间的请求就是跨域操作。<br>要解决跨域问题可以参考以下文章：</p><blockquote><p><a href="https://zhuanlan.zhihu.com/p/28562290" target="_blank" rel="noopener">《跨域的那些事儿》</a><br><a href="http://www.ruanyifeng.com/blog/2016/04/cors.html" target="_blank" rel="noopener">《跨域资源共享 CORS 详解》</a> 阮一峰大神的精品</p></blockquote><p>另外玉刚写了一篇不错的，角度是站在前端来看的<br><a href="https://mp.weixin.qq.com/s?src=11&amp;timestamp=1534170599&amp;ver=1058&amp;signature=NRB3iKd0Se5un2PbMTA2IjwRKl56ROP2rapEPAUXnW*zwI0itn4bonQ4ieM*-qxl0nLLyYQYkCkMrcpB5Bl1j8ujt6spFS3BEPfdIXVZvx5o6LXKHjDi74Zu6Mn2xsvB&amp;new=1" target="_blank" rel="noopener">《HTTP 必知必会的那些》</a></p>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;一、Http协议&quot;&gt;&lt;a href=&quot;#一、Http协议&quot; class=&quot;headerlink&quot; title=&quot;一、Http协议&quot;&gt;&lt;/a&gt;一、Http协议&lt;/h4&gt;&lt;p&gt;HTTP协议（HyperText Transfer Protocol，超文本传输协议）是用于从3W服务器传输超文本到本地浏览器的传送协议。它可以使浏览器更加高效，使网络传输减少。它不仅保证计算机正确快速地传输超文本文档，还确定传输文档中的哪一部分，以及哪部分内容首先显示(如文本先于图形)等。&lt;br&gt;
    
    </summary>
    
      <category term="网络" scheme="https://tisonkong.github.io/github.io/categories/%E7%BD%91%E7%BB%9C/"/>
    
    
      <category term="HTTP协议" scheme="https://tisonkong.github.io/github.io/tags/HTTP%E5%8D%8F%E8%AE%AE/"/>
    
      <category term="网络" scheme="https://tisonkong.github.io/github.io/tags/%E7%BD%91%E7%BB%9C/"/>
    
  </entry>
  
  <entry>
    <title>一致性哈希算法原理和实现</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/%E4%B8%80%E8%87%B4%E6%80%A7%E5%93%88%E5%B8%8C%E7%AE%97%E6%B3%95%E5%8E%9F%E7%90%86%E5%92%8C%E5%AE%9E%E7%8E%B0/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/一致性哈希算法原理和实现/</id>
    <published>2018-05-20T15:13:16.234Z</published>
    <updated>2018-05-20T15:17:54.085Z</updated>
    
    <content type="html"><![CDATA[<p>在做服务器<strong>负载均衡</strong>时候可供选择的算法有很多，包括： 轮循算法（Round Robin）、哈希算法（HASH）、最少连接算法（Least Connection）、响应速度算法（Response Time）、加权法（Weighted ）等。其中哈希算法是最为常用的算法，比如在<code>nginx+ats / haproxy+squid</code>等CDN架构中，nginx/haproxy所使用的负载均衡算法便是一致性哈希。不仅如此在分布式系统中哈希算法也得到了广泛应用，研究过memcached缓存数据库的人都知道，memcached服务器端本身不提供分布式cache的一致性，而是由客户端来提供，使用一致性哈希的好处在于，增减集群的缓存服务器时，只有少量的缓存会失效，回源量较小。<br><a id="more"></a></p><h3 id="1-问题的背景"><a href="#1-问题的背景" class="headerlink" title="1.问题的背景"></a>1.问题的背景</h3><p>假设一个分布式任务调度系统（负载均衡、分布式缓存服务器的常见该场景），执行任务的节点有n台机器，现有m个job在这n台机器上运行，这m个Job需要逐一映射到n个节点中一个，这时候可以选择一种简单的Hash算法来让m个Job可以均匀分布到n个节点中，比如:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hash(Job) % n</span><br></pre></td></tr></table></figure></p><p><strong><em>替换成分布式Cache也一样的有上述场景：有n个 cache 服务器（后面简称 cache ），那么如何将一个对象 object 映射到n个 cache 上呢</em></strong><br>上面的公式看上去很完美，但考虑如下两种情形：</p><ul><li>n个节点中有一个宕掉了，这时候节点数量变更为<code>n-1</code>，此时的映射公式变成 <code>hash(Job) % (n-1)</code></li><li>由于Job数量增加，需要新增机器，此时的映射公式变成 <code>hash(Job) % (n+1)</code></li></ul><p>两种情形可以看到，基本上所有的Job会被重新分配到跟节点变动前不同的节点上，意味着需要迁移几乎所有正在运行的Job，想想这样会给系统带来多大的复杂性和性能损耗。缓存失效相当于少了一个调节池，对于服务器而言也是一场灾难，洪水般的访问都会直接冲向后台服务器；再来考虑第三个问题，由于硬件能力越来越强，你可能想让后面添加的节点多做点活，显然上面的 hash 算法也做不到。有什么方法可以改变这个状况呢，一致性哈希的解决方案就出来了，<strong>它用于尽可能地降低节点变动带来的数据迁移开销</strong>。</p><h3 id="2-一致性Hash性质"><a href="#2-一致性Hash性质" class="headerlink" title="2.一致性Hash性质"></a>2.一致性Hash性质</h3><p>考虑到分布式系统每个节点都有可能失效，并且新的节点很可能动态的增加进来，如何保证当系统的节点数目发生变化时仍然能够对外提供良好的服务，这是值得考虑的，尤其实在设计分布式缓存系统时，如果某台服务器失效，对于整个系统来说如果不采用合适的算法来保证一致性，那么缓存于系统中的所有数据都可能会失效（即由于系统节点数目发生动态变化时，客户端在请求某一对象时需要重新计算其hash值，由于hash值已经改变，所以很可能找不到保存该对象的服务器节点），因此一致性hash就显得至关重要，良好的分布式cahce系统中的一致性hash算法应该满足以下几个方面：</p><ul><li><p>平衡性(Balance)<br>平衡性是指哈希的结果能够尽可能分布到所有的缓冲中去，这样可以使得所有的缓冲空间都得到利用。</p></li><li><p>单调性(Monotonicity)<br>该性质是最需要考量的因素，单调性是指如果已经有一些内容通过哈希分派到了相应的缓冲中，又有新的缓冲区加入到系统中，那么哈希的结果应能够保证原有已分配的内容可以被映射到新的缓冲区中去，而不会被映射到旧的缓冲集合中的其他缓冲区（<strong>简单理解：增加新节点，原有的部分内容可以映射过来</strong>）。简单的哈希算法往往不能满足单调性的要求，哈希结果的变化意味着当缓冲空间发生变化时，所有的映射关系需要在系统内全部更新，因此会带来极大计算和传输负荷。</p></li><li><p>分散性(Spread)<br>好的哈希算法应能够尽量避免不一致的情况发生，也就是尽量降低分散性。</p></li><li><p>负载(Load)<br>负载问题实际上是从另一个角度看待分散性问题。既然不同的终端可能将相同的内容映射到不同的缓冲区中，那么对于一个特定的缓冲区而言，也可能被不同的用户映射为不同的内容。与分散性一样，这种情况也是应当避免的，因此好的哈希算法应能够尽量降低缓冲的负荷。</p></li><li><p>平滑性(Smoothness)<br>平滑性是指缓存服务器的数目平滑改变和缓存对象的平滑改变是一致的。</p></li></ul><blockquote><p>参考： <a href="http://www.cnblogs.com/lpfuture/p/5796398.html" target="_blank" rel="noopener">一致性哈希算法原理</a></p></blockquote><h3 id="3-一致性哈希算法原理"><a href="#3-一致性哈希算法原理" class="headerlink" title="3.一致性哈希算法原理"></a>3.一致性哈希算法原理</h3><p>一致性哈希将整个哈希值空间组织成一个虚拟的圆环，如假设某哈希函数H的值空间为0 ~ 2^32^-1（即哈希值是一个32位无符号整形），整个空间按顺时针方向组织，0和2^32^-1在零点中方向重合，整个哈希空间环如下：<br><img src="https://upload-images.jianshu.io/upload_images/8926909-4afc55f9cdc9d9cc.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="虚拟闭环"></p><p>下一步将各个服务器使用Hash进行一个哈希，具体可以选择服务器的ip或主机名作为关键字进行哈希，这样每台机器就能确定其在哈希环上的位置，这里假设将上文中四台服务器使用ip地址哈希后在环空间的位置如下：<br><img src="https://upload-images.jianshu.io/upload_images/8926909-4d427d233da6e9e1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="计算服务器哈希位置"></p><p>接下来使用如下算法定位数据访问到相应服务器：将数据key使用相同的函数Hash计算出哈希值，并确定此数据在环上的位置，从此位置沿环顺时针“行走”，第一台遇到的服务器就是其应该定位到的服务器。例如有Object A、Object B、Object C、Object D四个数据对象，经过哈希计算后，在环空间上的位置如下：<br><img src="https://upload-images.jianshu.io/upload_images/8926909-395bbb57055f6273.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="计算数据哈希位置"></p><p>根据一致性哈希算法，数据A会被定为到Node A上，B被定为到Node B上，C被定为到Node C上，D被定为到Node D上。</p><p>下面分析一致性哈希算法的容错性和可扩展性。现假设Node C不幸宕机，可以看到此时对象A、B、D不会受到影响，只有C对象被重定位到Node D。一般的，在一致性哈希算法中，如果一台服务器不可用，则受影响的数据仅仅是此服务器到其环空间中前一台服务器（即沿着逆时针方向行走遇到的第一台服务器）之间数据，其它不会受到影响。<br>下面考虑另外一种情况，如果在系统中增加一台服务器Node X，如下图所示：<br><img src="https://upload-images.jianshu.io/upload_images/8926909-4b10238d1ac10779.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="增加服务器节点X"></p><p>此时对象Object A、B、D不受影响，只有对象C需要重定位到新的Node X 。一般的，在一致性哈希算法中，如果增加一台服务器，则受影响的数据仅仅是新服务器到其环空间中前一台服务器（即沿着逆时针方向行走遇到的第一台服务器）之间数据，其它数据也不会受到影响。</p><p>综上所述，一致性哈希算法对于节点的增减都只需重定位环空间中的一小部分数据，具有较好的容错性和可扩展性。另外，一致性哈希算法在服务节点太少时，容易因为节点分部不均匀而造成数据倾斜问题。例如系统中只有两台服务器，其环分布如下:<br><img src="https://upload-images.jianshu.io/upload_images/8926909-056e9e92f51c4925.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="稀疏节点的哈希闭环"></p><p>此时必然造成大量数据集中到Node A上，而只有极少量会定位到Node B上。为了解决这种数据倾斜问题，一致性哈希算法引入了虚拟节点机制，即对每一个服务节点计算多个哈希，每个计算结果位置都放置一个此服务节点，称为虚拟节点。具体做法可以在服务器ip或主机名的后面增加编号来实现。例如上面的情况，可以为每台服务器计算三个虚拟节点，于是可以分别计算 “Node A#1”、“Node A#2”、“Node A#3”、“Node B#1”、“Node B#2”、“Node B#3”的哈希值，于是形成六个虚拟节点：<br><img src="https://upload-images.jianshu.io/upload_images/8926909-34654c5540eb38e2.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="虚拟化的哈希闭环"></p><h3 id="4-一致性Hash的java实现"><a href="#4-一致性Hash的java实现" class="headerlink" title="4.一致性Hash的java实现"></a>4.一致性Hash的java实现</h3><p><strong>1.确定哈希值空间</strong><br>考虑通常的 hash 算法都是将 value 映射到一个 32 为的 key 值，也即是 0~2^32^-1 次方的数值空间</p><p><strong>2.计算节点哈希</strong><br>将节点Node向这个值空间映射，取Node的Hash值，选取一个可以固定标识一个Node的属性值进行Hashing，假设以字符串形式输入，可以取Node标识的md5值，然后截取其中32位作为映射值。md5取值如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">byte</span>[] md5(String value) &#123;</span><br><span class="line">    MessageDigest md5;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        md5 = MessageDigest.getInstance(<span class="string">"MD5"</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (NoSuchAlgorithmException e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(e.getMessage(), e);</span><br><span class="line">    &#125;</span><br><span class="line">    md5.reset();</span><br><span class="line">    <span class="keyword">byte</span>[] bytes;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        bytes = value.getBytes(<span class="string">"UTF-8"</span>);</span><br><span class="line">    &#125; <span class="keyword">catch</span> (UnsupportedEncodingException e) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(e.getMessage(), e);</span><br><span class="line">    &#125;</span><br><span class="line">    md5.update(bytes);</span><br><span class="line">    <span class="keyword">return</span> md5.digest();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p> 因为映射值只需要32位即可，所以可以利用以下方式计算最终值（number取0即可）：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">long</span> <span class="title">hash</span><span class="params">(<span class="keyword">byte</span>[] digest, <span class="keyword">int</span> number)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (((<span class="keyword">long</span>) (digest[<span class="number">3</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>) &lt;&lt; <span class="number">24</span>)</span><br><span class="line">            | ((<span class="keyword">long</span>) (digest[<span class="number">2</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>) &lt;&lt; <span class="number">16</span>)</span><br><span class="line">            | ((<span class="keyword">long</span>) (digest[<span class="number">1</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>) &lt;&lt; <span class="number">8</span>)</span><br><span class="line">            | (digest[<span class="number">0</span> + number * <span class="number">4</span>] &amp; <span class="number">0xFF</span>))</span><br><span class="line">            &amp; <span class="number">0xFFFFFFFFL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong>3.有序缓存节点信息</strong><br>把n个节点Node通过以上方式取得hash值，映射到环形值空间。算法中，将以有序Map的形式在内存中缓存每个节点的Hash值对应的物理节点信息。缓存于这个内存变量中：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> TreeMap&lt;Long, String&gt; virtualNodes ;</span><br></pre></td></tr></table></figure></p><p><strong>4.数据向值空间映射</strong><br>数据Job取hash的方式跟节点Node的方式一模一样，可以使用上述<code>md5 =&gt; hash</code>的方式同样将所有Job取得Hash映射到这个环中。</p><p><strong>5.数据和节点映射</strong><br>当节点和数据都被映射到这个环上后，可以设定一个规则把哪些数据hash值放在哪些节点Node Hash值上了，规则就是：<strong>沿着顺时针方向，数据hash值向后找到第一个Node Hash值即认为该数据hash值对应的数据映射到该Node上。至此，这一个从数据到节点的映射关系就确定了</strong>。顺时针找下一个Node Hash值算法如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">select</span><span class="params">(Trigger trigger)</span> </span>&#123;</span><br><span class="line">     String key = trigger.toString();</span><br><span class="line">     <span class="keyword">byte</span>[] digest = md5(key);</span><br><span class="line">     String node = selectForKey(hash(digest, <span class="number">0</span>));</span><br><span class="line">     <span class="keyword">return</span> node;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">//根据数据的哈希值计算出所归属的节点Node</span></span><br><span class="line"> <span class="function"><span class="keyword">private</span> String <span class="title">selectForKey</span><span class="params">(<span class="keyword">long</span> hash)</span> </span>&#123;</span><br><span class="line">     String node;</span><br><span class="line">     Long key = hash;</span><br><span class="line">     <span class="keyword">if</span> (!virtualNodes.containsKey(key)) &#123;</span><br><span class="line">         SortedMap&lt;Long, String&gt; tailMap = virtualNodes.tailMap(key);</span><br><span class="line">         <span class="keyword">if</span> (tailMap.isEmpty()) &#123;</span><br><span class="line">             key = virtualNodes.firstKey();</span><br><span class="line">         &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">             key = tailMap.firstKey();</span><br><span class="line">         &#125;</span><br><span class="line">     &#125;</span><br><span class="line">     node = virtualNodes.get(key);</span><br><span class="line">     <span class="keyword">return</span> node;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></p><p>Trigger是对Job一次触发任务的抽象，这里可忽略关注，重写了toString方法返回一个标记一个Job的唯一标志，计算Hash值，从节点Hash值中按规则寻找。</p><p><strong>6.算法优化-虚拟节点</strong><br>上述算法过程，会想到两个问题，第一，数据对象会不会分布不均匀，特别是新增节点或者减少节点时；第二，前文提到的如果想让部分节点多映射到一些数据对象，如何处理。虚拟节点这是解决这个问题。将一个物理节点虚拟出一定数量的虚拟节点，分散到这个值空间上，需要尽可能地随机分散开。</p><p>假设有4个物理节点Node，环上的每个色块代表一个虚拟节点涵盖的hash值区域，每种颜色代表一个物理节点。当物理节点较少时，虚拟节点数需要更高来确保更好的一致性表现。经测试，在物理节点为个位数时，虚拟节点可设置为160个，此时可带来较好的表现（后文会给出测试结果，160*n个总节点数情况下，如果发生一个节点变动，映射关系变化率基本为1/n，达到预期）。<br><img src="https://upload-images.jianshu.io/upload_images/8926909-e188847813d04828.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="虚拟化的hash环"></p><p>具体做算法实现时，已知物理节点，虚拟节点数设置为160，可将这160*n的节点计算出Hash值，以Hash值为key，以物理节点标识为value，以有序Map的形式在内存中缓存，作为后续计算数据对象对应的物理节点时的查询数据。代码如下，virtualNodes中缓存着所有虚拟节点Hash值对应的物理节点信息。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ConsistentHash</span><span class="params">(List&lt;String&gt; nodes)</span> </span>&#123;</span><br><span class="line">     <span class="keyword">this</span>.virtualNodes = <span class="keyword">new</span> TreeMap&lt;&gt;();</span><br><span class="line">     <span class="keyword">this</span>.identityHashCode = identityHashCode(nodes);</span><br><span class="line">     <span class="keyword">this</span>.replicaNumber = <span class="number">160</span>;</span><br><span class="line">     <span class="keyword">for</span> (String node : nodes) &#123;</span><br><span class="line">         <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; replicaNumber / <span class="number">4</span>; i++) &#123;</span><br><span class="line">             <span class="keyword">byte</span>[] digest = md5(node.toString() + i);</span><br><span class="line">             <span class="keyword">for</span> (<span class="keyword">int</span> h = <span class="number">0</span>; h &lt; <span class="number">4</span>; h++) &#123;</span><br><span class="line">                 <span class="keyword">long</span> m = hash(digest, h);</span><br><span class="line">                 virtualNodes.put(m, node);</span><br><span class="line">             &#125;</span><br><span class="line">         &#125;</span><br><span class="line">     &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></p><blockquote><p>参考：<a href="https://my.oschina.net/yaohonv/blog/1610096" target="_blank" rel="noopener">深入一致性哈希(Consistent Hashing)算法原理</a></p></blockquote><p>总结：</p><ul><li>一致性哈希算法解决了负载均衡和分布式缓存中动态分配问题，使得节点的动态变化（服务器宕机、新增服务器节点）所造成的代价降到最低。</li><li><strong>计算机的任何问题都可以通过增加一个虚拟层来解决</strong>，计算机硬件、网络和软件都是如此，网络的7层协议，每一层都可以看做是下一层的虚拟层，操作系统可以看做是计算机硬件的虚拟层，java虚拟机可以看做是操作系统的虚拟层。上述的解决节点负载不均衡的问题也采用了虚拟化的思路，将每台物理缓存服务器虚拟为缓存服务器，将虚拟服务器的hash值放在hash环上，数据key首先去寻找虚拟服务器节点，再映射得到真正的物理服务器的信息，这个思想可以在解决计算机问题上多多借鉴。</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在做服务器&lt;strong&gt;负载均衡&lt;/strong&gt;时候可供选择的算法有很多，包括： 轮循算法（Round Robin）、哈希算法（HASH）、最少连接算法（Least Connection）、响应速度算法（Response Time）、加权法（Weighted ）等。其中哈希算法是最为常用的算法，比如在&lt;code&gt;nginx+ats / haproxy+squid&lt;/code&gt;等CDN架构中，nginx/haproxy所使用的负载均衡算法便是一致性哈希。不仅如此在分布式系统中哈希算法也得到了广泛应用，研究过memcached缓存数据库的人都知道，memcached服务器端本身不提供分布式cache的一致性，而是由客户端来提供，使用一致性哈希的好处在于，增减集群的缓存服务器时，只有少量的缓存会失效，回源量较小。&lt;br&gt;
    
    </summary>
    
      <category term="算法" scheme="https://tisonkong.github.io/github.io/categories/%E7%AE%97%E6%B3%95/"/>
    
    
      <category term="一致性哈希" scheme="https://tisonkong.github.io/github.io/tags/%E4%B8%80%E8%87%B4%E6%80%A7%E5%93%88%E5%B8%8C/"/>
    
      <category term="算法，负载均衡" scheme="https://tisonkong.github.io/github.io/tags/%E7%AE%97%E6%B3%95%EF%BC%8C%E8%B4%9F%E8%BD%BD%E5%9D%87%E8%A1%A1/"/>
    
  </entry>
  
  <entry>
    <title>Web服务器及Tomcat总结</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%20web%E5%9F%BA%E7%A1%80%E7%AF%87%E2%80%94%E2%80%94Web%E6%9C%8D%E5%8A%A1%E5%8F%8ATomcat%E6%80%BB%E7%BB%93/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java web基础篇——Web服务及Tomcat总结/</id>
    <published>2018-05-20T14:36:53.393Z</published>
    <updated>2018-05-20T14:38:33.596Z</updated>
    
    <content type="html"><![CDATA[<h4 id="一、Web服务"><a href="#一、Web服务" class="headerlink" title="一、Web服务"></a>一、Web服务</h4><h5 id="1-web服务器分类"><a href="#1-web服务器分类" class="headerlink" title="1.web服务器分类"></a>1.web服务器分类</h5><p>Unix和Linux平台下使用最广泛的免费HTTP服务器是Apache服务器，而Windows平台的服务器通常使用IIS作为Web服务器。选择Web服务器应考虑的因素有：性能、安全性、日志和统计、虚拟主机、代理服务器、缓冲服务和集成应用程序等。<a id="more"></a>下面是对常见服务器的简介： </p><ul><li><p>IIS：Microsoft的Web服务器产品，全称是Internet Information Services。IIS是允许在公共Intranet或Internet上发布信息的Web服务器。IIS是目前最流行的Web服务器产品之一，很多著名的网站都是建立在IIS的平台上。IIS提供了一个图形界面的管理工具，称为Internet服务管理器，可用于监视配置和控制Internet服务。IIS是一种Web服务组件，其中包括Web服务器、FTP服务器、NNTP服务器和SMTP服务器，分别用于网页浏览、文件传输、新闻服务和邮件发送等方面，它使得在网络（包括互联网和局域网）上发布信息成了一件很容易的事。它提供ISAPI(Intranet Server API）作为扩展Web服务器功能的编程接口；同时，它还提供一个Internet数据库连接器，可以实现对数据库的查询和更新。 </p></li><li><p>Kangle：Kangle Web服务器是一款跨平台、功能强大、安全稳定、易操作的高性能Web服务器和反向代理服务器软件。此外，Kangle也是一款专为做虚拟主机研发的Web服务器。实现虚拟主机独立进程、独立身份运行。用户之间安全隔离，一个用户出问题不影响其他用户。支持PHP、ASP、ASP.NET、Java、Ruby等多种动态开发语言。 </p></li><li>WebSphere：WebSphere Application Server是功能完善、开放的Web应用程序服务器，是IBM电子商务计划的核心部分，它是基于Java的应用环境，用于建立、部署和管理Internet和Intranet Web应用程序，适应各种Web应用程序服务器的需要。 </li><li>WebLogic：WebLogic Server是一款多功能、基于标准的Web应用服务器，为企业构建企业应用提供了坚实的基础。针对各种应用开发、关键性任务的部署，各种系统和数据库的集成、跨Internet协作等Weblogic都提供了相应的支持。由于它具有全面的功能、对开放标准的遵从性、多层架构、支持基于组件的开发等优势，很多公司的企业级应用都选择它来作为开发和部署的环境。WebLogic Server在使应用服务器成为企业应用架构的基础方面一直处于领先地位，为构建集成化的企业级应用提供了稳固的基础。 </li><li>Apache：目前Apache仍然是世界上用得最多的Web服务器，其市场占有率很长时间都保持在60%以上（目前的市场份额约40%左右）。世界上很多著名的网站都是Apache的产物，它的成功之处主要在于它的源代码开放、有一支强大的开发团队、支持跨平台的应用（可以运行在几乎所有的Unix、Windows、Linux系统平台上）以及它的可移植性等方面。 </li><li>Tomcat：Tomcat是一个开放源代码、运行Servlet和JSP的容器。Tomcat实现了Servlet和JSP规范。此外，Tomcat还实现了Apache-Jakarta规范而且比绝大多数商业应用软件服务器要好，因此目前也有不少的Web服务器都选择了Tomcat。 </li><li>Nginx：读作”engine x”，是一个高性能的HTTP和反向代理服务器，也是一个IMAP/POP3/SMTP代理服务器。 Nginx是由Igor Sysoev为俄罗斯访问量第二的<a href="http://www.rambler.ru/" target="_blank" rel="noopener">Rambler</a>站点开发的，第一个公开版本0.1.0发布于2004年10月4日。其将源代码以类BSD许可证的形式发布，因它的稳定性、丰富的功能集、示例配置文件和低系统资源的消耗而闻名。在2014年下半年，Nginx的市场份额达到了14%。</li></ul><h5 id="2、web-xml文件中可以配置哪些内容？"><a href="#2、web-xml文件中可以配置哪些内容？" class="headerlink" title="2、web.xml文件中可以配置哪些内容？"></a>2、web.xml文件中可以配置哪些内容？</h5><p>答：web.xml用于配置Web应用的相关信息，如：监听器（listener）、过滤器（filter）、 Servlet、相关参数、会话超时时间、安全验证方式、错误页面等，下面是一些开发中常见的配置：<br>① 配置Spring上下文加载监听器加载Spring配置文件并创建IoC容器：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">context-param</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>classpath:applicationContext.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">context-param</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">listener</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">listener-class</span>&gt;</span></span><br><span class="line">     org.springframework.web.context.ContextLoaderListener</span><br><span class="line">   <span class="tag">&lt;/<span class="name">listener-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">listener</span>&gt;</span></span><br></pre></td></tr></table></figure></p><p>② 配置Spring的<code>OpenSessionInView</code>过滤器来解决延迟加载和Hibernate会话关闭的矛盾：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>openSessionInView<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span></span><br><span class="line">       org.springframework.orm.hibernate3.support.OpenSessionInViewFilter</span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>openSessionInView<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure></p><p>③ 配置会话超时时间为10分钟：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">session-config</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">session-timeout</span>&gt;</span>10<span class="tag">&lt;/<span class="name">session-timeout</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">session-config</span>&gt;</span></span><br></pre></td></tr></table></figure></p><p>④ 配置404和Exception的错误页面：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">error-code</span>&gt;</span>404<span class="tag">&lt;/<span class="name">error-code</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">location</span>&gt;</span>/error.jsp<span class="tag">&lt;/<span class="name">location</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exception-type</span>&gt;</span>java.lang.Exception<span class="tag">&lt;/<span class="name">exception-type</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">location</span>&gt;</span>/error.jsp<span class="tag">&lt;/<span class="name">location</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br></pre></td></tr></table></figure></p><p>⑤ 配置安全认证方式：<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">security-constraint</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">web-resource-collection</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">web-resource-name</span>&gt;</span>ProtectedArea<span class="tag">&lt;/<span class="name">web-resource-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/admin/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">http-method</span>&gt;</span>GET<span class="tag">&lt;/<span class="name">http-method</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">http-method</span>&gt;</span>POST<span class="tag">&lt;/<span class="name">http-method</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">web-resource-collection</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">auth-constraint</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">role-name</span>&gt;</span>admin<span class="tag">&lt;/<span class="name">role-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">auth-constraint</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">security-constraint</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">login-config</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">auth-method</span>&gt;</span>BASIC<span class="tag">&lt;/<span class="name">auth-method</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">login-config</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">security-role</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">role-name</span>&gt;</span>admin<span class="tag">&lt;/<span class="name">role-name</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">security-role</span>&gt;</span></span><br></pre></td></tr></table></figure></p><p>说明：对Servlet（小服务）、Listener（监听器）和Filter（过滤器）等Web组件的配置，Servlet 3规范提供了基于注解的配置方式，可以分别使用@WebServlet、@WebListener、@WebFilter注解进行配置。 </p><p>补充：如果Web提供了有价值的商业信息或者是敏感数据，那么站点的安全性就是必须考虑的问题。安全认证是实现安全性的重要手段，认证就是要解决“Are you who you say you are?”的问题。认证的方式非常多，简单说来可以分为三类：<br>A. What you know? — 口令<br>B. What you have? — 数字证书（U盾、密保卡）<br>C. Who you are? — 指纹识别、虹膜识别<br>在Tomcat中可以通过建立安全套接字层（Secure Socket Layer, SSL）以及通过基本验证或表单验证来实现对安全性的支持。</p><h4 id="二、Tomcat总结"><a href="#二、Tomcat总结" class="headerlink" title="二、Tomcat总结"></a>二、Tomcat总结</h4><h5 id="1-Tomcat架构"><a href="#1-Tomcat架构" class="headerlink" title="1.Tomcat架构"></a>1.Tomcat架构</h5><p><img src="http://upload-images.jianshu.io/upload_images/8926909-6b8166af025c93d1.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Tomcat架构"><br>Tomcat中最顶层的容器是Server，代表着整个服务器，从上图中可以看出，一个Server可以包含至少一个Service，用于具体提供服务。<br>Service主要包含两个部分：<code>Connector</code>和<code>Container</code>。从上图中可以看出 Tomcat 的心脏就是这两个组件，他们的作用如下：</p><ul><li>Connector用于处理连接相关的事情，并提供Socket与Request和Response相关的转化; </li><li>Container用于封装和管理Servlet，以及具体处理Request请求；</li></ul><p>一个Tomcat中只有一个Server，一个Server可以包含多个Service，一个Service只有一个Container，但是可以有多个Connectors，这是因为一个服务可以有多个连接，如同时提供Http和Https链接，也可以提供向相同协议不同端口的连接,示意图如下<br><img src="http://upload-images.jianshu.io/upload_images/8926909-efed3d1d9d12ac59.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="Tomcat组成"></p><p><strong>小结：</strong><br>（1）Tomcat中只有一个Server，一个Server可以有多个Service，一个Service可以有多个Connector和一个Container；<br>（2）Server掌管着整个Tomcat的生死大权；<br>（4）Service 是对外提供服务的；<br>（5）Connector用于接受请求并将请求封装成Request和Response来具体处理；<br>（6）Container用于封装和管理Servlet，以及具体处理request请求；</p><p><strong>Connector和Container的微妙关系</strong><br>由上述内容我们大致可以知道一个请求发送到Tomcat之后，首先经过Service然后会交给我们的Connector，Connector用于接收请求并将接收的请求封装为Request和Response来具体处理，Request和Response封装完之后再交由Container进行处理，Container处理完请求之后再返回给Connector，最后在由Connector通过Socket将处理的结果返回给客户端，这样整个请求的就处理完了！</p><p>Connector最底层使用的是Socket来进行连接的，Request和Response是按照HTTP协议来封装的，所以Connector同时需要实现TCP/IP协议和HTTP协议！</p><blockquote><p><a href="http://blog.csdn.net/xlgen157387/article/details/79006434" target="_blank" rel="noopener">《四张图带你了解Tomcat系统架构–让面试官颤抖的Tomcat回答系列！》</a></p></blockquote><h5 id="2-解释什么是Jasper"><a href="#2-解释什么是Jasper" class="headerlink" title="2.解释什么是Jasper?"></a>2.解释什么是Jasper?</h5><p>Jasper是Tomcat的JSP引擎，它解析JSP文件，将它们编译成JAVA代码作为servlet。在运行时，Jasper允许自动检测JSP文件的更改并重新编译它们</p><h5 id="3-请解释Tomcat的默认端口是什么"><a href="#3-请解释Tomcat的默认端口是什么" class="headerlink" title="3.请解释Tomcat的默认端口是什么?"></a>3.请解释Tomcat的默认端口是什么?</h5><p>Tomcat的默认端口是8080。在本地机器上初始化Tomcat之后，可以验证Tomcat是否正在运行URL:<a href="http://localhost:8080" target="_blank" rel="noopener">http://localhost:8080</a></p><h5 id="4-请解释Tomcat中使用的连接器是什么"><a href="#4-请解释Tomcat中使用的连接器是什么" class="headerlink" title="4.请解释Tomcat中使用的连接器是什么?"></a>4.请解释Tomcat中使用的连接器是什么?</h5><p>在Tomcat中，使用了两种类型的连接器：<br>HTTP连接器:它有许多可以更改的属性，以确定它的工作方式和访问功能，如重定向和代理转发<br>AJP连接器:它以与HTTP连接器相同的方式工作，但是他们使用的是HTTP的AJP协议。AJP连接器通常通过插件技术mod_jk在Tomcat中实现</p><h5 id="5-解释如何使用WAR文件部署web应用程序"><a href="#5-解释如何使用WAR文件部署web应用程序" class="headerlink" title="5.解释如何使用WAR文件部署web应用程序?"></a>5.解释如何使用WAR文件部署web应用程序?</h5><p>在Tomcat的web应用程序目录下，jsp、servlet和它们的支持文件被放置在适当的子目录中。你可以将web应用程序目录下的所有文件压缩到一个压缩文件中，以.war文件扩展名结束。你可以通过在webapps目录中放置WAR文件来执行web应用程序。当一个web服务器开始执行时，它会将WAR文件的内容提取到适当的webapps子目录中。</p><h5 id="6-解释什么是Tomcat-Valve-说明Tomcat配置了多少个Valve"><a href="#6-解释什么是Tomcat-Valve-说明Tomcat配置了多少个Valve" class="headerlink" title="6.解释什么是Tomcat Valve?说明Tomcat配置了多少个Valve?"></a>6.解释什么是Tomcat Valve?说明Tomcat配置了多少个Valve?</h5><p>Tomcat Valve——Tomcat 4引入的新技术，它允许您将Java类的实例链接到一个特定的Catalina容器。Tomcat配置了四种类型的Valve：</p><ul><li>访问日志</li><li>远程地址过滤</li><li>远程主机过滤器</li><li>客户请求记录器</li></ul><h5 id="7-解释servlet如何完成生命周期"><a href="#7-解释servlet如何完成生命周期" class="headerlink" title="7.解释servlet如何完成生命周期?"></a>7.解释servlet如何完成生命周期?</h5><p>在Tomcat上运行的典型servlet生命周期如下：</p><ul><li>Tomcat通过它的其中一个连接器接收来自客户端的请求</li><li>进程请求Tomcat将此请求映射为适当的Servlet</li><li>一旦请求被定向到适当的servlet，Tomcat就会验证servlet类是否已经加载。如果不是,Tomcat将servlet包装成Java字节码，这是由JVM执行的，并形成servlet的实例</li><li>Tomcat通过调用它的init来启动servlet，它包含能够筛选Tomcat配置文件并相应地采取行动的代码，并声明它可能需要的任何资源</li><li>一旦servlet启动，Tomcat就可以调用servlet的服务方法来进行请求</li><li>在servlet的生命周期中，Tomcat和servlet可以通过使用侦听器类来进行协调或通信，从而跟踪各种状态变化的servlet</li><li>删除servlet，Tomcat调用servlet销毁方法</li></ul><h5 id="8-Tomcat优化经验"><a href="#8-Tomcat优化经验" class="headerlink" title="8.Tomcat优化经验"></a>8.Tomcat优化经验</h5><p>一、关掉对web.xml的监视，把jsp提前编辑成Servlet。有富余物理内存的情况，加大tomcat使用的jvm的内存</p><p>二、服务器资源。服务器所能提供CPU、内存、硬盘的性能对处理能力有决定性影响。<br>(1) 对于高并发情况下会有大量的运算，那么CPU的速度会直接影响到处理速度。<br>(2) 内存在大量数据处理的情况下，将会有较大的内存容量需求，可以用-Xmx -Xms -XX:MaxPermSize等参数对内存不同功能块进行划分。我们之前就遇到过内存分配不足，导致虚拟机一直处于full GC，从而导致处理能力严重下降。<br>(3) 硬盘主要问题就是读写性能，当大量文件进行读写时，磁盘极容易成为性能瓶颈。最好的办法还是利用下面提到的缓存。</p><p>三、利用缓存和压缩<br>对于静态页面最好是能够缓存起来，这样就不必每次从磁盘上读。这里我们采用了Nginx作为缓存服务器，将图片、css、js文件都进行了缓存，有效的减少了后端tomcat的访问。另外，为了能加快网络传输速度，开启gzip压缩也是必不可少的。但考虑到tomcat已经需要处理很多东西了，所以把这个压缩的工作就交给前端的Nginx来完成。</p><p>除了文本可以用gzip压缩，其实很多图片也可以用图像处理工具预先进行压缩，找到一个平衡点可以让画质损失很小而文件可以减小很多。曾经我就见过一个图片从300多kb压缩到几十kb，自己几乎看不出来区别。</p><p> 四、采用集群<br>单个服务器性能总是有限的，最好的办法自然是实现横向扩展，那么组建tomcat集群是有效提升性能的手段。我们还是采用了Nginx来作为请求分流的服务器，后端多个tomcat共享session来协同工作。可以参考之前写的《利用nginx+tomcat+memcached组建web服务器负载均衡》。</p><p>五、 优化tomcat参数<br>这里以tomcat7的参数配置为例，需要修改conf/server.xml文件，主要是优化连接配置，关闭客户端dns查询。<br><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">Connector</span> <span class="attr">port</span>=<span class="string">"8080"</span>   </span></span><br><span class="line"><span class="tag">           <span class="attr">protocol</span>=<span class="string">"org.apache.coyote.http11.Http11NioProtocol"</span>  </span></span><br><span class="line"><span class="tag">           <span class="attr">connectionTimeout</span>=<span class="string">"20000"</span>  </span></span><br><span class="line"><span class="tag">           <span class="attr">redirectPort</span>=<span class="string">"8443"</span>   </span></span><br><span class="line"><span class="tag">           <span class="attr">maxThreads</span>=<span class="string">"500"</span>   </span></span><br><span class="line"><span class="tag">           <span class="attr">minSpareThreads</span>=<span class="string">"20"</span>  </span></span><br><span class="line"><span class="tag">           <span class="attr">acceptCount</span>=<span class="string">"100"</span> </span></span><br><span class="line"><span class="tag">           <span class="attr">disableUploadTimeout</span>=<span class="string">"true"</span> </span></span><br><span class="line"><span class="tag">           <span class="attr">enableLookups</span>=<span class="string">"false"</span>   </span></span><br><span class="line"><span class="tag">           <span class="attr">URIEncoding</span>=<span class="string">"UTF-8"</span> /&gt;</span></span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;一、Web服务&quot;&gt;&lt;a href=&quot;#一、Web服务&quot; class=&quot;headerlink&quot; title=&quot;一、Web服务&quot;&gt;&lt;/a&gt;一、Web服务&lt;/h4&gt;&lt;h5 id=&quot;1-web服务器分类&quot;&gt;&lt;a href=&quot;#1-web服务器分类&quot; class=&quot;headerlink&quot; title=&quot;1.web服务器分类&quot;&gt;&lt;/a&gt;1.web服务器分类&lt;/h5&gt;&lt;p&gt;Unix和Linux平台下使用最广泛的免费HTTP服务器是Apache服务器，而Windows平台的服务器通常使用IIS作为Web服务器。选择Web服务器应考虑的因素有：性能、安全性、日志和统计、虚拟主机、代理服务器、缓冲服务和集成应用程序等。
    
    </summary>
    
      <category term="Tomcat" scheme="https://tisonkong.github.io/github.io/categories/Tomcat/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java web" scheme="https://tisonkong.github.io/github.io/tags/java-web/"/>
    
      <category term="Tomcat" scheme="https://tisonkong.github.io/github.io/tags/Tomcat/"/>
    
  </entry>
  
  <entry>
    <title>详解IO的使用</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/%E8%AF%A6%E8%A7%A3IO%E7%9A%84%E4%BD%BF%E7%94%A8/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/详解IO的使用/</id>
    <published>2018-05-20T14:35:08.032Z</published>
    <updated>2018-05-20T14:35:08.034Z</updated>
    
    <content type="html"><![CDATA[<p>I/O类库中使用“流”这个抽象概念。Java对设备中数据的操作是通过流的方式。表示任何有能力产出数据的数据源对象，或者是有能力接受数据的接收端对象。“流”屏蔽了实际的I/O设备中处理数据的细节。IO流用来处理设备之间的数据传输。设备是指硬盘、内存、键盘录入、网络等。<br><a id="more"></a><br>IO的分类可以为：</p><ul><li>流按操作数据类型的不同分为两种：字节流与字符流。</li><li>流按流向分为：输入流，输出流（以程序为参照物，输入到程序，或是从程序输出）<br><img src="http://upload-images.jianshu.io/upload_images/8926909-b2b4859ffeb3ae26.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="IO流继承体系"><h4 id="一、字节流"><a href="#一、字节流" class="headerlink" title="一、字节流"></a>一、字节流</h4><h5 id="1、Inpustream"><a href="#1、Inpustream" class="headerlink" title="1、Inpustream"></a>1、Inpustream</h5><code>InputStream</code>有<code>read</code>方法，一次读取一个字节，<code>OutputStream</code>的<code>write</code>方法一次写一个<code>int</code>。这两个类都是抽象类。意味着不能创建对象，那么需要找到具体的子类来使用。操作流的步骤都是：</li></ul><p>第一步：1：打开流（即创建流）<br>第二步：2：通过流读取内容<br>第三步：3：用完后，关闭流资源</p><p>案例一：使用 <code>read()</code>方法，一次读取一个字节,读到文件末尾返回-1.<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">showContent</span><span class="params">(String path)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">       <span class="comment">// 打开流</span></span><br><span class="line">       FileInputStream fis = <span class="keyword">new</span> FileInputStream(path);</span><br><span class="line"></span><br><span class="line">       <span class="keyword">int</span> len;</span><br><span class="line">       <span class="keyword">while</span> ((len = fis.read()) != -<span class="number">1</span>) &#123;</span><br><span class="line">           System.out.print((<span class="keyword">char</span>) len);</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">// 使用完关闭流</span></span><br><span class="line">       fis.close();</span><br></pre></td></tr></table></figure></p><p>案例二：使用<code>read()</code>方法的时候，可以将读到的数据装入到字节数组中，一次性的操作数组，可以提高效率。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">showContent2</span><span class="params">(String path)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">// 打开流</span></span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(path);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 通过流读取内容</span></span><br><span class="line">        <span class="keyword">byte</span>[] byt = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> len = fis.read(byt);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt;len; i++) &#123;</span><br><span class="line">            System.out.print(byt[i]);</span><br><span class="line">    　　&#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用完关闭流</span></span><br><span class="line">        fis.close();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p>案例三：使用<code>read(byte[] b,int off,int len)</code><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">   * 把数组的一部分当做流的容器来使用</span></span><br><span class="line"><span class="comment">   * read(byte[] b,int off,int len)</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">  <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">showContent3</span><span class="params">(String path)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">      <span class="comment">// 打开流</span></span><br><span class="line">      FileInputStream fis = <span class="keyword">new</span> FileInputStream(path);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 通过流读取内容</span></span><br><span class="line">      <span class="keyword">byte</span>[] byt = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">      <span class="comment">// 从什么地方开始存读到的数据</span></span><br><span class="line">      <span class="keyword">int</span> start = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 希望最多读多少个(如果是流的末尾，流中没有足够数据)</span></span><br><span class="line">      <span class="keyword">int</span> maxLen = <span class="number">6</span>;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 实际存放了多少个</span></span><br><span class="line">      <span class="keyword">int</span> len = fis.read(byt, start, maxLen);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">for</span> (<span class="keyword">int</span> i = start; i &lt; start + maxLen; i++) &#123;</span><br><span class="line">          System.out.print((<span class="keyword">char</span>) byt[i]);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="comment">// 使用完关闭流</span></span><br><span class="line">      fis.close();</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></p><p>案例四（推荐使用）：使用缓冲(提高效率),并循环读取(读完所有内容).<br> <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 使用字节数组当缓冲</span></span><br><span class="line"><span class="comment">     * */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">showContent7</span><span class="params">(String path)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(path);</span><br><span class="line">        <span class="keyword">byte</span>[] byt = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((len = fis.read(byt)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            System.out.println(<span class="keyword">new</span> String(byt, <span class="number">0</span>, len));</span><br><span class="line">        &#125;</span><br><span class="line">        fis.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5 id="2、OutputStream"><a href="#2、OutputStream" class="headerlink" title="2、OutputStream"></a>2、OutputStream</h5><p><code>OutputStram</code> 的<code>write</code>方法，一次只能写一个字节。成功的向文件中写入了内容。但是并不高效，如何提高效率呢？可以使用缓冲，在<code>OutputStram</code>类中有<code>write(byte[] b)</code>方法，将 <code>b.length</code>个字节从指定的 <code>byte</code> 数组写入此输出流中。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">writeTxtFile</span><span class="params">(String path)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">// 1：打开文件输出流，流的目的地是指定的文件</span></span><br><span class="line">        FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(path,<span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2：通过流向文件写数据</span></span><br><span class="line">        <span class="keyword">byte</span>[] byt = <span class="string">"java"</span>.getBytes();</span><br><span class="line">        fos.write(byt);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3:用完流后关闭流</span></span><br><span class="line">        fos.close();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><h5 id="3、输入输出流综合使用——文件拷贝实现"><a href="#3、输入输出流综合使用——文件拷贝实现" class="headerlink" title="3、输入输出流综合使用——文件拷贝实现"></a>3、输入输出流综合使用——文件拷贝实现</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copyFile</span><span class="params">(String srcPath, String destPath)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">// 打开输入流，输出流</span></span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(srcPath);</span><br><span class="line">        FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(destPath);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 读取和写入信息</span></span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用字节数组，当做缓冲区</span></span><br><span class="line">        <span class="keyword">byte</span>[] byt = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">while</span> ((len = fis.read(byt)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            fos.write(byt, <span class="number">0</span>, len);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 关闭流</span></span><br><span class="line">        fis.close();</span><br><span class="line">        fos.close();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><p>可以根据拷贝的需求调整数组的大小，一般是1024的整数倍。使用缓冲后效率大大提高。目前我们是抛出处理，一旦出现了异常，<code>close</code>就没有执行，也就没有释放资源。那么为了保证<code>close</code>的执行该如何处理呢。那么就需要使用<code>try{} catch(){}finally{}</code>语句。<code>try</code>中放入可能出现异常的语句，<code>catch</code>是捕获异常对象，<code>fianlly</code>是一定要执行的代码。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copyFile</span><span class="params">(String srcPath, String destPath)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        FileInputStream fis = <span class="keyword">null</span>;</span><br><span class="line">        FileOutputStream fos = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            fis = <span class="keyword">new</span> FileInputStream(srcPath);</span><br><span class="line">            fos = <span class="keyword">new</span> FileOutputStream(destPath);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">byte</span>[] byt = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span> * <span class="number">1024</span>];</span><br><span class="line">            <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> ((len = fis.read(byt)) != -<span class="number">1</span>) &#123;</span><br><span class="line"></span><br><span class="line">                fos.write(byt, <span class="number">0</span>, len);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (fis != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    fis.close();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (fos != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        fos.close();</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (IOException e) &#123;</span><br><span class="line">                        <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p>大量的异常捕获代码使得以上代码变得十分臃肿难看，好在java7提供了<strong>TWR（try-with-resource）</strong>语法糖，由于很多外部资源类都间接的实现了<code>AutoCloseable</code>接口（单方法回调接口），因此可以利用TWR语法在<code>try</code>结束的时候通过回调的方式自动调用外部资源类的<code>close()</code>方法，避免书写冗长的<code>finally</code>代码块。`<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copyByTWR</span><span class="params">(String srcPath, String destPath)</span></span>&#123;</span><br><span class="line">    <span class="keyword">try</span>( FileInputStream fis = <span class="keyword">new</span> FileInputStream(srcPath);</span><br><span class="line">         FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(destPath))&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] byt = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span> * <span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((len = fis.read(byt)) != -<span class="number">1</span>) &#123;</span><br><span class="line">            fos.write(byt, <span class="number">0</span>, len);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;<span class="keyword">catch</span> (IOException e)&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>当<code>try</code>语句块运行结束时，<code>FileInputStream / FileOutputStream</code>会被自动关闭。这是因为<code>FileInputStream</code>实现了java中的<code>java.lang.AutoCloseable</code>接口。所有实现了这个接口的类都可以在<code>try-with-resources</code>结构中使用上面的例子在try关键字后的括号里创建了两个资源——<code>FileInputStream</code> 和<code>FileOutputStream</code>。当程序运行离开try语句块时，这两个资源都会被自动关闭,这些资源将按照他们被创建顺序的逆序来关闭。首先<code>FileOutputStream</code>会被关闭，然后<code>FileInputStream</code>会被关闭。<br>怎么实现对资源的关闭呢？当<code>try-with-resources</code>结构中抛出一个异常，同时<code>FileInputStream</code>被关闭时（调用了其<code>close</code>方法）也抛出一个异常，<code>try-with-resources</code>结构中抛出的异常会向外传播，而<code>FileInputStream</code>被关闭时抛出的异常被抑制了。这与文章开始处利用旧风格代码的例子（在finally语句块中关闭资源）相反。</p><blockquote><p>具体参考：<a href="http://ifeve.com/java-7%E4%B8%AD%E7%9A%84try-with-resources/" target="_blank" rel="noopener">Java 7中的Try-with-resources</a></p></blockquote><h5 id="4、缓冲流"><a href="#4、缓冲流" class="headerlink" title="4、缓冲流"></a>4、缓冲流</h5><p>Java其实提供了专门的字节流缓冲来提高效率。<code>BufferedInputStream</code> 和 <code>BufferedOutputStream。BufferedOutputStream</code>和<code>BufferedOutputStream</code>类可以通过减少读写次数来提高输入和输出的速度。它们内部有一个缓冲区，用来提高处理效率。查看API文档，发现可以指定缓冲区的大小。其实内部也是封装了字节数组。没有指定缓冲区大小，默认的字节是8192。显然缓冲区输入流和缓冲区输出流要配合使用。首先缓冲区输入流会将读取到的数据读入缓冲区，当缓冲区满时，或者调用<code>flush</code>方法，缓冲输出流会将数据写出。<br>注意：当然使用缓冲流来进行提高效率时，对于小文件可能看不到性能的提升。但是文件稍微大一些的话，就可以看到实质的性能提升了。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        String srcPath = <span class="string">"c:\\a.mp3"</span>;</span><br><span class="line">        String destPath = <span class="string">"d:\\copy.mp3"</span>;</span><br><span class="line">        copyFile(srcPath, destPath);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copyFile</span><span class="params">(String srcPath, String destPath)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">// 打开输入流，输出流</span></span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(srcPath);</span><br><span class="line">        FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(destPath);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用缓冲流</span></span><br><span class="line">        BufferedInputStream bis = <span class="keyword">new</span> BufferedInputStream(fis);</span><br><span class="line">        BufferedOutputStream bos = <span class="keyword">new</span> BufferedOutputStream(fos);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 读取和写入信息</span></span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> ((len = bis.read()) != -<span class="number">1</span>) &#123;</span><br><span class="line">            bos.write(len);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 关闭流</span></span><br><span class="line">        bis.close();</span><br><span class="line">        bos.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h4 id="二、字符流"><a href="#二、字符流" class="headerlink" title="二、字符流"></a>二、字符流</h4><p> 计算机并不区分二进制文件与文本文件。所有的文件都是以二进制形式来存储的，因此，从本质上说，所有的文件都是二进制文件。所以字符流是建立在字节流之上的，它能够提供字符层次的编码和解码。可以说字符流就是：字节流 + 编码表，为了更便于操作文字数据。字符流的抽象基类：<code>Reader ， Writer</code>。由这些类派生出来的子类名称都是以其父类名作为子类名的后缀，如<code>FileReader、FileWriter</code>。</p><h5 id="1、Reader"><a href="#1、Reader" class="headerlink" title="1、Reader"></a>1、Reader</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">read</span><span class="params">()</span></span></span><br></pre></td></tr></table></figure><p>读取一个字符。返回的是读到的那个字符。如果读到流的末尾，返回-1.<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">read</span><span class="params">(<span class="keyword">char</span>[])</span></span></span><br></pre></td></tr></table></figure></p><p>将读到的字符存入指定的数组中，返回的是读到的字符个数，也就是往数组里装的元素的个数。如果读到流的末尾，返回-1.<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">close()</span><br></pre></td></tr></table></figure></p><p>读取字符其实用的是window系统的功能，就希望使用完毕后，进行资源的释放<br>由于<code>Reader</code>也是抽象类，所以想要使用字符输入流需要使用<code>Reader</code>的实现类——<code>FileReader</code>。1，用于读取文本文件的流对象。2，用于关联文本文件。<br>构造函数：在读取流对象初始化的时候，必须要指定一个被读取的文件。如果该文件不存在会发生FileNotFoundException。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 使用字符流读取文件内容</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">readFileByReader</span><span class="params">(String path)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    Reader reader = <span class="keyword">new</span> FileReader(path);</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> ((len = reader.read()) != -<span class="number">1</span>) &#123;</span><br><span class="line">        System.out.print((<span class="keyword">char</span>) len);</span><br><span class="line">    &#125;</span><br><span class="line">    reader.close();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5 id="2、Writer"><a href="#2、Writer" class="headerlink" title="2、Writer"></a>2、Writer</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">write(ch): <span class="comment">//将一个字符写入到流中。</span></span><br><span class="line"></span><br><span class="line">write(<span class="keyword">char</span>[]):<span class="comment">// 将一个字符数组写入到流中。</span></span><br><span class="line"></span><br><span class="line">write(String): <span class="comment">//将一个字符串写入到流中。</span></span><br><span class="line"></span><br><span class="line">flush(): <span class="comment">//刷新流，将流中的数据刷新到目的地中，流还存在。</span></span><br><span class="line"></span><br><span class="line">close(): <span class="comment">//关闭资源：在关闭前会先调用flush()，刷新流中的数据去目的地。然流关闭。</span></span><br></pre></td></tr></table></figure><p>基本方法和<code>OutputStream</code> 类似，有<code>write</code>方法，功能更多一些。可以接收字符串。<code>Writer</code>是抽象类无法创建对象，<code>Writer</code>的实现子类为<code>FileWriter</code>。默认的<code>FileWriter</code>方法新值会覆盖旧值，想要实现追加功能需要使用如下构造函数创建输出流 <code>append</code>值为<code>true</code>了。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">FileWriter(String fileName, <span class="keyword">boolean</span> append)</span><br><span class="line">FileWriter(File file, <span class="keyword">boolean</span> append)</span><br></pre></td></tr></table></figure></p><h5 id="3、文件拷贝——完善异常处理风格"><a href="#3、文件拷贝——完善异常处理风格" class="headerlink" title="3、文件拷贝——完善异常处理风格"></a>3、文件拷贝——完善异常处理风格</h5><p>一次读一个字符就写一个字符，效率不高。把读到的字符放到字符数组中，再一次性的写出，缓冲数组可以提高效率。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 使用字符流拷贝文件，有完善的异常处理</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copyFile2</span><span class="params">(String path1, String path2)</span> </span>&#123;</span><br><span class="line">       Reader reader = <span class="keyword">null</span>;</span><br><span class="line">       Writer writer = <span class="keyword">null</span>;</span><br><span class="line">       <span class="keyword">try</span> &#123;</span><br><span class="line">           <span class="comment">// 打开流</span></span><br><span class="line">           reader = <span class="keyword">new</span> FileReader(path1);</span><br><span class="line">           writer = <span class="keyword">new</span> FileWriter(path2);</span><br><span class="line"></span><br><span class="line">           <span class="comment">// 进行拷贝</span></span><br><span class="line">           <span class="keyword">int</span> ch = -<span class="number">1</span>;</span><br><span class="line">　　　　　　　<span class="keyword">char</span> [] arr=<span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">1024</span>];</span><br><span class="line">           <span class="keyword">while</span> ((ch = reader.read(arr)) != -<span class="number">1</span>) &#123;</span><br><span class="line">               writer.write(ch);</span><br><span class="line">           &#125;</span><br><span class="line">       &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">           <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">       &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">           <span class="comment">// 关闭流，注意一定要能执行到close()方法，所以都要放到finally代码块中</span></span><br><span class="line">           <span class="keyword">try</span> &#123;</span><br><span class="line">               <span class="keyword">if</span> (reader != <span class="keyword">null</span>) &#123;</span><br><span class="line">                   reader.close();</span><br><span class="line">               &#125;</span><br><span class="line">           &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">           &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">               <span class="keyword">try</span> &#123;</span><br><span class="line">                   <span class="keyword">if</span> (writer != <span class="keyword">null</span>) &#123;</span><br><span class="line">                       writer.close();</span><br><span class="line">                   &#125;</span><br><span class="line">               &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                   <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(e);</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure></p><p>同样可以用TWR风格进行简写，将大量finally中的资源关闭操作省略掉，声明赋值语句放入到try()中即可。</p><h5 id="4、缓冲流-1"><a href="#4、缓冲流-1" class="headerlink" title="4、缓冲流"></a>4、缓冲流</h5><p>使用字符流缓冲区拷贝文本文件可以提高效率，<code>Reader</code>有一个子类<code>BufferedReader</code>， 子类继承父类显然子类可以重写父类的方法可以增加自己的新方法。例如一次读一行就是常用的操作.那么<code>BufferedReader</code>类就提供了这个方法,可以查看<code>readLine()</code>方法具备 一次读取一个文本行的功能。很显然,该子类可以对功能进行增强。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copyFile</span><span class="params">(File srcFile, File destFile)</span><span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="comment">// 创建字符输入流</span></span><br><span class="line">        FileReader fr = <span class="keyword">new</span> FileReader(srcFile);</span><br><span class="line">        <span class="comment">// 创建字符输出流</span></span><br><span class="line">        FileWriter fw = <span class="keyword">new</span> FileWriter(destFile);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 字符输入流的缓冲流</span></span><br><span class="line">        BufferedReader br = <span class="keyword">new</span> BufferedReader(fr);</span><br><span class="line">        <span class="comment">// 字符输出流的缓冲流</span></span><br><span class="line">        BufferedWriter bw = <span class="keyword">new</span> BufferedWriter(fw);</span><br><span class="line"></span><br><span class="line">        String line = <span class="keyword">null</span>;</span><br><span class="line">        <span class="comment">// 一次读取一行</span></span><br><span class="line">        <span class="keyword">while</span> ((line = br.readLine()) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 一次写出一行.</span></span><br><span class="line">            bw.write(line);</span><br><span class="line">            <span class="comment">// 刷新缓冲</span></span><br><span class="line">            bw.flush();</span><br><span class="line">            <span class="comment">// 进行换行,由于readLine方法默认没有换行.需要手动换行</span></span><br><span class="line">            bw.newLine();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 关闭流</span></span><br><span class="line">        br.close();</span><br><span class="line">        bw.close();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><h4 id="三、装饰器模式："><a href="#三、装饰器模式：" class="headerlink" title="三、装饰器模式："></a>三、装饰器模式：</h4><p>使用分层对象来动态透明的向单个对象中添加责任（功能）。装饰器指定包装在最初的对象周围的所有对象都具有相同的基本接口。某些对象是可装饰的，可以通过将其他类包装在这个可装饰对象的四周，来将功能分层。装饰器必须具有和他所装饰的对象相同的接口。Java I/O类库需要多种不同的功能组合，所以使用了装饰器模式。<code>Filterxxx</code>类是<code>JavaIO</code>提供的装饰器基类，即我们要想实现一个新的装饰器，就要继承这些类。</p><p>继承实现的增强类：<br>优点：代码结构清晰，而且实现简单<br>缺点：对于每一个的需要增强的类都要创建具体的子类来帮助其增强，这样会导致继承体系过于庞大。</p><p>修饰模式实现的增强类：<br>优点：内部可以通过多态技术对多个需要增强的类进行增强<br>缺点：需要内部通过多态技术维护需要增强的类的实例。进而使得代码稍微复杂。</p><h4 id="四、面试总结："><a href="#四、面试总结：" class="headerlink" title="四、面试总结："></a>四、面试总结：</h4><h5 id="1、为了提高读写性能，可以采用什么流"><a href="#1、为了提高读写性能，可以采用什么流" class="headerlink" title="1、为了提高读写性能，可以采用什么流"></a>1、为了提高读写性能，可以采用什么流</h5><p>针对读写对象的不同，字节流可以采用带缓冲区的BufferedInputStream和BufferedOutputStream，字符流可以采用带缓冲区的BufferedReader和BufferedWriter。</p><h5 id="2、Java中有几种类型的流"><a href="#2、Java中有几种类型的流" class="headerlink" title="2、Java中有几种类型的流"></a>2、Java中有几种类型的流</h5><p>字节流和字符流。字节流继承于<code>InputStream、OutputStream</code>，字符流继承于<code>Reader、Writer</code>。在<code>java.io</code>包中还有许多其他的流，主要是为了提高性能和使用方便。关于Java的I/O需要注意的有两点：一是两种对称性（输入和输出的对称性，字节和字符的对称性）；二是两种设计模式（适配器模式和装潢模式）。另外Java中的流不同于C#的是它只有一个维度一个方向。</p><h5 id="3、JDK-为每种类型的流提供了一些抽象类以供继承，分别是哪些类"><a href="#3、JDK-为每种类型的流提供了一些抽象类以供继承，分别是哪些类" class="headerlink" title="3、JDK 为每种类型的流提供了一些抽象类以供继承，分别是哪些类"></a>3、JDK 为每种类型的流提供了一些抽象类以供继承，分别是哪些类</h5><p>Java中的流分为两种，一种是字节流，另一种是字符流，分别由四个抽象类来表示（每种流包括输入和输出两种所以一共四个）:<code>InputStream，OutputStream，Reader，Writer</code>。Java中其他多种多样变化的流均是由它们派生出来的.</p><p>4、对文本文件操作用什么I/O流？<br><code>FileReader/FileWriter</code></p><p>5、对各种基本数据类型和String类型的读写，采用什么流？ <code>DataInputStream、DataOutputStream</code></p><p>6、能指定字符编码的 I/O 流类型是什么？<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">BufferedReader/BufferedWriter</span><br><span class="line">BufferedInputStream/BufferedOutputStream</span><br></pre></td></tr></table></figure></p><p>回答以上问题需要分清各个IO流子类的应用场景：</p><blockquote><p><code>FileInputStream/FileOutputStream</code>  需要逐个字节处理原始二进制流的时候使用，效率低下。<br><code>FileReader/FileWriter</code> 需要组个字符处理的时候使用。<br><code>StringReader/StringWriter</code> 需要处理字符串的时候，可以将字符串保存为字符数组。<br><code>PrintStream/PrintWriter</code> 用来包装<code>FileOutputStream</code> 对象，方便直接将<code>String</code>字符串写入文件 。<br><code>Scanner</code>　用来包装<code>System.in</code>流，很方便地将输入的<code>String</code>字符串转换成需要的数据类型。<br><code>InputStreamReader/OutputStreamReader</code> ,  字节和字符的转换桥梁，在网络通信或者处理键盘输入的时候用。<br><code>BufferedReader/BufferedWriter</code>， <code>BufferedInputStream/BufferedOutputStream</code>， 缓冲流用来包装字节流后者字符流，提升IO性能，<code>BufferedReader</code>还可以方便地读取一行，简化编程。<br><code>SequenceInputStream(InputStream s1, InputStream s2)</code>序列流，合并流对象时使用.<br><code>ObjectInputStream、ObjectOutputStream</code>，方法用于序列化对象并将它们写入一个流，另一个方法用于读取流并反序列化对象。<br><code>ByteArrayInputStream、ByteArrayOutputStream</code>，操作数组<br><code>DataInputStream、DataOutputStream</code>操作基本数据类型和字符串。</p></blockquote><h5 id="7、写一个方法，输入一个文件名和一个字符串，统计这个字符串在这个文件中出现的次数。"><a href="#7、写一个方法，输入一个文件名和一个字符串，统计这个字符串在这个文件中出现的次数。" class="headerlink" title="7、写一个方法，输入一个文件名和一个字符串，统计这个字符串在这个文件中出现的次数。"></a>7、写一个方法，输入一个文件名和一个字符串，统计这个字符串在这个文件中出现的次数。</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">MyUtil</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 工具类中的方法都是静态方式访问的因此将构造器私有不允许创建对象(绝对好习惯)</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">MyUtil</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> AssertionError();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 统计给定文件中给定字符串的出现次数</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> filename  文件名</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> word 字符串</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span> 字符串在文件中出现的次数</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">countWordInFile</span><span class="params">(String filename, String word)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> counter = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">try</span> (FileReader fr = <span class="keyword">new</span> FileReader(filename)) &#123;</span><br><span class="line">            <span class="keyword">try</span> (BufferedReader br = <span class="keyword">new</span> BufferedReader(fr)) &#123;</span><br><span class="line">                String line = <span class="keyword">null</span>;</span><br><span class="line">                <span class="keyword">while</span> ((line = br.readLine()) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="keyword">int</span> index = -<span class="number">1</span>;</span><br><span class="line">                    <span class="keyword">while</span> (line.length() &gt;= word.length() &amp;&amp; (index = line.indexOf(word)) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        counter++;</span><br><span class="line">                        line = line.substring(index + word.length());</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception ex) &#123;</span><br><span class="line">            ex.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> counter;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I/O类库中使用“流”这个抽象概念。Java对设备中数据的操作是通过流的方式。表示任何有能力产出数据的数据源对象，或者是有能力接受数据的接收端对象。“流”屏蔽了实际的I/O设备中处理数据的细节。IO流用来处理设备之间的数据传输。设备是指硬盘、内存、键盘录入、网络等。&lt;br&gt;
    
    </summary>
    
      <category term="Java IO" scheme="https://tisonkong.github.io/github.io/categories/Java-IO/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="IO" scheme="https://tisonkong.github.io/github.io/tags/IO/"/>
    
  </entry>
  
  <entry>
    <title>结合IO类讲讲装饰者模式</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%E5%9F%BA%E7%A1%80%E4%B9%8BIO%E6%B5%81%EF%BC%88%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%EF%BC%89/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java基础之IO流（设计模式）/</id>
    <published>2018-05-20T14:35:08.029Z</published>
    <updated>2018-05-20T14:35:08.029Z</updated>
    
    <content type="html"><![CDATA[<p>java IO流的设计是基于装饰者模式&amp;适配模式，面对IO流庞大的包装类体系，核心是要抓住其功能所对应的装饰类。</p><p>装饰模式又名包装（Wrapper）模式。装饰模式以对客户端透明的方式扩展对象的功能，是继承关系的一个替代方案。装饰模式通过创建一个包装对象，也就是装饰，来包裹真实的对象。装饰模式以对客户端透明的方式动态地给一个对象附加上更多的责任。换言之，客户端并不会觉得对象在装饰前和装饰后有什么不同。装饰模式可以在不创造更多子类的情况下，将对象的功能加以扩展。装饰模式把客户端的调用委派到被装饰类。装饰模式的关键在于这种扩展是完全透明的。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-dc3cf3d3ce99df2d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="装饰者模式"><br><a id="more"></a><br>装饰者的角色：<br><strong>抽象构件角色（Component）</strong>：给出一个抽象接口，以规范准备接收附加责任的对象。<br><strong>具体构件角色（Concrete Component）</strong>：定义将要接收附加责任的类。<br><strong>装饰角色（Decorator）</strong>：持有一个构件（Component）对象的引用，并定义一个与抽象构件接口一致的接口。<br><strong>具体装饰角色（Concrete Decorator）</strong>：负责给构件对象“贴上”附加的责任。</p><p>实现案例：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//抽象构件角色</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Component</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//具体构件角色</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConcreteComponent</span> <span class="keyword">implements</span> <span class="title">Component</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"功能A"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//装饰者角色</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Decorator</span> <span class="keyword">implements</span> <span class="title">Component</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="comment">//维护一个抽象构件角色</span></span><br><span class="line">    <span class="keyword">private</span> Component component;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Decorator</span><span class="params">(Component component)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.component = component;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        component.doSomething();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//具体装饰者角色</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConcreteDecorator1</span> <span class="keyword">extends</span> <span class="title">Decorator</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConcreteDecorator1</span><span class="params">(Component component)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(component);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doSomething</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.doSomething();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">this</span>.doAnotherThing();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doAnotherThing</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"功能B"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>以上就是装饰者模式的一个极简代码思路，实际上IO流的装饰体系也是在对上面思路的一中具体实现。<br><strong>JAVA-IO流体系：</strong><br>在IO中，具体构件角色是节点流，装饰角色是过滤流。<br>1、继承自<code>InputStream/OutputStream</code>的流都是用于向程序中输入/输出数据，且数据的单位都是字节(byte=8bit)，如图，深色的为节点流，浅色的为过滤流。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-88c33f33bf9b3218.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="类继承图"><br>2、继承自<code>Reader/Writer</code>的流都是用于向程序中输入/输出数据，且数据的单位都是字符(2byte=16bit)，如图，深色的为节点流，浅色的为过滤流。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-1f9a7b90439b0784.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="类继承图"><br>从图中可以看出，<code>InputStream</code>就是装饰者模式中的超类<code>（Component）</code>，<code>ByteArrayInputStream，FileInputStream</code>相当于被装饰者<code>（ConcreteComponent）</code>，这些类都提供了最基本的字节读取功能。而另外一个和这两个类是同一级的类<code>FilterInputStream</code>即是装饰者<code>（Decorator）</code>，<code>BufferedInputStream，DataInputStream，PushbackInputStream</code>…这些都是被装饰者装饰后形成的成品。为什么可以说：装饰模式可以在不创造更多子类的情况下，将对象的功能加以扩展，能理解这一点就能很好的掌握装饰者设计模式的精髓，如果在<code>InputStream</code>这里扩展出<code>FilterInputStream</code>类下面的装饰类，那么针对<code>FileInputStream</code>和<code>ByteArrayInputStream</code>就都要去实现一次<code>BufferedInputStream</code>了，那么可能就会衍生出<code>BufferedFileInputStream</code>和<code>BufferedByteArrayInputStream</code>这样的类，如果按照这样的扩展方式去添加功能，对于添加功能的子类来说简直是一场噩梦，好在装饰着模式很好的解决了这个问题，现在我们只需要在过滤流类这里维护一个超类，不论传入的是什么具体的节点流，那么都只要套一层装饰，就能对功能方法进行加强。<br>如果想要对文件输入流进行缓存加强可以这样装饰：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">File file = <span class="keyword">new</span> File (<span class="string">"hello.txt"</span>);</span><br><span class="line">BufferedInputStream inBuffered=<span class="keyword">new</span> BufferedInputStream (<span class="keyword">new</span> FileInputStream(file));</span><br></pre></td></tr></table></figure></p><p>如果想要对字节数组输入流进行缓存加强可以这样装饰：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">byte</span>[] byts=<span class="string">"Hello"</span>.getBytes();</span><br><span class="line">BufferedInputStream bf=<span class="keyword">new</span> BufferedInputStream(<span class="keyword">new</span> ByteArrayInputStream(byts));</span><br></pre></td></tr></table></figure></p><p>那么节点流上的类就可以平行扩展，而装饰者同样可以按照功能进行另外一个维度的扩展，调用的时候就可以按需进行组合装饰，这样就可以减少了子类还将对象的功能进行扩展，不得不佩服前人在该设计模式上的智慧，理解了这装饰着模式后，就应该对java中IO流的体系进行梳理：<br>节点流类型<br>对文件操作的字符流有<code>FileReader/FileWriter</code>，<br>字节流有<code>FileInputStream/FileOutputStream</code>。<br>过滤流类型</p><p>缓冲流：缓冲流要“套接”在相应的节点流之上，对读写的数据提供了缓冲的功能，提高了读写效率，同时增加了一些新的方法。<br>字节缓冲流有<code>BufferedInputStream / BufferedOutputStream</code>，字符缓冲流有<code>BufferedReader / BufferedWriter</code>，字符缓冲流分别提供了读取和写入一行的方法<code>ReadLine</code>和<code>NewLine</code>方法。<br>对于输出的缓冲流，写出的数据，会先写入到内存中，再使用<code>flush</code>方法将内存中的数据刷到硬盘。所以，在使用字符缓冲流的时候，一定要先<code>flush</code>，然后再<code>close</code>，避免数据丢失。<br>转换流：用于字节数据到字符数据之间的转换。<br>字符流<code>InputStreamReader / OutputStreamWriter</code>。其中，<code>InputStreamReader</code>需要与<code>InputStream</code>“套接”，<code>OutputStreamWriter</code>需要与<code>OutputStream</code>“套接”。<br>数据流：提供了读写Java中的基本数据类型的功能。<br><code>DataInputStream</code>和<code>DataOutputStream</code>分别继承自<code>InputStream</code>和<code>OutputStream</code>，需要“套接”在<code>InputStream</code>和<code>OutputStream</code>类型的节点流之上。<br>对象流：用于直接将对象写入写出。<br>流类有<code>ObjectInputStream</code>和<code>ObjectOutputStream</code>，本身这两个方法没什么，但是其要写出的对象有要求，该对象必须实现<code>Serializable</code>接口，来声明其是可以序列化的。否则，不能用对象流读写。(api以及demo在文末)<br>重点梳理一下：Java中Inputstream/OutputStream与Reader/Writer的区别</p><p><code>Reader/Writer</code>和<code>InputStream/OutputStream</code>分别是I/O库提供的两套平行独立的等级机构，</p><ul><li><code>InputStream、OutputStream</code>是用来处理8位元的流，也就是用于读写ASCII字符和二进制数据；<code>Reader、Writer</code>是用来处理16位元的流，也就是用于读写Unicode编码的字符。</li><li>在JAVA语言中，byte类型是8位的，char类型是16位的，所以在处理中文的时候需要用<code>Reader</code>和<code>Writer</code>。</li><li>两种等级机构下，有一道桥梁<code>InputStreamReader</code>、<code>OutputStreamWriter</code>负责进行<code>InputStream</code>到<code>Reader</code>的适配和由<code>OutputStream</code>到<code>Writer</code>的适配。</li></ul><p>在Java中，有不同类型的<code>Reader/InputStream</code>输入流对应于不同的数据源：</p><ul><li><code>FileReader/FileInputStream</code> 用于从文件输入；<br><code>CharArrayReader/ByteArrayInputStream</code>用于从程序中的字符数组输入；</li><li><code>StringReader/StringBufferInputStream</code> 用于从程序中的字符串输入；<br><code>PipedReader/PipeInputStream</code>用于读取从另一个线程中的 -</li><li><code>PipedWriter/PipeOutputStream</code>写入管道的数据。</li><li>相应的也有不同类型的<code>Writer/OutputStream</code>输出流对应于不同的数据源：<code>FileWriter/FileOutputStream，CharArrayWriter/ByteArrayOutputStream，StringWriter，PipeWriter/PipedOutputStream</code>。</li></ul><h4 id="IO流的应用选择"><a href="#IO流的应用选择" class="headerlink" title="IO流的应用选择"></a>IO流的应用选择</h4><h5 id="1、确定选用流对象的步骤"><a href="#1、确定选用流对象的步骤" class="headerlink" title="1、确定选用流对象的步骤"></a>1、确定选用流对象的步骤</h5><p>确定原始数据的格式<br>确定是输入还是输出<br>是否需要转换流<br>数据的来源（去向）<br>是否需要缓冲<br>是否需要格式化输出</p><h5 id="按照数据格式分"><a href="#按照数据格式分" class="headerlink" title="按照数据格式分"></a>按照数据格式分</h5><p>二进制格式（只要确定不是纯文本格式的），<code>InputStream</code>, <code>OutputStream</code>, 及其所有带<code>Stream</code>子类</p><ul><li>纯文本格式（比如英文/汉字/或其他编码文字）：<code>Reader, Writer</code>, 及其相关子类</li></ul><h5 id="按照输入输出分"><a href="#按照输入输出分" class="headerlink" title="按照输入输出分"></a>按照输入输出分</h5><p>输入：<code>Reader， InputStream</code>，及其相关子类<br>输出：<code>Writer，OutputStream</code>，及其相关子类</p><h5 id="按缓冲功能分"><a href="#按缓冲功能分" class="headerlink" title="按缓冲功能分"></a>按缓冲功能分</h5><p>要缓冲：<code>BufferedInputStream, BufferedOuputStream, BuffereaReader, BufferedWriter</code></p><h5 id="按照格式化输出"><a href="#按照格式化输出" class="headerlink" title="按照格式化输出"></a>按照格式化输出</h5><p>需要格式化输出：<code>PrintStream</code>（输出字节），<code>PrintWriter</code>（输出字符）</p><h5 id="特殊需求"><a href="#特殊需求" class="headerlink" title="特殊需求"></a>特殊需求</h5><p>从<code>Stream</code>转化为<code>Reader，Writer：InputStreamReader，OutputStreamWriter</code><br>对象输入输出流：<code>ObjectInputStream，ObjectOutputStream</code><br>进程间通信：<code>PipeInputStream，PipeOutputStream，PipeReader，PipeWriter</code><br>合并输入：<code>SequenceInputStream</code><br>更特殊的需要：<code>PushbackInputStream, PushbackReader, LineNumberInputStream, LineNumberReader</code><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//对象流案例</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo3</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException,</span></span><br><span class="line"><span class="function">            ClassNotFoundException </span>&#123;</span><br><span class="line">        Cat cat = <span class="keyword">new</span> Cat(<span class="string">"tom"</span>, <span class="number">3</span>);</span><br><span class="line">        FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(<span class="keyword">new</span> File(<span class="string">"c:\\Cat.txt"</span>));</span><br><span class="line">        ObjectOutputStream oos = <span class="keyword">new</span> ObjectOutputStream(fos);</span><br><span class="line">        oos.writeObject(cat);</span><br><span class="line">        System.out.println(cat);</span><br><span class="line">        oos.close();</span><br><span class="line">        <span class="comment">// 反序列化</span></span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(<span class="keyword">new</span> File(<span class="string">"c:\\Cat.txt"</span>));</span><br><span class="line">        ObjectInputStream ois = <span class="keyword">new</span> ObjectInputStream(fis);</span><br><span class="line">        Object readObject = ois.readObject();</span><br><span class="line">        Cat cat2 = (Cat) readObject;</span><br><span class="line">        System.out.println(cat2);</span><br><span class="line">        fis.close();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Cat</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> String name;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Cat</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Cat</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Cat [name="</span> + name + <span class="string">", age="</span> + age + <span class="string">"]"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//DataInputStream 基本数据类型和String</span></span><br><span class="line"><span class="comment">//操作基本数据类型的方法：</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">readInt</span><span class="params">()</span>:<span class="comment">//一次读取四个字节，并将其转成int值。</span></span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">readBoolean</span><span class="params">()</span>:<span class="comment">//一次读取一个字节。</span></span></span><br><span class="line"><span class="function"><span class="keyword">short</span> <span class="title">readShort</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">readLong</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//剩下的数据类型一样。</span></span><br><span class="line"><span class="function">String <span class="title">readUTF</span><span class="params">()</span>:<span class="comment">//按照utf-8修改版读取字符。注意，它只能读writeUTF()//写入的字符数据。</span></span></span><br><span class="line"><span class="function">DataOutputStream</span></span><br><span class="line"><span class="function"><span class="title">DataOutputStream</span><span class="params">(OutputStream)</span>:</span></span><br><span class="line"><span class="function"><span class="comment">//操作基本数据类型的方法:</span></span></span><br><span class="line"><span class="function"><span class="title">writeInt</span><span class="params">(<span class="keyword">int</span>)</span>：<span class="comment">//一次写入四个字节。</span></span></span><br><span class="line"><span class="function"><span class="comment">//注意和write(int)不同。write(int)只将该整数的最低一个8位写入。剩余三个8位丢弃。</span></span></span><br><span class="line"><span class="function"><span class="title">writeBoolean</span><span class="params">(<span class="keyword">boolean</span>)</span></span>;</span><br><span class="line">writeShort(<span class="keyword">short</span>);</span><br><span class="line">writeLong(<span class="keyword">long</span>);</span><br><span class="line"><span class="comment">//剩下是数据类型也也一样。</span></span><br><span class="line">writeUTF(String):<span class="comment">//按照utf-8修改版将字符数据进行存储。只能通过readUTF读取。</span></span><br></pre></td></tr></table></figure><p>转换流：<br><code>InputStreamReader</code>：字节到字符的桥梁。<br><code>OutputStreamWriter</code>：字符到字节的桥梁。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//从字节流中读取字符信息</span></span><br><span class="line">BufferedReader bf=<span class="keyword">new</span> InputStreamReader(<span class="keyword">new</span> FileInputStream(<span class="string">"src"</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">//将字符信息用指定字节编码写出</span></span><br><span class="line">OutputStreamWriter bw=<span class="keyword">new</span> OutputStreamWriter(<span class="keyword">new</span> FileOutputStream(<span class="string">"target"</span>),<span class="string">"utf-8"</span>);</span><br><span class="line">        bw.write(<span class="string">"Hello"</span>);</span><br></pre></td></tr></table></figure></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TestIo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Demo4</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        File file = <span class="keyword">new</span> File(<span class="string">"c:\\a.txt"</span>);</span><br><span class="line">        File fileGBK = <span class="keyword">new</span> File(<span class="string">"c:\\gbk.txt"</span>);</span><br><span class="line">        File fileUTF = <span class="keyword">new</span> File(<span class="string">"c:\\utf.txt"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 写入</span></span><br><span class="line">        <span class="comment">// 使用系统默认码表写入</span></span><br><span class="line">        testWriteFile(file);</span><br><span class="line">        <span class="comment">// 使用gbk编码向gbk文件写入信息</span></span><br><span class="line">        testWriteFile(fileGBK, <span class="string">"gbk"</span>);</span><br><span class="line">        <span class="comment">// 使用utf-8向utf-8文件中写入信息</span></span><br><span class="line">        testWriteFile(fileUTF, <span class="string">"utf-8"</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 读取</span></span><br><span class="line">        <span class="comment">// 默认编码</span></span><br><span class="line">        testReadFile(file);</span><br><span class="line">        <span class="comment">// 传入gbk编码文件,使用gbk解码</span></span><br><span class="line">        testReadFile(fileGBK, <span class="string">"gbk"</span>);</span><br><span class="line">        <span class="comment">// 传入utf-8文件,使用utf-8解码</span></span><br><span class="line">        testReadFile(fileUTF, <span class="string">"utf-8"</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用系统码表将信息写入到文件中</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testWriteFile</span><span class="params">(File file)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(file);</span><br><span class="line">        OutputStreamWriter ops = <span class="keyword">new</span> OutputStreamWriter(fos);</span><br><span class="line">        ops.write(<span class="string">"中国"</span>);</span><br><span class="line">        ops.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用指定码表,将信息写入到文件中</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testWriteFile</span><span class="params">(File file, String encod)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileOutputStream fos = <span class="keyword">new</span> FileOutputStream(file);</span><br><span class="line">        OutputStreamWriter ops = <span class="keyword">new</span> OutputStreamWriter(fos, encod);</span><br><span class="line">        ops.write(<span class="string">"中国"</span>);</span><br><span class="line">        ops.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 该方法中nputStreamReader使用系统默认编码读取文件.</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testReadFile</span><span class="params">(File file)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(file);</span><br><span class="line">        InputStreamReader ins = <span class="keyword">new</span> InputStreamReader(fis);</span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((len = ins.read()) != -<span class="number">1</span>) &#123;</span><br><span class="line">            System.out.print((<span class="keyword">char</span>) len);</span><br><span class="line">        &#125;</span><br><span class="line">        ins.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 该方法适合用指定编码读取文件</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">testReadFile</span><span class="params">(File file, String encod)</span></span></span><br><span class="line"><span class="function">            <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        FileInputStream fis = <span class="keyword">new</span> FileInputStream(file);</span><br><span class="line">        InputStreamReader ins = <span class="keyword">new</span> InputStreamReader(fis, encod);</span><br><span class="line">        <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> ((len = ins.read()) != -<span class="number">1</span>) &#123;</span><br><span class="line">            System.out.print((<span class="keyword">char</span>) len);</span><br><span class="line">        &#125;</span><br><span class="line">        ins.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;java IO流的设计是基于装饰者模式&amp;amp;适配模式，面对IO流庞大的包装类体系，核心是要抓住其功能所对应的装饰类。&lt;/p&gt;
&lt;p&gt;装饰模式又名包装（Wrapper）模式。装饰模式以对客户端透明的方式扩展对象的功能，是继承关系的一个替代方案。装饰模式通过创建一个包装对象，也就是装饰，来包裹真实的对象。装饰模式以对客户端透明的方式动态地给一个对象附加上更多的责任。换言之，客户端并不会觉得对象在装饰前和装饰后有什么不同。装饰模式可以在不创造更多子类的情况下，将对象的功能加以扩展。装饰模式把客户端的调用委派到被装饰类。装饰模式的关键在于这种扩展是完全透明的。&lt;br&gt;&lt;img src=&quot;http://upload-images.jianshu.io/upload_images/8926909-dc3cf3d3ce99df2d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240&quot; alt=&quot;装饰者模式&quot;&gt;&lt;br&gt;
    
    </summary>
    
      <category term="Java IO" scheme="https://tisonkong.github.io/github.io/categories/Java-IO/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="IO" scheme="https://tisonkong.github.io/github.io/tags/IO/"/>
    
      <category term="设计模式" scheme="https://tisonkong.github.io/github.io/tags/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/"/>
    
  </entry>
  
  <entry>
    <title>IO里的老熟人File类</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%E5%9F%BA%E7%A1%80%E4%B9%8BIO%E6%B5%81%EF%BC%88File%E7%AF%87%EF%BC%89/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java基础之IO流（File篇）/</id>
    <published>2018-05-20T14:35:08.028Z</published>
    <updated>2018-05-20T14:35:08.028Z</updated>
    
    <content type="html"><![CDATA[<p>File类描述的是一个文件或文件夹。（文件夹也可以称为目录），该类的出现是对文件系统的中的文件以及文件夹进行对象的封装。可以通过对象的思想来操作文件以及文件夹，通过该对象的方法得到文件或文件夹的信息方便了对文件与文件夹的属性信息进行操作。<a id="more"></a>文件包含很多的信息:如文件名、创建修改时间、大小、可读可写属性等。</p><h5 id="1-基本API"><a href="#1-基本API" class="headerlink" title="1.基本API"></a>1.基本API</h5><p>（1）.通过将给定路径来创建一个新File实例。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> File(String pathname);</span><br></pre></td></tr></table></figure></p><p>（2）.根据parent路径名字符串和child路径名创建一个新File实例。parent是指上级目录的路径，完整的路径为parent+child.<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> File(String parent, String child);</span><br></pre></td></tr></table></figure></p><p>（3）.根据parent抽象路径名和child路径名创建一个新File实例。 parent是指上级目录的路径，完整的路径为parent.getPath()+child.<br>说明：如果指定的路径不存在（没有这个文件或是文件夹），不会抛异常，这时file.exists()返回false。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">new</span> File(File parent, String child);</span><br></pre></td></tr></table></figure></p><p><strong><em>说明：如果指定的路径不存在（没有这个文件或是文件夹），不会抛异常，这时file.exists()返回false。</em></strong></p><p>（4）创建：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">createNewFile()   <span class="comment">//在指定位置创建一个空文件，成功就返回true，如果已存在就不创建然后返回false</span></span><br><span class="line"></span><br><span class="line">mkdir()           <span class="comment">//在指定位置创建目录，这只会创建最后一级目录，如果上级目录不存在就抛异常。</span></span><br><span class="line"></span><br><span class="line">mkdirs()          <span class="comment">//在指定位置创建目录，这会创建路径中所有不存在的目录。</span></span><br><span class="line"></span><br><span class="line">renameTo(File dest)  <span class="comment">//重命名文件或文件夹，也可以操作非空的文件夹，文件不同时相当于文件的剪切,剪切时候不能操作非空的文件夹。</span></span><br><span class="line">                     <span class="comment">//移动/重命名成功则返回true，失败则返回false。</span></span><br></pre></td></tr></table></figure></p><p>（5）.删除：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">delete()</span><br><span class="line"><span class="comment">//删除文件或一个空文件夹，如果是文件夹且不为空，则不能删除，成功返回true，失败返回false。</span></span><br><span class="line"></span><br><span class="line">deleteOnExit()</span><br><span class="line"><span class="comment">//在虚拟机终止时，请求删除此抽象路径名表示的文件或目录，保证程序异常时创建的临时文件也可以被删除</span></span><br></pre></td></tr></table></figure></p><p>（6）.判断：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">exists()         <span class="comment">//文件或文件夹是否存在。</span></span><br><span class="line"></span><br><span class="line">isFile()          <span class="comment">//是否是一个文件，如果不存在，则始终为false。</span></span><br><span class="line"></span><br><span class="line">isDirectory()     <span class="comment">//是否是一个目录，如果不存在，则始终为false。</span></span><br><span class="line"></span><br><span class="line">isHidden()        <span class="comment">//是否是一个隐藏的文件或是否是隐藏的目录。</span></span><br><span class="line"></span><br><span class="line">isAbsolute()      <span class="comment">//测试此抽象路径名是否为绝对路径名。</span></span><br></pre></td></tr></table></figure></p><p>（7）.获取：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">getName()    <span class="comment">//获取文件或文件夹的名称，不包含上级路径。</span></span><br><span class="line"></span><br><span class="line">getPath()    <span class="comment">//返回绝对路径，可以是相对路径，但是目录要指定</span></span><br><span class="line"></span><br><span class="line">getAbsolutePath()    <span class="comment">//获取文件的绝对路径，与文件是否存在没关系</span></span><br><span class="line"></span><br><span class="line">length()    <span class="comment">// 获取文件的大小（字节数），如果文件不存在则返回0L，如果是文件夹也返回0L。</span></span><br><span class="line"></span><br><span class="line">getParent()    <span class="comment">//返回此抽象路径名父目录的路径名字符串；如果此路径名没有指定父目录，则返回null。</span></span><br><span class="line"></span><br><span class="line">lastModified()    <span class="comment">//获取最后一次被修改的时间。</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">staic File[] listRoots()    <span class="comment">//列出所有的根目录（Window中就是所有系统的盘符）</span></span><br><span class="line"></span><br><span class="line">list()    <span class="comment">//返回目录下的文件或者目录名，包含隐藏文件。对于文件这样操作会返回null。</span></span><br><span class="line"></span><br><span class="line">list(FilenameFilter filter)    <span class="comment">//返回指定当前目录中符合过滤条件的子文件或子目录。对于文件这样操作会返回null。</span></span><br><span class="line"></span><br><span class="line">listFiles()    <span class="comment">//返回目录下的文件或者目录对象（File类实例），包含隐藏文件。对于文件这样操作会返回null。</span></span><br><span class="line"></span><br><span class="line">listFiles(FilenameFilter filter)    <span class="comment">//返回指定当前目录中符合过滤条件的子文件或子目录。对于文件这样操作会返回null。</span></span><br></pre></td></tr></table></figure></p><blockquote><p>路径问题</p><ul><li>对于UNIX平台，绝对路径名的前缀是”/“。相对路径名没有前缀。</li><li>对于Windows平台，绝对路径名的前缀由驱动器号和一个”:”组成，例”c:\…”。相对路径没有盘符前缀。<br>更专业的做法是使用<strong>File.separatorChar</strong>或者<strong>File.separator</strong>（前者为char，后者为String），这个值就会根据系统得到的相应的分割符。</li></ul></blockquote><h4 id="2-常见File类面试题"><a href="#2-常见File类面试题" class="headerlink" title="2.常见File类面试题"></a>2.常见File类面试题</h4><p>1、File类型中定义了什么方法来创建一级目录<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mkdirs() <span class="comment">//在指定位置创建目录，这会创建路径中所有不存在的目录。</span></span><br></pre></td></tr></table></figure></p><p>2、File类型中定义了什么方法来判断一个文件是否存在<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">exists()         <span class="comment">//文件或文件夹是否存在。</span></span><br></pre></td></tr></table></figure></p><p>3、如何用Java代码列出一个目录下所有的文件？<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//只查询当前目录</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        File f = <span class="keyword">new</span> File(<span class="string">"d:"</span>);</span><br><span class="line">        <span class="keyword">for</span>(File temp : f.listFiles()) &#123;</span><br><span class="line">            <span class="keyword">if</span>(temp.isFile()) &#123;</span><br><span class="line">                System.out.println(temp.getName());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果需要对文件夹继续展开</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        showDirectory(<span class="keyword">new</span> File(<span class="string">"d:\\file"</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">showDirectory</span><span class="params">(File f)</span> </span>&#123;</span><br><span class="line">        _walkDirectory(f, <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">_walkDirectory</span><span class="params">(File f, <span class="keyword">int</span> level)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(f.isDirectory()) &#123;</span><br><span class="line">            <span class="keyword">for</span>(File temp : f.listFiles()) &#123;</span><br><span class="line">                _walkDirectory(temp, level + <span class="number">1</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; level - <span class="number">1</span>; i++) &#123;</span><br><span class="line">                System.out.print(<span class="string">"\t"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            System.out.println(f.getName());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p>在Java 7中可以使用NIO.2的API来做同样的事情，代码如下所示：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">       Path initPath = Paths.get(<span class="string">"/Users/Hao/Downloads"</span>);</span><br><span class="line">       Files.walkFileTree(initPath, <span class="keyword">new</span> SimpleFileVisitor&lt;Path&gt;() &#123;</span><br><span class="line"></span><br><span class="line">           <span class="meta">@Override</span></span><br><span class="line">           <span class="function"><span class="keyword">public</span> FileVisitResult <span class="title">visitFile</span><span class="params">(Path file, BasicFileAttributes attrs)</span></span></span><br><span class="line"><span class="function">                   <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">               System.out.println(file.getFileName().toString());</span><br><span class="line">               <span class="keyword">return</span> FileVisitResult.CONTINUE;</span><br><span class="line">           &#125;</span><br><span class="line"></span><br><span class="line">       &#125;);</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;File类描述的是一个文件或文件夹。（文件夹也可以称为目录），该类的出现是对文件系统的中的文件以及文件夹进行对象的封装。可以通过对象的思想来操作文件以及文件夹，通过该对象的方法得到文件或文件夹的信息方便了对文件与文件夹的属性信息进行操作。
    
    </summary>
    
      <category term="Java IO" scheme="https://tisonkong.github.io/github.io/categories/Java-IO/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="IO" scheme="https://tisonkong.github.io/github.io/tags/IO/"/>
    
  </entry>
  
  <entry>
    <title>多线程从入门到放弃【4】</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%20%E5%A4%9A%E7%BA%BF%E7%A8%8B%E6%80%BB%E7%BB%93%E7%AF%874%E2%80%94%E2%80%94%E9%94%81%E6%9C%BA%E5%88%B6/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java 多线程总结篇4——锁机制/</id>
    <published>2018-05-20T14:35:08.027Z</published>
    <updated>2018-05-20T14:35:08.027Z</updated>
    
    <content type="html"><![CDATA[<p>在开发Java多线程应用程序中，各个线程之间由于要共享资源，必须用到锁机制。Java提供了多种多线程锁机制的实现方式，常见的有·synchronized、ReentrantLock、Semaphore、AtomicInteger·等。每种机制都有优缺点与各自的适用场景，必须熟练掌握他们的特点才能在Java多线程应用开发时得心应手。——<a href="https://www.cnblogs.com/hanganglin/p/3577096.html" target="_blank" rel="noopener">《Java锁机制详解》</a>。<br><a id="more"></a><br>线程同步有关的类图关系可用以下的图总结：<br><img src="http://upload-images.jianshu.io/upload_images/8926909-8ffafbe023d370cf.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="线程同步的类图"></p><h5 id="1、Java-Concurrency-API-中的-Lock接口是什么？对比同步它有什么优势？"><a href="#1、Java-Concurrency-API-中的-Lock接口是什么？对比同步它有什么优势？" class="headerlink" title="1、Java Concurrency API 中的 Lock接口是什么？对比同步它有什么优势？"></a>1、Java Concurrency API 中的 Lock接口是什么？对比同步它有什么优势？</h5><p><code>Lock</code>接口比同步方法和同步块（这里的同步就是考察<code>Synchronized</code>关键字）提供了更具扩展性的锁操作。<br>如果一个代码块被<code>synchronized</code>修饰了，当一个线程获取了对应的锁，并执行该代码块时，其他线程便只能一直等待，等待获取锁的线程释放锁，而这里获取锁的线程释放锁只会有两种情况：</p><ul><li>获取锁的线程执行完了该代码块，然后线程释放对锁的占有；</li><li>线程执行发生异常，此时JVM会让线程自动释放锁。</li></ul><p>那么如果这个获取锁的线程由于要等待IO或者其他原因（比如调用sleep方法）被阻塞了，但是又没有释放锁，其他线程便只能阻塞等待，非常影响效率。因此就需要有一种机制可以不让等待的线程一直无期限地等待下去（比如只等待一定的时间或者能够响应中断），通过<code>Lock</code>就可以办到。</p><p>再举个例子：当有多个线程读写文件时，读操作和写操作会发生冲突现象，写操作和写操作会发生冲突现象，但是读操作和读操作不会发生冲突现象。<br>但是采用synchronized关键字来实现同步的话，就会导致一个问题：如果多个线程都只是进行读操作，所以当一个线程在进行读操作时，其他线程只能等待无法进行读操作。因此就需要一种机制来使得多个线程都只是进行读操作时，线程之间不会发生冲突，通过<code>Lock</code>就可以办到。</p><p><code>Lock</code>不是Java语言内置的，<code>synchronized</code>是Java语言的关键字，因此是内置特性，<code>Lock</code>是一个类，通过这个类可以实现同步访问；他们允许更灵活的结构，可以具有完全不同的性质，并且可以支持多个相关类的条件对象。它的优势有：可以使锁更公平；可以使线程在等待锁的时候响应中断；可以让线程尝试获取锁，并在无法获取锁的时候立即返回或者等待一段时间；可以在不同的范围，以不同的顺序获取和释放锁。</p><p>关于API及代码的例子请移步：<a href="https://www.cnblogs.com/dolphin0520/p/3923167.html" target="_blank" rel="noopener">《java并发编程Lock》</a>。常用接口方法如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Lock</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">lockInterruptibly</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">(<span class="keyword">long</span> time, TimeUnit unit)</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function">Condition <span class="title">newCondition</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>首先<code>lock()</code>方法是平常使用得最多的一个方法，就是用来获取锁。如果锁已被其他线程获取，则进行等待。由于在前面讲到如果采用<code>Lock</code>，必须主动去释放锁，并且在发生异常时，不会自动释放锁。因此一般来说，使用<code>Lock</code>必须在<code>try{}catch{}</code>块中进行，并且将释放锁的操作放在<code>finally</code>块中进行，以保证锁一定被被释放，防止死锁的发生。通常使用<code>Lock</code>来进行同步的话，是以下面这种形式去使用的：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line">Lock lock = ...;</span><br><span class="line">lock.lock();</span><br><span class="line"><span class="keyword">try</span>&#123;</span><br><span class="line">    <span class="comment">//处理任务</span></span><br><span class="line">&#125;<span class="keyword">catch</span>(Exception ex)&#123;</span><br><span class="line"></span><br><span class="line">&#125;<span class="keyword">finally</span>&#123;</span><br><span class="line">    lock.unlock();   <span class="comment">//释放锁</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><code>tryLock()</code>方法是有返回值的，它表示用来尝试获取锁，如果获取成功，则返回<code>true</code>，如果获取失败（即锁已被其他线程获取），则返回<code>false</code>，也就说这个方法无论如何都会立即返回。在拿不到锁时不会一直在那等待。<code>tryLock(long time, TimeUnit unit)</code>方法和<code>tryLock()</code>方法是类似的，只不过区别在于这个方法在拿不到锁时会等待一定的时间，在时间期限之内如果还拿不到锁，就返回<code>false</code>。如果如果一开始拿到锁或者在等待期间内拿到了锁，则返回<code>true</code>。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">Lock lock = ...;</span><br><span class="line"><span class="keyword">if</span>(lock.tryLock()) &#123;</span><br><span class="line">     <span class="keyword">try</span>&#123;</span><br><span class="line">         <span class="comment">//处理任务</span></span><br><span class="line">     &#125;<span class="keyword">catch</span>(Exception ex)&#123;</span><br><span class="line"></span><br><span class="line">     &#125;<span class="keyword">finally</span>&#123;</span><br><span class="line">         lock.unlock();   <span class="comment">//释放锁</span></span><br><span class="line">     &#125;</span><br><span class="line">&#125;<span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">//如果不能获取锁，则直接做其他事情</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><code>lockInterruptibly()</code>方法比较特殊，当通过这个方法去获取锁时，如果线程正在等待获取锁，则这个线程能够响应中断，即中断线程的等待状态。也就使说，当两个线程同时通过<code>lock.lockInterruptibly()</code>想获取某个锁时，假若此时线程A获取到了锁，而线程B只有在等待，那么对线程B调用<code>threadB.interrupt()</code>方法能够中断线程B的等待过程。由于<code>lockInterruptibly()</code>的声明中抛出了异常，所以<code>lock.lockInterruptibly()</code>必须放在try块中或者在调用<code>lockInterruptibly()</code>的方法外声明抛出<code>InterruptedException</code>。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">method</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">    lock.lockInterruptibly();</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">     <span class="comment">//.....</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">finally</span> &#123;</span><br><span class="line">        lock.unlock();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p><code>ReentrantLock</code>，意思是“可重入锁”，<code>ReentrantLock</code>是唯一实现了<code>Lock</code>接口的类，并且<code>ReentrantLock</code>提供了更多的方法。以下给出一个<code>ReentrantLock</code>的运行实例：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> ArrayList&lt;Integer&gt; arrayList = <span class="keyword">new</span> ArrayList&lt;Integer&gt;();</span><br><span class="line">    <span class="keyword">private</span> Lock lock = <span class="keyword">new</span> ReentrantLock();    <span class="comment">//注意这个地方，声明为类的属性</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span>  </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> Test test = <span class="keyword">new</span> Test();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                test.insert(Thread.currentThread());</span><br><span class="line">            &#125;;</span><br><span class="line">        &#125;.start();</span><br><span class="line">         <span class="comment">//可以用Java箭头函数特性改写上述冗余代码：</span></span><br><span class="line">         <span class="comment">// new Thread()&#123;()-&gt;Thread.currentThread&#125;.start();</span></span><br><span class="line">        <span class="keyword">new</span> Thread()&#123;</span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                test.insert(Thread.currentThread());</span><br><span class="line">            &#125;;</span><br><span class="line">        &#125;.start();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insert</span><span class="params">(Thread thread)</span> </span>&#123;</span><br><span class="line">        lock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(thread.getName()+<span class="string">"得到了锁"</span>);</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">5</span>;i++) &#123;</span><br><span class="line">                arrayList.add(i);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">            <span class="comment">// <span class="doctag">TODO:</span> handle exception</span></span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            System.out.println(thread.getName()+<span class="string">"释放了锁"</span>);</span><br><span class="line">            lock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p>上文中提到了Lock接口以及对象，使用它，很优雅的控制了竞争资源的安全访问，但是这种锁不区分读写，称这种锁为普通锁。为了提高性能，Java提供了读写锁，在读的地方使用读锁，在写的地方使用写锁，灵活控制，如果没有写锁的情况下，读是无阻塞的,在一定程度上提高了程序的执行效率。Java中读写锁有个接口<code>java.util.concurrent.locks. ReadWriteLock</code>，也有具体的实现<code>ReentrantReadWriteLock</code>，因而会有下面的提问：</p><h5 id="2、ReadWriteLock是什么？"><a href="#2、ReadWriteLock是什么？" class="headerlink" title="2、ReadWriteLock是什么？"></a>2、ReadWriteLock是什么？</h5><p>当有写线程时，则写线程独占同步状态，当没有写线程时只有读线程时，则多个读线程可以共享同步状态。读写锁就是为了实现这种效果而生。</p><p>读写锁：分为读锁和写锁，多个读锁不互斥，读锁与写锁互斥，这是由jvm自己控制的，我们只要上好相应的锁即可。如果你的代码只读数据，可以很多人同时读，但不能同时写，那就上读锁；如果你的代码修改数据，只能有一个人在写，且不能同时读取，那就上写锁。总之，读的时候上读锁，写的时候上写锁！读写锁接口：<code>ReadWriteLock</code>，它的具体实现类为：<code>ReentrantReadWriteLock</code>。<br><a href="https://www.cnblogs.com/liang1101/p/6475555.html?utm_source=itdadao&amp;utm_medium=referral" target="_blank" rel="noopener">《ReadWriteLock场景应用》</a>：在多线程的环境下，对同一份数据进行读写，会涉及到线程安全的问题。比如在一个线程读取数据的时候，另外一个线程在写数据，而导致前后数据的不一致性；一个线程在写数据的时候，另一个线程也在写，同样也会导致线程前后看到的数据的不一致性。这时候可以在读写方法中加入互斥锁，任何时候只能允许一个线程的一个读或写操作，而不允许其他线程的读或写操作，这样是可以解决这样以上的问题，但是效率却大打折扣了。因为在真实的业务场景中，一份数据，读取数据的操作次数通常高于写入数据的操作，而线程与线程间的读读操作是不涉及到线程安全的问题，没有必要加入互斥锁，只要在读-写，写-写期间上锁就行了。<a href="https://www.cnblogs.com/sheeva/p/6480116.html" target="_blank" rel="noopener">API调用请移步</a></p><p>构造了一个线程安全的缓存，先创建一个<code>ReentrantReadWriteLock</code>对象，构造函数 <code>false</code> 代表是非公平的（非公平的含义和ReentrantLock相同）。然后通过<code>readLock</code>、<code>writeLock</code>方法分别获取读锁和写锁。在做读操作的时候，也就是<code>get</code>方法，我们要先获取读锁；在做写操作的时候，即put方法，我们要先获取写锁。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReadWriteCache</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Map&lt;String, Object&gt; data = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> ReadWriteLock lock = <span class="keyword">new</span> ReentrantReadWriteLock(<span class="keyword">false</span>);</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Lock rlock = lock.readLock();</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> Lock wlock = lock.writeLock();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">get</span><span class="params">(String key)</span> </span>&#123;</span><br><span class="line">        rlock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> data.get(key);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            rlock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">put</span><span class="params">(String key, Object value)</span> </span>&#123;</span><br><span class="line">        wlock.lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> data.put(key, value);</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            wlock.unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5 id="3、锁机制有什么用"><a href="#3、锁机制有什么用" class="headerlink" title="3、锁机制有什么用"></a>3、锁机制有什么用</h5><p>有些业务逻辑在执行过程中要求对数据进行排他性的访问，于是需要通过一些机制保证在此过程中数据被锁住不会被外界修改，这就是所谓的锁机制。</p><h5 id="4、什么是乐观锁（Optimistic-Locking）？如何实现乐观锁？如何避免ABA问题"><a href="#4、什么是乐观锁（Optimistic-Locking）？如何实现乐观锁？如何避免ABA问题" class="headerlink" title="4、什么是乐观锁（Optimistic Locking）？如何实现乐观锁？如何避免ABA问题"></a>4、什么是乐观锁（Optimistic Locking）？如何实现乐观锁？如何避免ABA问题</h5><p>悲观锁(<code>Pessimistic Lock</code>), 顾名思义，就是很悲观，每次去拿数据的时候都认为别人会修改，所以每次在拿数据的时候都会上锁，这样别人想拿这个数据就会block直到它拿到锁。传统的关系型数据库里边就用到了很多这种锁机制，比如行锁，表锁等，读锁，写锁等，都是在做操作之前先上锁。</p><p>乐观锁(<code>Optimistic Lock</code>), 顾名思义，就是很乐观，每次去拿数据的时候都认为别人不会修改，所以不会上锁，但是在更新的时候会判断一下在此期间别人有没有去更新这个数据，可以使用版本号等机制。乐观锁适用于多读的应用类型，这样可以提高吞吐量，像数据库如果提供类似于<code>write_condition</code>机制的其实都是提供的乐观锁。</p><h5 id="5、解释以下名词：重排序，自旋锁，偏向锁，轻量级锁，可重入锁，公平锁，非公平锁，乐观锁，悲观锁"><a href="#5、解释以下名词：重排序，自旋锁，偏向锁，轻量级锁，可重入锁，公平锁，非公平锁，乐观锁，悲观锁" class="headerlink" title="5、解释以下名词：重排序，自旋锁，偏向锁，轻量级锁，可重入锁，公平锁，非公平锁，乐观锁，悲观锁"></a>5、解释以下名词：重排序，自旋锁，偏向锁，轻量级锁，可重入锁，公平锁，非公平锁，乐观锁，悲观锁</h5><p><strong>重入锁（ReentrantLock）</strong>是一种递归无阻塞的同步机制。重入锁，也叫做递归锁，指的是同一线程 外层函数获得锁之后 ，内层递归函数仍然有获取该锁的代码，但不受影响。在JAVA环境下 <code>ReentrantLock</code> 和<code>synchronized</code>都是 可重入锁。</p><p><strong>自旋锁</strong>，由于自旋锁使用者一般保持锁时间非常短，因此选择自旋而不是睡眠是非常必要的，自旋锁的效率远高于互斥锁。如何旋转呢？何为自旋锁，就是如果发现锁定了，不是睡眠等待，而是采用让当前线程不停地的在循环体内执行实现的，当循环的条件被其他线程改变时 才能进入临界区。</p><p><strong>偏向锁(Biased Locking)</strong>是Java6引入的一项多线程优化，它会偏向于第一个访问锁的线程，如果在运行过程中，同步锁只有一个线程访问，不存在多线程争用的情况，则线程是不需要触发同步的，这种情况下，就会给线程加一个偏向锁。 如果在运行过程中，遇到了其他线程抢占锁，则持有偏向锁的线程会被挂起，JVM会消除它身上的偏向锁，将锁恢复到标准的轻量级锁。</p><p><strong>轻量级锁</strong>是由偏向所升级来的，偏向锁运行在一个线程进入同步块的情况下，当第二个线程加入锁争用的时候，偏向锁就会升级为轻量级锁。</p><p><strong>重入锁（ReentrantLock）</strong>是一种递归无阻塞的同步机制，也叫做递归锁，指的是同一线程 外层函数获得锁之后 ，内层递归函数仍然有获取该锁的代码，但不受影响。 在JAVA环境下 <code>ReentrantLock</code> 和<code>synchronized</code>都是 可重入锁。</p><p><strong>公平锁</strong>，就是很公平，在并发环境中，每个线程在获取锁时会先查看此锁维护的等待队列，如果为空，或者当前线程线程是等待队列的第一个，就占有锁，否则就会加入到等待队列中，以后会按照FIFO的规则从队列中取到自己</p><p><strong>非公平锁</strong>比较粗鲁，上来就直接尝试占有锁。在公平的锁上，线程按照他们发出请求的顺序获取锁，但在非公平锁上，则允许‘插队’：当一个线程请求非公平锁时，如果在发出请求的同时该锁变成可用状态，那么这个线程会跳过队列中所有的等待线程而获得锁。     非公平的ReentrantLock 并不提倡 插队行为，但是无法防止某个线程在合适的时候进行插队。</p><h5 id="6、什么时候应该使用可重入锁？"><a href="#6、什么时候应该使用可重入锁？" class="headerlink" title="6、什么时候应该使用可重入锁？"></a>6、什么时候应该使用可重入锁？</h5><p>场景1：如果已加锁，则不再重复加锁。a、忽略重复加锁。b、用在界面交互时点击执行较长时间请求操作时，防止多次点击导致后台重复执行（忽略重复触发）。以上两种情况多用于进行非重要任务防止重复执行，（如：清除无用临时文件，检查某些资源的可用性，数据备份操作等）</p><p>场景2：如果发现该操作已经在执行，则尝试等待一段时间，等待超时则不执行（尝试等待执行）这种其实属于场景2的改进，等待获得锁的操作有一个时间的限制，如果超时则放弃执行。用来防止由于资源处理不当长时间占用导致死锁情况（大家都在等待资源，导致线程队列溢出）。</p><p>场景3：如果发现该操作已经加锁，则等待一个一个加锁（同步执行，类似<code>synchronized</code>）这种比较常见大家也都在用，主要是防止资源使用冲突，保证同一时间内只有一个操作可以使用该资源。但与synchronized的明显区别是性能优势（伴随jvm的优化这个差距在减小）。同时Lock有更灵活的锁定方式，公平锁与不公平锁，而synchronized永远是公平的。这种情况主要用于对资源的争抢（如：文件操作，同步消息发送，有状态的操作等）</p><p>场景4：可中断锁。<code>synchronized</code>与<code>Lock</code>在默认情况下是不会响应中断(interrupt)操作，会继续执行完。<code>lockInterruptibly()</code>提供了可中断锁来解决此问题。（场景3的另一种改进，没有超时，只能等待中断或执行完毕）这种情况主要用于取消某些操作对资源的占用。如：（取消正在同步运行的操作，来防止不正常操作长时间占用造成的阻塞）</p><h5 id="7、简述锁的等级方法锁、对象锁、类锁"><a href="#7、简述锁的等级方法锁、对象锁、类锁" class="headerlink" title="7、简述锁的等级方法锁、对象锁、类锁"></a>7、简述锁的等级方法锁、对象锁、类锁</h5><p>方法锁（<code>synchronized</code>修饰方法时）通过在方法声明中加入 <code>synchronized</code>关键字来声明 <code>synchronized</code>方法。<code>synchronized</code>方法控制对类成员变量的访问： 每个类实例对应一把锁，每个<code>synchronized</code>方法都必须获得调用该方法的类实例的锁方能执行，否则所属线程阻塞，方法一旦执行，就独占该锁，直到从该方法返回时才将锁释放，此后被阻塞的线程方能获得该锁，重新进入可执行状态。这种机制确保了同一时刻对于每一个类实例，其所有声明为 <code>synchronized</code>的成员函数中至多只有一个处于可执行状态，从而有效避免了类成员变量的访问冲突。</p><p>对象锁（<code>synchronized</code>修饰方法或代码块）当一个对象中有<code>synchronized method</code>或<code>synchronized block</code>的时候调用此对象的同步方法或进入其同步区域时，就必须先获得对象锁。如果此对象的对象锁已被其他调用者占用，则需要等待此锁被释放。（方法锁也是对象锁）。java的所有对象都含有1个互斥锁，这个锁由JVM自动获取和释放。线程进入<code>synchronized</code>方法的时候获取该对象的锁，当然如果已经有线程获取了这个对象的锁，那么当前线程会等待；<code>synchronized</code>方法正常返回或者抛异常而终止，JVM会自动释放对象锁。这里也体现了用<code>synchronized</code>来加锁的1个好处，方法抛异常的时候，锁仍然可以由JVM来自动释放。　</p><p>类锁(<code>synchronized</code>修饰静态的方法或代码块)，由于一个<code>class</code>不论被实例化多少次，其中的静态方法和静态变量在内存中都只有一份。所以，一旦一个静态的方法被申明为<code>synchronized</code>。此类所有的实例化对象在调用此方法，共用同一把锁，我们称之为类锁。对象锁是用来控制实例方法之间的同步，类锁是用来控制静态方法（或静态变量互斥体）之间的同步。类锁只是一个概念上的东西，并不是真实存在的，它只是用来帮助我们理解锁定实例方法和静态方法的区别的。java类可能会有很多个对象，但是只有1个<code>Class</code>对象，也就是说类的不同实例之间共享该类的<code>Class</code>对象。<code>Class</code>对象其实也仅仅是1个java对象，只不过有点特殊而已。由于每个java对象都有1个互斥锁，而类的静态方法是需要<code>Class</code>对象。所以所谓的类锁，不过是<code>Class</code>对象的锁而已。获取类的<code>Class</code>对象有好几种，最简单的就是［类名.class］的方式。</p><h5 id="8、Java中活锁和死锁有什么区别？"><a href="#8、Java中活锁和死锁有什么区别？" class="headerlink" title="8、Java中活锁和死锁有什么区别？"></a>8、Java中活锁和死锁有什么区别？</h5><p>死锁：是指两个或两个以上的进程（或线程）在执行过程中，因争夺资源而造成的一种互相等待的现象，若无外力作用，它们都将无法推进下去。此时称系统处于死锁状态或系统产生了死锁，这些永远在互相等待的进程称为死锁进程。死锁发生的四个条件</p><p>1、互斥条件：线程对资源的访问是排他性的，如果一个线程对占用了某资源，那么其他线程必须处于等待状态，直到资源被释放。</p><p>2、请求和保持条件：线程T1至少已经保持了一个资源R1占用,但又提出对另一个资源R2请求，而此时，资源R2被其他线程T2占用，于是该线程T1也必须等待，但又对自己保持的资源R1不释放。</p><p>3、不剥夺条件：线程已获得的资源，在未使用完之前，不能被其他线程剥夺，只能在使用完以后由自己释放。</p><p>4、环路等待条件：在死锁发生时，必然存在一个“进程-资源环形链”，即：<code>{p0,p1,p2,...pn}</code>,进程p0（或线程）等待p1占用的资源，p1等待p2占用的资源，pn等待p0占用的资源。（最直观的理解是，p0等待p1占用的资源，而p1而在等待p0占用的资源，于是两个进程就相互等待）</p><p>活锁：是指线程1可以使用资源，但它很礼貌，让其他线程先使用资源，线程2也可以使用资源，但它很绅士，也让其他线程先使用资源。这样你让我，我让你，最后两个线程都无法使用资源。</p><h5 id="9、如何确保-N-个线程可以访问-N-个资源同时又不导致死锁？"><a href="#9、如何确保-N-个线程可以访问-N-个资源同时又不导致死锁？" class="headerlink" title="9、如何确保 N 个线程可以访问 N 个资源同时又不导致死锁？"></a>9、如何确保 N 个线程可以访问 N 个资源同时又不导致死锁？</h5><p>预防死锁，预先破坏产生死锁的四个条件。互斥不可能破坏，所以有如下3种方法：</p><p>1.破坏，请求和保持条件1.1）进程等所有要请求的资源都空闲时才能申请资源，这种方法会使资源严重浪费（有些资源可能仅在运行初期或结束时才使用，甚至根本不使用）1.2）允许进程获取初期所需资源后，便开始运行，运行过程中再逐步释放自己占有的资源。比如有一个进程的任务是把数据复制到磁盘中再打印，前期只需要获得磁盘资源而不需要获得打印机资源，待复制完毕后再释放掉磁盘资源。这种方法比上一种好，会使资源利用率上升。</p><p>2.破坏，不可抢占条件。这种方法代价大，实现复杂</p><p>3.破坏，循坏等待条件。对各进程请求资源的顺序做一个规定，避免相互等待。这种方法对资源的利用率比前两种都高，但是前期要为设备指定序号，新设备加入会有一个问题，其次对用户编程也有限制</p><h5 id="10、死锁与饥饿的区别？"><a href="#10、死锁与饥饿的区别？" class="headerlink" title="10、死锁与饥饿的区别？"></a>10、死锁与饥饿的区别？</h5><p>相同点：二者都是由于竞争资源而引起的。</p><p>不同点：</p><ul><li>从进程状态考虑，死锁进程都处于等待状态，忙等待(处于运行或就绪状态)的进程并非处于等待状态，但却可能被饿死；</li><li>死锁进程等待永远不会被释放的资源，饿死进程等待会被释放但却不会分配给自己的资源，表现为等待时限没有上界(排队等待或忙式等待)；</li><li>死锁一定发生了循环等待，而饿死则不然。这也表明通过资源分配图可以检测死锁存在与否，但却不能检测是否有进程饿死；</li><li>死锁一定涉及多个进程，而饥饿或被饿死的进程可能只有一个。</li><li>在饥饿的情形下，系统中有至少一个进程能正常运行，只是饥饿进程得不到执行机会。而死锁则可能会最终使整个系统陷入死锁并崩溃<h5 id="11、怎么检测一个线程是否拥有锁？"><a href="#11、怎么检测一个线程是否拥有锁？" class="headerlink" title="11、怎么检测一个线程是否拥有锁？"></a>11、怎么检测一个线程是否拥有锁？</h5><code>java.lang.Thread</code>中有一个方法叫<code>holdsLock()</code>，它返回true如果当且仅当当前线程拥有某个具体对象的锁<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">Object o = <span class="keyword">new</span> Object();</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">new</span> Thread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">synchronized</span>(o) &#123;</span><br><span class="line">                System.out.println(<span class="string">"child thread: holdLock: "</span> +</span><br><span class="line">                    Thread.holdsLock(o));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;).start();</span><br><span class="line"></span><br><span class="line">    System.out.println(<span class="string">"main thread: holdLock: "</span> + Thread.holdsLock(o));</span><br><span class="line">    Thread.sleep(<span class="number">2000</span>);</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">main thread: holdLock: <span class="keyword">false</span></span><br><span class="line">child thread: holdLock: <span class="keyword">true</span></span><br></pre></td></tr></table></figure><h5 id="12、如何实现分布式锁？"><a href="#12、如何实现分布式锁？" class="headerlink" title="12、如何实现分布式锁？"></a>12、如何实现分布式锁？</h5><p>基于数据库实现分布式锁<br>基于缓存（redis，memcached，tair）实现分布式锁<br>基于Zookeeper实现分布式锁<br>可以参考详情<a href="http://www.hollischuang.com/archives/1716" target="_blank" rel="noopener">《分布式锁的几种实现方式》</a> 、 <a href="https://www.cnblogs.com/rwxwsblog/p/6046034.html" target="_blank" rel="noopener">《分布式锁的3种方式》</a></p><h5 id="13、有哪些无锁数据结构，他们实现的原理是什么？"><a href="#13、有哪些无锁数据结构，他们实现的原理是什么？" class="headerlink" title="13、有哪些无锁数据结构，他们实现的原理是什么？"></a>13、有哪些无锁数据结构，他们实现的原理是什么？</h5><p>java 1.5提供了一种无锁队列（<code>wait-free/lock-free</code>）<code>ConcurrentLinkedQueue</code>，可支持多个生产者多个消费者线程的环境：网上别人自己实现的一种<a href="http://aigo.iteye.com/blog/2292229" target="_blank" rel="noopener">无锁算法队列</a>，原理和jdk官方的<code>ConcurrentLinkedQueue</code>相似：通过<code>volatile</code>关键字来保证数据唯一性（注：java的volatile和c++的volatile关键字是两码事！），但是里面又用到atomic，感觉有点boost::lockfree::queue的风格，估计参考了boost的代码来编写这个java无锁队列。</p><h5 id="14、Executors类是什么？-Executor和Executors的区别"><a href="#14、Executors类是什么？-Executor和Executors的区别" class="headerlink" title="14、Executors类是什么？ Executor和Executors的区别"></a>14、Executors类是什么？ Executor和Executors的区别</h5><p>正如上面所说，这三者均是 Executor 框架中的一部分。Java 开发者很有必要学习和理解他们，以便更高效的使用 Java 提供的不同类型的线程池。总结一下这三者间的区别，以便大家更好的理解：</p><p><code>Executor</code>和 <code>ExecutorService</code> 这两个接口主要的区别是：<code>ExecutorService</code> 接口继承了<code>Executor</code> 接口，是<code>Executor</code>的子接口<br><code>Executor</code>和<code>ExecutorService</code> 第二个区别是：<code>Executor</code>接口定义了 <code>execute()</code>方法用来接收一个<code>Runnable</code>接口的对象，而 <code>ExecutorService</code>接口中的 <code>submit()</code>方法可以接受<code>Runnable</code>和<code>Callable</code>接口的对象。<br><code>Executor</code> 和 <code>ExecutorService</code> 接口第三个区别是<code>Executor</code>中的 <code>execute()</code>方法不返回任何结果，而 <code>ExecutorService</code>中的 <code>submit()</code>方法可以通过一个 <code>Future</code>对象返回运算结果。<br><code>Executor</code> 和 <code>ExecutorService</code> 接口第四个区别是除了允许客户端提交一个任务，<code>ExecutorService</code> 还提供用来控制线程池的方法。比如：调用 <code>shutDown()</code>方法终止线程池。可以通过 《Java Concurrency in Practice》 一书了解更多关于关闭线程池和如何处理 <code>pending</code> 的任务的知识。<br><code>Executors</code> 类提供工厂方法用来创建不同类型的线程池。比如: <code>newSingleThreadExecutor()</code> 创建一个只有一个线程的线程池，<code>newFixedThreadPool(int numOfThreads)</code>来创建固定线程数的线程池，<code>newCachedThreadPool()</code>可以根据需要创建新的线程，但如果已有线程是空闲的会重用已有线程。</p><p>Executor    ExecutorService<br>Executor 是 Java 线程池的核心接口，用来并发执行提交的任务    ExecutorService 是 Executor 接口的扩展，提供了异步执行和关闭线程池的方法<br>提供execute()方法用来提交任务    提供submit()方法用来提交任务<br>execute()方法无返回值    submit()方法返回Future对象，可用来获取任务执行结果<br>不能取消任务    可以通过Future.cancel()取消pending中的任务<br>没有提供和关闭线程池有关的方法    提供了关闭线程池的方法<br><img src="http://upload-images.jianshu.io/upload_images/8926909-d2a25c310db90f5b.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="区别"></p><h5 id="16、什么是Java线程转储-Thread-Dump-，如何得到它？"><a href="#16、什么是Java线程转储-Thread-Dump-，如何得到它？" class="headerlink" title="16、什么是Java线程转储(Thread Dump)，如何得到它？"></a>16、什么是Java线程转储(Thread Dump)，如何得到它？</h5><p>线程转储是一个JVM活动线程的列表，它对于分析系统瓶颈和死锁非常有用。<br>有很多方法可以获取线程转储——使用Profiler，Kill -3命令，jstack工具等等。我更喜欢jstack工具，因为它容易使用并且是JDK自带的。由于它是一个基于终端的工具，所以我们可以编写一些脚本去定时的产生线程转储以待分析。</p><h5 id="17、如何在Java中获取线程堆栈？"><a href="#17、如何在Java中获取线程堆栈？" class="headerlink" title="17、如何在Java中获取线程堆栈？"></a>17、如何在Java中获取线程堆栈？</h5><p>Java虚拟机提供了线程转储（thread dump）的后门，通过这个后门可以把线程堆栈打印出来。通常我们将堆栈信息重定向到一个文件中，便于我们分析，由于信息量太大，很可能超出控制台缓冲区的最大行数限制造成信息丢失。这里介绍一个jdk自带的打印线程堆栈的工具，jstack用于打印出给定的Java进程ID或core file或远程调试服务的Java堆栈信息。（<a href="http://blog.csdn.net/weiweicao0429/article/details/53185999" target="_blank" rel="noopener">Java问题定位之Java线程堆栈分析</a>）<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">示例：$jstack –l <span class="number">23561</span> &gt;&gt; xxx.dump</span><br><span class="line">命令 : $jstack [option] pid &gt;&gt; 文件</span><br></pre></td></tr></table></figure></p><p>表示输出到文件尾部，实际运行中，往往一次dump的信息，还不足以确认问题，建议产生三次dump信息，如果每次dump都指向同一个问题，我们才确定问题的典型性。</p><h5 id="18、说出-3-条在-Java-中使用线程的最佳实践"><a href="#18、说出-3-条在-Java-中使用线程的最佳实践" class="headerlink" title="18、说出 3 条在 Java 中使用线程的最佳实践"></a>18、说出 3 条在 Java 中使用线程的最佳实践</h5><p>给你的线程起个有意义的名字。这样可以方便找bug或追踪。<code>OrderProcessor, QuoteProcessor or TradeProcessor</code>这种名字比<code>Thread-1. Thread-2 and Thread-3</code>好多了，给线程起一个和它要完成的任务相关的名字，所有的主要框架甚至JDK都遵循这个最佳实践。</p><p>避免锁定和缩小同步的范围锁花费的代价高昂且上下文切换更耗费时间空间，试试最低限度的使用同步和锁，缩小临界区。因此相对于同步方法我更喜欢同步块，它给我拥有对锁的绝对控制权。</p><p>多用同步类少用wait和notify首先，<code>CountDownLatch, Semaphore, CyclicBarrier</code>和<code>Exchanger</code>这些同步类简化了编码操作，而用<code>wait</code>和<code>notify</code>很难实现对复杂控制流的控制。其次，这些类是由最好的企业编写和维护在后续的JDK中它们还会不断优化和完善，使用这些更高等级的同步工具你的程序可以不费吹灰之力获得优化。</p><p>多用并发集合少用同步集合，这是另外一个容易遵循且受益巨大的最佳实践，并发集合比同步集合的可扩展性更好，所以在并发编程时使用并发集合效果更好。如果下一次你需要用到map，你应该首先想到用<code>ConcurrentHashMap</code>。</p><h5 id="19、【情景开放题】"><a href="#19、【情景开放题】" class="headerlink" title="19、【情景开放题】"></a>19、【情景开放题】</h5><h5 id="实际项目中使用多线程举例。你在多线程环境中遇到的常见的问题是什么？你是怎么解决它的？"><a href="#实际项目中使用多线程举例。你在多线程环境中遇到的常见的问题是什么？你是怎么解决它的？" class="headerlink" title="实际项目中使用多线程举例。你在多线程环境中遇到的常见的问题是什么？你是怎么解决它的？"></a>实际项目中使用多线程举例。你在多线程环境中遇到的常见的问题是什么？你是怎么解决它的？</h5><h5 id="请说出与线程同步以及线程调度相关的方法"><a href="#请说出与线程同步以及线程调度相关的方法" class="headerlink" title="请说出与线程同步以及线程调度相关的方法"></a>请说出与线程同步以及线程调度相关的方法</h5><h5 id="程序中有3个-socket，需要多少个线程来处理"><a href="#程序中有3个-socket，需要多少个线程来处理" class="headerlink" title="程序中有3个 socket，需要多少个线程来处理"></a>程序中有3个 socket，需要多少个线程来处理</h5><h5 id="假如有一个第三方接口，有很多个线程去调用获取数据，现在规定每秒钟最多有-10-个线程同时调用它，如何做到"><a href="#假如有一个第三方接口，有很多个线程去调用获取数据，现在规定每秒钟最多有-10-个线程同时调用它，如何做到" class="headerlink" title="假如有一个第三方接口，有很多个线程去调用获取数据，现在规定每秒钟最多有 10 个线程同时调用它，如何做到"></a>假如有一个第三方接口，有很多个线程去调用获取数据，现在规定每秒钟最多有 10 个线程同时调用它，如何做到</h5><h5 id="如何在-Windows-和-Linux-上查找哪个线程使用的-CPU-时间最长"><a href="#如何在-Windows-和-Linux-上查找哪个线程使用的-CPU-时间最长" class="headerlink" title="如何在 Windows 和 Linux 上查找哪个线程使用的 CPU 时间最长"></a>如何在 Windows 和 Linux 上查找哪个线程使用的 CPU 时间最长</h5><h5 id="如何确保-main-方法所在的线程是-Java-程序最后结束的线程"><a href="#如何确保-main-方法所在的线程是-Java-程序最后结束的线程" class="headerlink" title="如何确保 main() 方法所在的线程是 Java 程序最后结束的线程"></a>如何确保 main() 方法所在的线程是 Java 程序最后结束的线程</h5><h5 id="非常多个线程（可能是不同机器），相互之间需要等待协调才能完成某种工作，问怎么设计这种协调方案"><a href="#非常多个线程（可能是不同机器），相互之间需要等待协调才能完成某种工作，问怎么设计这种协调方案" class="headerlink" title="非常多个线程（可能是不同机器），相互之间需要等待协调才能完成某种工作，问怎么设计这种协调方案"></a>非常多个线程（可能是不同机器），相互之间需要等待协调才能完成某种工作，问怎么设计这种协调方案</h5><h5 id="你需要实现一个高效的缓存，它允许多个用户读，但只允许一个用户写，以此来保持它的完整性，你会怎样去实现它"><a href="#你需要实现一个高效的缓存，它允许多个用户读，但只允许一个用户写，以此来保持它的完整性，你会怎样去实现它" class="headerlink" title="你需要实现一个高效的缓存，它允许多个用户读，但只允许一个用户写，以此来保持它的完整性，你会怎样去实现它"></a>你需要实现一个高效的缓存，它允许多个用户读，但只允许一个用户写，以此来保持它的完整性，你会怎样去实现它</h5>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在开发Java多线程应用程序中，各个线程之间由于要共享资源，必须用到锁机制。Java提供了多种多线程锁机制的实现方式，常见的有·synchronized、ReentrantLock、Semaphore、AtomicInteger·等。每种机制都有优缺点与各自的适用场景，必须熟练掌握他们的特点才能在Java多线程应用开发时得心应手。——&lt;a href=&quot;https://www.cnblogs.com/hanganglin/p/3577096.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;《Java锁机制详解》&lt;/a&gt;。&lt;br&gt;
    
    </summary>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/categories/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/tags/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>多线程从入门到放弃【3】</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%20%E5%A4%9A%E7%BA%BF%E7%A8%8B%E6%80%BB%E7%BB%93%E7%AF%873%E4%B9%8B%E2%80%94%E2%80%94%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F%E5%92%8C%E7%BA%BF%E7%A8%8B%E5%90%8C%E6%AD%A5/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java 多线程总结篇3之——生命周期和线程同步/</id>
    <published>2018-05-20T14:35:08.025Z</published>
    <updated>2018-05-20T14:35:08.026Z</updated>
    
    <content type="html"><![CDATA[<p>线程的生命周期全在一张图中，理解此图是基本：<br><img src="http://upload-images.jianshu.io/upload_images/8926909-b9592af555b7ecda.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="线程生命状态"><br><a id="more"></a></p><h4 id="一、新建和就绪状态"><a href="#一、新建和就绪状态" class="headerlink" title="一、新建和就绪状态"></a>一、新建和就绪状态</h4><p>当程序使用<code>new</code>关键字创建了一个线程之后，该线程就处于新建状态，此时它和其他的Java对象一样，仅仅由Java虚拟机为其分配内存，并初始化其成员变量的值。此时的线程对象没有表现出任何线程的动态特征，程序也不会执行线程的线程执行体。</p><p>当线程对象调用了<code>start()</code>方法之后，该线程处于就绪状态。Java虚拟机会为其创建方法调用栈和程序计数器，处于这个状态中的线程并没有开始运行，只是表示该线程可以运行了。至于该线程何时开始运行，取决于JVM里线程调度器的调度。</p><p>注意：启动线程使用<code>start()</code>方法，而不是<code>run()</code>方法。永远不要调用线程对象的<code>run()</code>方法。调用<code>start</code>方法来启动线程，系统会把该<code>run()</code>方法当成线程执行体来处理；但如果直按调用线程对象的<code>run()</code>方法，则<code>run()</code>方法立即就会被执行，而且在<code>run()</code>方法返回之前其他线程无法并发执行。也就是说，系统把线程对象当成一个普通对象，而<code>run()</code>方法也是一个普通方法，而不是线程执行体。需要指出的是，调用了线程的<code>run()</code>方法之后，该线程已经不再处于新建状态，不要再次调用线程对象的<code>start()</code>方法。只能对处于新建状态的线程调用<code>start()</code>方法，否则将引发<code>IllegaIThreadStateExccption</code>异常。<br>调用线程对象的<code>start()</code>方法之后，该线程立即进入就绪状态——就绪状态相当于”等待执行”，但该线程并未真正进入运行状态。如果希望调用子线程的<code>start()</code>方法后子线程立即开始执行，程序可以使用<code>Thread.sleep(1)</code>来让当前运行的线程（主线程）睡眠1毫秒，1毫秒就够了，因为在这1毫秒内CPU不会空闲，它会去执行另一个处于就绪状态的线程，这样就可以让子线程立即开始执行。</p><h4 id="二、运行和阻塞状态"><a href="#二、运行和阻塞状态" class="headerlink" title="二、运行和阻塞状态"></a>二、运行和阻塞状态</h4><h5 id="2-1-线程调度"><a href="#2-1-线程调度" class="headerlink" title="2.1 线程调度"></a>2.1 线程调度</h5><p>如果处于就绪状态的线程获得了CPU，开始执行<code>run()</code>方法的线程执行体，则该线程处于运行状态，如果计算机只有一个CPU。那么在任何时刻只有一个线程处于运行状态，当然在一个多处理器的机器上，将会有多个线程并行执行；当线程数大于处理器数时，依然会存在多个线程在同一个CPU上轮换的现象。</p><p>当一个线程开始运行后，它不可能一直处于运行状态（除非它的线程执行体足够短，瞬间就执行结束了）。线程在运行过程中需要被中断，目的是使其他线程获得执行的机会，线程调度的细节取决于底层平台所采用的策略。对于采用抢占式策略的系统而言，系统会给每个可执行的线程一个小时间段来处理任务；当该时间段用完后，系统就会剥夺该线程所占用的资源，让其他线程获得执行的机会。在选择下一个线程时，系统会考虑线程的优先级。</p><p>所有现代的桌面和服务器操作系统都采用抢占式调度策略，但一些小型设备如手机则可能采用协作式调度策略，在这样的系统中，只有当一个线程调用了它的<code>sleep()</code>或<code>yield()</code>方法后才会放弃所占用的资源——也就是必须由该线程主动放弃所占用的资源。</p><h5 id="2-2-线程阻塞"><a href="#2-2-线程阻塞" class="headerlink" title="2.2 线程阻塞"></a>2.2 线程阻塞</h5><p>当发生如下情况时，线程将会进入阻塞状态<br>① 线程调用<code>sleep()</code>方法主动放弃所占用的处理器资源<br>② 线程调用了一个阻塞式IO方法，在该方法返回之前，该线程被阻塞<br>③ 线程试图获得一个同步监视器，但该同步监视器正被其他线程所持有。关于同步监视器的知识、后面将存更深入的介绍<br>④ 线程在等待某个通知（notify）<br>⑤ 程序调用了线程的<code>suspend()</code>方法将该线程挂起。但这个方法容易导致死锁，所以应该尽量避免使用该方法</p><p>当前正在执行的线程被阻塞之后，其他线程就可以获得执行的机会。被阻塞的线程会在合适的时候重新进入就绪状态，注意是就绪状态而不是运行状态。也就是说，被阻塞线程的阻塞解除后，必须重新等待线程调度器再次调度它。</p><h5 id="2-3-解除阻塞"><a href="#2-3-解除阻塞" class="headerlink" title="2.3 解除阻塞"></a>2.3 解除阻塞</h5><p>针对上面几种情况，当发生如下特定的情况时可以解除上面的阻塞，让该线程重新进入就绪状态：<br>① 调用<code>sleep()</code>方法的线程经过了指定时间。<br>② 线程调用的阻塞式IO方法已经返回。<br>③ 线程成功地获得了试图取得的同步监视器。<br>④ 线程正在等待某个通知时，其他线程发出了个通知。<br>⑤ 处于挂起状态的线程被调甩了<code>resume()</code>恢复方法。<br>线程从阻塞状态只能进入就绪状态，无法直接进入运行状态。而就绪和运行状态之间的转换通常不受程序控制，而是由系统线程调度所决定。当处于就绪状态的线程获得处理器资源时，该线程进入运行状态；当处于运行状态的线程失去处理器资源时，该线程进入就绪状态。但有一个方法例外，调用<code>yield()</code>方法可以让运行状态的线程转入就绪状态。关于<code>yield()</code>方法后面有更详细的介纽。</p><h4 id="三、线程死亡"><a href="#三、线程死亡" class="headerlink" title="三、线程死亡"></a>三、线程死亡</h4><h5 id="3-1-死亡状态"><a href="#3-1-死亡状态" class="headerlink" title="3.1 死亡状态"></a>3.1 死亡状态</h5><p>线程会以如下3种方式结束，结束后就处于死亡状态：<br>① <code>run()</code>或<code>call()</code>方法执行完成，线程正常结束。<br>② 线程抛出一个未捕获的<code>Exception</code>或<code>Error</code>。<br>③ 直接调用该线程<code>stop()</code>方法来结束该线程——该方法容易导致死锁，通常不推荐使用。</p><h4 id="四、重难点考察"><a href="#四、重难点考察" class="headerlink" title="四、重难点考察"></a>四、重难点考察</h4><h5 id="1-有哪些不同的线程生命周期？"><a href="#1-有哪些不同的线程生命周期？" class="headerlink" title="1. 有哪些不同的线程生命周期？"></a>1. 有哪些不同的线程生命周期？</h5><p>当线程被创建并启动以后，它既不是一启动就进入了执行状态，也不是一直处于执行状态。在线程的生命周期中，它要经过新建(<code>New</code>)、就绪（<code>Runnable</code>）、运行（<code>Running</code>）、阻塞(<code>Blocked</code>)和死亡(<code>Dead</code>)5种状态。尤其是当线程启动以后，它不可能一直”霸占”着CPU独自运行，所以CPU需要在多条线程之间切换，于是线程状态也会多次在运行、阻塞之间切换</p><p><strong>1. 新建状态</strong>，当程序使用new关键字创建了一个线程之后，该线程就处于新建状态，此时仅由JVM为其分配内存，并初始化其成员变量的值</p><p><strong>2. 就绪状态</strong>，当线程对象调用了start()方法之后，该线程处于就绪状态。Java虚拟机会为其创建方法调用栈和程序计数器，等待调度运行</p><p><strong>3. 运行状态</strong>，如果处于就绪状态的线程获得了CPU，开始执行run()方法的线程执行体，则该线程处于运行状态</p><p><strong>4. 阻塞状态</strong>，当处于运行状态的线程失去所占用资源之后，便进入阻塞状态.</p><p><strong>5. 转换过程</strong>, 在线程的生命周期当中，线程的各种状态的<strong>转换过程</strong></p><h5 id="2-线程状态，BLOCKED-和-WAITING-有什么区别"><a href="#2-线程状态，BLOCKED-和-WAITING-有什么区别" class="headerlink" title="2.线程状态，BLOCKED 和 WAITING 有什么区别"></a>2.线程状态，BLOCKED 和 WAITING 有什么区别</h5><p>线程可以通过<code>wait,join,LockSupport.park</code>方式进入<code>wating</code>状态，进入<code>wating</code>状态的线程等待唤醒(<code>notify</code>或<code>notifyAll</code>)才有机会获取cpu的时间片段来继续执行。线程的 <code>blocked</code>状态往往是无法进入同步方法/代码块来完成的。这是因为无法获取到与同步方法/代码块相关联的锁。与<code>wating</code>状态相关联的是等待队列，与<code>blocked</code>状态相关的是同步队列，一个线程由等待队列迁移到同步队列时，线程状态将会由<code>wating</code>转化为<code>blocked</code>。可以这样说，<code>blocked</code>状态是处于<code>wating</code>状态的线程重新焕发生命力的必由之路。</p><p>不过个人觉得实际上不用可以区分两者, 因为两者都会暂停线程的执行. 两者的区别是: 进入<code>waiting</code>状态是线程主动的, 而进入<code>blocked</code>状态是被动的. 更进一步的说, 进入<code>blocked</code>状态是在同步(<code>synchronized</code>代码之外), 而进入<code>waiting</code>状态是在同步代码之内.</p><h5 id="3-ThreadLocal用途是什么，原理是什么，用的时候要注意什么？"><a href="#3-ThreadLocal用途是什么，原理是什么，用的时候要注意什么？" class="headerlink" title="3.ThreadLocal用途是什么，原理是什么，用的时候要注意什么？"></a>3.ThreadLocal用途是什么，原理是什么，用的时候要注意什么？</h5><p>在多线程程序中，同一个线程在某个时间段只能处理一个任务，我们希望在这个时间段内,任务的某些变量能够和处理它的线程进行绑定，,在任务需要使用这个变量的时候，这个变量能够方便的从线程中取出来。<code>ThreadLocal</code>能很好的满足这个需求，用<code>ThreadLocal</code>变量的程序看起来也会简洁很多，因为减少了变量在程序中的传递，每个运行的线程都会有一个类型为<code>ThreadLocal</code>。<code>ThreadLocalMap</code>的<code>map</code>，这个<code>map</code>就是用来存储与这个线程绑定的变量,<code>map</code>的<code>key</code>就是<code>ThreadLocal</code>对象，<code>value</code>就是线程正在执行的任务中的某个变量的包装类<code>Entry</code>、在使用<code>ThreadLocal</code>对象，尽量使用<code>static</code>，不然会使线程的<code>ThreadLocalMap</code>产生太多<code>Entry</code>，从而造成内存泄露。</p><h5 id="4-Java中用到的线程调度算法是什么？"><a href="#4-Java中用到的线程调度算法是什么？" class="headerlink" title="4.Java中用到的线程调度算法是什么？"></a>4.Java中用到的线程调度算法是什么？</h5><h5 id="5-什么是多线程中的上下文切换？"><a href="#5-什么是多线程中的上下文切换？" class="headerlink" title="5.什么是多线程中的上下文切换？"></a>5.什么是多线程中的上下文切换？</h5><p>即使是单核CPU也支持多线程执行代码，CPU通过给每个线程分配CPU时间片来实现这个机制。时间片是CPU分配给各个线程的时间，因为时间片非常短，所以CPU通过不停地切换线程执行，让我们感觉多个线程时同时执行的，时间片一般是几十毫秒（ms）。CPU通过时间片分配算法来循环执行任务，当前任务执行一个时间片后会切换到下一个任务。但是，在切换前会保存上一个任务的状态，以便下次切换回这个任务时，可以再次加载这个任务的状态，从任务保存到再加载的过程就是一次上下文切换。</p><h5 id="6、你对线程优先级的理解是什么？"><a href="#6、你对线程优先级的理解是什么？" class="headerlink" title="6、你对线程优先级的理解是什么？"></a>6、你对线程优先级的理解是什么？</h5><p>每一个线程都是有优先级的, 一般来说, 高优先级的线程在运行时会具有优先权, 但这依赖于线程调度的实现, 这个实现是和操作系统相关的. 我们可以定义线程的优先级<code>getPriority() setPriority()</code>, 但是这并不能保证高优先级的线程会在低优先级的线程前执行. 县城优先级是一个<code>int</code>变量, <code>1</code>代表最低, <code>10</code>代表最高。</p><h5 id="7、什么是线程调度器-Thread-Scheduler-和时间分片-Time-Slicing"><a href="#7、什么是线程调度器-Thread-Scheduler-和时间分片-Time-Slicing" class="headerlink" title="7、什么是线程调度器 (Thread Scheduler) 和时间分片 (Time Slicing)?"></a>7、什么是线程调度器 (Thread Scheduler) 和时间分片 (Time Slicing)?</h5><p>线程调度器是一个操作系统服务, 它负责为<code>Runnable</code>状态的线程分配CPU时间, 一旦我们创建一个线程并启动它, 它的执行便依赖于线程调度器的实现。时间分片是指将可用的CPU时间分配给可用的<code>Runnable</code>线程的过程。分配的CPU时间可以基于线程的优先级或者线程的等待时间, 线程调度并不受到Java虚拟机控制, 所以由应用程序来控制它是更好的选择(也就是说不要让你的程序依赖于线程优先级)。</p><h4 id="二、线程同步"><a href="#二、线程同步" class="headerlink" title="二、线程同步"></a>二、线程同步</h4><p>java允许多线程并发控制，当多个线程同时操作一个可共享的资源变量时（如数据的增删改查）， 将会导致数据不准确，相互之间产生冲突，因此加入同步锁以避免在该线程没有完成操作之前，被其他线程的调用， 从而保证了该变量的唯一性和准确性。</p><h5 id="1、-请说出你所知的线程同步的方法"><a href="#1、-请说出你所知的线程同步的方法" class="headerlink" title="1、 请说出你所知的线程同步的方法"></a>1、 请说出你所知的线程同步的方法</h5><p><strong>1.同步方法。</strong>即有synchronized关键字修饰的方法。 由于java的每个对象都有一个内置锁，当用此关键字修饰方法时， 内置锁会保护整个方法。在调用该方法前，需要获得内置锁，否则就处于阻塞状态。代码如：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">save</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line"><span class="comment">//注：synchronized关键字也可以修饰静态方法，此时如果调用该静态方法，将会锁住整个类</span></span><br></pre></td></tr></table></figure></p><p><strong>2.同步代码块</strong>，即有<code>synchronized</code>关键字修饰的语句块。 被该关键字修饰的语句块会自动被加上内置锁，从而实现同步<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//代码如：</span></span><br><span class="line"><span class="keyword">synchronized</span>(object)&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><strong><em>注：同步是一种高开销的操作，因此应该尽量减少同步的内容。 通常没有必要同步整个方法，使用synchronized代码块同步关键代码即可。</em></strong><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 线程同步的运用</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SynchronizedThread</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">Bank</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">private</span> <span class="keyword">int</span> account = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAccount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> account;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/**</span></span><br><span class="line"><span class="comment">             * 用同步方法实现</span></span><br><span class="line"><span class="comment">             * <span class="doctag">@param</span> money</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">save</span><span class="params">(<span class="keyword">int</span> money)</span> </span>&#123;</span><br><span class="line">                account += money;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/**</span></span><br><span class="line"><span class="comment">             * 用同步代码块实现</span></span><br><span class="line"><span class="comment">             * <span class="doctag">@param</span> money</span></span><br><span class="line"><span class="comment">             */</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">save1</span><span class="params">(<span class="keyword">int</span> money)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">synchronized</span> (<span class="keyword">this</span>) &#123;</span><br><span class="line">                    account += money;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">NewThread</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">            <span class="keyword">private</span> Bank bank;</span><br><span class="line"></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="title">NewThread</span><span class="params">(Bank bank)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">this</span>.bank = bank;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">                    <span class="comment">// bank.save1(10);</span></span><br><span class="line">                    bank.save(<span class="number">10</span>);</span><br><span class="line">                    System.out.println(i + <span class="string">"账户余额为："</span> + bank.getAccount());</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 建立线程，调用内部类</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">useThread</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            Bank bank = <span class="keyword">new</span> Bank();</span><br><span class="line">            NewThread new_thread = <span class="keyword">new</span> NewThread(bank);</span><br><span class="line">            System.out.println(<span class="string">"线程1"</span>);</span><br><span class="line">            Thread thread1 = <span class="keyword">new</span> Thread(new_thread);</span><br><span class="line">            thread1.start();</span><br><span class="line">            System.out.println(<span class="string">"线程2"</span>);</span><br><span class="line">            Thread thread2 = <span class="keyword">new</span> Thread(new_thread);</span><br><span class="line">            thread2.start();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">            SynchronizedThread st = <span class="keyword">new</span> SynchronizedThread();</span><br><span class="line">            st.useThread();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure></p><p><strong>3.使用特殊域变量(volatile)实现线程同步</strong><br>a.<code>volatile</code>关键字为域变量的访问提供了一种免锁机制，<br>b.使用<code>volatile</code>修饰域相当于告诉虚拟机该域可能会被其他线程更新，<br>c.因此每次使用该域就要重新计算，而不是使用寄存器中的值<br>d.<code>volatile</code>不会提供任何原子操作，它也不能用来修饰final类型的变量<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//只给出要修改的代码，其余代码与上同</span></span><br><span class="line">        <span class="class"><span class="keyword">class</span> <span class="title">Bank</span> </span>&#123;</span><br><span class="line">            <span class="comment">//需要同步的变量加上volatile</span></span><br><span class="line">            <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> account = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAccount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> account;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//这里不再需要synchronized</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">save</span><span class="params">(<span class="keyword">int</span> money)</span> </span>&#123;</span><br><span class="line">                account += money;</span><br><span class="line">            &#125;</span><br><span class="line">        ｝</span><br></pre></td></tr></table></figure></p><p><strong><em>注：多线程中的非同步问题主要出现在对域的读写上，如果让域自身避免这个问题，则就不需要修改操作该域的方法。 用final域，有锁保护的域和volatile域可以避免非同步的问题。</em></strong><br><strong>4.使用重入锁实现线程同步</strong>，在JavaSE5.0中新增了一个<code>java.util.concurrent</code>包来支持同步。 <code>ReentrantLock</code>类是可重入、互斥、实现了<code>Lock</code>接口的锁， 它与使用<code>synchronized</code>方法和快具有相同的基本行为和语义，并且扩展了其能力<code>ReenreantLock</code>类的常用方法有：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">ReentrantLock() : 创建一个ReentrantLock实例</span><br><span class="line">lock() : 获得锁</span><br><span class="line">unlock() : 释放锁</span><br></pre></td></tr></table></figure></p><p><strong><em>注：ReentrantLock()还有一个可以创建公平锁的构造方法，但由于能大幅度降低程序运行效率，不推荐使用</em></strong><br><strong>5.使用局部变量实现线程同步</strong>。如果使用<code>ThreadLocal</code>管理变量，则每一个使用该变量的线程都获得该变量的副本， 副本之间相互独立，这样每一个线程都可以随意修改自己的变量副本，而不会对其他线程产生影响。<br><code>ThreadLocal</code> 类的常用方法<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ThreadLocal() : 创建一个线程本地变量</span><br><span class="line">get() : 返回此线程局部变量的当前线程副本中的值</span><br><span class="line">initialValue() : 返回此线程局部变量的当前线程的<span class="string">"初始值"</span></span><br><span class="line">set(T value) : 将此线程局部变量的当前线程副本中的值设置为value</span><br></pre></td></tr></table></figure></p><p><strong><em>注：ThreadLocal与同步机制：a.ThreadLocal与同步机制都是为了解决多线程中相同变量的访问冲突问题。 b.前者采用以”空间换时间”的方法，后者采用以”时间换空间”的方式</em></strong><br> <strong>6.使用阻塞队列实现线程同步，</strong>7.使用原子变量实现线程同步<em>**</em></p><blockquote><p><a href="https://www.cnblogs.com/XHJT/p/3897440.html" title="java笔记--关于线程同步（7种同步方式）" target="_blank" rel="noopener">《关于线程同步的7种方式》</a></p></blockquote><h5 id="2、-synchronized-的原理是什么"><a href="#2、-synchronized-的原理是什么" class="headerlink" title="2、 synchronized 的原理是什么"></a>2、 synchronized 的原理是什么</h5><p><code>Synchronized</code>的语义底层是通过一个<code>monitor</code>（监视器锁）来实现的。<code>Synchronized</code>进过编译，会在同步块的前后分别形成<code>monitorenter</code>和<code>monitorexit</code>这个两个字节码指令。在执行<code>monitorenter</code>指令时，首先要尝试获取对象锁。如果这个对象没被锁定，或者当前线程已经拥有了那个对象锁，把锁的计算器加1，相应的，在执行<code>monitorexit</code>指令时会将锁计算器就减1，当计算器为0时，锁就被释放了。如果获取对象锁失败，那当前线程就要阻塞，直到对象锁被另一个线程释放为止。</p><blockquote><p>monitorenter ：<br>Each object is associated with a monitor. A monitor is locked if and only if it has an owner. The thread that executes monitorenter attempts to gain ownership of the monitor associated with objectref, as follows:<br>• If the entry count of the monitor associated with objectref is zero, the thread enters the monitor and sets its entry count to one. The thread is then the owner of the monitor.<br>• If the thread already owns the monitor associated with objectref, it reenters the monitor, incrementing its entry count.<br>• If another thread already owns the monitor associated with objectref, the thread blocks until the monitor’s entry count is zero, then tries again to gain ownership.</p></blockquote><p>每个对象有一个监视器锁（<code>monitor</code>）。当<code>monitor</code>被占用时就会处于锁定状态，线程执行<code>monitorenter</code>指令时尝试获取<code>monitor</code>的所有权，过程如下：<br>1、如果<code>monitor</code>的进入数为<code>0</code>，则该线程进入<code>monitor</code>，然后将进入数设置为1，该线程即为<code>monitor</code>的所有者。<br>2、如果线程已经占有该<code>monitor</code>，只是重新进入，则进入<code>monitor</code>的进入数加1.<br>3.如果其他线程已经占用了<code>monitor</code>，则该线程进入阻塞状态，直到<code>monitor</code>的进入数为0，再重新尝试获取<code>monitor</code>的所有权。</p><blockquote><p>monitorexit：　<br>The thread that executes monitorexit must be the owner of the monitor associated with the instance referenced by objectref.<br>The thread decrements the entry count of the monitor associated with objectref. If as a result the value of the entry count is zero, the thread exits the monitor and is no longer its owner. Other threads that are blocking to enter the monitor are allowed to attempt to do so.</p></blockquote><p>执行<code>monitorexit</code>的线程必须是<code>objectref</code>所对应的<code>monitor</code>的所有者。指令执行时，<code>monitor</code>的进入数减1，如果减1后进入数为0，那线程退出<code>monitor</code>，不再是这个<code>monitor</code>的所有者。其他被这个<code>monitor</code>阻塞的线程可以尝试去获取这个 <code>monitor</code> 的所有权。</p><blockquote><p><a href="http://blog.csdn.net/shandian000/article/details/54927876" target="_blank" rel="noopener">《深入分析Synchronized》</a> <a href="https://www.cnblogs.com/paddix/p/5367116.html" target="_blank" rel="noopener">《Java并发编程之——Synchronized》</a></p></blockquote><h5 id="3、-synchronized-和ReentrantLock有什么不同"><a href="#3、-synchronized-和ReentrantLock有什么不同" class="headerlink" title="3、 synchronized 和ReentrantLock有什么不同"></a>3、 synchronized 和ReentrantLock有什么不同</h5><p>相似点：这两种同步方式有很多相似之处，它们都是加锁方式同步，而且都是阻塞式的同步，也就是说当如果一个线程获得了对象锁，进入了同步块，其他访问该同步块的线程都必须阻塞在同步块外面等待，而进行线程阻塞和唤醒的代价是比较高的（操作系统需要在用户态与内核态之间来回切换，代价很高，不过可以通过对锁优化进行改善）。</p><p>区别：这两种方式最大区别就是对于<code>Synchronized</code>来说，它是java语言的关键字，是原生语法层面的互斥，需要jvm实现。而<code>ReentrantLock</code>它是JDK 1.5之后提供的API层面的互斥锁，需要<code>lock()</code>和<code>unlock()</code>方法配合<code>try/finally</code>语句块来完成。</p><p>总的来说，<code>Lock</code>提供了比<code>synchronized</code>更多的功能。但是要注意以下几点：</p><p>1）<code>Lock</code>不是<code>Java</code>语言内置的，<code>synchronized</code>是Java语言的关键字，因此是内置特性。<code>Lock</code>是一个类，通过这个类可以实现同步访问；</p><p>2）<code>Lock</code>和<code>synchronized</code>有一点非常大的不同，采用<code>synchronized</code>不需要用户去手动释放锁，当<code>synchronized</code>方法或者<code>synchronized</code>代码块执行完之后，系统会自动让线程释放对锁的占用；而<code>Lock</code>则必须要用户去手动释放锁，如果没有主动释放锁，就有可能导致出现死锁现象。<br><strong><em>synchronized在发生异常时，会自动释放线程占有的锁，因此不会导致死锁现象发生；而Lock在发生异常时，如果没有主动通过unLock()去释放锁，则很可能造成死锁现象，因此使用Lock时需要在finally块中释放锁；</em></strong></p><p>3）<code>Lock</code>可以让等待锁的线程响应中断，而<code>synchronized</code>却不行，使用<code>synchronized</code>时，等待的线程会一直等待下去，不能够响应中断；</p><p>4）通过<code>Lock</code>可以知道有没有成功获取锁，而<code>synchronized</code>却无法办到。</p><p>5）<code>Lock</code>可以提高多个线程进行读操作的效率。</p><p>在性能上来说，如果竞争资源不激烈，两者的性能是差不多的，而当竞争资源非常激烈时（即有大量线程同时竞争），此时<code>Lock</code>的性能要远远优于<code>synchronized</code>。所以说，在具体使用时要根据适当情况选择。</p><blockquote><p><a href="http://blog.csdn.net/chenchaofuck1/article/details/51045134" target="_blank" rel="noopener">《java两种线程同步方式的区别——Synchronized和ReentrantLock》</a></p></blockquote><h5 id="4、什么场景下可以使用-volatile-替换-synchronized"><a href="#4、什么场景下可以使用-volatile-替换-synchronized" class="headerlink" title="4、什么场景下可以使用 volatile 替换 synchronized"></a>4、什么场景下可以使用 volatile 替换 synchronized</h5><p>只需要保证共享资源的可见性的时候可以使用<code>volatile</code>替代，<code>synchronized</code>保证可操作的原子性一致性和可见性。<code>volatile</code>适用于新值不依赖于旧值的情形</p><h5 id="5、有T1，T2，T3三个线程，怎么确保它们按顺序执行？怎样保证T2在T1执行完后执行，T3在T2执行完后执行"><a href="#5、有T1，T2，T3三个线程，怎么确保它们按顺序执行？怎样保证T2在T1执行完后执行，T3在T2执行完后执行" class="headerlink" title="5、有T1，T2，T3三个线程，怎么确保它们按顺序执行？怎样保证T2在T1执行完后执行，T3在T2执行完后执行"></a>5、有T1，T2，T3三个线程，怎么确保它们按顺序执行？怎样保证T2在T1执行完后执行，T3在T2执行完后执行</h5><p>这里考察的主要知识点就是线程同步机制和锁的问题，<a href="http://blog.csdn.net/difffate/article/details/63684290" target="_blank" rel="noopener">《Java 指定线程执行顺序（三种方式）》</a><br>另外关于执行顺序的问题，可以有很多的考察点，可以刷刷这个博客<a href="http://blog.csdn.net/BeauXie/article/details/53018570" target="_blank" rel="noopener">《java指定线程执行的顺序》</a></p><h5 id="6、同步块内的线程抛出异常会发生什么"><a href="#6、同步块内的线程抛出异常会发生什么" class="headerlink" title="6、同步块内的线程抛出异常会发生什么"></a>6、同步块内的线程抛出异常会发生什么</h5><p>这个问题坑了很多Java程序员，若你能想到锁是否释放这条线索来回答还有点希望答对。无论你的同步块是正常还是异常退出的，里面的线程都会释放锁，所以对比锁接口我更喜欢同步块，因为它不用我花费精力去释放锁，该功能可以在<code>finally block</code>里释放锁实现。</p><h5 id="7、当一个线程进入一个对象的synchronized-方法A-之后，其它线程是否可进入此对象的synchronized-方法B"><a href="#7、当一个线程进入一个对象的synchronized-方法A-之后，其它线程是否可进入此对象的synchronized-方法B" class="headerlink" title="7、当一个线程进入一个对象的synchronized 方法A 之后，其它线程是否可进入此对象的synchronized 方法B"></a>7、当一个线程进入一个对象的<code>synchronized</code> 方法A 之后，其它线程是否可进入此对象的<code>synchronized</code> 方法B</h5><p>不能。其它线程只能访问该对象的非同步方法，同步方法则不能进入。因为非静态方法上的<code>synchronized</code>修饰符要求执行方法时要获得对象的锁，如果已经进入A方法说明对象锁已经被取走，那么试图进入B方法的线程就只能在等锁池（注意不是等待池哦 ）中等待对象的锁。</p><h5 id="8、使用-synchronized-修饰静态方法和非静态方法有什么区别"><a href="#8、使用-synchronized-修饰静态方法和非静态方法有什么区别" class="headerlink" title="8、使用 synchronized 修饰静态方法和非静态方法有什么区别"></a>8、使用 synchronized 修饰静态方法和非静态方法有什么区别</h5><p>在<code>static</code>方法前加<code>synchronizedstatic</code>：静态方法属于类方法，它属于这个类，获取到的锁，是属于类的锁。 在普通方法前加<code>synchronizedstatic</code>：非<code>static</code>方法获取到的锁，是属于当前对象的锁。 结论：类锁和对象锁不同，他们之间不会产生互斥。</p><h5 id="9、如何从给定集合那里创建一个-synchronized-的集合"><a href="#9、如何从给定集合那里创建一个-synchronized-的集合" class="headerlink" title="9、如何从给定集合那里创建一个 synchronized 的集合"></a>9、如何从给定集合那里创建一个 synchronized 的集合</h5><p>我们可以使用<code>Collections.synchronizedCollection(Collection c)</code>根据指定集合来获取一个<code>synchronized</code>（线程安全的）集合。比如<code>HashMap</code>可以这样来实现线程安全：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Map m = Collections.synchronizedMap(<span class="keyword">new</span> HashMap);</span><br></pre></td></tr></table></figure></p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;线程的生命周期全在一张图中，理解此图是基本：&lt;br&gt;&lt;img src=&quot;http://upload-images.jianshu.io/upload_images/8926909-b9592af555b7ecda.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240&quot; alt=&quot;线程生命状态&quot;&gt;&lt;br&gt;
    
    </summary>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/categories/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/tags/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>多线程从入门到放弃【2】</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%20%E5%A4%9A%E7%BA%BF%E7%A8%8B%E6%80%BB%E7%BB%93%E7%AF%872%E4%B9%8B%E2%80%94%E2%80%94Thread%E7%B1%BB%E5%8F%8A%E5%B8%B8%E7%94%A8%E5%87%BD%E6%95%B0/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java 多线程总结篇2之——Thread类及常用函数/</id>
    <published>2018-05-20T14:35:08.023Z</published>
    <updated>2018-05-20T14:35:08.024Z</updated>
    
    <content type="html"><![CDATA[<p>此片文章主要总结的是Thread类及相关的基础概念和API，首先需要厘清线程调度中的几个基本概念：<br><a id="more"></a></p><h4 id="一、线程调度的基本方法"><a href="#一、线程调度的基本方法" class="headerlink" title="一、线程调度的基本方法"></a>一、线程调度的基本方法</h4><h5 id="1、调整线程优先级："><a href="#1、调整线程优先级：" class="headerlink" title="1、调整线程优先级："></a>1、调整线程优先级：</h5><p>Java线程有优先级，优先级高的线程会获得较多的运行机会。Java线程的优先级用整数表示，取值范围是1~10，Thread类有以下三个静态常量：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> MAX_PRIORITY    <span class="comment">//线程可以具有的最高优先级，取值为10。</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> MIN_PRIORITY    <span class="comment">//线程可以具有的最低优先级，取值为1。</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> NORM_PRIORITY  <span class="comment">//分配给线程的默认优先级，取值为5。</span></span><br></pre></td></tr></table></figure></p><p><code>Thread</code>类的<code>setPriority()</code>和<code>getPriority()</code>方法分别用来设置和获取线程的优先级。每个线程都有默认的优先级。主线程的默认优先级为<code>Thread.NORM_PRIORITY</code>。线程的优先级有继承关系，比如A线程中创建了B线程，那么B将和A具有相同的优先级。JVM提供了10个线程优先级，但与常见的操作系统都不能很好的映射。如果希望程序能移植到各个操作系统中，应该仅仅使用<code>Thread</code>类有以下三个静态常量作为优先级，这样能保证同样的优先级采用了同样的调度方式。</p><h5 id="2、线程睡眠："><a href="#2、线程睡眠：" class="headerlink" title="2、线程睡眠："></a>2、线程睡眠：</h5><p><code>Thread.sleep(long millis)</code>方法，使线程转到阻塞状态。millis参数设定睡眠的时间，以毫秒为单位。当睡眠结束后，就转为就绪（<code>Runnable</code>）状态。<code>sleep()</code>平台移植性好。</p><h5 id="3、线程等待："><a href="#3、线程等待：" class="headerlink" title="3、线程等待："></a>3、线程等待：</h5><p><code>Object</code>类中的<code>wait()</code>方法，导致当前的线程等待，直到其他线程调用此对象的<code>notify()</code>方法或 <code>notifyAll()</code> 唤醒方法。这个两个唤醒方法也是<code>Object</code>类中的方法，行为等价于调用 <code>wait(0)</code> 一样。</p><h5 id="4、线程让步："><a href="#4、线程让步：" class="headerlink" title="4、线程让步："></a>4、线程让步：</h5><p><code>Thread.yield()</code>方法，暂停当前正在执行的线程对象，把执行机会让给相同或者更高优先级的线程。</p><h5 id="5、线程加入："><a href="#5、线程加入：" class="headerlink" title="5、线程加入："></a>5、线程加入：</h5><p><code>join()</code>方法，等待其他线程终止。在当前线程中调用另一个线程的<code>join()</code>方法，则当前线程转入阻塞状态，直到另一个进程运行结束，当前线程再由阻塞转为就绪状态。</p><h5 id="6、线程唤醒："><a href="#6、线程唤醒：" class="headerlink" title="6、线程唤醒："></a>6、线程唤醒：</h5><p><code>Object</code>类中的<code>notify()</code>方法，唤醒在此对象监视器上等待的单个线程。如果所有线程都在此对象上等待，则会选择唤醒其中一个线程。选择是任意性的，并在对实现做出决定时发生。线程通过调用其中一个 <code>wait</code>方法，在对象的监视器上等待。 直到当前的线程放弃此对象上的锁定，才能继续执行被唤醒的线程。被唤醒的线程将以常规方式与在该对象上主动同步的其他所有线程进行竞争；例如，唤醒的线程在作为锁定此对象的下一个线程方面没有可靠的特权或劣势。类似的方法还有一个<code>notifyAll()</code>，唤醒在此对象监视器上等待的所有线程。</p><p>注意：<code>Thread</code>中<code>suspend()</code>和<code>resume()</code>两个方法在JDK1.5中已经废除，不再介绍。因为有死锁倾向。</p><h4 id="二、常用函数"><a href="#二、常用函数" class="headerlink" title="二、常用函数"></a>二、常用函数</h4><p><strong>①sleep(long millis)</strong>: 在指定的毫秒数内让当前正在执行的线程休眠（暂停执行）<br><strong>②join()</strong>:指等待t线程终止。join是<code>Thread</code>类的一个方法，启动线程后直接调用，即<code>join()</code>的作用是：“等待该线程终止”，这里需要理解的就是该线程是指的主线程等待子线程的终止。也就是在子线程调用了<code>join()</code>方法后面的代码，只有等到子线程结束了才能执行。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Thread t = <span class="keyword">new</span> AThread();</span><br><span class="line">t.start();</span><br><span class="line">t.join();</span><br></pre></td></tr></table></figure></p><p>为什么要用<code>join()</code>方法?在很多情况下，主线程生成并起动了子线程，如果子线程里要进行大量的耗时的运算，主线程往往将于子线程之前结束，但是如果主线程处理完其他的事务后，需要用到子线程的处理结果，也就是主线程需要等待子线程执行完成之后再结束，这个时候就要用到<code>join()</code>方法了。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main2</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">"主线程运行开始!"</span>);</span><br><span class="line">        Thread1 mTh1=<span class="keyword">new</span> Thread1(<span class="string">"A"</span>);</span><br><span class="line">        Thread1 mTh2=<span class="keyword">new</span> Thread1(<span class="string">"B"</span>);</span><br><span class="line">        mTh1.start();</span><br><span class="line">        mTh2.start();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mTh1.join();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            mTh2.join();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+ <span class="string">"主线程运行结束!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Thread1</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Thread1</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name);</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">" 线程运行开始!"</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            System.out.println(<span class="string">"子线程"</span>+name + <span class="string">"运行 : "</span> + i);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                sleep((<span class="keyword">int</span>) Math.random() * <span class="number">10</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName() + <span class="string">" 线程运行结束!"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">main主线程运行开始!</span><br><span class="line">A 线程运行开始!</span><br><span class="line">B 线程运行开始!</span><br><span class="line">子线程A运行 : <span class="number">0</span></span><br><span class="line">子线程B运行 : <span class="number">0</span></span><br><span class="line">子线程A运行 : <span class="number">1</span></span><br><span class="line">子线程A运行 : <span class="number">2</span></span><br><span class="line">子线程A运行 : <span class="number">3</span></span><br><span class="line">子线程A运行 : <span class="number">4</span></span><br><span class="line">A 线程运行结束!</span><br><span class="line">子线程B运行 : <span class="number">1</span></span><br><span class="line">子线程B运行 : <span class="number">2</span></span><br><span class="line">子线程B运行 : <span class="number">3</span></span><br><span class="line">子线程B运行 : <span class="number">4</span></span><br><span class="line">B 线程运行结束!</span><br><span class="line">main主线程运行结束!</span><br></pre></td></tr></table></figure><p><strong>③yield():</strong>暂停当前正在执行的线程对象，并执行其他线程。</p><p><code>Thread.yield()</code>方法作用是：暂停当前正在执行的线程对象，并执行其他线程。<code>yield()</code>应该做的是让当前运行线程回到可运行状态，以允许具有相同优先级的其他线程获得运行机会。因此，使用<code>yield()</code>的目的是让相同优先级的线程之间能适当的轮转执行。但是，实际中无法保证<code>yield()</code>达到让步目的，因为让步的线程还有可能被线程调度程序再次选中。</p><p>结论：<code>yield()</code>从未导致线程转到等待/睡眠/阻塞状态。在大多数情况下，<code>yield()</code>将导致线程从运行状态转到可运行状态，但有可能没有效果（注意线程的生命周期）。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main2</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ThreadYield yt1 = <span class="keyword">new</span> ThreadYield(<span class="string">"张三"</span>);</span><br><span class="line">        ThreadYield yt2 = <span class="keyword">new</span> ThreadYield(<span class="string">"李四"</span>);</span><br><span class="line">        yt1.start();</span><br><span class="line">        yt2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ThreadYield</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ThreadYield</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(name);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">50</span>; i++) &#123;</span><br><span class="line">            System.out.println(<span class="string">""</span> + <span class="keyword">this</span>.getName() + <span class="string">"-----"</span> + i);</span><br><span class="line">            <span class="comment">// 当i为30时，该线程就会把CPU时间让掉，让其他或者自己的线程执行（也就是谁先抢到谁执行）</span></span><br><span class="line">            <span class="keyword">if</span> (i ==<span class="number">30</span>) &#123;</span><br><span class="line">                <span class="keyword">this</span>.yield();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">运行结果：</span><br><span class="line">第一种情况：李四（线程）当执行到<span class="number">30</span>时会CPU时间让掉，这时张三（线程）抢到CPU时间并执行。</span><br><span class="line">第二种情况：李四（线程）当执行到<span class="number">30</span>时会CPU时间让掉，这时李四（线程）抢到CPU时间并执行。</span><br></pre></td></tr></table></figure><p><strong>④setPriority():</strong> 更改线程的优先级。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">MIN_PRIORITY = <span class="number">1</span></span><br><span class="line">NORM_PRIORITY = <span class="number">5</span></span><br><span class="line">MAX_PRIORITY = <span class="number">10</span></span><br></pre></td></tr></table></figure></p><p>用法：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Thread4 t1 = <span class="keyword">new</span> Thread4(<span class="string">"t1"</span>);</span><br><span class="line">Thread4 t2 = <span class="keyword">new</span> Thread4(<span class="string">"t2"</span>);</span><br><span class="line">t1.setPriority(Thread.MAX_PRIORITY);</span><br><span class="line">t2.setPriority(Thread.MIN_PRIORITY);</span><br></pre></td></tr></table></figure></p><p><strong>⑤interrupt():</strong>不要以为它是中断某个线程！它只是线线程发送一个中断信号，让线程在无限等待时（如死锁时）能抛出抛出，从而结束线程，但是如果你吃掉了这个异常，那么这个线程还是不会中断的！</p><p><strong>⑥wait()：</strong><code>Obj.wait()</code>，与<code>Obj.notify()</code>必须要与<code>synchronized(Obj)</code>一起使用，也就是<code>wait,</code>与<code>notify</code>是针对已经获取了Obj锁进行操作，从语法角度来说就是<code>Obj.wait(),Obj.notify</code>必须在<code>synchronized(Obj){...}</code>语句块内。从功能上来说wait就是说线程在获取对象锁后，主动释放对象锁，同时本线程休眠。直到有其它线程调用对象的<code>notify()</code>唤醒该线程，才能继续获取对象锁，并继续执行。相应的<code>notify()</code>就是对对象锁的唤醒操作。但有一点需要注意的是<code>notify()</code>调用后，并不是马上就释放对象锁的，而是在相应的<code>synchronized(){}</code>语句块执行结束，自动释放锁后，JVM会在<code>wait()</code>对象锁的线程中随机选取一线程，赋予其对象锁，唤醒线程，继续执行。这样就提供了在线程间同步、唤醒的操作。<code>Thread.sleep()</code>与<code>Object.wait()</code>二者都可以暂停当前线程，释放CPU控制权，主要的区别在于<code>Object.wait()</code>在释放CPU同时，释放了对象锁的控制。</p><p>单单在概念上理解清楚了还不够，需要在实际的例子中进行测试才能更好的理解。对<code>Object.wait()，Object.notify()</code>的应用最经典的例子，应该是三线程打印ABC的问题了吧，这是一道比较经典的面试题，题目要求如下：建立三个线程，A线程打印10次A，B线程打印10次B,C线程打印10次C，要求线程同时运行，交替打印10次ABC。这个问题用Object的<code>wait()，notify()</code>就可以很方便的解决。代码如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyThreadPrinter2</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Object prev;</span><br><span class="line">    <span class="keyword">private</span> Object self;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">MyThreadPrinter2</span><span class="params">(String name, Object prev, Object self)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.prev = prev;</span><br><span class="line">        <span class="keyword">this</span>.self = self;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">10</span>;</span><br><span class="line">        <span class="keyword">while</span> (count &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (prev) &#123;</span><br><span class="line">                <span class="keyword">synchronized</span> (self) &#123;</span><br><span class="line">                    System.out.print(name);</span><br><span class="line">                    count--;</span><br><span class="line"></span><br><span class="line">                    self.notify();</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    prev.wait();</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Object a = <span class="keyword">new</span> Object();</span><br><span class="line">        Object b = <span class="keyword">new</span> Object();</span><br><span class="line">        Object c = <span class="keyword">new</span> Object();</span><br><span class="line">        MyThreadPrinter2 pa = <span class="keyword">new</span> MyThreadPrinter2(<span class="string">"A"</span>, c, a);</span><br><span class="line">        MyThreadPrinter2 pb = <span class="keyword">new</span> MyThreadPrinter2(<span class="string">"B"</span>, a, b);</span><br><span class="line">        MyThreadPrinter2 pc = <span class="keyword">new</span> MyThreadPrinter2(<span class="string">"C"</span>, b, c);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">new</span> Thread(pa).start();</span><br><span class="line">        Thread.sleep(<span class="number">100</span>);  <span class="comment">//确保按顺序A、B、C执行</span></span><br><span class="line">        <span class="keyword">new</span> Thread(pb).start();</span><br><span class="line">        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">        <span class="keyword">new</span> Thread(pc).start();</span><br><span class="line">        Thread.sleep(<span class="number">100</span>);</span><br><span class="line">        &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>先来解释一下其整体思路，从大的方向上来讲，该问题为三线程间的同步唤醒操作，主要的目的就是ThreadA-&gt;ThreadB-&gt;ThreadC-&gt;ThreadA循环执行三个线程。为了控制线程执行的顺序，那么就必须要确定唤醒、等待的顺序，所以每一个线程必须同时持有两个对象锁，才能继续执行。一个对象锁是prev，就是前一个线程所持有的对象锁。还有一个就是自身对象锁。主要的思想就是，为了控制执行的顺序，必须要先持有prev锁，也就前一个线程要释放自身对象锁，再去申请自身对象锁，两者兼备时打印，之后首先调用<code>self.notify()</code>释放自身对象锁，唤醒下一个等待线程，再调用<code>prev.wait()</code>释放prev对象锁，终止当前线程，等待循环结束后再次被唤醒。运行上述代码，可以发现三个线程循环打印ABC，共10次。程序运行的主要过程就是A线程最先运行，持有C,A对象锁，后释放A,C锁，唤醒B。线程B等待A锁，再申请B锁，后打印B，再释放B，A锁，唤醒C，线程C等待B锁，再申请C锁，后打印C，再释放C,B锁，唤醒A。看起来似乎没什么问题，但如果你仔细想一下，就会发现有问题，就是初始条件，三个线程按照A,B,C的顺序来启动，按照前面的思考，A唤醒B，B唤醒C，C再唤醒A。但是这种假设依赖于JVM中线程调度、执行的顺序。</p><h4 id="三、深入考察及疑难点"><a href="#三、深入考察及疑难点" class="headerlink" title="三、深入考察及疑难点"></a>三、深入考察及疑难点</h4><h5 id="1、启动一个线程是调用-run-还是-start-方法？start-和-run-方法有什么区别"><a href="#1、启动一个线程是调用-run-还是-start-方法？start-和-run-方法有什么区别" class="headerlink" title="1、启动一个线程是调用 run() 还是 start() 方法？start() 和 run() 方法有什么区别"></a>1、启动一个线程是调用 run() 还是 start() 方法？start() 和 run() 方法有什么区别</h5><p>相当于玩游戏机，只有一个游戏机（cpu），可是有很多人要玩，于是，start是排队！等CPU选中你就是轮到你，你就<code>run（）</code>，当CPU的运行的时间片执行完，这个线程就继续排队，等待下一次的<code>run（）</code>。调用<code>start（）</code>后，线程会被放到等待队列，等待CPU调度，并不一定要马上开始执行，只是将这个线程置于可动行状态。然后通过JVM，线程Thread会调用<code>run（）</code>方法，执行本线程的线程体。先调用start后调用run，这么麻烦，为了不直接调用run？就是为了实现多线程的优点，没这个start不行。</p><p><strong>（1）.start（）</strong>方法来启动线程，真正实现了多线程运行。这时无需等待run方法体代码执行完毕，可以直接继续执行下面的代码；通过调用Thread类的start()方法来启动一个线程， 这时此线程是处于就绪状态， 并没有运行。 然后通过此Thread类调用方法run()来完成其运行操作的， 这里方法run()称为线程体，它包含了要执行的这个线程的内容， Run方法运行结束， 此线程终止。然后CPU再调度其它线程。</p><p><strong>（2）.run（）</strong>方法当作普通方法的方式调用。程序还是要顺序执行，要等待run方法体执行完毕后，才可继续执行下面的代码； 程序中只有主线程——这一个线程， 其程序执行路径还是只有一条， 这样就没有达到写线程的目的。<br>记住：多线程就是分时利用CPU，宏观上让所有线程一起执行 ，也叫并发</p><p><strong>start() :</strong>它的作用是启动一个新线程，新线程处于就绪状态，拿到cpu执行权就会执行相应的run()方法。start()不能被重复调用。<br><strong>run():</strong> run()就和普通的成员方法一样，可以被重复调用。单独调用run()的话，会在当前线程中执行run()，而并不会启动新线程！（为什么不直接调用run方法的原因也在此。）</p><h5 id="2、sleep-方法和对象的-wait-方法都可以让线程暂停执行，它们有什么区别？"><a href="#2、sleep-方法和对象的-wait-方法都可以让线程暂停执行，它们有什么区别？" class="headerlink" title="2、sleep() 方法和对象的 wait() 方法都可以让线程暂停执行，它们有什么区别？"></a>2、sleep() 方法和对象的 wait() 方法都可以让线程暂停执行，它们有什么区别？</h5><p><strong>sleep（）方法</strong><br><code>sleep()</code>使当前线程进入停滞状态（阻塞当前线程），让出CUP的使用、目的是不让当前线程独自霸占该进程所获的CPU资源，以留一定时间给其他线程执行的机会;<code>sleep()</code>是<code>Thread</code>类的<code>Static</code>(静态)的方法；因此他不能改变对象的机锁，所以当在一个<code>Synchronized</code>块中调用<code>Sleep()</code>方法是，线程虽然休眠了，但是对象的机锁并木有被释放，其他线程无法访问这个对象（即使睡着也持有对象锁）。在<code>sleep()</code>休眠时间期满后，该线程不一定会立即执行，这是因为其它线程可能正在运行而且没有被调度为放弃执行，除非此线程具有更高的优先级。</p><p><strong>wait（）方法</strong><br><code>wait()</code>方法是Object类里的方法；当一个线程执行到<code>wait()</code>方法时，它就进入到一个和该对象相关的等待池中，同时失去（释放）了对象的机锁（暂时失去机锁，<code>wait(long timeout)</code>超时时间到后还需要返还对象锁）；其他线程可以访问；<code>wait()</code>使用<code>notify</code>或者<code>notifyAlll</code>或者指定睡眠时间来唤醒当前等待池中的线程。<code>wiat()</code>必须放在<code>synchronized block</code>中，否则会在<code>program runtime</code>时扔出”<code>java.lang.IllegalMonitorStateException</code>“异常。<br>共同点：</p><ol><li><p>他们都是在多线程的环境下，都可以在程序的调用处阻塞指定的毫秒数，并返回。</p></li><li><p><code>wait()</code>和<code>sleep()</code>都可以通过<code>interrupt()</code>方法 打断线程的暂停状态，从而使线程立刻抛出<code>InterruptedException</code>。</p><p>如果线程A希望立即结束线程B，则可以对线程B对应的<code>Thread</code>实例调用<code>interrupt</code>方法。如果此刻线程B正在<code>wait/sleep /join</code>，则线程B会立刻抛出<code>InterruptedException</code>，在<code>catch() {}</code> 中直接<code>return</code>即可安全地结束线程。 需要注意的是，<code>InterruptedException</code>是线程自己从内部抛出的，并不是<code>interrupt()</code>方法抛出的。对某一线程调用 <code>interrupt()</code>时，如果该线程正在执行普通的代码，那么该线程根本就不会抛出<code>InterruptedException</code>。但是，一旦该线程进入到 <code>wait()/sleep()/join()</code>后，就会立刻抛出<code>InterruptedException</code> 。</p></li></ol><p>不同点：</p><ol><li><p><code>Thread</code>类的方法：<code>sleep(),yield()</code>等。<code>Object</code>的方法：<code>wait()</code>和<code>notify()</code>等</p></li><li><p>每个对象都有一个锁来控制同步访问。<code>Synchronized</code>关键字可以和对象的锁交互，来实现线程的同步。<code>sleep</code>方法没有释放锁，而wait方法释放了锁，使得其他线程可以使用同步控制块或者方法。</p></li><li><p><code>wait，notify</code>和<code>notifyAll</code>只能在同步控制方法或者同步控制块里面使用，而<code>sleep</code>可以在任何地方使用</p></li><li><p><code>sleep</code>必须捕获异常，而<code>wait，notify</code>和<code>notifyAll</code>不需要捕获异常</p></li></ol><p>所以<code>sleep()</code>和<code>wait()</code>方法的最大区别是：<code>sleep()</code>睡眠时，保持对象锁，仍然占有该锁；而<code>wait()</code>睡眠时，释放对象锁。但是<code>wait()</code>和<code>sleep()</code>都可以通过<code>interrupt()</code>方法打断线程的暂停状态，从而使线程立刻抛出<code>InterruptedException</code>（但不建议使用该方法）。</p><h5 id="3、yield方法有什么作用？sleep-方法和-yield-方法有什么区别"><a href="#3、yield方法有什么作用？sleep-方法和-yield-方法有什么区别" class="headerlink" title="3、yield方法有什么作用？sleep() 方法和 yield()方法有什么区别?"></a>3、yield方法有什么作用？sleep() 方法和 yield()方法有什么区别?</h5><p><strong>sleep()和yield()</strong>的区别:<code>sleep()</code>使当前线程进入停滞状态，所以执行<code>sleep()</code>的线程在指定的时间内肯定不会被执行；<code>yield()</code>只是使当前线程重新回到可执行状态，所以执行<code>yield()</code>的线程有可能在进入到可执行状态后马上又被执行。</p><p><code>sleep</code> 方法使当前运行中的线程睡眼一段时间，进入不可运行状态，这段时间的长短是由程序设定的，<code>yield</code> 方法使当前线程让出 CPU 占有权，但让出的时间是不可设定的。实际上，<code>yield()</code>方法对应了如下操作：先检测当前是否有相同优先级的线程处于同可运行状态，如有，则把 CPU  的占有权交给此线程，否则，继续运行原来的线程。所以<code>yield()</code>方法称为“退让”，它把运行机会让给了同等优先级的其他线程</p><p>另外，sleep 方法允许较低优先级的线程获得运行机会，但 <code>yield()</code> 方法执行时，当前线程仍处在可运行状态，所以，不可能让出较低优先级的线程些时获得 CPU 占有权。在一个运行系统中，如果较高优先级的线程没有调用 sleep 方法，又没有受到 I\O 阻塞，那么，较低优先级线程只能等待所有较高优先级的线程运行结束，才有机会运行。</p><h5 id="4、Java-中如何停止一个线程？"><a href="#4、Java-中如何停止一个线程？" class="headerlink" title="4、Java 中如何停止一个线程？**"></a>4、Java 中如何停止一个线程？**</h5><p>停止一个线程意味着在任务处理完任务之前停掉正在做的操作，也就是放弃当前的操作。停止一个线程可以用<code>Thread.stop()</code>方法，但最好不要用它。虽然它确实可以停止一个正在运行的线程，但是这个方法是不安全的，而且是已被废弃的方法。在java中有以下3种方法可以终止正在运行的线程：<br>1.使用退出标志，使线程正常退出，也就是当run方法完成后线程终止。<br>2.使用stop方法强行终止，但是不推荐这个方法，因为stop和suspend及resume一样都是过期作废的方法。<br>3.使用interrupt方法中断线程。具体案例和api可以参考文章<a href="https://www.cnblogs.com/carmanloneliness/p/3516405.html" target="_blank" rel="noopener">《java多线程之——interrupt深入研究》</a>。</p><h5 id="5、stop-和-suspend-方法为何不推荐使用"><a href="#5、stop-和-suspend-方法为何不推荐使用" class="headerlink" title="5、stop()和 suspend()方法为何不推荐使用?"></a>5、stop()和 suspend()方法为何不推荐使用?</h5><p><code>Stop()</code>方法作为一种粗暴的线程终止行为，在线程终止之前没有对其做任何的清除操作，因此具有固有的不安全性。 用<code>Thread.stop()</code>方法来终止线程将会释放该线程对象已经锁定的所有监视器。如果以前受这些监视器保护的任何对象都处于不连贯状态，那么损坏的对象对其他线程可见，这有可能导致不安全的操作。 由于上述原因，因此不应该使用<code>stop()</code>方法，而应该在自己的<code>Thread</code>类中置入一个标志，用于控制目标线程是活动还是停止。如果该标志指示它要停止运行，可使其结束<code>run（）</code>方法。如果目标线程等待很长时间，则应使用<code>interrupt()</code>方法来中断该等待。</p><p><code>suspend()</code>方法 该方法已经遭到反对，因为它具有固有的死锁倾向。调用<code>suspend（）</code>方法的时候，目标线程会停下来。如果目标线程挂起时在保护关键系统资源的监视器上保持有锁，则在目标线程重新开始以前，其他线程都不能访问该资源。除非被挂起的线程恢复运行。对任何其他线程来说，如果想恢复目标线程，同时又试图使用任何一个锁定的资源，就会造成死锁。由于上述原因，因此不应该使用<code>suspend（）</code>方法，而应在自己的thread类中置入一个标志，用于控制线程是活动还是挂起。如果标志指出线程应该挂起，那么用<code>wait（）</code>方法命令其进入等待状态。如果标志指出线程应当恢复，那么用<code>notify()</code>方法重新启动线程。</p><h5 id="6、如何在两个线程间共享数据"><a href="#6、如何在两个线程间共享数据" class="headerlink" title="6、如何在两个线程间共享数据?"></a>6、如何在两个线程间共享数据?</h5><p>通过在线程之间共享对象就可以了，然后通过<code>wait/notify/notifyAll、await/signal/signalAll</code>进行唤起和等待，比方说阻塞队列<code>BlockingQueue</code>就是为线程之间共享数据而设计的。</p><h5 id="7、强制启动一个线程？"><a href="#7、强制启动一个线程？" class="headerlink" title="7、强制启动一个线程？"></a>7、强制启动一个线程？</h5><ul><li>实现Runnable接口优势：<br>1）适合多个相同的程序代码的线程去处理同一个资源。<br>2）可以避免java中的单继承的限制。<br>3）增加程序的健壮性，代码可以被多个线程共享，代码和数据独立。</li><li>继承Thread类优势：<br>1）可以将线程类抽象出来，当需要使用抽象工厂模式设计时。<br>2）多线程同步</li><li>在函数体使用<br>1）无需继承thread或者实现Runnable，缩小作用域。<h5 id="8、如何让正在运行的线程暂停一段时间"><a href="#8、如何让正在运行的线程暂停一段时间" class="headerlink" title="8、如何让正在运行的线程暂停一段时间"></a>8、如何让正在运行的线程暂停一段时间</h5>1、<code>sleep()</code>方法（休眠）是线程类（<code>Thread</code>）的静态方法，调用此方法会让当前线程暂停执行指定的时间，将执行机会（CPU）让给其他线程，但是对象的锁依然保持，因此休眠时间结束后会自动恢复（线程回到就绪状态，请参考第66题中的线程状态转换图）。<br>2、<code>wait()</code>是Object类的方法，调用对象的wait()方法导致当前线程放弃对象的锁（线程暂停执行），进入对象的等待池（wait pool），只有调用对象的<code>notify()</code>方法（或<code>notifyAll()</code>方法）时才能唤醒等待池中的线程进入等锁池（lockpool），如果线程重新获得对象的锁就可以进入就绪状态<h5 id="9、什么是线程组，为什么在Java中不推荐使用？"><a href="#9、什么是线程组，为什么在Java中不推荐使用？" class="headerlink" title="9、什么是线程组，为什么在Java中不推荐使用？"></a>9、什么是线程组，为什么在Java中不推荐使用？</h5><code>ThreadGroup</code>是一个类, 它的目的是提供关于线程组的信息.<br><code>ThreadGroup</code>API比较薄弱, 它并没有为<code>Thread</code>提供了更多的功能. 它主要有两个功能: 一是获取线程组中处于活跃状态线程的列表; 二是甚至为线程设置未捕获异常处理器(<code>uncaught exception handler</code>) . 但在java 1.5中Thread类也添加了<code>setUncaughtExceptionHandler(UncaughtExceptionHandler eh)</code>方法, 所以<code>ThreadGroup</code>是已经过时的, 不建议使用.<h5 id="10、你是如何调用-wait（方法的）？使用-if-块还是循环？为什么"><a href="#10、你是如何调用-wait（方法的）？使用-if-块还是循环？为什么" class="headerlink" title="10、你是如何调用 wait（方法的）？使用 if 块还是循环？为什么"></a>10、你是如何调用 wait（方法的）？使用 if 块还是循环？为什么</h5></li></ul><p><code>wait()</code> 方法应该在循环调用，因为当线程获取到 CPU 开始执行的时候，其他条件可能还没有满足，所以在处理前，循环检测条件是否满足会更好。</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;此片文章主要总结的是Thread类及相关的基础概念和API，首先需要厘清线程调度中的几个基本概念：&lt;br&gt;
    
    </summary>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/categories/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/tags/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>多线程从入门到放弃【1】</title>
    <link href="https://tisonkong.github.io/github.io/2018/05/20/java%20%E5%A4%9A%E7%BA%BF%E7%A8%8B%E6%80%BB%E7%BB%93%E7%AF%871%E4%B9%8B%E2%80%94%E2%80%94%E5%9F%BA%E6%9C%AC%E6%A6%82%E5%BF%B5/"/>
    <id>https://tisonkong.github.io/github.io/2018/05/20/java 多线程总结篇1之——基本概念/</id>
    <published>2018-05-20T14:35:08.018Z</published>
    <updated>2018-05-20T14:35:08.019Z</updated>
    
    <content type="html"><![CDATA[<h5 id="1、什么是线程"><a href="#1、什么是线程" class="headerlink" title="1、什么是线程"></a>1、什么是线程</h5><p>进程：每个进程都有独立的代码和数据空间（进程上下文），进程间的切换会有较大的开销，一个进程包含1–n个线程。（进程是资源分配的最小单位）<br>线程：同一类线程共享代码和数据空间，每个线程有独立的运行栈和程序计数器(PC)，线程切换开销小。（线程是cpu调度的最小单位）<br><a id="more"></a></p><h5 id="2、为什么要使用多线程？或者说使用多线程的好处"><a href="#2、为什么要使用多线程？或者说使用多线程的好处" class="headerlink" title="2、为什么要使用多线程？或者说使用多线程的好处"></a>2、为什么要使用多线程？或者说使用多线程的好处</h5><p>（1）发挥多核CPU的优势<br>随着工业的进步，现在的笔记本、台式机乃至商用的应用服务器至少也都是双核的，4核、8核甚至16核的也都不少见，如果是单线程的程序，那么在双核CPU上就浪费了50%，在4核CPU上就浪费了75%。单核CPU上所谓的”多线程”那是假的多线程，同一时间处理器只会处理一段逻辑，只不过线程之间切换得比较快，看着像多个线程”同时”运行罢了。多核CPU上的多线程才是真正的多线程，它能让你的多段逻辑同时工作，多线程，可以真正发挥出多核CPU的优势来，达到充分利用CPU的目的。<br>（2）防止阻塞<br>从程序运行效率的角度来看，单核CPU不但不会发挥出多线程的优势，反而会因为在单核CPU上运行多线程导致线程上下文的切换，而降低程序整体的效率。但是单核CPU我们还是要应用多线程，就是为了防止阻塞。试想，如果单核CPU使用单线程，那么只要这个线程阻塞了，比方说远程读取某个数据吧，对端迟迟未返回又没有设置超时时间，那么你的整个程序在数据返回回来之前就停止运行了。多线程可以防止这个问题，多条线程同时运行，哪怕一条线程的代码执行读取数据阻塞，也不会影响其它任务的执行。<br>（3）便于建模<br>这是另外一个没有这么明显的优点了。假设有一个大的任务A，单线程编程，那么就要考虑很多，建立整个程序模型比较麻烦。但是如果把这个大的任务A分解成几个小任务，任务B、任务C、任务D，分别<br>建立程序模型，并通过多线程分别运行这几个任务，那就简单很多了。</p><h5 id="3、多线程的几种实现方式？"><a href="#3、多线程的几种实现方式？" class="headerlink" title="3、多线程的几种实现方式？"></a>3、多线程的几种实现方式？</h5><p>（1）继承<code>Thread</code>类<br>（2）实现<code>Runnable</code>接口<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//继承Thread类</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Thread1</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Thread1</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            System.out.println(name + <span class="string">"运行  :  "</span> + i);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                sleep((<span class="keyword">int</span>) Math.random() * <span class="number">10</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Thread1 mTh1=<span class="keyword">new</span> Thread1(<span class="string">"A"</span>);</span><br><span class="line">        Thread1 mTh2=<span class="keyword">new</span> Thread1(<span class="string">"B"</span>);</span><br><span class="line">        mTh1.start();</span><br><span class="line">        mTh2.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>说明：<br>程序启动运行main时候，java虚拟机启动一个进程，主线程main在<code>main()</code>调用时候被创建。随着调用两个对象的<code>start</code>方法，另外两个线程也启动了，这样，整个应用就在多线程下运行。</p><p>注意：<code>start()</code>方法的调用后并不是立即执行多线程代码，而是使得该线程变为可运行态（<code>Runnable</code>），什么时候运行是由操作系统决定的。从程序运行的结果可以发现，多线程程序是乱序执行。因此，只有乱序执行的代码才有必要设计为多线程。<code>Thread.sleep()</code>方法调用目的是不让当前线程独自霸占该进程所获取的CPU资源，以留出一定时间给其他线程执行的机会。实际上所有的多线程代码执行顺序都是不确定的，每次执行的结果都是随机的。</p><p>实现接口的方式比继承类的方式更灵活，也能减少程序之间的耦合度，<strong>面向接口编程</strong>也是<a href="http://www.amazon.cn/gp/product/B001130JN8/ref=as_li_qf_sp_asin_il_tl?ie=UTF8&amp;tag=importnew-23&amp;linkCode=as2&amp;camp=536&amp;creative=3200&amp;creativeASIN=B001130JN8" title="设计模式:可复用面向对象软件的基础" target="_blank" rel="noopener">设计模式</a>6大原则的核心。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//实现Runnable接口</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Thread2</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Thread2</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">          <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">                System.out.println(name + <span class="string">"运行  :  "</span> + i);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep((<span class="keyword">int</span>) Math.random() * <span class="number">10</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> Thread2(<span class="string">"C"</span>)).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> Thread2(<span class="string">"D"</span>)).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>说明：<br><code>Thread2</code>类通过实现<code>Runnable</code>接口，使得该类有了多线程类的特征。<code>run（）</code>方法是多线程程序的一个约定。所有的多线程代码都在run方法里面。<code>Thread</code>类实际上也是实现了<code>Runnable</code>接口的类。<br>在启动的多线程的时候，需要先通过<code>Thread</code>类的构造方法<code>Thread(Runnable target)</code>构造出对象，然后调用<code>Thread</code>对象的<code>start()</code>方法来运行多线程代码。<br>实际上所有的多线程代码都是通过运行<code>Thread</code>的<code>start()</code>方法来运行的。因此，不管是扩展<code>Thread</code>类还是实现<code>Runnable</code>接口来实现多线程，最终还是通过<code>Thread</code>的对象的API来控制线程的，熟悉<code>Thread</code>类的API是进行多线程编程的基础。</p><h5 id="4、用Thread和Runnable哪种方式更好？区别是什么？"><a href="#4、用Thread和Runnable哪种方式更好？区别是什么？" class="headerlink" title="4、用Thread和Runnable哪种方式更好？区别是什么？"></a>4、用Thread和Runnable哪种方式更好？区别是什么？</h5><p>显然是用<code>Runnable</code>更好，实现<code>Runnable</code>接口比继承<code>Thread</code>类所具有的优势：<br>1）：适合多个相同的程序代码的线程去处理同一个资源<br>2）：可以避免java中的单继承的限制<br>3）：增加程序的健壮性，代码可以被多个线程共享，代码和数据独立<br>4）：线程池只能放入实现<code>Runable</code>或<code>callable</code>类线程，不能直接放入继承<code>Thread</code>的类</p><h5 id="5、什么是线程安全？"><a href="#5、什么是线程安全？" class="headerlink" title="5、什么是线程安全？"></a>5、什么是线程安全？</h5><p>线程安全就是多线程访问时，采用了加锁机制，当一个线程访问该类的某个数据时，进行保护，其他线程不能进行访问直到该线程读取完，其他线程才可使用。不会出现数据不一致或者数据污染。线程不安全就是不提供数据访问保护，有可能出现多个线程先后更改数据造成所得到的数据是脏数据<br>还有一种通俗的解释：如果你的代码在多线程下执行和在单线程下执行永远都能获得一样的结果，那么你的代码就是线程安全的。<br>这个问题有值得一提的地方，就是线程安全也是有几个级别的：<br>（1）不可变<br>像<code>String、Integer、Long</code>这些，都是<code>final</code>类型的类，任何一个线程都改变不了它们的值，要改变除非新创建一个，因此这些不可变对象不需要任何同步手段就可以直接在多线程环境下使用<br>（2）绝对线程安全<br>不管运行时环境如何，调用者都不需要额外的同步措施。要做到这一点通常需要付出许多额外的代价，Java中标注自己是线程安全的类，实际上绝大多数都不是线程安全的，不过绝对线程安全的类，Java中也有，比方说<code>CopyOnWriteArrayList、CopyOnWriteArraySet</code><br>（3）相对线程安全<br>相对线程安全也就是我们通常意义上所说的线程安全，像<code>Vector</code>这种，<code>add、remove</code>方法都是原子操作，不会被打断，但也仅限于此，如果有个线程在遍历某个Vector、有个线程同时在add这个Vector，99%的情况下都会出现<code>ConcurrentModificationException</code>，也就是<code>fail-fast</code>机制。<br>（4）线程非安全<br>这个就没什么好说的了，<code>ArrayList、LinkedList、HashMap</code>等都是线程非安全的类.</p><h5 id="6、Vector-SimpleDateFormat是线程安全类吗？"><a href="#6、Vector-SimpleDateFormat是线程安全类吗？" class="headerlink" title="6、Vector, SimpleDateFormat是线程安全类吗？"></a>6、Vector, SimpleDateFormat是线程安全类吗？</h5><p><code>SimpleDateFormate</code>不是线程安全的，如果我们把<code>SimpleDateFormat</code>定义成<code>static</code>成员变量，那么多个<code>thread</code>之间会共享这个sdf对象， 所以<code>Calendar</code>对象也会共享。假定线程A和线程B都进入了<code>parse(text, pos)</code>方法， 线程B执行到<code>calendar.clear()</code>后，线程A执行到<code>calendar.getTime()</code>, 那么就会有问题。</p><p>Vector 在方法上使用同步这样做本身没有解决多线程问题，反而，在引入了概念的混乱的同时，导致性能问题，因为 <code>synchronized</code> 的开销是巨大的：阻止编译器乱序 。详情请看<a href="http://blog.csdn.net/xdonx/article/details/9465489" target="_blank" rel="noopener">《Vector是线程安全吗？》</a>。看下面Vector代码就知道了：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (!vector.contains(element))</span><br><span class="line">    vector.add(element);</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>这是经典的 <code>put-if-absent</code> 情况，尽管<code>contains, add</code>方法都正确地同步了，但作为 <code>vector</code>之外的使用环境，仍然存在 <code>race condition</code>: 因为虽然条件判断<code>if (!vector.contains(element))</code>与方法调用 <code>vector.add(element)</code>;  都是原子性的操作 (atomic)，但在 if 条件判断为真后，那个用来访问<code>vector.contains</code>方法的锁已经释放，在即将的 <code>vector.add</code>方法调用 之间有间隙，在多线程环境中，完全有可能被其他线程获得 <code>vector</code>的 <code>lock</code>并改变其状态, 此时当前线程的<code>vector.add(element)</code>;  正在等待（只不过我们不知道而已）。只有当其他线程释放了 <code>vector</code> 的 <code>lock</code> 后，<code>vector.add(element)</code>; 继续，但此时它已经基于一个错误的假设了。单个的方法<code>synchronized</code> 了并不代表组合<code>（compound）</code>的方法调用具有原子性，使 <code>compound actions</code>成为线程安全的可能解决办法之一还是离不开<code>intrinsic lock</code>(这个锁应该是 <code>vector</code>的，但由 <code>client</code> 维护)：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Vector v = ...</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span>  <span class="keyword">boolean</span> <span class="title">putIfAbsent</span><span class="params">(E x)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">synchronized</span>(v) &#123;</span><br><span class="line">           <span class="keyword">boolean</span> absent = !contains(x);</span><br><span class="line">           <span class="keyword">if</span> (absent) &#123;</span><br><span class="line">               add(x);</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> absent;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure></p><p>所以，正确地回答那个“愚蠢”的问题是：<code>Vector</code> 和 <code>ArrayList</code> 实现了同一接口 <code>List</code>, 但所有的 <code>Vector</code> 的方法都具有 <code>synchronized</code> 关键修饰。但对于复合操作，<code>Vector</code>仍然需要进行同步处理。</p><h5 id="7、什么-Java-原型不是线程安全的？"><a href="#7、什么-Java-原型不是线程安全的？" class="headerlink" title="7、什么 Java 原型不是线程安全的？"></a>7、什么 Java 原型不是线程安全的？</h5><h5 id="8、哪些集合类是线程安全的？"><a href="#8、哪些集合类是线程安全的？" class="headerlink" title="8、哪些集合类是线程安全的？"></a>8、哪些集合类是线程安全的？</h5><p><img src="http://upload-images.jianshu.io/upload_images/8926909-5a588cedfa1a46ed.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="map类"><br>1.在Map类中，提供两种线程安全容器。<br><strong>(1)java.util.Hashtable</strong><br><code>Hashtable</code>和<code>HashMap</code>类似，都是散列表，存储键值对映射。主要区别在于<code>Hashtable</code>是线程安全的。当我们查看<code>Hashtable</code>源码的时候，可以看到<code>Hashtable</code>的方法都是通过<code>synchronized</code>来进行方法层次的同步，以达到线程安全的作用。<br><strong>(2)java.util.concurrent.ConcurrentHashMap</strong><br><code>ConcurrentHashMap</code>是性能更好的散列表。在兼顾线程安全的同时，相对于<code>Hashtable</code>，在效率上有很大的提高。我们可以猜想，<code>Hashtable</code>的线程安全实现是对方法进行<code>synchronized</code>，很明显可以通过其他并发方式，如<code>ReentrantLock</code>进行优化。而<code>ConcurrentHashMap</code>正是采用了<code>ReentrantLock</code>。运用锁分离技术，即在代码块上加锁，而不是方法上加。同时<code>ConcurrentHashMap</code>的一个特色是允许多个修改并发操作。这就有意思了，我们知道一般写都是互斥的，为什么这个还能多个同时写呢？那是因为<code>ConcurrentHashMap</code>采用了内部使用段机制，将<code>ConcurrentHashMap</code>分成了很多小段。只要不在一个小段上写就可以并发写<br><img src="http://upload-images.jianshu.io/upload_images/8926909-8416895cf65f47c0.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="collection类图"></p><p>2.<code>Collection</code>部分主要是运用的<code>CopyOnWrite</code>机制，即写时复制机制。从字面上就能理解什么意思，就是当我们往一个容器里添加元素的时候，先对这个容器进行一次复制，对副本进行写操作。写操作结束后，将原容器的引用指向新副本容器，就完成了写的刷新。<br>从它的实现原理，我们可以看出这种机制是存在缺点的。<br>(1).内存占用：毫无疑问，每次写时需要首先复制一遍原容器，假如复制了很多，或者本身原容器就比较大，那么肯定会占用很多内存。可以采用压缩容器中的元素来防止内存消耗过大。<br>(2).数据一致性问题：当我们在副本中进行写操组时，只能在最终结束后使数据同步，不能实时同步<br>可以看到，这种机制适用于读操作多，写操作少的应用场景。</p><ul><li><p><strong>java.util.concurrent.CopyOnWriteArrayList</strong><br><code>Collection</code>类的线程安全容器主要都是利用的<code>ReentrantLock</code>实现的线程安全，<code>CopyOnWriteArrayList</code>也不例外。在并发写的时候，需要获取<code>lock</code>。读的时候不需要进行<code>lock</code></p></li><li><p><strong>java.util.concurrent.CopyOnWriteArraySet</strong><br><code>CopyOnWriteArraySet</code>的实现就是基于<code>CopyOnWriteArrayList</code>实现的，采用的装饰器进行实现。二者的区别和<code>List</code>和<code>Set</code>的区别一样。</p></li><li><strong>Vector</strong><br>一般我们都不用<code>Vector</code>了，不过它确实也是线程安全的。相对于其他容器，能够提供随机访问功能。<h5 id="9、多线程中的忙循环是什么"><a href="#9、多线程中的忙循环是什么" class="headerlink" title="9、多线程中的忙循环是什么?"></a>9、多线程中的忙循环是什么?</h5>忙循环就是程序员用循环让一个线程等待，不像传统方法<code>wait(), sleep()</code>或 <code>yield()</code>它们都放弃了CPU控制，而忙循环不会放弃CPU，它就是在运行一个空循环。这么做的目的是为了保留CPU缓存，在多核系统中，一个等待线程醒来的时候可能会在另一个内核运行，这样会重建缓存。为了避免重建缓存和减少等待重建的时间就可以使用它了。</li></ul><h5 id="10、什么是线程局部变量"><a href="#10、什么是线程局部变量" class="headerlink" title="10、什么是线程局部变量"></a>10、什么是线程局部变量</h5><p><code>ThreadLocal</code>，很多地方叫做线程本地变量，也有些地方叫做线程本地存储。变量值的共享可以使用<code>public static</code>变量的形式，所有的线程都使用同一个<code>public static</code>变量，但是如果每一个线程都有自己的变量该如何共享呢，就是通过<code>ThreadLocal</code>，<code>ThreadLocal</code>为变量在每个线程中都创建了一个副本，那么每个线程可以访问自己内部的副本变量。<br>最常见的<code>ThreadLocal</code>使用场景为 用来解决 数据库连接、<code>Session</code>管理等。譬如在数据库链接的时候可以这样实现：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ConnectionManager</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span>  Connection connect = <span class="keyword">null</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Connection <span class="title">openConnection</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(connect == <span class="keyword">null</span>)&#123;</span><br><span class="line">            connect = DriverManager.getConnection();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> connect;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">closeConnection</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(connect!=<span class="keyword">null</span>)</span><br><span class="line">            connect.close();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Dao</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insert</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ConnectionManager connectionManager = <span class="keyword">new</span> ConnectionManager();</span><br><span class="line">        Connection connection = connectionManager.openConnection();</span><br><span class="line">        <span class="comment">//使用connection进行操作</span></span><br><span class="line">        connectionManager.closeConnection();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>每次都是在方法内部创建的连接，那么线程之间避免了线程安全问题。但是这样会有一个致命的影响：导致服务器压力非常大，并且严重影响程序执行性能。由于在方法中需要频繁地开启和关闭数据库连接，这样不尽严重影响程序执行效率，还可能导致服务器压力巨大。　那么这种情况下使用<code>ThreadLocal</code>是再适合不过的了，因为<code>ThreadLocal</code>在每个线程中对该变量会创建一个副本，即每个线程内部都会有一个该变量，且在线程内部任何地方都可以使用，线程之间互不影响，这样一来就不存在线程安全问题，也不会严重影响程序执行性能。但是要注意，虽然<code>ThreadLocal</code>能够解决上面说的问题，但是由于在每个线程中都创建了副本，所以要考虑它对资源的消耗，比如内存的占用会比不使用<code>ThreadLocal</code>要大。<br>关于<code>ThreadLocal</code>的API：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//关于ThreadLocal的API：</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> T <span class="title">get</span><span class="params">()</span> </span>&#123; &#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">set</span><span class="params">(T value)</span> </span>&#123; &#125;</span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">remove</span><span class="params">()</span> </span>&#123; &#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> T <span class="title">initialValue</span><span class="params">()</span> </span>&#123; &#125;</span><br></pre></td></tr></table></figure></p><p><code>get()</code>方法是用来获取<code>ThreadLocal</code>在当前线程中保存的变量副本，<code>set()</code>用来设置当前线程中变量的副本，<code>remove()</code>用来移除当前线程中变量的副本，<code>initialValue()</code>是一个<code>protected</code>方法，一般是用来在使用时进行重写的，它是一个延迟加载方法。这部分可以网上参考：<a href="https://www.cnblogs.com/dolphin0520/p/3920407.html" target="_blank" rel="noopener">《Java并发编程：深入剖析ThreadLocal》</a> ，往上关于此部分知识的有几篇高访问量博文都有争议，切勿照搬，可看评论区。</p><h5 id="11、线程和进程有什么区别？进程间如何通讯，线程间如何通讯"><a href="#11、线程和进程有什么区别？进程间如何通讯，线程间如何通讯" class="headerlink" title="11、线程和进程有什么区别？进程间如何通讯，线程间如何通讯"></a>11、线程和进程有什么区别？进程间如何通讯，线程间如何通讯</h5><p>进程：每个进程都有独立的代码和数据空间（进程上下文），进程间的切换会有较大的开销，一个进程包含1–n个线程。（进程是资源分配的最小单位）<br>线程：同一类线程共享代码和数据空间，每个线程有独立的运行栈和程序计数器(PC)，线程切换开销小。（线程是cpu调度的最小单位）<br><strong>一、进程间的通讯</strong><br><strong>管道( pipe )</strong>：管道是一种半双工的通信方式，数据只能单向流动，而且只能在具有亲缘关系的进程间使用。进程的亲缘关系通常是指父子进程关系。<br><strong>有名管道 (namedpipe)</strong>： 有名管道也是半双工的通信方式，但是它允许无亲缘关系进程间的通信。<br><strong>信号量(semophore )</strong>： 信号量是一个计数器，可以用来控制多个进程对共享资源的访问。它常作为一种锁机制，防止某进程正在访问共享资源时，其他进程也访问该资源。因此，主要作为进程间以及同一进程内不同线程之间的同步手段。<br><strong>消息队列( messagequeue )</strong> ： 消息队列是由消息的链表，存放在内核中并由消息队列标识符标识。消息队列克服了信号传递信息少、管道只能承载无格式字节流以及缓冲区大小受限等缺点。<br><strong>信号</strong>： 信号是一种比较复杂的通信方式，用于通知接收进程某个事件已经发生。<br><strong>共享内存(shared memory )</strong>：共享内存就是映射一段能被其他进程所访问的内存，这段共享内存由一个进程创建，但多个进程都可以访问。共享内存是最快的 IPC 方式，它是针对其他进程间通信方式运行效率低而专门设计的。它往往与其他通信机制，如信号两，配合使用，来实现进程间的同步和通信。<br><strong>套接字(socket )</strong> ： 套解口也是一种进程间通信机制，与其他通信机制不同的是，它可用于不同及其间的进程通信。</p><p><strong>二、线程间的通信方式</strong><br><strong>锁机制</strong>：包括互斥锁、条件变量、读写锁<br>互斥锁提供了以排他方式防止数据结构被并发修改的方法。<br>读写锁允许多个线程同时读共享数据，而对写操作是互斥的。<br>条件变量可以以原子的方式阻塞进程，直到某个特定条件为真为止。对条件的测试是在互斥锁的保护下进行的。条件变量始终与互斥锁一起使用。<br><strong>信号量机制(Semaphore)</strong>：包括无名线程信号量和命名线程信号量<br><strong>信号机制(Signal)</strong>：类似进程间的信号处理<br>线程间的通信目的主要是用于线程同步，所以线程没有像进程通信中的用于数据交换的通信机制。<br><strong>管道( pipe )</strong>：管道是一种半双工的通信方式，数据只能单向流动，而且只能在具有亲缘关系的进程间使用。进程的亲缘关系通常是指父子进程关系。<br><strong>有名管道 (namedpipe)</strong>： 有名管道也是半双工的通信方式，但是它允许无亲缘关系进程间的通信。<br><strong>信号量(semophore )</strong> ： 信号量是一个计数器，可以用来控制多个进程对共享资源的访问。它常作为一种锁机制，防止某进程正在访问共享资源时，其他进程也访问该资源。因此，主要作为进程间以及同一进程内不同线程之间的同步手段。<br><strong>消息队列( messagequeue )</strong> ： 消息队列是由消息的链表，存放在内核中并由消息队列标识符标识。消息队列克服了信号传递信息少、管道只能承载无格式字节流以及缓冲区大小受限等缺点。<br><strong>信号 (sinal )</strong> ： 信号是一种比较复杂的通信方式，用于通知接收进程某个事件已经发生。<br><strong>共享内存(shared memory )</strong> ：共享内存就是映射一段能被其他进程所访问的内存，这段共享内存由一个进程创建，但多个进程都可以访问。共享内存是最快的 IPC 方式，它是针对其他进程间通信方式运行效率低而专门设计的。它往往与其他通信机制，如信号两，配合使用，来实现进程间的同步和通信。<br><strong>套接字(socket )</strong> ： 套解口也是一种进程间通信机制，与其他通信机制不同的是，它可用于不同及其间的进程通信。</p><h5 id="12、什么是多线程环境下的伪共享（false-sharing）"><a href="#12、什么是多线程环境下的伪共享（false-sharing）" class="headerlink" title="12、什么是多线程环境下的伪共享（false sharing）"></a>12、什么是多线程环境下的伪共享（false sharing）</h5><p>共享就是一个内存区域的数据被多个处理器访问，伪共享就是不是真的共享。这里的共享这个概念是基于逻辑层面的。实际上伪共享与共享在cache line 上实际都是共享的。CPU访问的数据都是从cache line中读取的。如果cpu 在cache 中找不到需要的变量，则称缓存未命中。<br>未命中时，需要通过总线从内存中读取进cache 中。每次读取的内存大小就是一个cache line 的大小。如果多个CPU访问的不同内存变量被装载到了同一个cache line 中，则从程序逻辑层上讲，并没有共享变量，但实际上在cache line 上他们是共享访问的，这个就是典型的伪共享。伪共享与共享 在 cache line 的层面上必须都是共享的。多个CPU对共享内存的访问安全通过缓存一致性来保证。伪共享问题很难被发现，因为线程可能访问完全不同的全局变量，内存中却碰巧在很相近的位置上。如其他诸多的并发问题，避免伪共享的最基本方式是仔细审查代码，根据缓存行来调整你的数据结构。</p><h5 id="13、同步和异步有何异同，在什么情况下分别使用他们？举例说明"><a href="#13、同步和异步有何异同，在什么情况下分别使用他们？举例说明" class="headerlink" title="13、同步和异步有何异同，在什么情况下分别使用他们？举例说明"></a>13、同步和异步有何异同，在什么情况下分别使用他们？举例说明</h5><p>如果数据将在线程间共享。例如正在写的数据以后可能被另一个线程读到，或者正在读的数据可能已经被另一个线程写过了，那么这些数据就是共享数据，必须进行同步存取。当应用程序在对象上调用了一个需要花费很长时间来执行的方法，并且不希望让程序等待方法的返回时，就应该使用异步编程，在很多情况下采用异步途径往往更有效率。<br>Java中交互方式分为同步和异步两种：</p><ul><li>同步交互：指发送一个请求,需要等待返回,然后才能够发送下一个请求，有个等待过程；</li><li>异步交互：指发送一个请求,不需要等待返回,随时可以再发送下一个请求，即不需要等待。</li></ul><p>区别：一个需要等待，一个不需要等待，在部分情况下，我们的项目开发中都会优先选择不需要等待的异步交互方式。<br>哪些情况建议使用同步交互呢？比如银行的转账系统，对数据库的保存操作等等，都会使用同步交互操作，其余情况都优先使用异步交互</p>]]></content>
    
    <summary type="html">
    
      &lt;h5 id=&quot;1、什么是线程&quot;&gt;&lt;a href=&quot;#1、什么是线程&quot; class=&quot;headerlink&quot; title=&quot;1、什么是线程&quot;&gt;&lt;/a&gt;1、什么是线程&lt;/h5&gt;&lt;p&gt;进程：每个进程都有独立的代码和数据空间（进程上下文），进程间的切换会有较大的开销，一个进程包含1–n个线程。（进程是资源分配的最小单位）&lt;br&gt;线程：同一类线程共享代码和数据空间，每个线程有独立的运行栈和程序计数器(PC)，线程切换开销小。（线程是cpu调度的最小单位）&lt;br&gt;
    
    </summary>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/categories/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="多线程" scheme="https://tisonkong.github.io/github.io/tags/%E5%A4%9A%E7%BA%BF%E7%A8%8B/"/>
    
  </entry>
  
  <entry>
    <title>详解Java中的Set和List</title>
    <link href="https://tisonkong.github.io/github.io/2018/02/28/java%E5%9F%BA%E7%A1%80%E4%B9%8B%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%844%EF%BC%88Set%E5%92%8CList%EF%BC%89/"/>
    <id>https://tisonkong.github.io/github.io/2018/02/28/java基础之数据结构4（Set和List）/</id>
    <published>2018-02-28T01:40:21.411Z</published>
    <updated>2018-02-28T01:40:21.412Z</updated>
    
    <content type="html"><![CDATA[<h4 id="一、Set"><a href="#一、Set" class="headerlink" title="一、Set"></a>一、Set</h4><h5 id="1-Set-里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用-还是-equals-？-它们有何区别"><a href="#1-Set-里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用-还是-equals-？-它们有何区别" class="headerlink" title="1.Set 里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用 == 还是 equals()？ 它们有何区别?"></a>1.Set 里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用 == 还是 equals()？ 它们有何区别?</h5><a id="more"></a><ul><li>如果<code>hash</code>码值不相同，说明是一个新元素，存；<br>如果没有元素和传入对象（也就是<code>add</code>的元素）的<code>hash</code>值相等，那么就认为这个元素在<code>table</code>中不存在，将其添加进<code>table</code>；</li><li>如果<code>hash</code>码值相同，且<code>equles</code>判断相等，说明元素已经存在，不存；如果<code>hash</code>码值相同，且<code>equles</code>判断不相等，说明元素不存在，存；</li></ul><p>java中的数据类型，可分为两类：<br><strong>1.基本数据类型</strong><br>也称原始数据类型。<strong>byte,short,char,int,long,float,double,boolean</strong>,他们之间的比较，应用双等号（==），比较的是他们的值。基本数据类型没有equals方法哦。<br><strong>2.复合数据类型(类)</strong><br>当他们用（==）进行比较的时候，比较的是他们在内存中的存放地址，所以，除非是同一个<code>new</code>出来的对象，他们的比较后的结果为<code>true</code>，否则比较后结果为<code>false</code>。 JAVA当中所有的类都是继承于<code>Object</code>这个基类的，在<code>Object</code>中的基类中定义了一个<code>equals</code>的方法，这个方法的初始行为是比较对象的内存地址，但在一些类库当中这个方法被覆盖掉了，如<code>String,Integer,Date</code>在这些类当中<code>equals</code>有其自身的实现，而不再是比较类在堆内存中的存放地址了。<br>  对于复合数据类型之间进行<code>equals</code>比较，在没有覆写<code>equals</code>方法的情况下，他们之间的比较还是基于他们在内存中的存放位置的地址值的，因为<code>Object</code>的<code>equals</code>方法也是用双等号（==）进行比较的，所以比较后的结果跟双等号（==）的结果相同。</p><h5 id="2-TreeMap：TreeMap-是采用什么树实现的？TreeMap、HashMap、LindedHashMap的区别。"><a href="#2-TreeMap：TreeMap-是采用什么树实现的？TreeMap、HashMap、LindedHashMap的区别。" class="headerlink" title="2.TreeMap：TreeMap 是采用什么树实现的？TreeMap、HashMap、LindedHashMap的区别。"></a>2.TreeMap：TreeMap 是采用什么树实现的？TreeMap、HashMap、LindedHashMap的区别。</h5><p>TreeMap 是一个有序的<strong>key-value</strong>集合，它是通过<a href="http://www.cnblogs.com/skywang12345/p/3245399.html" target="_blank" rel="noopener">红黑树</a>实现的。  TreeMap 继承于<strong>AbstractMap</strong>，所以它是一个Map，即一个key-value集合。TreeMap 实现了NavigableMap接口，意味着它<strong>支持一系列的导航方法。</strong>比如返回有序的key集合。TreeMap 实现了Cloneable接口，意味着<strong>它能被克隆</strong>。  TreeMap 实现了java.io.Serializable接口，意味着<strong>它支持序列化</strong>。<br>TreeMap基于<strong>红黑树</strong>（Red-Black tree）实现。该映射根据<strong>其键的自然顺序进行排序</strong>，或者根据<strong>创建映射时提供的</strong> <strong>Comparator</strong> <strong>进行排序</strong>，具体取决于使用的构造方法。  TreeMap的基本操作 containsKey、get、put 和 remove 的时间复杂度是 log(n) 。  另外，TreeMap是<strong>非同步</strong>的。  它的iterator 方法返回的<strong>迭代器是fail-fastl</strong>的。</p><h5 id="3-TreeMap和TreeSet在排序时如何比较元素？Collections工具类中的sort-方法如何比较元素？"><a href="#3-TreeMap和TreeSet在排序时如何比较元素？Collections工具类中的sort-方法如何比较元素？" class="headerlink" title="3.TreeMap和TreeSet在排序时如何比较元素？Collections工具类中的sort()方法如何比较元素？"></a>3.TreeMap和TreeSet在排序时如何比较元素？Collections工具类中的sort()方法如何比较元素？</h5><p><strong>TreeSet</strong>要求存放的对象所属的类必须实现<code>Comparable</code>接口，该接口提供了比较元素的<code>compareTo()</code>方法，当插入元素时会回调该方法比较元素的大小。<code>TreeMap</code>要求存放的键值对映射的键必须实现<code>Comparable</code>接口从而根据键对元素进行排序。<code>Collections</code>工具类的<code>sort</code>方法有两种重载的形式，第一种要求传入的待排序容器中存放的对象必须实现<code>Comparable</code>接口以实现元素的比较；第二种不强制性的要求容器中的元素必须可比较，但是要求传入第二个参数，参数是<code>Comparator</code>接口的子类型（需要重写<code>compare</code>方法实现元素的比较），相当于一个临时定义的排序规则，其实就是通过接口注入比较元素大小的算法，也是对回调模式的应用（Java中对函数式编程的支持）。</p><h5 id="3-TreeSet：一个已经构建好的-TreeSet，怎么完成倒排序。"><a href="#3-TreeSet：一个已经构建好的-TreeSet，怎么完成倒排序。" class="headerlink" title="3.TreeSet：一个已经构建好的 TreeSet，怎么完成倒排序。"></a>3.TreeSet：一个已经构建好的 TreeSet，怎么完成倒排序。</h5><p><strong>1、自然顺序</strong><br>即类要实现<code>Comparable</code>接口，并重写<code>compareTo()</code>方法，<code>TreeSet</code>对象调用<code>add()</code>方法时，会将存入的对象提升为<code>Comparable</code>类型，然后调用对象中的<code>compareTo()</code>方法进行比较，根据比较的返回值进行存储。<br>因为<code>TreeSet</code>底层是二叉树，当<code>compareTo</code>方法返回<code>0</code>时，不存储；当<code>compareTo</code>方法返回正数时，存入二叉树的右子树；当<code>compareTo</code>方法返回负数时，存入二叉树的左子树。如果一个类没有实现<code>Comparable</code>接口就将该类对象存入<code>TreeSet</code>集合，会发生类型转换异常。<br><strong>2、比较器顺序Comparator</strong><br>创建<code>TreeSet</code>对象的时候可以指定一个比较器，即传入一个<code>Comparator</code>对象，那么<code>TreeSet</code>会优先按照<code>Comparator</code>中的<code>compare()</code>方法排序，<code>compare</code>方法中有两个参数，第一个是调用该方法的对象，第二个值集合中已经存入的对象。</p><h5 id="4-EnumSet-是什么"><a href="#4-EnumSet-是什么" class="headerlink" title="4.EnumSet 是什么"></a>4.EnumSet 是什么</h5><h5 id="5-HashSet和TreeSet有什么区别"><a href="#5-HashSet和TreeSet有什么区别" class="headerlink" title="5.HashSet和TreeSet有什么区别?"></a>5.HashSet和TreeSet有什么区别?</h5><ul><li>底层存储的数据结构不同<br><code>HashSet</code>底层用的是<code>HashMap</code>哈希表结构存储，而<code>TreeSet</code>底层用的是<code>TreeMap</code>树结构存储</li><li>存储时保证数据唯一性依据不同<br><code>HashSet</code>是通过复写<code>hashCode()</code>方法和<code>equals()</code>方法来保证的，而<code>HashSet</code>通过<code>Compareable</code>接口的<code>compareTo()</code>方法来保证的</li><li>有序性不一样<br><code>HashSet</code>无序，<code>TreeSet</code>有序<h5 id="6-HashSet-内部是如何工作的"><a href="#6-HashSet-内部是如何工作的" class="headerlink" title="6.HashSet 内部是如何工作的"></a>6.HashSet 内部是如何工作的</h5><code>HashSet</code>:底层数据结构是哈希表，本质就是对哈希值的存储，通过判断元素的<code>hashCode</code>方法和<code>equals</code>方法来保证元素的唯一性，当<code>hashCode</code>值不相同，就直接存储了，不用在判断<code>equals</code>了，当<code>hashCode</code>值相同时，会在判断一次<code>euqals</code>方法的返回值是否为<code>true</code>，如果为<code>true</code>则视为用一个元素，不用存储，如果为<code>false</code>，这些相同哈希值不同内容的元素都存放一个<code>bucket</code>桶里（当哈希表中有一个桶结构，每一个桶都有一个哈希值）<br><code>TreeSet</code>:底层的数据结构是二叉树，可以对<code>Set</code>集合中的元素进行排序,这种结构，可以提高排序性能, 根据比较方法的返回值确定的,只要返回的是0.就代表元素重复</li></ul><h5 id="7-WeakHashMap-是怎么工作的？"><a href="#7-WeakHashMap-是怎么工作的？" class="headerlink" title="7.WeakHashMap 是怎么工作的？"></a>7.WeakHashMap 是怎么工作的？</h5><h4 id="二、-List"><a href="#二、-List" class="headerlink" title="二、 List"></a>二、 List</h4><h5 id="1-List-Set-Map三个接口，存取元素时各有什么特点？"><a href="#1-List-Set-Map三个接口，存取元素时各有什么特点？" class="headerlink" title="1.List, Set, Map三个接口，存取元素时各有什么特点？"></a>1.List, Set, Map三个接口，存取元素时各有什么特点？</h5><p><code>List</code>与<code>Set</code>都是单列元素的集合，它们有一个功共同的父接口<code>Collection</code>。<br><strong>Set</strong>里面不允许有重复的元素，<br>存元素：<code>add</code>方法有一个<code>boolean</code>的返回值，当集合中没有某个元素，此时<code>add</code>方法可成功加入该元素时，则返回<code>true</code>；当集合含有与某个元素<code>equals</code>相等的元素时，此时<code>add</code>方法无法加入该元素，返回结果为<code>false</code>。<br>取元素：没法说取第几个，只能以<code>Iterator</code>接口取得所有的元素，再逐一遍历各个元素。</p><p><strong>List</strong>表示有先后顺序的集合，<br>存元素：多次调用<code>add(Object)</code>方法时，每次加入的对象按先来后到的顺序排序，也可以插队，即调用<code>add(int index,Object)</code>方法，就可以指定当前对象在集合中的存放位置。<br>取元素：<br>方法1：<code>Iterator</code>接口取得所有，逐一遍历各个元素<br>方法2：调用<code>get(index i)</code>来明确说明取第几个。使用此接口能够精确的控制每个元素插入的位置。用户能够使用索引（元素在<code>List</code>中的位置，类似于数组下标）来访问<code>List</code>中的元素，这类似于Java的数组。</p><p><strong>Map</strong>是双列的集合，存放用<code>put</code>方法:<code>put(obj key,obj value)</code>，每次存储时，要存储一对<code>key/value</code>，不能存储重复的<code>key</code>，这个重复的规则也是按equals比较相等。<br>取元素：用<code>get(Object key)</code>方法根据<code>key</code>获得相应的<code>value</code>。<br>也可以获得所有的<code>key</code>的集合，还可以获得所有的<code>value</code>的集合，<br>还可以获得<code>key</code>和<code>value</code>组合成的<code>Map.Entry</code>对象的集合。</p><p><strong>List</strong>以特定次序来持有元素，可有重复元素。<code>Set</code>无法拥有重复元素,内部排序。<code>Map</code> 保存<code>key-value</code>值，<code>value</code>可多值。</p><h5 id="2-List-Set-Map-是否继承自-Collection-接口"><a href="#2-List-Set-Map-是否继承自-Collection-接口" class="headerlink" title="2.List, Set, Map 是否继承自 Collection 接口"></a>2.List, Set, Map 是否继承自 Collection 接口</h5><p><code>List</code>和<code>Set</code>是的，而<code>Map</code>不是。</p><h5 id="3-遍历一个-List-有哪些不同的方式"><a href="#3-遍历一个-List-有哪些不同的方式" class="headerlink" title="3.遍历一个 List 有哪些不同的方式"></a>3.遍历一个 List 有哪些不同的方式</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line">Iterator it1 = list.iterator();</span><br><span class="line"><span class="keyword">while</span>(it1.hasNext())&#123;</span><br><span class="line">    System.out.println(it1.next());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//方法2</span></span><br><span class="line"><span class="keyword">for</span>(Iterator it2 = list.iterator();it2.hasNext();)&#123;</span><br><span class="line">     System.out.println(it2.next());</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//方法3</span></span><br><span class="line"><span class="keyword">for</span>(String tmp:list)&#123;</span><br><span class="line">    System.out.println(tmp);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//方法4</span></span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">int</span> i = <span class="number">0</span>;i &lt; list.size(); i ++)&#123;</span><br><span class="line">    System.out.println(list.get(i));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="三、LinkedList"><a href="#三、LinkedList" class="headerlink" title="三、LinkedList"></a>三、LinkedList</h4><h5 id="1-LinkedList-是单向链表还是双向链表"><a href="#1-LinkedList-是单向链表还是双向链表" class="headerlink" title="1.LinkedList 是单向链表还是双向链表"></a>1.LinkedList 是单向链表还是双向链表</h5><p><strong>Linkedlist</strong>，双向链表，优点，增加删除，用时间很短，但是因为没有索引，对索引的操作，比较麻烦，只能循环遍历，但是每次循环的时候，都会先判断一下，这个索引位于链表的前部分还是后部分，每次都会遍历链表的一半 ，而不是全部遍历。<br>双向链表，都有一个<code>previous</code>和<code>next</code>， 链表最开始的部分都有一个<code>fiest</code>和<code>last</code>指向第一个元素，和最后一个元素。增加和删除的时候，只需要更改一个<code>previous</code>和<code>next</code>，就可以实现增加和删除，所以说，<code>LinkedList</code>对于数据的删除和增加相当的方便。</p><h5 id="2-LinkedList-与-ArrayList-有什么区别"><a href="#2-LinkedList-与-ArrayList-有什么区别" class="headerlink" title="2.LinkedList 与 ArrayList 有什么区别?"></a>2.LinkedList 与 ArrayList 有什么区别?</h5><ul><li>因为<code>Array</code>是基于索引<code>(index)</code>的数据结构，它使用索引在数组中搜索和读取数据是很快的。<code>Array</code>获取数据的时间复杂度是<code>O(1)</code>,但是要删除数据却是开销很大的，因为这需要重排数组中的所有数据。</li><li>相对于<code>ArrayList</code>，<code>LinkedList</code>插入是更快的。因为<code>LinkedList</code>不像<code>ArrayList</code>一样，不需要改变数组的大小，也不需要在数组装满的时候要将所有的数据重新装入一个新的数组，这是<code>ArrayList</code>最坏的一种情况，时间复杂度是<code>O(n)</code>，而<code>LinkedList</code>中插入或删除的时间复杂度仅为<code>O(1)</code>。<code>ArrayList</code>在插入数据时还需要更新索引（除了插入数组的尾部）。</li><li>类似于插入数据，删除数据时，<code>LinkedList</code>也优于<code>ArrayList</code>。</li><li><code>LinkedList</code>需要更多的内存，因为<code>ArrayList</code>的每个索引的位置是实际的数据，而<code>LinkedList</code>中的每个节点中存储的是实际的数据和前后节点的位置。</li></ul><h5 id="3-描述下-Java-中集合（Collections），接口（Interfaces），实现（Implementations）的概念。"><a href="#3-描述下-Java-中集合（Collections），接口（Interfaces），实现（Implementations）的概念。" class="headerlink" title="3.描述下 Java 中集合（Collections），接口（Interfaces），实现（Implementations）的概念。"></a>3.描述下 Java 中集合（Collections），接口（Interfaces），实现（Implementations）的概念。</h5><h5 id="4-插入数据时，ArrayList-LinkedList-Vector谁速度较快？"><a href="#4-插入数据时，ArrayList-LinkedList-Vector谁速度较快？" class="headerlink" title="4.插入数据时，ArrayList, LinkedList, Vector谁速度较快？"></a>4.插入数据时，ArrayList, LinkedList, Vector谁速度较快？</h5><p><code>ArrayList</code>和<code>Vector</code>都是数组实现，但不同的是，<code>Vector</code>是线程安全，加了同步，所以原则上<code>ArrayList</code>比<code>Vector</code>比快；<br><code>LinkekList</code>是链表实现，增删快，查找慢，所以你要是插入数据时，显然<code>LinkedList</code>是最快的，其次是<code>ArrayList</code>，再者<code>Vector</code>属于遗留容器（Java早期的版本中提供的容器，除此之外，<code>Hashtable、Dictionary、BitSet、Stack、Properties</code>都是遗留容器），已经不推荐使用，但是由于<code>ArrayList</code>和<code>LinkedListed</code>都是非线程安全的，如果遇到多个线程操作同一个容器的场景，则可以通过工具类<code>Collections</code>中的<code>synchronizedList</code>方法将其转换成线程安全的容器后再使用（这是对装潢模式的应用，将已有对象传入另一个类的构造器中创建新的对象来增强实现）。</p><h4 id="四、ArrayList"><a href="#四、ArrayList" class="headerlink" title="四、ArrayList"></a>四、ArrayList</h4><h5 id="1-ArrayList-和-HashMap-的默认大小是多数"><a href="#1-ArrayList-和-HashMap-的默认大小是多数" class="headerlink" title="1.ArrayList 和 HashMap 的默认大小是多数?"></a>1.ArrayList 和 HashMap 的默认大小是多数?</h5><p>这里要讨论这些常用的默认初始容量和扩容的原因是：<br>当底层实现涉及到扩容时，容器或重新分配一段更大的连续内存（如果是离散分配则不需要重新分配，离散分配都是插入新元素时动态分配内存），要将容器原来的数据全部复制到新的内存上，这无疑使效率大大降低。<br>加载因子的系数小于等于1，意指  即当元素个数 超过 容量长度*加载因子的系数 时，进行扩容。另外，扩容也是有默认的倍数的，不同的容器扩容情况不同。</p><p><code>ArrayList、Vector</code>默认初始容量为<code>10</code>。<br><code>Vector</code>：线程安全，但速度慢。底层数据结构是数组结构，加载因子为1：即当 元素个数 超过 容量长度 时，进行扩容。扩容增量：原容量的 1倍。如 <code>Vector</code>的容量为<code>10</code>，一次扩容后是容量为<code>20</code>。<br><code>ArrayList</code>：线程不安全，查询速度快。底层数据结构是数组结构，扩容增量：原容量的 <code>0.5倍+1</code>，如 <code>ArrayList</code>的容量为<code>10</code>，一次扩容后是容量为<code>16</code>。</p><p><code>Set</code>(集) 元素无序的、不可重复。<br><code>HashSet</code>：线程不安全，存取速度快。<br>底层实现是一个<code>HashMap</code>（保存数据），实现<code>Set</code>接口<br>默认初始容量为<code>16</code>（为何是16，见下方对HashMap的描述）<br>加载因子为<code>0.75</code>：即当 元素个数 超过 容量长度的<code>0.75</code>倍 时，进行扩容<br>扩容增量：原容量的<code>1</code>倍<br>如 <code>HashSet</code>的容量为<code>16</code>，一次扩容后是容量为<code>32</code>。</p><p><code>Map</code>是一个双列集合<br><code>HashMap</code>：默认初始容量为<code>16</code><br>（为何是<code>16</code>：<code>16</code>是<code>2^4</code>，可以提高查询效率，另外，<code>32=16&lt;&lt;1</code>      –&gt;至于详细的原因可另行分析，或分析源代码）<br>加载因子为<code>0.75</code>：即当 元素个数 超过 容量长度的<code>0.75</code>倍 时，进行扩容<br>扩容增量：原容量的 <code>1</code>倍<br>如 <code>HashSet</code>的容量为<code>16</code>，一次扩容后是容量为<code>32</code>。</p><h5 id="2-ArrayList-和-Set-的区别？"><a href="#2-ArrayList-和-Set-的区别？" class="headerlink" title="2.ArrayList 和 Set 的区别？"></a>2.ArrayList 和 Set 的区别？</h5><ul><li><code>Set</code> 集合是无序不可以重复的的、<code>List</code>集合是有序可以重复的。</li><li><code>ArrayList</code>是数组存储的方式，<code>HashSet</code>存储会先进行<code>HashCode</code>值得比较(<code>hashcode</code>和<code>equals</code>方法)，若相同就不会再存储。</li></ul><p>补充一下：<code>Hashset</code>就是采用哈希算法存取对象的集合，对象用完之后没有回收就是内存泄漏。一个对象一旦<code>hashCode</code>生成之后，再对属性值修改后其<code>Hashcode</code>值就会发生改变，再通过<code>hashSet</code>删除就删除不掉了。</p><p>以上的问题还可以继续有如下变形，理解了就能融会贯通：<br>ArrayList, LinkedList, Vector的区别<br>ArrayList是如何实现的，ArrayList 和 LinkedList 的区别<br>ArrayList如何实现扩容</p><h5 id="6-Array-和-ArrayList-有何区别？什么时候更适合用Array？"><a href="#6-Array-和-ArrayList-有何区别？什么时候更适合用Array？" class="headerlink" title="6.Array 和 ArrayList 有何区别？什么时候更适合用Array？"></a>6.Array 和 ArrayList 有何区别？什么时候更适合用Array？</h5><p><strong>ArrayList可以算是Array的加强版，（对array有所取舍的加强）。</strong><br><strong>存储内容比较： </strong><br>•<code>Array</code>数组可以包含基本类型和对象类型，<br>•<code>ArrayList</code>却只能包含对象类型。<br>但是需要注意的是：<code>Array</code>数组在存放的时候一定是同种类型的元素。<code>ArrayList</code>就不一定了，因为<code>ArrayList</code>可以存储<code>Object</code>。</p><p><strong>空间大小比较：</strong><br>•    它的空间大小是固定的，空间不够时也不能再次申请，所以需要事前确定合适的空间大小。<br>•    <code>ArrayList</code>的空间是动态增长的，如果空间不够，它会创建一个空间比原空间大一倍的新数组，然后将所有元素复制到新数组中，接着抛弃旧数组。而且，每次添加新的元素的时候都会检查内部数组的空间是否足够。（比较麻烦的地方）。</p><p><strong>方法上的比较： </strong><br><code>ArrayList</code>作为<code>Array</code>的增强版，当然是在方法上比<code>Array</code>更多样化，比如添加全部<code>addAll()</code>、删除全部<code>removeAll()</code>、返回迭代器<code>iterator()</code>等。</p><p>适用场景：<br>如果想要保存一些在整个程序运行期间都会存在而且不变的数据，我们可以将它们放进一个全局数组里，但是如果我们单纯只是想要以数组的形式保存数据，而不对数据进行增加等操作，只是方便我们进行查找的话，那么，我们就选择<code>ArrayList</code>。而且还有一个地方是必须知道的，就是如果我们需要对元素进行频繁的移动或删除，或者是处理的是超大量的数据，那么，使用<code>ArrayList</code>就真的不是一个好的选择，因为它的效率很低，使用数组进行这样的动作就很麻烦，那么，我们可以考虑选择<code>LinkedList</code>。</p>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;一、Set&quot;&gt;&lt;a href=&quot;#一、Set&quot; class=&quot;headerlink&quot; title=&quot;一、Set&quot;&gt;&lt;/a&gt;一、Set&lt;/h4&gt;&lt;h5 id=&quot;1-Set-里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用-还是-equals-？-它们有何区别&quot;&gt;&lt;a href=&quot;#1-Set-里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用-还是-equals-？-它们有何区别&quot; class=&quot;headerlink&quot; title=&quot;1.Set 里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用 == 还是 equals()？ 它们有何区别?&quot;&gt;&lt;/a&gt;1.Set 里的元素是不能重复的，那么用什么方法来区分重复与否呢？是用 == 还是 equals()？ 它们有何区别?&lt;/h5&gt;
    
    </summary>
    
      <category term="Java Basic" scheme="https://tisonkong.github.io/github.io/categories/Java-Basic/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="数据结构" scheme="https://tisonkong.github.io/github.io/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>Java里必须要会的Hash</title>
    <link href="https://tisonkong.github.io/github.io/2018/02/28/java%E5%9F%BA%E7%A1%80%E4%B9%8B%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%842%EF%BC%88Hash%E7%AF%87%EF%BC%89/"/>
    <id>https://tisonkong.github.io/github.io/2018/02/28/java基础之数据结构2（Hash篇）/</id>
    <published>2018-02-28T01:40:21.410Z</published>
    <updated>2018-02-28T01:40:21.411Z</updated>
    
    <content type="html"><![CDATA[<h5 id="1-Hashcode-的作用"><a href="#1-Hashcode-的作用" class="headerlink" title="1.Hashcode 的作用"></a>1.Hashcode 的作用</h5><p>对于包含容器类型的程序设计语言来说，基本上都会涉及到 <code>hashCode</code>。在Java中也一样，<code>hashCode</code>方法的主要作用是为了配合基于散列的集合一起正常运行，这样的散列集合包括<code>HashSet、HashMap</code>以及<code>HashTable</code>。<a id="more"></a><br>为什么这么说呢？考虑一种情况，当向集合中插入对象时，如何判别在集合中是否已经存在该对象了？（注意：集合中不允许重复的元素存在）<br>也许大多数人都会想到调用<code>equals</code>方法来逐个进行比较，这个方法确实可行。但是如果集合中已经存在一万条数据或者更多的数据，如果采用<code>equals</code>方法去逐一比较，效率必然是一个问题。此时<code>hashCode</code>方法的作用就体现出来了，当集合要添加新的对象时，先调用这个对象的<code>hashCode</code>方法，得到对应的<code>hashcode</code>值，实际上在<code>HashMap</code>的具体实现中会用一个<code>table</code>保存已经存进去的对象的<code>hashcode</code>值，如果<code>table</code>中没有该<code>hashcode</code>值，它就可以直接存进去，不用再进行任何比较了；如果存在该<code>hashcode</code>值， 就调用它的<code>equals</code>方法与新元素进行比较，相同的话就不存了，不相同就散列其它的地址，所以这里存在一个冲突解决的问题，这样一来实际调用<code>equals</code>方法的次数就大大降低了，说通俗一点：Java中的<code>hashCode</code>方法就是根据一定的规则将与对象相关的信息（比如对象的存储地址，对象的字段等）映射成一个数值，这个数值称作为散列值。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//java.util.HashMap的中put方法的具体实现：</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (key == <span class="keyword">null</span>)</span><br><span class="line">            <span class="keyword">return</span> putForNullKey(value);</span><br><span class="line">        <span class="keyword">int</span> hash = hash(key.hashCode());</span><br><span class="line">        <span class="keyword">int</span> i = indexFor(hash, table.length);</span><br><span class="line">        <span class="keyword">for</span> (Entry&lt;K,V&gt; e = table[i]; e != <span class="keyword">null</span>; e = e.next) &#123;</span><br><span class="line">            Object k;</span><br><span class="line">            <span class="keyword">if</span> (e.hash == hash &amp;&amp; ((k = e.key) == key || key.equals(k))) &#123;</span><br><span class="line">                V oldValue = e.value;</span><br><span class="line">                e.value = value;</span><br><span class="line">                e.recordAccess(<span class="keyword">this</span>);</span><br><span class="line">                <span class="keyword">return</span> oldValue;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        modCount++;</span><br><span class="line">        addEntry(hash, key, value, i);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><code>put</code>方法是用来向<code>HashMap</code>中添加新的元素，从<code>put</code>方法的具体实现可知，会先调用<code>hashCode</code>方法得到该元素的<code>hashCode</code>值，然后查看<code>table</code>中是否存在该<code>hashCode</code>值，如果存在则调用<code>equals</code>方法重新确定是否存在该元素，如果存在，则更新<code>value</code>值，否则将新的元素添加到<code>HashMap</code>中。从这里可以看出，<code>hashCode</code>方法的存在是为了减少<code>equals</code>方法的调用次数，从而提高程序效率。</p><blockquote><p><a href="http://www.importnew.com/18851.html" target="_blank" rel="noopener">《浅谈Java中的hashcode方法》</a></p></blockquote><h5 id="2-简述一致性-Hash-算法"><a href="#2-简述一致性-Hash-算法" class="headerlink" title="2.简述一致性 Hash 算法"></a>2.简述一致性 Hash 算法</h5><p>一致性哈希修正了CARP使用的简单哈希算法带来的问题，在分布式系统中也得到了广泛应用。一致性hash算法（DHT）通过减少影响范围的方式解决了增减服务器导致的数据散列问题，从而解决了分布式环境下负载均衡问题，如果存在热点数据，那么通过增添节点的方式，对热点区间进行划分，将压力分配至其他服务器。重新达到负载均衡的状态。</p><blockquote><p><a href="https://www.cnblogs.com/color-my-life/p/5799903.html" target="_blank" rel="noopener">《一致性哈希算法1》</a><br><a href="https://www.cnblogs.com/lpfuture/p/5796398.html" target="_blank" rel="noopener">《一致性哈希算法2》</a></p></blockquote><h5 id="3-有没有可能两个不相等的对象有相同的-hashcode？当两个对象hashcode-相同怎么办？如何获取值对象？"><a href="#3-有没有可能两个不相等的对象有相同的-hashcode？当两个对象hashcode-相同怎么办？如何获取值对象？" class="headerlink" title="3.有没有可能两个不相等的对象有相同的 hashcode？当两个对象hashcode 相同怎么办？如何获取值对象？"></a>3.有没有可能两个不相等的对象有相同的 hashcode？当两个对象hashcode 相同怎么办？如何获取值对象？</h5><p>对于两个对象：</p><ul><li>如果调用<code>equals</code>方法得到的结果为<code>true</code>，则两个对象的<code>hashcode</code>值必定相等；</li><li>如果<code>equals</code>方法得到的结果为<code>false</code>，则两个对象的<code>hashcode</code>值不一定不同；</li><li>如果两个对象的<code>hashcode</code>值不等，则<code>equals</code>方法得到的结果必定为<code>false</code>；</li><li>如果两个对象的<code>hashcode</code>值相等，则<code>equals</code>方法得到的结果未知。</li></ul><p>总之一句话：<strong>等价对象产生相同整数的哈希码，不同对象不一定要不同的哈希码</strong>。后面两个命题其实就是这句话的逆否命题。</p><p>所以，针对上面问题提到的，两个不相等的对象其实就是问的<code>equals</code>为<code>false</code>，那么<code>hashcode</code>不一定是不同，也就是有可能会相同了。为什呢会这样呢？<code>hashCode</code>是所有java对象的固有方法，如果不重载的话，返回的实际上是该对象在jvm的堆上的内存地址，而不同对象的内存地址肯定不同，所以这个<code>hashCode</code>也就肯定不同了。如果重载了的话，由于采用的算法的问题，有可能导致两个不同对象的<code>hashCode</code>相同。<br>后面的问题其实会比较多的出现在<strong>Map</strong>的面试考察中，<br>当我们调用<code>Map</code>的<code>get()</code>方法，<code>HashMap</code>会使用键对象的<code>hashcode</code>找到<code>bucket</code>位置，然后获取值对象。面试官提醒他如果有两个值对象储存在同一个<code>bucket</code>，他给出答案：将会遍历链表直到找到值对象。面试官会问因为你并没有值对象去比较，你是如何确定确定找到值对象的？除非面试者直<code>HashMap</code>在链表中存储的是键值对，否则他们不可能回答出这一题。其中一些记得这个重要知识点的面试者会说，找到bucket位置之后，会调用<code>keys.equals()</code>方法去找到链表中正确的节点，最终找到要找的值对象。完美的答案！</p><h5 id="4-为什么在重写-equals-方法的时候需要重写-hashCode-方法？equals与hashCode-的异同点在哪里"><a href="#4-为什么在重写-equals-方法的时候需要重写-hashCode-方法？equals与hashCode-的异同点在哪里" class="headerlink" title="4.为什么在重写 equals 方法的时候需要重写 hashCode 方法？equals与hashCode 的异同点在哪里?"></a>4.为什么在重写 equals 方法的时候需要重写 hashCode 方法？equals与hashCode 的异同点在哪里?</h5><p>这里头牵扯到另外隐含的问题，一并拿出来解决：</p><blockquote><p>1、首先我们为什么需要重写<code>hashCode()</code>方法和<code>equals()</code>方法<br>2、为什么在重写 equals 方法的时候需要重写 hashCode 方法？<br>3、如何重写这两个方法?</p></blockquote><p><strong>回答第一个问题：</strong><br>Java中的超类<code>Object</code>类中定义的<code>equals()</code>方法是用来比较两个引用所指向的对象的内存地址是否一致，<code>Object</code>类中<code>equals()</code>方法的源码<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object obj)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">return</span> (<span class="keyword">this</span> == obj);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>Object类中的<code>hashCode()</code>方法，用<code>native</code>关键字修饰，说明这个方法是个原生函数，也就说这个方法的实现不是用java语言实现的，是使用c/c++实现的，并且被编译成了DLL，由java去调用，jdk源码中不包含。对于不同的平台它们是不同的，java在不同的操作系统中调用不同的<code>native</code>方法实现对操作系统的访问，因为java语言不能直接访问操作系统底层，因为它没有指针。<br>Java的API文档对<code>hashCode()</code>方法做了详细的说明，这也是我们重写<code>hashCode(</code>)方法时的原则【Object类】<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">native</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure></p><p><strong>我们在定义类时，我们经常会希望两个不同对象的某些属性值相同时就认为他们相同，所以我们要重写equals()方法，但同时也要改写<code>hashCode()</code>方法，所以java中的很多类都重写了这两个方法,例如String类，包装类。</strong><br><strong>第二个问题：为什么在重写 equals 方法的时候需要重写 hashCode 方法？</strong><br>Java 对于hashCode方法的规约：</p><ul><li>在java应用程序运行时，无论何时多次调用同一个对象时的<code>hsahCode()</code>方法，这个对象的<code>hashCode()</code>方法的返回值必须是同一个<code>int</code>值</li><li>如果两个对象<code>equals()</code>返回值为<code>true</code>,则他们的<code>hashCode()</code>也必须返回相同的int值</li><li>如果两个对象根据<code>equals()</code>比较是不等的，则<code>hashCode()</code>方法不一定得返回不同的整数。</li></ul><p>根据规约，为了保证同一个对象，<code>equals</code>相同的情况下<code>hashcode</code>值必定相同，如果重写了<code>equals</code>而未重写<code>hashcode</code>方法，可能就会出现两个对象<code>equals</code>相同的（因为equal都是根据对象的特征进行重写的），但<code>hashcode</code>确实不相同的。如果不这样做程序也可以执行，只不过会隐藏bug。比如重写了<code>equals</code>方法，属性相同就认为相同，但不重写<code>hashcode</code>，那么我们再<code>new</code>一个新的对象，当原对象<code>equals</code>（新对象）等于<code>true</code>时，两者的<code>hashcode</code>却是不一样的，由此将产生了理解的不一致，如在存储散列集合时（如<code>Set</code>类），将会存储了两个值一样的对象，导致混淆，因此就也需要重写<code>hashcode()</code>。【一句话，容易在要求散列存储的时候，把相同对象给放到一个集合】<br>有这个要求的症结在于，要考虑到类似HashMap、HashTable、HashSet的这种散列的数据类型的运用。</p><h5 id="5-a-hashCode-有什么用？与-a-equals-b-有什么关系？"><a href="#5-a-hashCode-有什么用？与-a-equals-b-有什么关系？" class="headerlink" title="5.a.hashCode() 有什么用？与 a.equals(b) 有什么关系？"></a>5.a.hashCode() 有什么用？与 a.equals(b) 有什么关系？</h5><ul><li>如果调用<code>equals</code>方法得到的结果为<code>true</code>，则两个对象的<code>hashcode</code>值必定相等；</li><li>如果<code>equals</code>方法得到的结果为<code>false</code>，则两个对象的<code>hashcode</code>值不一定不同；</li><li>如果两个对象的<code>hashcode</code>值不等，则<code>equals</code>方法得到的结果必定为<code>false</code>；</li><li>如果两个对象的<code>hashcode</code>值相等，则<code>equals</code>方法得到的结果未知。</li></ul><p>总之一句话：<strong>等价对象产生相同整数的哈希码，不同对象不一定要不同的哈希码</strong>。后面两个命题其实就是这句话的逆否命题。</p><h5 id="6-hashCode-和-equals-方法的重要性体现在什么地方？"><a href="#6-hashCode-和-equals-方法的重要性体现在什么地方？" class="headerlink" title="6.hashCode() 和 equals() 方法的重要性体现在什么地方？"></a>6.hashCode() 和 equals() 方法的重要性体现在什么地方？</h5><p>散列存储集合如<code>HashMap</code>的很多函数要基于<code>equal()</code>函数和<code>hashCode()</code>函数。<code>hashCode()</code>用来定位要存放的位置，<code>equal()</code>用来判断是否<strong>相等</strong>。<br>那么，<em>相等的概念是什么？</em><br>Object版本的equal只是简单地判断是不是<strong>同一个</strong>实例。但是有的时候，我们想要的的是逻辑上的相等。比如有一个学生类student，有一个属性studentID，只要studentID相等，不是同一个实例我们也认为是同一学生。当我们认为判定equals的相等应该是逻辑上的相等而不是只是判断是不是内存中的同一个东西的时候，就需要重写equal()。<strong>而涉及到HashMap的时候，重写了equals()，就需要重写hashCode()</strong></p><h5 id="7-Object类hashcode-equals-设计原则？-sun为什么这么设计？"><a href="#7-Object类hashcode-equals-设计原则？-sun为什么这么设计？" class="headerlink" title="7.Object类hashcode,equals 设计原则？ sun为什么这么设计？"></a>7.Object类hashcode,equals 设计原则？ sun为什么这么设计？</h5><blockquote><ul><li>在程序执行期间，只要<code>equals</code>方法的比较操作用到的信息没有被修改，那么对这同一个对象调用多次，<code>hashCode</code>方法必须始终如一地返回同一个整数。</li><li>如果两个对象根据<code>equals</code>方法比较是相等的，那么调用两个对象的<code>hashCode</code>方法必须返回相同的整数结果。</li><li>如果两个对象根据<code>equals</code>方法比较是不等的，则<code>hashCode</code>方法不一定得返回不同的整数。</li></ul></blockquote><p><strong>以上是摘自Effective Java的原话，下面进行具体解读：</strong></p><ul><li>1.同一个对象（没有发生过修改）无论何时调用<code>hashCode()</code>得到的返回值必须一样。<br>如果一个<code>key</code>对象在<code>put</code>的时候调用<code>hashCode()</code>决定了存放的位置，而在<code>get</code>的时候调用<code>hashCode()</code>得到了不一样的返回值，这个值映射到了一个和原来不一样的地方，那么肯定就找不到原来那个键值对了。</li><li>2.<code>hashCode()</code>的返回值相等的对象不一定相等，通过<code>hashCode()</code>和<code>equals()</code>必须能唯一确定一个对象<br>不相等的对象的<code>hashCode()</code>的结果可以相等。<code>hashCode()</code>在注意关注碰撞问题的时候，也要关注生成速度问题，完美<code>hash</code>不现实。</li><li>3.一旦重写了<code>equals()</code>函数（重写<code>equals</code>的时候还要注意要满足<strong>自反性、对称性、传递性、一致性</strong>），就必须重写<code>hashCode()</code>函数。而且<code>hashCode()</code>的生成哈希值的依据应该是<code>equals()</code>中用来比较是否相等的字段 。如果两个由<code>equals()</code>规定相等的对象生成的<code>hashCode</code>不等，对于<code>hashMap</code>来说，他们很可能分别映射到不同位置，没有调用<code>equals()</code>比较是否相等的机会，两个实际上相等的对象可能被插入不同位置，出现错误。其他一些基于哈希方法的集合类可能也会有这个问题。<br><a href="https://www.nowcoder.net/questionTerminal/040f6133720941b3b782b5cceaa27198" target="_blank" rel="noopener">【牛客网面试题】</a></li></ul><h5 id="8-Object：Object有哪些公用方法？Object类的概述。"><a href="#8-Object：Object有哪些公用方法？Object类的概述。" class="headerlink" title="8.Object：Object有哪些公用方法？Object类的概述。"></a>8.Object：Object有哪些公用方法？Object类的概述。</h5><p><code>Object</code>是所有类的父类，任何类都默认继承<code>Object</code>。<br><code>clone</code>：保护方法，实现对象的浅复制，只有实现了<code>Cloneable</code>接口才可以调用该方法，否则抛出<code>CloneNotSupportedException</code>异常<br><code>equals</code>：在<code>Object</code>中与<code>==</code>是一样的，子类一般需要重写该方法<br><code>hashCode</code>：该方法用于哈希查找，重写了<code>equals</code>方法一般都要重写<code>hashCode</code>方法。这个方法在一些具有哈希功能的<code>Collection</code>中用到<br><code>getClass</code>：<code>final</code>方法，获得运行时类型<br><code>wait</code>：使当前线程等待该对象的锁，当前线程必须是该对象的拥有者，也就是具有该对象的锁。<code>wait()</code>方法一直等待，直到获得锁或者被中断。<code>wait(long timeout)</code>设定一个超时间隔，如果在规定时间内没有获得锁就返回。 调用该方法后当前线程进入睡眠状态，直到以下事件发生：</p><ul><li><ol><li>其他线程调用了该对象的<code>notify</code>方法</li></ol></li><li><ol><li>其他线程调用了该对象的<code>notifyAll</code>方法</li></ol></li><li><ol><li>其他线程调用了<code>interrupt</code>中断该线程</li></ol></li><li><ol><li>时间间隔到了，此时该线程就可以被调度了，如果是被中断的话就抛出一个<code>InterruptedException</code>异常</li></ol></li></ul><p><code>notify</code>：唤醒在该对象上等待的某个线程<br><code>notifyAll</code>：唤醒在该对象上等待的所有线程<br><code>toString</code>：转换成字符串，一般子类都有重写，否则打印句柄</p><h5 id="如何在父类中为子类自动完成所有的-hashcode-和-equals-实现？这么做有何优劣。"><a href="#如何在父类中为子类自动完成所有的-hashcode-和-equals-实现？这么做有何优劣。" class="headerlink" title="如何在父类中为子类自动完成所有的 hashcode 和 equals 实现？这么做有何优劣。"></a>如何在父类中为子类自动完成所有的 hashcode 和 equals 实现？这么做有何优劣。</h5><h5 id="可以在-hashcode-中使用随机数字吗？"><a href="#可以在-hashcode-中使用随机数字吗？" class="headerlink" title="可以在 hashcode() 中使用随机数字吗？"></a>可以在 hashcode() 中使用随机数字吗？</h5>]]></content>
    
    <summary type="html">
    
      &lt;h5 id=&quot;1-Hashcode-的作用&quot;&gt;&lt;a href=&quot;#1-Hashcode-的作用&quot; class=&quot;headerlink&quot; title=&quot;1.Hashcode 的作用&quot;&gt;&lt;/a&gt;1.Hashcode 的作用&lt;/h5&gt;&lt;p&gt;对于包含容器类型的程序设计语言来说，基本上都会涉及到 &lt;code&gt;hashCode&lt;/code&gt;。在Java中也一样，&lt;code&gt;hashCode&lt;/code&gt;方法的主要作用是为了配合基于散列的集合一起正常运行，这样的散列集合包括&lt;code&gt;HashSet、HashMap&lt;/code&gt;以及&lt;code&gt;HashTable&lt;/code&gt;。
    
    </summary>
    
      <category term="Java Basic" scheme="https://tisonkong.github.io/github.io/categories/Java-Basic/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="数据结构" scheme="https://tisonkong.github.io/github.io/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>java基础之数据结构（基础篇）</title>
    <link href="https://tisonkong.github.io/github.io/2018/02/28/java%E5%9F%BA%E7%A1%80%E4%B9%8B%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%841%EF%BC%88%E5%9F%BA%E7%A1%80%E7%AF%87%EF%BC%89/"/>
    <id>https://tisonkong.github.io/github.io/2018/02/28/java基础之数据结构1（基础篇）/</id>
    <published>2018-02-28T01:40:21.410Z</published>
    <updated>2018-02-28T01:40:21.410Z</updated>
    
    <content type="html"><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/8926909-080c94b2247e13cb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="java集合框架图"><br><a id="more"></a></p><h4 id="一、基础类型-Primitives"><a href="#一、基础类型-Primitives" class="headerlink" title="一、基础类型(Primitives)"></a>一、基础类型(Primitives)</h4><h5 id="1-基础类型-Primitives-与封装类型-Wrappers-的区别在哪里？"><a href="#1-基础类型-Primitives-与封装类型-Wrappers-的区别在哪里？" class="headerlink" title="1.基础类型(Primitives)与封装类型(Wrappers)的区别在哪里？"></a>1.基础类型(Primitives)与封装类型(Wrappers)的区别在哪里？</h5><p><strong>1 传递方式不同</strong><br>封装类是引用类型。基本类型（原始数据类型）在传递参数时都是按值传递，而封装类型是按引用传递的(其实“引用也是按值传递的”，传递的是对象的地址)。由于包装类型都是final修饰的不可变量，因此没有提供改变它值的方法，增加了对“按引用传递”的理解难度。<br><code>int</code>是基本类型，直接存放数值；<code>Integer</code>是类，产生对象时用一个引用指向这个对象。<br><strong>2 封装类可以有方法和属性</strong><br>封装类可以有方法和属性，利用这些方法和属性来处理数据，如<code>Integer.parseInt(Strings)</code>。<strong>基本数据类型都是final修饰的</strong>，不能继承扩展新的类、新的方法。<br><strong>3 默认值不同</strong><br>基本类型跟封装类型的默认值是不一样的。如<code>int i</code>,<code>i</code>的预设为<code>0</code>；<code>Integer j</code>，<code>j</code>的预设为<code>null</code>,因为封装类产生的是对象，对象默认值为null。<br><strong>4 存储位置</strong><br>基本类型在内存中是存储在栈中，引用类型的引用（值的地址）存储在栈中，而实际的对象（值）是存在堆中。<br><strong>基本数据类型的好处就是速度快（不涉及到对象的构造和回收），封装类的目的主要是更好的处理数据之间的转换。</strong>JDK5.0开始可以自动封包了，<strong>基本数据类型可以自动封装成封装类</strong>。</p><blockquote><p><a href="http://blog.csdn.net/xzp_12345/article/details/79038251" target="_blank" rel="noopener">《 基础类型(Primitives)与封装类型(Wrappers)的区别》</a></p></blockquote><h5 id="2-简述九种基本数据类型的大小，以及他们的封装类。"><a href="#2-简述九种基本数据类型的大小，以及他们的封装类。" class="headerlink" title="2.简述九种基本数据类型的大小，以及他们的封装类。"></a>2.简述九种基本数据类型的大小，以及他们的封装类。</h5><p><img src="http://upload-images.jianshu.io/upload_images/8926909-6ff5f555b500eab8.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="基本数据类型和包装类"></p><h5 id="3-int和-Integer-哪个会占用更多的内存？int和-Integer-有什么区别？parseInt-函数在什么时候使用到？"><a href="#3-int和-Integer-哪个会占用更多的内存？int和-Integer-有什么区别？parseInt-函数在什么时候使用到？" class="headerlink" title="3.int和 Integer 哪个会占用更多的内存？int和 Integer 有什么区别？parseInt()函数在什么时候使用到？"></a>3.int和 Integer 哪个会占用更多的内存？int和 Integer 有什么区别？parseInt()函数在什么时候使用到？</h5><p>当然是<code>Integer</code>会占用更多的内存。以下为<code>int</code>和<code>Integer</code>的区别：<br>1、<code>Integer</code>是<code>int</code>的包装类，<code>int</code>则是java的一种基本数据类型<br>2、<code>Integer</code>变量必须实例化后才能使用，而<code>int</code>变量不需要<br>3、<code>Integer</code>实际是对象的引用，当new一个Integer时，实际上是生成一个指针指向此对象；而int则是直接存储数据值<br>4、<code>Integer</code>的默认值是<code>null</code>，<code>int</code>的默认值是0<br>延伸关于Integer和int的比较 ：</p><ul><li><p>由于<code>Integer</code>变量实际上是对一个<code>Integer</code>对象的引用，所以两个通过new生成的Integer变量永远是不相等的（因为new生成的是两个对象，其内存地址不同）。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Integer i = <span class="keyword">new</span> Integer(<span class="number">100</span>);</span><br><span class="line">Integer j = <span class="keyword">new</span> Integer(<span class="number">100</span>);</span><br><span class="line">System.out.print(i == j); <span class="comment">//false</span></span><br></pre></td></tr></table></figure></li><li><p><code>Integer</code>变量和<code>int</code>变量比较时，只要两个变量的值是向等的，则结果为true（因为包装类<code>Integer</code>和基本数据类型<code>int</code>比较时，java会自动拆包装为<code>int</code>，然后进行比较，实际上就变为两个int变量的比较）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Integer i = <span class="keyword">new</span> Integer(<span class="number">100</span>);</span><br><span class="line"><span class="keyword">int</span> j = <span class="number">100</span>；</span><br><span class="line">System.out.print(i == j); <span class="comment">//true</span></span><br></pre></td></tr></table></figure></li><li><p>非<code>new</code>生成的<code>Integer</code>变量和<code>new Integer()</code>生成的变量比较时，结果为<code>false</code>。（因为非new生成的Integer变量指向的是java常量池中的对象，而new Integer()生成的变量指向堆中新建的对象，两者在内存中的地址不同）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Integer i = <span class="keyword">new</span> Integer(<span class="number">100</span>);</span><br><span class="line">Integer j = <span class="number">100</span>;</span><br><span class="line">System.out.print(i == j); <span class="comment">//false</span></span><br></pre></td></tr></table></figure></li><li><p>对于两个非<code>new</code>生成的<code>Integer</code>对象，进行比较时，如果两个变量的值在区间<code>-128</code>到<code>127</code>之间，则比较结果为<code>true</code>，如果两个变量的值不在此区间，则比较结果为<code>false</code></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Integer i = <span class="number">100</span>;</span><br><span class="line">Integer j = <span class="number">100</span>;</span><br><span class="line">System.out.print(i == j); <span class="comment">//true</span></span><br><span class="line">Integer i = <span class="number">128</span>;</span><br><span class="line">Integer j = <span class="number">128</span>;</span><br><span class="line">System.out.print(i == j); <span class="comment">//false</span></span><br></pre></td></tr></table></figure></li></ul><p>对于第4条的原因：<br>java在编译<code>Integer i = 100 ;</code>时，会翻译成为<code>Integer i = Integer.valueOf(100)；</code>，而java API中对<code>Integer</code>类型的<code>valueOf</code>的定义如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Integer <span class="title">valueOf</span><span class="params">(<span class="keyword">int</span> i)</span></span>&#123;</span><br><span class="line">    <span class="keyword">assert</span> IntegerCache.high &gt;= <span class="number">127</span>;</span><br><span class="line">    <span class="keyword">if</span> (i &gt;= IntegerCache.low &amp;&amp; i &lt;= IntegerCache.high)&#123;</span><br><span class="line">        <span class="keyword">return</span> IntegerCache.cache[i + (-IntegerCache.low)];</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> Integer(i);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>java对于<code>-128</code>到<code>127</code>之间的数，会进行缓存，<code>Integer i = 127</code>时，会将<code>127</code>进行缓存，下次再写<code>Integer j = 127</code>时，就会直接从缓存中取，就不会<code>new</code>了。</p><blockquote><p><a href="http://www.cnblogs.com/guodongdidi/p/6953217.html" target="_blank" rel="noopener">《int和Integer的区别》</a></p></blockquote><p><strong>parseInt()</strong><br><code>parseInt()</code>将把该字符之前的字符串转换成数字。<code>parseInt()</code>方法还有基模式，可以把二进制、八进制、十六进制或其他任何进制的字符串转换成整数。基是由<code>parseInt()</code>方法的第二个参数指定的，所以要解析十六进制的值，当然，对二进制、八进制，甚至十进制（默认模式），都可以这样调用<code>parseInt()</code>方法。<br>如果十进制数包含前导0，那么最好采用基数10，这样才不会意外地得到八进制的值。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">parseInt</span><span class="params">(String s)</span></span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">parseInt</span><span class="params">(String s, <span class="keyword">int</span> radix)</span></span></span><br></pre></td></tr></table></figure></p><h5 id="4-如何去小数四舍五入保留小数点后两位？"><a href="#4-如何去小数四舍五入保留小数点后两位？" class="headerlink" title="4.如何去小数四舍五入保留小数点后两位？"></a>4.如何去小数四舍五入保留小数点后两位？</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//使用银行家算法</span></span><br><span class="line">BigDecimal i = d.multiply(r).setScale(<span class="number">2</span>,RoundingMode.HALF_EVEN);</span><br></pre></td></tr></table></figure><p>推荐使用<code>BigDecimal</code> ，并且采用<code>setScale</code>方法来设置精确度，同时使用<code>RoundingMode.HALF_UP</code>表示使用最近数字舍入法则来近似计算。在这里我们可以看出<code>BigDecimal</code>和四舍五入是绝妙的搭配。<br><a href="http://www.cnblogs.com/chenssy/p/3366632.html" target="_blank" rel="noopener">《java提高篇(三)—–java的四舍五入》</a></p><h5 id="5-char-型变量中能不能存贮一个中文汉字，为什么？"><a href="#5-char-型变量中能不能存贮一个中文汉字，为什么？" class="headerlink" title="5.char 型变量中能不能存贮一个中文汉字，为什么？"></a>5.char 型变量中能不能存贮一个中文汉字，为什么？</h5><p><code>char</code>型变量是用来存储<code>Unicode</code>编码的字符的，<code>unicode</code>编码字符集中包含了汉字，所以<code>char</code>型变量中当然可以存储汉字啦。不过，如果某个特殊的汉字没有被包含在<code>unicode</code>编码字符集中，那么，这个char型变量中就不能存储这个特殊汉字。补充<br>说明：<code>unicode</code>编码占用两个字节，所以，<code>char</code>类型的变量也是占用两个字节。</p><h4 id="二、类型转换"><a href="#二、类型转换" class="headerlink" title="二、类型转换"></a>二、类型转换</h4><h5 id="1-怎样将-bytes-转换为-long-类型"><a href="#1-怎样将-bytes-转换为-long-类型" class="headerlink" title="1.怎样将 bytes 转换为 long 类型"></a>1.怎样将 bytes 转换为 long 类型</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">long</span> <span class="title">bytes2long</span><span class="params">(<span class="keyword">byte</span>[] b)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">long</span> temp = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">long</span> res = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">8</span>;i++) &#123;</span><br><span class="line">        res &lt;&lt;= <span class="number">8</span>;</span><br><span class="line">        temp = b[i] &amp; <span class="number">0xff</span>;</span><br><span class="line">        res |= temp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="2-怎么将-byte-转换为-String"><a href="#2-怎么将-byte-转换为-String" class="headerlink" title="2.怎么将 byte 转换为 String**"></a>2.怎么将 byte 转换为 String**</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//string 转成 byte</span></span><br><span class="line">string s = <span class="string">"Hello!!"</span>;</span><br><span class="line"><span class="keyword">byte</span>[] b = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>*<span class="number">1024</span>];</span><br><span class="line">b = System.Text.Encoding.ASCII.GetBytes(s);</span><br><span class="line"><span class="comment">//当string含有中文字符时用 System.Text.Encoding.UTF8.GetBytes(s);</span></span><br><span class="line">sock.Send(b);</span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//byte 转成 string</span></span><br><span class="line"><span class="keyword">byte</span>[] b1 = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>*<span class="number">1024</span>*<span class="number">2</span>];</span><br><span class="line">sock.Receive(b1);</span><br><span class="line">string s1 = System.Text.Encoding.ASCII.GetString(b1);</span><br><span class="line"><span class="comment">// System.Text.Encoding.UTF8.GetString(b1);</span></span><br></pre></td></tr></table></figure><p>注意： 在把<code>byte</code>数组转换成<code>string</code>的时候，由于<code>byte</code>数组有2M的字节，所以转换后得到的字符串s1也会填充到2M的字符（用\0来填充）<br>所以，为了避免这个问题，可以使用<code>Receive</code>返回的字节数来确定接收到byte的长度<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">int</span> length = sock.Receive(b1);</span><br><span class="line">string s1 = System.Text.Encoding.ASCII.GetString(b1, <span class="number">0</span>, length);</span><br><span class="line"><span class="comment">//这样，s1就为byte实际的值</span></span><br></pre></td></tr></table></figure></p><h5 id="3-如何将数值型字符转换为数字"><a href="#3-如何将数值型字符转换为数字" class="headerlink" title="3.如何将数值型字符转换为数字"></a>3.如何将数值型字符转换为数字</h5><p><code>string</code>和<code>int</code>之间的转换</p><ul><li><code>string</code>转换成<code>int</code>  : <code>Integer.valueOf(&quot;12&quot;)</code></li><li><code>int</code>转换成<code>string</code> : <code>String.valueOf(12)</code></li></ul><p><code>char</code>转<code>int</code>之间的转换</p><ul><li>首先将char转换成string<br><code>String str=String.valueOf(&#39;2&#39;)</code><br><code>Integer.valueof(str)</code> 或者<code>Integer.PaseInt(str)</code><br>Integer.valueof返回的是Integer对象，Integer.paseInt返回的是int</li></ul><h5 id="4-我们能将-int-强制转换为-byte-类型的变量吗？如果该值大于-byte-类型的范围，将会出现什么现象"><a href="#4-我们能将-int-强制转换为-byte-类型的变量吗？如果该值大于-byte-类型的范围，将会出现什么现象" class="headerlink" title="4.我们能将 int 强制转换为 byte 类型的变量吗？如果该值大于 byte 类型的范围，将会出现什么现象"></a>4.我们能将 int 强制转换为 byte 类型的变量吗？如果该值大于 byte 类型的范围，将会出现什么现象</h5><p><code>Byte</code>转<code>int</code><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">bytes2int</span><span class="params">(<span class="keyword">byte</span>[] bytes)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> num = bytes[<span class="number">0</span>] &amp; <span class="number">0xFF</span>;</span><br><span class="line">        num |= ((bytes[<span class="number">1</span>] &lt;&lt; <span class="number">8</span>) &amp; <span class="number">0xFF00</span>);</span><br><span class="line">        num |= ((bytes[<span class="number">2</span>] &lt;&lt; <span class="number">16</span>) &amp; <span class="number">0xFF0000</span>);</span><br><span class="line">        num |= ((bytes[<span class="number">3</span>] &lt;&lt; <span class="number">24</span>) &amp; <span class="number">0xFF000000</span>);</span><br><span class="line">        <span class="keyword">return</span> num;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p><code>int</code>转 <code>byte</code><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">byte</span>[] int2bytes(<span class="keyword">int</span> i) &#123;</span><br><span class="line">        <span class="keyword">byte</span>[] b = <span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">4</span>];</span><br><span class="line">        b[<span class="number">0</span>] = (<span class="keyword">byte</span>) (<span class="number">0xff</span>&amp;i);</span><br><span class="line">        b[<span class="number">1</span>] = (<span class="keyword">byte</span>) ((<span class="number">0xff00</span>&amp;i) &gt;&gt; <span class="number">8</span>);</span><br><span class="line">        b[<span class="number">2</span>] = (<span class="keyword">byte</span>) ((<span class="number">0xff0000</span>&amp;i) &gt;&gt; <span class="number">16</span>);</span><br><span class="line">        b[<span class="number">3</span>] = (<span class="keyword">byte</span>) ((<span class="number">0xff000000</span>&amp;i) &gt;&gt; <span class="number">24</span>);</span><br><span class="line">        <span class="keyword">return</span> b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><h5 id="5-能在不进行强制转换的情况下将一个-double-值赋值给-long-类型的变量吗"><a href="#5-能在不进行强制转换的情况下将一个-double-值赋值给-long-类型的变量吗" class="headerlink" title="5.能在不进行强制转换的情况下将一个 double 值赋值给 long 类型的变量吗?"></a>5.能在不进行强制转换的情况下将一个 double 值赋值给 long 类型的变量吗?</h5><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">double</span> d = <span class="number">88.88</span>;</span><br><span class="line">    <span class="keyword">long</span> l = Math.round(d);</span><br><span class="line">    System.out.println(l);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">long</span> ll = <span class="number">100L</span>;</span><br><span class="line">    <span class="keyword">double</span> dd = (<span class="keyword">double</span>) ll;</span><br><span class="line">    System.out.println(dd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h5 id="6-类型向下转换是什么"><a href="#6-类型向下转换是什么" class="headerlink" title="6.类型向下转换是什么?"></a>6.类型向下转换是什么?</h5><p>由低层次类型转换为高层次类型称为向上类型转换。向上类型转换是自动进行的，比如把int型变量赋给为long型变量，把long型变量赋给double型变量，转换都是自动进行的。由派生类转换为基类也是向上提升，也是自动进行的，但转换后，基类的引用符不能应用派生类对象特有的函数。<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Human  jean = <span class="keyword">new</span> Human();</span><br><span class="line">Vervebrata   someone = jean;</span><br><span class="line">some.Work();</span><br></pre></td></tr></table></figure></p><p>运行上面语句会出错，虽然<code>someone</code>指向了一个<code>Human</code>类的对象，但是它不能调用<code>Work()</code>函数，因为<code>someone</code>的类型为<code>Vertebrata</code>，而基类<code>Vertebrata</code>中没有申明<code>Work()</code>函数。要想通过基类引用符someone调用派生类特有的函数，必须将<code>someone</code>的类型强制转换为派生类。这种由基类向派生类转换的过程称为向下类型转换。</p><h4 id="三、-数组"><a href="#三、-数组" class="headerlink" title="三、 数组"></a>三、 数组</h4><h5 id="1-如何权衡是使用无序的数组还是有序的数组"><a href="#1-如何权衡是使用无序的数组还是有序的数组" class="headerlink" title="1.如何权衡是使用无序的数组还是有序的数组"></a>1.如何权衡是使用无序的数组还是有序的数组</h5><p>在数据偏向查找操作的时候用有序数组快一些，在数据偏向插入的时候，无序数组好一些。删除操作效率一样。</p><h5 id="2-怎么判断数组是-null-还是为空"><a href="#2-怎么判断数组是-null-还是为空" class="headerlink" title="2.怎么判断数组是 null 还是为空"></a>2.怎么判断数组是 null 还是为空</h5><p>（无论使用哪种类型的数组，数组标识符其实只是一个引用，指向在堆中创建的一个真实对象 <code>Int[] A =new int[10];</code>new 一下就是实例化了，开辟了内存空间，基本数据类型的元素会被赋初始值，数组建立后长度不能改变，但是还是可以重新赋值）<br>有如下两个变量定义：</p><ul><li>1 <code>int[] zero = new int[0];</code></li><li>2 <code>int[] nil = null;</code><br>这两种定义有什么区别呢？<br>zero是一个长度为0的数组，我们称之为“空数组”，空数组也是一个对象，只是包含元素个数为0。nil是一个数组类型的空引用。</li></ul><h5 id="3-怎么打印数组？-怎样打印数组中的重复元素"><a href="#3-怎么打印数组？-怎样打印数组中的重复元素" class="headerlink" title="3.怎么打印数组？ 怎样打印数组中的重复元素"></a>3.怎么打印数组？ 怎样打印数组中的重复元素</h5><h5 id="4-Array-和-ArrayList有什么区别？什么时候应该使用Array而不是ArrayList"><a href="#4-Array-和-ArrayList有什么区别？什么时候应该使用Array而不是ArrayList" class="headerlink" title="4.Array 和 ArrayList有什么区别？什么时候应该使用Array而不是ArrayList?"></a>4.Array 和 ArrayList有什么区别？什么时候应该使用Array而不是ArrayList?</h5><p><strong>1）</strong>精辟阐述：<br>可以将 <code>ArrayList</code>想象成一种“会自动扩增容量的<code>Array</code>”。<br><strong>2）</strong><code>Array（[]）</code>：最高效；但是其容量固定且无法动态改变；<br>      <code>ArrayList</code>：  容量可动态增长；但牺牲效率；<br><strong>3）</strong>建议：<br>基于效率和类型检验，应尽可能使用Array，无法确定数组大小时才使用ArrayList！<br>不过当你试着解决更一般化的问题时，Array的功能就可能过于受限。<br><strong>4）</strong>Java中一切皆对象，Array也是对象。不论你所使用得Array型别为何，Array名称本身实际上是个reference，指向heap之内得某个实际对象。这个对象可经由“Array初始化语法”被自动产生，也可以以new表达式手动产生。<br><strong>5）</strong>Array可做为函数返回值，因为它本身是对象的reference；<br><strong>6）</strong>对象数组与基本类型数组在运用上几乎一模一样，唯一差别在于，前者持有得是<code>reference</code>，后者直接持有基本型别之值；<br>例如：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">string [] staff=<span class="keyword">new</span> string[<span class="number">100</span>];</span><br><span class="line"><span class="keyword">int</span> [] num=<span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">10</span>];</span><br></pre></td></tr></table></figure></p><p><strong>7）</strong>容器所持有的其实是一个<code>reference</code>指向<code>Object</code>，进而才能存储任意型别。当然这不包括基本型别，因为基本型别并不继承自任何<code>classes</code>。<br><strong>8）</strong>面对<code>Array</code>，我们可以直接持有基本型别数值的<code>Array</code>（例如：<code>int [] num;</code>),也可以持有<code>reference</code>（指向对象）的<code>Array</code>；但是容器类仅能持有<code>reference</code>（指向对象），若要将基本型别置于容器内，需要使用<code>wrapper</code>类。但是<code>wrapper</code>类使用起来可能不很容易上手，此外，<code>primitives Array</code>的效率比起“容纳基本型别之外覆类（的<code>reference</code>）”的容器好太多了。<br>当然，如果你的操作对象是基本型别，而且需要在空间不足时自动扩增容量，<code>Array</code>便不适合，此时就得使用外覆类的容器了。<br><strong>9）</strong>某些情况下，容器类即使没有转型至原来的型别，仍然可以运作无误。有一种情况尤其特别：编译器对String class提供了一些额外的支持，使它可以平滑运作。<br><strong>10）</strong>对数组的一些基本操作，像排序、搜索与比较等是很常见的。因此在Java中提供了<code>Arrays</code>类协助这几个操作：<code>sort(),binarySearch(),equals(),fill(),asList().</code><br>不过<code>Arrays</code>类没有提供删除方法，而ArrayList中有remove()方法，不知道是否是不需要在<code>Array</code>中做删除等操作的原因（因为此时应该使用链表）。<br><strong>11）</strong>ArrayList的使用也很简单：产生<code>ArrayList</code>，利用<code>add()</code>将对象置入，利用get(i）配合索引值将它们取出。这一切就和Array的使用方式完全相同，只不过少了[]而已。</p><p><strong>换一种简单说法：</strong><br><strong>1）效率：</strong><br>数组扩容是对<code>ArrayList</code>效率影响比较大的一个因素。<br>每当执行<code>Add、AddRange、Insert、InsertRange</code>等添加元素的方法，都会检查内部数组的容量是否不够了，如果是，它就会以当前容量的两倍来重新构建一个数组，将旧元素Copy到新数组中，然后丢弃旧数组，在这个临界点的扩容操作，应该来说是比较影响效率的。<br><code>ArrayList</code>是<code>Array</code>的复杂版本<br><code>ArrayList</code>内部封装了一个<code>Object</code>类型的数组，从一般的意义来说，它和数组没有本质的差别，甚至于ArrayList的许多方法，如<code>Index、IndexOf、Contains、Sort</code>等都是在内部数组的基础上直接调用<code>Array</code>的对应方法。<br><strong>2）类型识别：</strong><br><code>ArrayList</code>存入对象时，抛弃类型信息，所有对象屏蔽为<code>Object</code>，编译时不检查类型，但是运行时会报错。但是现在有jdk1.5后引入泛型来进行编译检查类型，如错存入了不同类型会直接报错。<br><code>ArrayList</code>与数组的区别主要就是由于动态增容的效率问题了<br><strong>3）<code>ArrayList</code>可以存任何<code>Object</code>，如<code>String</code>等。</strong></p><h5 id="5-数组和链表数据结构描述，各自的时间复杂度"><a href="#5-数组和链表数据结构描述，各自的时间复杂度" class="headerlink" title="5.数组和链表数据结构描述，各自的时间复杂度"></a>5.数组和链表数据结构描述，各自的时间复杂度</h5><p><strong>数组</strong>是将元素在内存中连续存放，由于每个元素占用内存相同，可以通过下标迅速访问数组中任何元素。但是如果要在数组中增加一个元素，需要移动大量元素，在内存中空出一个元素的空间，然后将要增加的元素放在其中。同样的道理，如果想删除一个元素，同样需要移动大量元素去填掉被移动的元素。如果应用需要快速访问数据，很少插入和删除元素，就应该用数组。<br><strong>链表</strong>中的元素在内存中不是顺序存储的，而是通过存在元素中的指针联系到一起，每个结点包括两个部分：一个是存储 数据元素的数据域，另一个是存储下一个结点地址的 指针。 如果要访问链表中一个元素，需要从第一个元素开始，一直找到需要的元素位置。但是增加和删除一个元素对于链表数据结构就非常简单了，只要修改元素中的指针就可以了。如果应用需要经常插入和删除元素你就需要用链表。</p><ul><li>内存存储区别<br>数组从栈中分配空间, 对于程序员方便快速,但自由度小。链表从堆中分配空间, 自由度大但申请管理比较麻烦.　</li><li>逻辑结构区别<br>数组必须事先定义固定的长度（元素个数），不能适应数据动态地增减的情况。当数据增加时，可能超出原先定义的元素个数；当数据减少时，造成内存浪费。　<br>链表动态地进行存储分配，可以适应数据动态地增减的情况，且可以方便地插入、删除数据项。（数组中插入、删除数据项时，需要移动其它数据项）　</li></ul><p>总结<br>1、存取方式上，数组可以顺序存取或者随机存取，而链表只能顺序存取；　<br>2、存储位置上，数组逻辑上相邻的元素在物理存储位置上也相邻，而链表不一定；　<br>3、存储空间上，链表由于带有指针域，存储密度不如数组大；　<br>4、按序号查找时，数组可以随机访问，时间复杂度为O(1)，而链表不支持随机访问，平均需要O(n)；　<br>5、按值查找时，若数组无序，数组和链表时间复杂度均为O(1)，但是当数组有序时，可以采用折半查找将时间复杂度降为O(logn)；　<br>6、插入和删除时，数组平均需要移动n/2个元素，而链表只需修改指针即可；　<br>7、空间分配方面：<br>数组在静态存储分配情形下，存储元素数量受限制，动态存储分配情形下，虽然存储空间可以扩充，但需要移动大量元素，导致操作效率降低，而且如果内存中没有更大块连续存储空间将导致分配失败；<br>链表存储的节点空间只在需要的时候申请分配，只要内存中有空间就可以分配，操作比较灵活高效；<br>数组有没有length()这个方法? String有没有length()这个方法</p><h4 id="四、队列"><a href="#四、队列" class="headerlink" title="四、队列"></a>四、队列</h4><h5 id="1-队列和栈是什么，列出它们的区别"><a href="#1-队列和栈是什么，列出它们的区别" class="headerlink" title="1.队列和栈是什么，列出它们的区别"></a>1.队列和栈是什么，列出它们的区别</h5><p><strong>队列（Queue）</strong>：是限定只能在表的一端进行插入和在另一端进行删除操作的线性表；<br><strong>栈（Stack）</strong>：是限定只能在表的一端进行插入和删除操作的线性表。<br>区别如下：</p><ul><li><p>规则不同</p><pre><code>1. 队列：先进先出（First In First Out）FIFO2. 栈：先进后出（First In Last Out ）FILO</code></pre></li><li><p>对插入和删除操作的限定不同</p><pre><code>1. 队列：只能在表的一端进行插入，并在表的另一端进行删除；2. 栈：只能在表的一端插入和删除。</code></pre></li><li><p>遍历数据速度不同</p><pre><code>1. 队列：基于地址指针进行遍历，而且可以从头部或者尾部进行遍历，但不能同时遍历，无需开辟空间，因为在遍历的过程中不影响数据结构，所以遍历速度要快；2. 栈：只能从顶部取数据，也就是说最先进入栈底的，需要遍历整个栈才能取出来，而且在遍历数据的同时需要为数据开辟临时空间，保持数据在遍历前的一致性。</code></pre></li></ul><h5 id="2-BlockingQueue是什么"><a href="#2-BlockingQueue是什么" class="headerlink" title="2.BlockingQueue是什么"></a>2.BlockingQueue是什么</h5><p> 阻塞队列（BlockingQueue）是一个支持两个附加操作的队列。这两个附加的操作是：在队列为空时，获取元素的线程会等待队列变为非空。当队列满时，存储元素的线程会等待队列可用。阻塞队列常用于生产者和消费者的场景，生产者是往队列里添加元素的线程，消费者是从队列里拿元素的线程。阻塞队列就是生产者存放元素的容器，而消费者也只从容器里拿元素。</p><blockquote><p><a href="http://www.importnew.com/17537.html" target="_blank" rel="noopener">《聊聊并发（7）Java中的阻塞队列》</a></p></blockquote><h5 id="3-简述-ConcurrentLinkedQueue-LinkedBlockingQueue-的用处和不同之处。"><a href="#3-简述-ConcurrentLinkedQueue-LinkedBlockingQueue-的用处和不同之处。" class="headerlink" title="3.简述 ConcurrentLinkedQueue LinkedBlockingQueue 的用处和不同之处。"></a>3.简述 ConcurrentLinkedQueue LinkedBlockingQueue 的用处和不同之处。</h5><ul><li><strong>阻塞队列：线程安全</strong><br>按 FIFO（先进先出）排序元素。队列的头部是在队列中时间最长的元素。队列的尾部是在队列中时间最短的元素。新元素插入到队列的尾部，并且队列检索操作会获得位于队列头部的元素。链接队列的吞吐量通常要高于基于数组的队列，但是在大多数并发应用程序中，其可预知的性能要低。<br>注意：<br>1、必须要使用take()方法在获取的时候达成阻塞结果<br>2、使用<code>poll()</code>方法将产生非阻塞效果</li><li>非阻塞队列<br>基于链接节点的、无界的、线程安全。此队列按照 FIFO（先进先出）原则对元素进行排序。队列的头部 是队列中时间最长的元素。队列的尾部 是队列中时间最短的元素。新的元素插入到队列的尾部，队列检索操作从队列头部获得元素。当许多线程共享访问一个公共 <code>collection</code> 时，<code>ConcurrentLinkedQueue</code>是一个恰当的选择。此队列不允许 null 元素。</li></ul><p>在并发编程中，一般推荐使用阻塞队列，这样实现可以尽量地避免程序出现意外的错误。阻塞队列使用最经典的场景就是socket客户端数据的读取和解析，读取数据的线程不断将数据放入队列，然后解析线程不断从队列取数据解析。还有其他类似的场景，只要符合生产者-消费者模型的都可以使用阻塞队列。<br>使用非阻塞队列，虽然能即时返回结果（消费结果），但必须自行编码解决返回为空的情况处理（以及消费重试等问题）。另外他们都是线程安全的，不用考虑线程同步问题。</p><blockquote><p><a href="http://www.cnblogs.com/starcrm/p/4998067.html" target="_blank" rel="noopener">《JAVA阻塞队列以及非阻塞队列的区别》</a></p></blockquote><h5 id="4-ArrayList、Vector、LinkedList的存储性能和特性"><a href="#4-ArrayList、Vector、LinkedList的存储性能和特性" class="headerlink" title="4.ArrayList、Vector、LinkedList的存储性能和特性"></a>4.ArrayList、Vector、LinkedList的存储性能和特性</h5><p><code>ArrayList</code> 和<code>Vector</code>他们底层的实现都是一样的，都是使用数组方式存储数据，此数组元素数大于实际存储的数据以便增加和插入元素，它们都允许直接按序号索引元素，但是插入元素要涉及数组元素移动等内存操作，所以索引数据快而插入数据慢。</p><p><code>Vector</code>中的方法由于添加了<code>synchronized</code>修饰，因此<code>Vector</code>是线程安全的容器，但性能上较<code>ArrayList</code>差，因此已经是Java中的遗留容器。</p><p><code>LinkedList</code>使用双向链表实现存储（将内存中零散的内存单元通过附加的引用关联起来，形成一个可以按序号索引的线性结构，这种链式存储方式与数组的连续存储方式相比，内存的利用率更高），按序号索引数据需要进行前向或后向遍历，但是插入数据时只需要记录本项的前后项即可，所以插入速度较快。</p><p><code>Vector</code>属于遗留容器（Java早期的版本中提供的容器，除此之外，<code>Hashtable、Dictionary、BitSet、Stack、Properties</code>都是遗留容器），已经不推荐使用，但是由于ArrayList和LinkedListed都是非线程安全的，如果遇到多个线程操作同一个容器的场景，则可以通过工具类<code>Collections</code>中的<code>synchronized List</code>方法将其转换成线程安全的容器后再使用（这是对装潢模式的应用，将已有对象传入另一个类的构造器中创建新的对象来增强实现）。</p><h4 id="五、String"><a href="#五、String" class="headerlink" title="五、String"></a>五、String</h4><h5 id="1-ByteBuffer-与-StringBuffer有什么区别"><a href="#1-ByteBuffer-与-StringBuffer有什么区别" class="headerlink" title="1.ByteBuffer 与 StringBuffer有什么区别"></a>1.ByteBuffer 与 StringBuffer有什么区别</h5><h4 id="六、Collections"><a href="#六、Collections" class="headerlink" title="六、Collections"></a>六、Collections</h4><h5 id="1-介绍Java中的Collection-FrameWork。集合类框架的基本接口有哪些？"><a href="#1-介绍Java中的Collection-FrameWork。集合类框架的基本接口有哪些？" class="headerlink" title="1.介绍Java中的Collection FrameWork。集合类框架的基本接口有哪些？"></a>1.介绍Java中的Collection FrameWork。集合类框架的基本接口有哪些？</h5><p>总共有两大接口：<code>Collection</code> 和<code>Map</code> ，一个元素集合，一个是键值对集合；</p><ul><li>其中<code>List</code>和<code>Set</code>接口继承了<code>Collection</code>接口，一个是有序元素集合，一个是无序元素集合；</li><li><code>ArrayList</code>和<code>LinkedList</code>实现了<code>List</code>接口，<code>HashSet</code>实现了<code>Set</code>接口，这几个都比较常用；</li><li><code>HashMap</code>和<code>HashTable</code>实现了<code>Map</code>接口，并且<code>HashTable</code>是线程安全的，但是<code>HashMap</code>性能更好；</li></ul><h5 id="2-Collections类是什么？Collection-和-Collections的区别？Collection、Map的实现"><a href="#2-Collections类是什么？Collection-和-Collections的区别？Collection、Map的实现" class="headerlink" title="2.Collections类是什么？Collection 和 Collections的区别？Collection、Map的实现."></a>2.Collections类是什么？Collection 和 Collections的区别？Collection、Map的实现.</h5><p><code>Collection</code>是单列集合</p><ul><li><p><code>List</code>元素是有序的、可重复。有序的<code>collection</code>，可以对列表中每个元素的插入位置进行精确地控制。可以根据元素的整数索引（在列表中的位置）访问元素，并搜索列表中的元素。 可存放重复元素，元素存取是有序的。</p><ul><li><code>List</code>接口中常用类<br><code>Vector</code>：线程安全，但速度慢，已被ArrayList替代。底层数据结构是数组结构；<br><code>ArrayList</code>：线程不安全，查询速度快。底层数据结构是数组结构；<br><code>LinkedList</code>：线程不安全。增删速度快。底层数据结构是列表结构；</li></ul></li><li><p><code>Set</code>(集) 元素无序的、不可重复。取出元素的方法只有迭代器。不可以存放重复元素，元素存取是无序的。</p><ul><li><code>Set</code>接口中常用的类<br><code>HashSet</code>：线程不安全，存取速度快。它是如何保证元素唯一性的呢？依赖的是元素的<code>hashCode</code>方法和<code>euqals</code>方法。<br><code>TreeSet</code>：线程不安全，可以对<code>Set</code>集合中的元素进行排序。它的排序是如何进行的呢？通过<code>compareTo</code>或者<code>compare</code>方法中的来保证元素的唯一性。元素是以二叉树的形式存放的。</li></ul></li></ul><p><code>Map</code> 是一个双列集合</p><ul><li><code>Hashtable</code>:线程安全，速度快。底层是哈希表数据结构。是同步的。不允许null作为键，null作为值。</li><li><code>Properties</code>:用于配置文件的定义和操作，使用频率非常高，同时键和值都是字符串。是集合中可以和IO技术相结合的对象。</li><li><code>HashMap</code>:线程不安全，速度慢。底层也是哈希表数据结构。是不同步的。允许null作为键，null作为值。替代了Hashtable.</li><li><code>LinkedHashMap</code>: 可以保证<code>HashMap</code>集合有序。存入的顺序和取出的顺序一致。</li><li><code>TreeMap</code>：可以用来对<code>Map</code>集合中的键进行排序.</li></ul><p><code>Collection</code>是集合类的上级接口，子接口主要有Set 和List。<br><code>Collections</code>是针对集合类的一个帮助类，提供了操作集合的工具方法：一系列静态方法实现对各种集合的搜索、排序、线程安全化等操作。</p><blockquote><p><a href="http://www.cnblogs.com/jinlinFighting/p/5713356.html" target="_blank" rel="noopener">《介绍Collection框架的结构；Collection 和 Collections的区别》</a></p></blockquote><h5 id="3-集合类框架的最佳实践有哪些"><a href="#3-集合类框架的最佳实践有哪些" class="headerlink" title="3.集合类框架的最佳实践有哪些"></a>3.集合类框架的最佳实践有哪些</h5><p>根据应用的需要合理的选择集合的类型对性能非常重要</p><ol><li>假如元素的大小是固定的，而且能事先知道，我们就该用Array而不是ArrayList.</li><li>有些集合类允许指定初始容量。因此，如果我们能估计出存储元素的数目，我们可以设置初始容量来避免重新计算hash值或者扩容.</li><li>为了类型安全，可读性和健壮性的原因总要使用翻新。同时，使用泛型还能皮面运行时的<code>ClassCastException</code>.</li><li>使用JDK提供的不变类（<code>immutable class</code>）作为<code>Map</code>的键可以避免为我们自己的类实现<code>hashCode()</code>和<code>equals()</code>方法。</li><li>编程的时候接口优于实现。</li><li>底层的集合实际上是空的情况下返回长度是0的集合或者是数组，不要返回null.</li></ol><h5 id="4-为什么-Collection-不从-Cloneable-和-Serializable-接口继承"><a href="#4-为什么-Collection-不从-Cloneable-和-Serializable-接口继承" class="headerlink" title="4.为什么 Collection 不从 Cloneable 和 Serializable 接口继承?"></a>4.为什么 Collection 不从 Cloneable 和 Serializable 接口继承?</h5><p><code>Collection</code>接口指定一组对象，对象即为它的元素。如何维护这些元素由<code>Collection</code>的具体实现决定。例如，一些如<code>List</code>的<code>Collection</code>实现允许重复的元素，而其它的如<code>Set</code>就不允许。很多<code>Collection</code>实现有一个公有的<code>clone</code>方法。然而，把它放到集合的所有实现中也是没有意义的。这是因为<code>Collection</code>是一个抽象表现。<br>重要的是实现，克隆（cloning）或者序列化（serialization）的语义和含义是跟具体的实现相关的。因此应该由<strong>集合类的具体实现类来决定如何被克隆或者序列化</strong>。</p><h5 id="5-说出几点-Java-中使用-Collections-的最佳实践？"><a href="#5-说出几点-Java-中使用-Collections-的最佳实践？" class="headerlink" title="5.说出几点 Java 中使用 Collections 的最佳实践？"></a>5.说出几点 Java 中使用 Collections 的最佳实践？</h5><p>a）使用正确的集合类，例如，如果不需要同步列表，使用 <code>ArrayList</code> 而不是 <code>Vector</code>。<br>b）优先使用并发集合，而不是对集合进行同步。并发集合提供更好的可扩展性。<br>c）使用接口代表和访问集合，如使用<code>List</code>存储 <code>ArrayList</code>，使用 <code>Map</code> 存储 <code>HashMap</code> 等等。<br>d）使用迭代器来循环集合。<br>e）使用集合的时候使用泛型</p><h5 id="6-Collections-中-遗留类-HashTable、Vector-和-现有类的区别"><a href="#6-Collections-中-遗留类-HashTable、Vector-和-现有类的区别" class="headerlink" title="6.Collections 中 遗留类 (HashTable、Vector) 和 现有类的区别"></a>6.Collections 中 遗留类 (HashTable、Vector) 和 现有类的区别</h5><h5 id="7-什么是-B-树，B-树，列出实际的使用场景。"><a href="#7-什么是-B-树，B-树，列出实际的使用场景。" class="headerlink" title="7.什么是 B+树，B-树，列出实际的使用场景。"></a>7.什么是 B+树，B-树，列出实际的使用场景。</h5>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;img src=&quot;http://upload-images.jianshu.io/upload_images/8926909-080c94b2247e13cb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240&quot; alt=&quot;java集合框架图&quot;&gt;&lt;br&gt;
    
    </summary>
    
      <category term="Java Basic" scheme="https://tisonkong.github.io/github.io/categories/Java-Basic/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="数据结构" scheme="https://tisonkong.github.io/github.io/tags/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84/"/>
    
  </entry>
  
  <entry>
    <title>区块链技术栈及学习路线</title>
    <link href="https://tisonkong.github.io/github.io/2018/02/07/%E5%8C%BA%E5%9D%97%E9%93%BE%E6%8A%80%E6%9C%AF%E4%BD%93%E7%B3%BB%E5%8F%8A%E5%AD%A6%E4%B9%A0%E8%B7%AF%E7%BA%BF/"/>
    <id>https://tisonkong.github.io/github.io/2018/02/07/区块链技术体系及学习路线/</id>
    <published>2018-02-07T08:32:39.449Z</published>
    <updated>2018-02-28T01:40:21.413Z</updated>
    
    <content type="html"><![CDATA[<p>仿佛一夜之间，互联网和金融圈都在谈论区块链，而区块链的发展无疑是借着数字货币之浪潮而起，而后被熟知、被认可到如今的大势所趋，目前科技与工业界的共识是区块链将会成为引领第四次工业革命的关键技术，回看2015年火爆的虚拟现实，2016年流行的大数据，2017年独领风骚的人工智能，2018年毫无疑问将会是区块链的元年，<a id="more"></a>可以看到各种新技术的创新迭代正在以前所未有的速度在加快，热门技术的退潮并不是简单的历史更迭，更像是在蓄势引爆一个临界的奇点，有人如此概括：在第四次工业革命中，<a href="https://mp.weixin.qq.com/s/Zou7gGR9jq_tc8VnAGGpfg" target="_blank" rel="noopener">大数据是生产资料，人工智能是生产力，区块链就是生产关系</a>。如此来看，不管其发展趋势如何，区块链至少是值得我们去学习和了解的。区块链的核心价值在于建立去中心、去信任、不可篡改的分布式应用场景，愿景很美好，但目前该项技术的发展还处在探索的初级阶段，预计3-5年将会大规模增长，未来10年区块链市场将趋于成熟，在区块链时代来临之前，相关技术从业者应当提前做好知识储备，搭上即将来临的顺风车。在此总结了相关学习资料和知识体系图，而目前来看一位合格区块链研发工程师所具备的技术栈应当如下：<br><img src="http://upload-images.jianshu.io/upload_images/8926909-a01952194ba4601f.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="区块链技术体系.jpg"></p><h3 id="一、比特币相关资源"><a href="#一、比特币相关资源" class="headerlink" title="一、比特币相关资源"></a>一、比特币相关资源</h3><p>1.<a href="http://www.8btc.com/wiki/bitcoin-a-peer-to-peer-electronic-cash-system" target="_blank" rel="noopener">《比特币白皮书》网址浏览</a><br>2.<a href="https://github.com/bitcoin/bitcoin" target="_blank" rel="noopener">【比特币源码下载】</a>，<a href="https://github.com/GammaGao/bitcoinwhitepaper" target="_blank" rel="noopener">《比特币白皮书下载》</a><br>3.<a href="http://8btc.com/topic-mastering-bitcoin.html" target="_blank" rel="noopener">《精通比特币》</a><br>讲比特币很详细的一本书，看完基本对比特币的认识就清楚了。<br>4.<a href="http://nakamotoinstitute.org/mempool/the-proof-of-work-concept/" target="_blank" rel="noopener">《TheProof-of-Work Concept》</a>PoW机制理论介绍，英文<br>5.<a href="http://blog.codinglabs.org/articles/bitcoin-mechanism-make-easy.html" target="_blank" rel="noopener">《比特币的原理及运作机制》</a>适合向圈外人介绍什么是比特币什么是区块链。<br>6.<a href="https://bbs.bumeng.cn/thread-815-1-1.html" target="_blank" rel="noopener">《比特币pow难度调节机制》</a>PoW的难度调节是一个要点，一篇简介</p><h3 id="二、区块链"><a href="#二、区块链" class="headerlink" title="二、区块链"></a>二、区块链</h3><p>1.<a href="https://yeasy.gitbooks.io/blockchain_guide/content/" target="_blank" rel="noopener">《区块链技术指南》</a>网页在线电子书，对区块链技术讲解得非常全面<br>2.<a href="http://lib.csdn.net/case/blockchain" target="_blank" rel="noopener">【csdn的blockchain知识库】</a><br>3.<a href="https://bbs.bumeng.cn/thread-849-1-1.html" target="_blank" rel="noopener">【区块链可应用场景】</a>应用场景探讨，布萌社区下的一篇帖子<br>4.<a href="http://wangxiaoming.com/" target="_blank" rel="noopener">【汪晓明对区块链、以太坊的思考】</a><br>汪晓明作为朝夕网络CEO，区块链技术的推广者和布道者，参与并制作了区块链视频节目<a href="https://v.qq.com/x/page/f0327r59q30.html" target="_blank" rel="noopener">《明说》</a>，向开源技术大牛学习。</p><h3 id="三、以太坊"><a href="#三、以太坊" class="headerlink" title="三、以太坊"></a>三、以太坊</h3><p>1.<a href="https://github.com/ethereum/wiki/wiki/White-Paper" target="_blank" rel="noopener">《以太坊白皮书英文版》</a><br>2.<a href="http://gavwood.com/paper.pdf" target="_blank" rel="noopener">《以太坊黄皮书》</a>介绍以太坊原理的论文，文长慎入<br>3.<a href="https://github.com/ethereum/wiki/wiki/FAQ" target="_blank" rel="noopener">【以太坊常见问题】</a>以上三篇都是外文资源<br>4.<a href="https://solidity.readthedocs.io/en/develop/" target="_blank" rel="noopener">【Solidity语言】</a>Solidity语言的文档，还是英文。极客学院翻译了一档勉强凑合的doc，<a href="http://wiki.jikexueyuan.com/project/solidity-zh/" target="_blank" rel="noopener">http://wiki.jikexueyuan.com/project/solidity-zh/</a><br>7.<a href="http://ethfans.org/posts/101-noob-intro" target="_blank" rel="noopener">《以太坊智能合约编程之菜鸟教程》</a>以太坊中文网站出的<br>8.<a href="https://bitshuo.com/topic/584241f863baf1df6cad0d40" target="_blank" rel="noopener">通过truffle部署以太坊智能合约</a><br>9.<a href="https://juejin.im/post/58f97521b123db41195481f3" target="_blank" rel="noopener">Truffle 3.0部署智能合约至Ethereum节点</a><br>10.<a href="http://wiki.jikexueyuan.com/project/blockchain/" target="_blank" rel="noopener">以太坊智能合约编写实例</a><br>11.<a href="http://blog.csdn.net/u013137970/article/details/53018423" target="_blank" rel="noopener">以太坊智能合约编写实例2</a><br>12.<a href="http://www.huiyanghua.com/article/plant/469/4707.html" target="_blank" rel="noopener">在CentOS6.5上搭建以太坊私有链</a></p><h3 id="四、Hyperledger-fabric"><a href="#四、Hyperledger-fabric" class="headerlink" title="四、Hyperledger fabric"></a>四、Hyperledger fabric</h3><p>1.<a href="https://zhuanlan.zhihu.com/p/25070745" target="_blank" rel="noopener">《Hyperledger Fabric V1.0– 开发者快速入门》</a>万达网络研究中心副总季宙栋写的开发者入门指南，这档知乎专栏可以订阅。<br>2.<a href="https://www.gitbook.com/book/yeasy/hyperledger_code_fabric/details" target="_blank" rel="noopener">【Hyperledger 源码分析之 Fabric】</a>源码分析，感兴趣研究的同学可以看看<br>3.<a href="http://blog.csdn.net/zhaoliang1131/article/details/54896276" target="_blank" rel="noopener">【Hyperledgerfablic 1.0 在centos7环境下的安装与部署和动态增加节点】</a>csdn上的一篇技术帖<br>4.<a href="https://hyperledger-fabric.readthedocs.io/en/latest/dev-setup/build.html" target="_blank" rel="noopener">【fabric源码搭建】</a>托管在github上的doc说明，纯英文。<br>5.<a href="http://www.8btc.com/hyperledger-fabric1-0" target="_blank" rel="noopener">《Hyperledger Fabric1.0架构概览》</a><br>6.<a href="https://github.com/hyperledger-archives/fabric/blob/master/docs/protocol-spec_zh.md" target="_blank" rel="noopener">《fabric中文文档》</a><br>7.<a href="https://g2ex.github.io/2016/10/14/Deploy-Hyperledger-Fabric-with-Docker/" target="_blank" rel="noopener">【Ubuntu中使用 Docker 部署 HyperledgerFabric】</a>学好docker很重要</p><h3 id="五、布萌"><a href="#五、布萌" class="headerlink" title="五、布萌"></a>五、布萌</h3><p>1.<a href="https://bbs.bumeng.cn/thread-851-1-1.html" target="_blank" rel="noopener">布萌接入指南</a><br>布萌官方的指南有些模糊，个人整理了一版出来仅供参考<br>2.<a href="https://www.bumeng.cn/document/API" target="_blank" rel="noopener">布萌API文档</a><br>3.<a href="https://www.bumeng.cn/document/SDK" target="_blank" rel="noopener">布萌SDK文档</a><br>两个官方文档，其实官网就有，顺手列在这了：D<br>4.<a href="https://bbs.bumeng.cn/thread-917-1-1.html" target="_blank" rel="noopener">布萌PHP SDK</a><br>今天看到有朋友放出的PHP的SDK，膜拜大神</p><h3 id="六、其他"><a href="#六、其他" class="headerlink" title="六、其他"></a>六、其他</h3><p>1.<a href="http://bitcoin-on-nodejs.ebookchain.org/" target="_blank" rel="noopener">《Nodejs开发加密货币》</a>基于Ebookcoin（亿书币）的开发教程，有一定参考价值<br>2.<a href="https://bitshuo.com/topic/5847b86b63baf1df6cad0d6f" target="_blank" rel="noopener">《创建自己的私有比特币测试链》</a>比特币私链搭建教程</p><p>最后还谈一下个人感想：现在百度搜一下区块链，培训市场的速成班广告已经占据了头条，计算了一下他们五个月左右的脱产培训班收费是2.5w上下，又是一波红利，而有些培训机构的课程体系，其实只是整合了过去的编程培训内容再添加一点区块链技术就算完事，简直一锅乱炖，这样的学习方式其实会害死人。相比商人的闻风而动，承担教育责任的大学校园却有点不作为，许多大学的计算机课程体系是远远落后的，大部分学生群体也对科技前沿和趋势动向不是很敏感，前些天看到了一篇钛媒体的报道<a href="https://mp.weixin.qq.com/s?src=11&amp;timestamp=1517991779&amp;ver=683&amp;signature=OAYaOycdJz2ErS7650iVIRX5i-QqMOeRlhdQGJNjCnw8jF-ksRqUS0koKo4QMMWKvWg0sf0yL4IHPLHRLAJmAwqp8*HWRSFIdvlvbFAO2jZoqFsf4CM-Wg6XUjfStxxt&amp;new=1" target="_blank" rel="noopener">《中国学生狂“吃鸡”，美国学生猛“挖矿”》</a>，学习上的差距可见一斑，不论怎样，学习之事关乎己身，上士闻道勤而行之。</p><blockquote><p>后续将会不断补充相关学习体系，欢迎访问我的<a href="http://yitaicloud.com/" target="_blank" rel="noopener">博客站点【YitaiCloud】</a>。<br>部分资料参考了这篇<a href="http://blog.csdn.net/qq_35624642/article/details/78175907" target="_blank" rel="noopener">入门到放弃</a>，</p></blockquote>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;仿佛一夜之间，互联网和金融圈都在谈论区块链，而区块链的发展无疑是借着数字货币之浪潮而起，而后被熟知、被认可到如今的大势所趋，目前科技与工业界的共识是区块链将会成为引领第四次工业革命的关键技术，回看2015年火爆的虚拟现实，2016年流行的大数据，2017年独领风骚的人工智能，2018年毫无疑问将会是区块链的元年，
    
    </summary>
    
      <category term="区块链" scheme="https://tisonkong.github.io/github.io/categories/%E5%8C%BA%E5%9D%97%E9%93%BE/"/>
    
    
      <category term="区块链" scheme="https://tisonkong.github.io/github.io/tags/%E5%8C%BA%E5%9D%97%E9%93%BE/"/>
    
      <category term="区块链技术栈" scheme="https://tisonkong.github.io/github.io/tags/%E5%8C%BA%E5%9D%97%E9%93%BE%E6%8A%80%E6%9C%AF%E6%A0%88/"/>
    
  </entry>
  
  <entry>
    <title>java关键字和操作符的总结</title>
    <link href="https://tisonkong.github.io/github.io/2018/02/04/java%E5%9F%BA%E7%A1%80%E4%B9%8B%E5%85%B3%E9%94%AE%E5%AD%97%E5%92%8C%E6%93%8D%E4%BD%9C%E7%AC%A6/"/>
    <id>https://tisonkong.github.io/github.io/2018/02/04/java基础之关键字和操作符/</id>
    <published>2018-02-04T02:08:11.044Z</published>
    <updated>2018-02-28T01:40:21.409Z</updated>
    
    <content type="html"><![CDATA[<h4 id="final-finalize-finally关键字"><a href="#final-finalize-finally关键字" class="headerlink" title="final,finalize,finally关键字"></a>final,finalize,finally关键字</h4><h5 id="1-finalize和final关键字"><a href="#1-finalize和final关键字" class="headerlink" title="1.finalize和final关键字"></a>1.finalize和final关键字</h5><p><strong>什么是finalize()方法？finalize()方法什么时候被调用？</strong><br>答： Java允许在类中定义一个名为<code>finalize()</code>的方法，一旦垃圾回收器准备好释放对象占用的存储空间，将首先调用<code>finalize()</code>方法，并且在下一次垃圾回收动作发生时，才会真正回收对象占用的内存。</p><p><strong>析构函数(finalization)的目的是什么</strong><br>析构函数目的是撤销对象前、完成一些清理工作，比如释放资源。释放了之后这些资源可以被回收，重新利用。<br><a id="more"></a></p><p><strong>final关键字有哪些用法</strong><br>final关键字主要用于修饰类、类成员、方法、以及方法的形参。</p><ul><li>final修饰成员属性：说明该成员属性是常量，不能被修改；</li><li>final修饰类，该类是最终类，不能被继承。</li><li>final修饰方法：该方法是最终方法，不能被重写。</li><li>final关键字修饰形参：1：当形参被修饰为final,那么该形参所属的方法中不能被篡改。</li></ul><h5 id="2-final-与-static-关键字可以用于哪里？它们的作用是什么？"><a href="#2-final-与-static-关键字可以用于哪里？它们的作用是什么？" class="headerlink" title="2. final 与 static 关键字可以用于哪里？它们的作用是什么？"></a>2. final 与 static 关键字可以用于哪里？它们的作用是什么？</h5><p>用于修饰成员变量和成员方法，可以理解为“全局常量”，对于变量表示一旦给定值就不可以修改，并且通过类名可以访问；对于方法表示不可覆盖，并且可以通过类名直接访问</p><h5 id="3-final-finally-finalize的区别（或者说final、finalize-和-finally-的不同之处？）"><a href="#3-final-finally-finalize的区别（或者说final、finalize-和-finally-的不同之处？）" class="headerlink" title="3. final, finally, finalize的区别（或者说final、finalize 和 finally 的不同之处？）**"></a>3. final, finally, finalize的区别（或者说final、finalize 和 finally 的不同之处？）**</h5><p>final关键字可以用于类，方法，变量前，用来表示该关键字修饰的类，方法，变量具有不可变的特性。</p><ul><li>final关键字用于基本数据类型前：这时表明该关键字修饰的变量是一个常量，在定义后该变量的值就不能被修改。</li><li>final关键字用于方法声明前：这时意味着该方法时最终方法，只能被调用，不能被覆盖，但是可以被重载。</li><li>final关键字用于类名前：此时该类被称为最终类，该类不能被其他类继承。</li></ul><p><strong><code>finalize()</code></strong>方法来自于<code>java.lang.Object</code>，用于回收资源。可以为任何一个类添加finalize方法。finalize方法将在垃圾回收器清除对象之前调用。在实际应用中，<strong>不要依赖使用该方法回收任何短缺的资源，这是因为很难知道这个方法什么时候被调用</strong>。<br><strong>finally</strong>，当代码抛出一个异常时，就会终止方法中剩余代码的处理，并退出这个方法的执行。finally块是程序在正常情况下或异常情况下都会运行的。比较适合用于既要处理异常又有资源释放的代码，保证了资源的合理回收。</p><h5 id="4-能否在运行时向-static-final-类型的赋值"><a href="#4-能否在运行时向-static-final-类型的赋值" class="headerlink" title="4. 能否在运行时向 static  final 类型的赋值"></a>4. 能否在运行时向 static  final 类型的赋值</h5><p>不可以，被<code>static final</code>修饰的变量只能在被定义的时候或者类的静态代码块中初始化，一旦赋值后就不能在改变了。<code>static final</code>相当于类常量，就是在类被加载进内存的时候就要为属性分配内存，<code>static</code>块就是类被加载的时候执行且被执行一次，所以可以在其中进行初始化。</p><h5 id="5-使用final关键字修饰一个变量时，是引用不能变，还是引用的对象不能变"><a href="#5-使用final关键字修饰一个变量时，是引用不能变，还是引用的对象不能变" class="headerlink" title="5. 使用final关键字修饰一个变量时，是引用不能变，还是引用的对象不能变?"></a>5. 使用final关键字修饰一个变量时，是引用不能变，还是引用的对象不能变?</h5><p>是引用不能变（final引用恒定不变），引用的对象内容还是可以变的</p><h5 id="8-throws-throw分别代表什么意义"><a href="#8-throws-throw分别代表什么意义" class="headerlink" title="8. throws, throw分别代表什么意义?"></a>8. throws, throw分别代表什么意义?</h5><p><code>throw</code>是指的语句抛出一个异常，<code>throws</code>指的是声明方法可能抛出的异常类型</p><p><strong>9、Java 有几种修饰符？分别用来修饰什么</strong><br><strong>类的修饰符：</strong></p><ul><li><code>public</code>可以在其他任何类中使用，默认为统一包下的任意类。</li><li><code>abstract</code>抽象类，不能被实例化，可以包含抽象方法，抽象方法没有被实现，无具体功能，只能衍生子类。</li><li><code>final</code>不能被继承。</li></ul><p><strong>成员变量</strong></p><ul><li><p>访问修饰符：<br><img src="http://upload-images.jianshu.io/upload_images/8926909-16902c873e4ed9d6.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="访问修饰符"></p></li><li><p><code>static</code>类变量：一个类所拥有的变量，不是类的每个实例有的变量。类变量是指不管类创建了多少对象，系统仅在第一次调用类的时候为类变量分配内存，所有对象共享该类的类变量，因此可以通过类本身或者某个对象来访问类变量。</p></li><li><code>final</code>：常量。</li><li><code>volatile</code>：声明一个可能同时被并存运行的几个线程所控制和修改的变量。</li><li><code>abstract</code>：只有声明部分，方法体为空，具体在子类中完成。</li><li><code>transient</code>：（过度修饰符）指定该变量是系统保留，暂无特别作用的临时性变量。</li></ul><p><strong>方法修饰符：</strong></p><ul><li>访问修饰符<br><code>public</code>（公共控制符）<br><code>private</code>（私有控制符）指定此方法只能有自己类等方法访问，其他的类不能访问（包括子类）<br><code>protected</code>（保护访问控制符）指定该方法可以被它的类和子类进行访问。</li><li><code>final</code>，指定该方法不能被重载。</li><li><code>static</code>，指定不需要实例化就可以激活的一个方法。</li><li><code>synchronize</code>，同步修饰符，在多个线程中，该修饰符用于在运行前，对他所属的方法加锁，以防止其他线程的访问，运行结束后解锁。</li><li><code>native</code>，本地修饰符。指定此方法的方法体是用其他语言在程序外部编写的。</li></ul><blockquote><p><a href="https://www.cnblogs.com/tjudzj/p/4443066.html" target="_blank" rel="noopener">《java中的访问修饰符》</a><br><a href="http://www.cnblogs.com/lixiaolun/p/4311727.html" target="_blank" rel="noopener">《java中的类修饰符、成员变量修饰符、方法修饰符》</a></p></blockquote><h4 id="volatile关键字"><a href="#volatile关键字" class="headerlink" title="volatile关键字"></a>volatile关键字</h4><p><code>volatile</code>是一个特殊的修饰符，只有成员变量才能使用它。在Java并发程序缺少同步类的情况下，多线程对成员变量的操作对其它线程是透明的。volatile变量可以保证下一个读取操作会在前一个写操作之后发生，就是上一题的volatile变量规则。<br>原理以及底层实现可参考：</p><blockquote><p><a href="http://www.importnew.com/27863.html" target="_blank" rel="noopener">《面试必问的 volatile，你了解多少？》</a><br><a href="http://www.importnew.com/27002.html" target="_blank" rel="noopener">《volatile 关键字实现原理》汇编层面的讲解，推荐！</a></p></blockquote><h5 id="1、volatile-修饰符的有过什么实践"><a href="#1、volatile-修饰符的有过什么实践" class="headerlink" title="1、volatile 修饰符的有过什么实践"></a>1、volatile 修饰符的有过什么实践</h5><h5 id="2、volatile-变量是什么？volatile-变量和-atomic-变量有什么不同"><a href="#2、volatile-变量是什么？volatile-变量和-atomic-变量有什么不同" class="headerlink" title="2、volatile 变量是什么？volatile 变量和 atomic 变量有什么不同"></a>2、volatile 变量是什么？volatile 变量和 atomic 变量有什么不同</h5><p>Java语言提供了一种稍弱的同步机制，即<code>volatile</code>变量，用来确保将变量的更新操作通知到其他线程。当把变量声明为<code>volatile</code>类型后，编译器与运行时都会注意到这个变量是共享的，因此不会将该变量上的操作与其他内存操作一起重排序。<code>volatile</code>变量不会被缓存在寄存器或者对其他处理器不可见的地方，因此在读取<code>volatile</code>类型的变量时总会返回最新写入的值。<br>在访问<code>volatile</code>变量时不会执行加锁操作，因此也就不会使执行线程阻塞，因此<code>volatile</code>变量是一种比<code>sychronized</code>关键字更轻量级的同步机制。</p><h5 id="3、volatile-类型变量提供什么保证？能使得一个非原子操作变成原子操作吗"><a href="#3、volatile-类型变量提供什么保证？能使得一个非原子操作变成原子操作吗" class="headerlink" title="3、volatile 类型变量提供什么保证？能使得一个非原子操作变成原子操作吗?"></a>3、volatile 类型变量提供什么保证？能使得一个非原子操作变成原子操作吗?</h5><p>volatile只提供了保证访问该变量时，每次都是从内存中读取最新值，并不会使用寄存器缓存该值——每次都会从内存中读取。而对该变量的修改，<code>volatile</code>并不提供原子性的保证。那么编译器究竟是直接修改内存的值，还是使用寄存器修改都符合volatile的定义。所以，一句话，<code>volatile</code>并不提供原子性的保证。</p><h5 id="4、能创建-volatile-数组吗？"><a href="#4、能创建-volatile-数组吗？" class="headerlink" title="4、能创建 volatile 数组吗？"></a>4、能创建 volatile 数组吗？</h5><p>可以，<code>volatile</code>修饰的变量如果是对象或数组之类的，其含义是对象获数组的地址具有可见性，但是数组或对象内部的成员改变不具备可见性</p><h5 id="5、transient变量有什么特点"><a href="#5、transient变量有什么特点" class="headerlink" title="5、transient变量有什么特点?"></a>5、transient变量有什么特点?</h5><ul><li>一旦变量被<code>transient</code>修饰，变量将不再是对象持久化的一部分，该变量内容在序列化后无法获得访问。</li><li><code>transient</code>关键字只能修饰变量，而不能修饰方法和类。注意，本地变量是不能被<code>transient</code>关键字修饰的。变量如果是用户自定义类变量，则该类需要实现Serializable接口。</li><li>被<code>transient</code>关键字修饰的变量不能被序列化，一个静态变量不管是否被transient修饰，均不能被序列化。</li></ul><h5 id="6、super什么时候使用"><a href="#6、super什么时候使用" class="headerlink" title="6、super什么时候使用?"></a>6、super什么时候使用?</h5><p>super主要存在于子类方法中，用于指向子类对象中父类对象。<br>1：访问父类的属性<br>2：访问父类的函数<br>3：访问父类的构造函数</p><h5 id="7、public-static-void-写成-static-public-void会怎样"><a href="#7、public-static-void-写成-static-public-void会怎样" class="headerlink" title="7、public static void 写成 static public void会怎样?"></a>7、public static void 写成 static public void会怎样?</h5><p>一样的</p><h5 id="8、说明一下public-static-void-main-String-args-这段声明里每个关键字的作用"><a href="#8、说明一下public-static-void-main-String-args-这段声明里每个关键字的作用" class="headerlink" title="8、说明一下public static void main(String args[])这段声明里每个关键字的作用?"></a>8、说明一下public static void main(String args[])这段声明里每个关键字的作用?</h5><p>主函数是什么：主函数是一个特殊的函数，作为程序的入口，可以被jvm识别。<br>主函数的定义：</p><ul><li><code>public</code> ：代表该函数的访问权限是最大的。</li><li><code>static</code> ：代表主函数随着类的加载，就已经存在了。</li><li><code>void</code>：  主函数没有具体的返回值</li><li><code>main</code> ： 不是关键字，是一个特殊的单词可以被jvm识别。</li><li><code>(String[] args)</code> 函数的参数，参数类型是一个数组，该数组中的元素是字符串。字符串类型的数组。<br>  主函数的格式是固定的：jvm能够识别</li></ul><h5 id="9、sizeof-是Java-的关键字吗"><a href="#9、sizeof-是Java-的关键字吗" class="headerlink" title="9、sizeof 是Java 的关键字吗?"></a>9、sizeof 是Java 的关键字吗?</h5><p>不是，C和C++用<code>sizeof()</code>解决移植问题，java不需要。</p><h4 id="static关键字"><a href="#static关键字" class="headerlink" title="static关键字"></a>static关键字</h4><h5 id="1、static-class-与-non-static-class的区别"><a href="#1、static-class-与-non-static-class的区别" class="headerlink" title="1、static class 与 non static class的区别"></a>1、static class 与 non static class的区别</h5><ul><li>内部静态类不需要有指向外部类的引用。但非静态内部类需要持有对外部类的引用。</li><li>非静态内部类能够访问外部类的静态和非静态成员。静态类不能访问外部类的非静态成员。他只能访问外部类的静态成员。</li><li>一个非静态内部类不能脱离外部类实体被创建，一个非静态内部类可以访问外部类的数据和方法，因为他就在外部类里面。</li></ul><p>生命周期（Lifecycle）：</p><ul><li>静态方法（Static Method）与静态成员变量一样，属于类本身，<strong>在类装载的时候被装载到内存</strong>（Memory），不自动进行销毁，会一直存在于内存中，直到JVM关闭。</li><li>非静态方法（Non-Static Method）又叫实例化方法，属于实例对象，实例化后才会分配内存，必须通过类的实例来引用。不会常驻内存，当实例对象被JVM 回收之后，也跟着消失。</li></ul><p>效率：静态方法的使用效率比非静态方法的效率高。</p><p>线程安全</p><ul><li>静态方法是共享代码段，静态变量是共享数据段。既然是“共享”就有并发（Concurrence）的问题。</li><li>非静态方法是针对确定的一个对象的，所以不会存在线程安全的问题。</li><li>静态方法和实例方法是一样的，在类型第一次被使用时加载。调用的速度基本上没有差别。</li></ul><h5 id="2、static-关键字是什么意思？Java中是否可以覆盖-override-一个private或者是static的方法，静态类型有什么特点？"><a href="#2、static-关键字是什么意思？Java中是否可以覆盖-override-一个private或者是static的方法，静态类型有什么特点？" class="headerlink" title="2、static 关键字是什么意思？Java中是否可以覆盖(override)一个private或者是static的方法，静态类型有什么特点？"></a>2、static 关键字是什么意思？Java中是否可以覆盖(override)一个private或者是static的方法，静态类型有什么特点？</h5><p><code>static</code>表示静态的意思，可用于修饰成员变量和成员函数，被静态修饰的成员函数只能访问静态成员，不可以访问非静态成员。静态是随着类的加载而加载的，因此可以直接用类进行访问。 重写是子类中的方法和子类继承的父类中的方法一样（函数名，参数，参数类型，反回值类型），但是子类中的访问权限要不低于父类中的访问权限。重写的前提是必须要继承，<strong><code>private</code>修饰不支持继承，因此被私有的方法不可以被重写</strong>。在Java中，如果父类中含有一个静态方法，且在子类中也含有一个返回类型、方法名、参数列表均与之相同的静态方法，那么该<strong>子类实际上只是将父类中的该同名方法进行了隐藏，而非重写</strong>。换句话说，父类和子类中含有的其实是两个没有关系的方法，它们的行为也并不具有多态性。</p><blockquote><p><a href="http://blog.csdn.net/xiangwanpeng/article/details/52504274?locationNum=12&amp;fps=1" target="_blank" rel="noopener">《static方法能否被重写》</a></p></blockquote><h5 id="3、main-方法为什么必须是静态的？能不能声明-main-方法为非静态？"><a href="#3、main-方法为什么必须是静态的？能不能声明-main-方法为非静态？" class="headerlink" title="3、main() 方法为什么必须是静态的？能不能声明 main() 方法为非静态？"></a>3、main() 方法为什么必须是静态的？能不能声明 main() 方法为非静态？</h5><p>用<code>static</code>修饰的就是静态方法。静态方法不依靠对象而存在。其直接与类有关，只要包含在类中，就可以得到执行，而不一定依附于对象的存在而执行。因此，<code>main</code>方法作为程序的入口方法，在这之前是不可能有任何对象被建立的，也就在<code>main</code>之前包括<code>main</code>自身不可能是非静态方法。所以<code>main</code>方法一定是静态的，有类就行——从而得到执行，进而有更多静态或非静态方法得到执行。</p><h5 id="4、是否可以从一个静态（static）方法内部发出对非静态（non-static）方法的调用"><a href="#4、是否可以从一个静态（static）方法内部发出对非静态（non-static）方法的调用" class="headerlink" title="4、是否可以从一个静态（static）方法内部发出对非静态（non-static）方法的调用"></a>4、是否可以从一个静态（static）方法内部发出对非静态（non-static）方法的调用</h5><p>不可以，静态函数中不能访问非静态成员变量，只能访问静态变量。因为静态优先于对象存在.静态方法中更不可以出现this</p><h5 id="5、静态变量在什么时候加载？编译期还是运行期？静态代码块加载的时机呢？"><a href="#5、静态变量在什么时候加载？编译期还是运行期？静态代码块加载的时机呢？" class="headerlink" title="5、静态变量在什么时候加载？编译期还是运行期？静态代码块加载的时机呢？"></a>5、静态变量在什么时候加载？编译期还是运行期？静态代码块加载的时机呢？</h5><p>静态是随着类的加载而加载的，JVM的代码编译运行顺序是编译、类的加载到执行，属于二者的过渡期。静态代码块也是如此。</p><h5 id="6、成员方法是否可以访问静态变量？为什么静态方法不能访问成员变量？"><a href="#6、成员方法是否可以访问静态变量？为什么静态方法不能访问成员变量？" class="headerlink" title="6、成员方法是否可以访问静态变量？为什么静态方法不能访问成员变量？"></a>6、成员方法是否可以访问静态变量？为什么静态方法不能访问成员变量？</h5><p>成员方法中可以访问静态成员变量。</p><p><strong>请看下面代码来确定程序的打印先后顺序：</strong><br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> test();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> num = <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        num += <span class="number">3</span>;</span><br><span class="line">        System.out.println(b);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">5</span>;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(c);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    test() &#123;System.out.println(d);&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;System.out.println(a);&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;System.out.println(e);&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>执行顺序如下：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">test</span> </span>&#123;        <span class="comment">//1.第一步，准备加载类</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> test();        <span class="comment">//4.第四步，new一个类，但在new之前要处理匿名代码块</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> num = <span class="number">4</span>;    <span class="comment">//2.第二步，静态变量和静态代码块的加载顺序由编写先后决定</span></span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        num += <span class="number">3</span>;</span><br><span class="line">        System.out.println(b);<span class="comment">//5.第五步，按照顺序加载匿名代码块，代码块中有打印</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">5</span>;                <span class="comment">//6.第六步，按照顺序加载变量</span></span><br><span class="line"></span><br><span class="line">    &#123;  成员变量第三个</span><br><span class="line">        System.out.println(c);    <span class="comment">//7.第七步，按照顺序打印c</span></span><br><span class="line">    &#125;</span><br><span class="line">              <span class="comment">//如果将构造函数和构造代码块互换，依旧还是先执行构造代码块。</span></span><br><span class="line">    test() &#123;  <span class="comment">//类的构造函数，第四个加载</span></span><br><span class="line">        System.out.println(d);     <span class="comment">//8.第八步，最后加载构造函数，完成对象的建立</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &#123;             <span class="comment">//3.第三步，静态块，然后执行静态代码块，因为有输出，故打印a</span></span><br><span class="line">        System.out.println(a);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span>              <span class="comment">//静态方法，调用的时候才加载 注意看，e没有加载</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        System.out.println(e);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>静态块（静态变量）——成员变量——构造方法——静态方法<br>1、静态代码块（只加载一次） 2、构造方法（创建一个实例就加载一次）3、静态方法需要调用才会执行.</p><p><strong>如果类还没有被加载：</strong></p><ul><li>1、先执行父类的静态代码块和静态变量初始化，并且静态代码块和静态变量的执行顺序只跟代码中出现的顺序有关。</li><li>2、执行子类的静态代码块和静态变量初始化。</li><li>3、执行父类的实例变量初始化</li><li>4、执行父类的构造函数（有构造代码块则先执行构造代码块）</li><li>5、执行子类的实例变量初始化</li><li>6、执行子类的构造函数</li></ul><p><strong>如果类已经被加载：</strong><br>则静态代码块和静态变量就不用重复执行，再创建类对象时，只执行与实例相关的变量初始化和构造方法。</p><p>补充构造代码块：给对象进行初始化。对象一建立就运行并且优先于构造函数。<br>构造代码块和构造函数的区别，构造代码块是给所有对象进行统一初始化， 构造函数给对应的对象初始化。</p><h4 id="switch关键字"><a href="#switch关键字" class="headerlink" title="switch关键字"></a>switch关键字</h4><h5 id="1、switch-语句中的表达式可以是什么类型数据？"><a href="#1、switch-语句中的表达式可以是什么类型数据？" class="headerlink" title="1、switch 语句中的表达式可以是什么类型数据？"></a>1、switch 语句中的表达式可以是什么类型数据？</h5><p>switch(A),括号中A的取值可以是<code>byte</code>、<code>short</code>、<code>int</code>、<code>char</code>、<code>String</code>，还有枚举类型。</p><h5 id="2、switch-是否能作用在byte-上，是否能作用在long-上，是否能作用在String上？"><a href="#2、switch-是否能作用在byte-上，是否能作用在long-上，是否能作用在String上？" class="headerlink" title="2、switch 是否能作用在byte 上，是否能作用在long 上，是否能作用在String上？"></a>2、switch 是否能作用在byte 上，是否能作用在long 上，是否能作用在String上？</h5><p> Java 7之前，switch后面的括号里面只能放int类型的值，注意是只能放int类型，但是放byte，short，char类型的也可以，是因为byte，short，shar可以自动提升（自动类型转换）为int，不是说就可以放它们，说白了，你放的byte，short，shar类型，然后他们会自动转换为int类型（宽化，自动转换并且安全），其实最后放的还是int类型。String可以了，但是long仍然不行。</p><blockquote><p><strong>1.</strong>小的往大的转换(宽化)，自动转换，有些时候就会自动提升为大的类型，比如switch中<br><strong>2.</strong>大的往小的转换(窄化)必须强制类型转换所以long不行，要想行就得强转如（int）long。同理，float、double也是不行的，要想行就强转。</p></blockquote><h5 id="3、while-循环和-do-循环有什么不同？"><a href="#3、while-循环和-do-循环有什么不同？" class="headerlink" title="3、while 循环和 do 循环有什么不同？"></a>3、while 循环和 do 循环有什么不同？</h5><p><code>while</code>语法格式：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">while</span>(布尔表达式)&#123;</span><br><span class="line"><span class="comment">//语句</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>先判断布尔表达式，如果为true就会执行循环体中的语句，然后再判断布尔表达式，如果为true就执行循环体中的语句，一直到布尔表达式为false，然后循环结束。通常用算术运算符（++ – 累减）<br><code>do/while</code>语法格式：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">do</span>&#123;</span><br><span class="line"><span class="comment">//语句</span></span><br><span class="line">&#125;<span class="keyword">while</span>(布尔表达式);</span><br></pre></td></tr></table></figure></p><p>先执行一次循环体，然后在判断布尔表达式是不是true，如果是就继续执行循环体，在判断布尔表达式，直到为false就结束循环。<br>两者的区别：<code>while</code>是先判断在执行如果判断不成立，就不会执行；<code>do/while</code>是先执行在判断，不管判断是否成立都会执行一次</p><h4 id="操作符"><a href="#操作符" class="headerlink" title="操作符"></a>操作符</h4><h5 id="1、-amp-操作符和-amp-amp-操作符有什么区别"><a href="#1、-amp-操作符和-amp-amp-操作符有什么区别" class="headerlink" title="1、&amp;操作符和&amp;&amp;操作符有什么区别"></a>1、&amp;操作符和&amp;&amp;操作符有什么区别</h5><p><code>&amp;&amp;</code>运算符是短路与运算。逻辑与跟短路与的差别是非常巨大的，虽然二者都要求运算符左右两端的布尔值都是true整个表达式的值才是true。<code>&amp;&amp;</code>之所以称为短路运算是因为，如果<code>&amp;&amp;</code>左边的表达式的值是false，右边的表达式会被直接短路掉，不会进行运算。很多时候我们可能都需要用<code>&amp;&amp;</code>而不是<code>&amp;</code>。</p><h5 id="2、a-a-b-与-a-b-的区别？"><a href="#2、a-a-b-与-a-b-的区别？" class="headerlink" title="2、a = a + b 与 a += b 的区别？"></a>2、a = a + b 与 a += b 的区别？</h5><p>★ =：<strong>赋值运算符</strong>，在编译器将右边的表达式结果计算出来后，和左边的变量类型比较精度，如果左边的变量精度低于右边的结果的精度，编译器会显式的报错，告诉程序员去强制转型。（若a精度类型弱于b，a = a + b出错，编译检查报错）最后将表达式的结果复制到变量所在的内存区。<br>★ +=：暂且称之为<strong>运算符</strong>，编译器自动隐式直接将+=运算符后面的操作数强制装换为前面变量的类型，然后在变量所在的内存区上直接根据右边的操作数修改左边变量内存存储的二进制数值最后达到和赋值运算符相同的目的。与前者相比，由于后者是位操作，效率也较前者高。</p><h5 id="3、30-1-0-3-将会返回什么？true-还是-false？"><a href="#3、30-1-0-3-将会返回什么？true-还是-false？" class="headerlink" title="3、30.1 == 0.3 将会返回什么？true 还是 false？"></a>3、30.1 == 0.3 将会返回什么？true 还是 false？</h5><p>False，类型不一致。</p><h5 id="4、float-f-3-4-是否正确？"><a href="#4、float-f-3-4-是否正确？" class="headerlink" title="4、float f=3.4; 是否正确？"></a>4、float f=3.4; 是否正确？</h5><p>不正确。<code>3.4</code>是双精度数，将双精度型（double）赋值给浮点型（float）属于下转型（down-casting，也称为窄化）会造成精度损失，因此需要强制类型转换<code>float f =(float)3.4</code>; 或者写成<code>float f =3.4F;</code>。</p><h5 id="5、short-s1-1-s1-s1-1-有错吗-short-s1-1-s1-1-有错吗？"><a href="#5、short-s1-1-s1-s1-1-有错吗-short-s1-1-s1-1-有错吗？" class="headerlink" title="5、short s1 = 1; s1 = s1 + 1;有错吗?short s1 = 1; s1 += 1;有错吗？"></a>5、short s1 = 1; s1 = s1 + 1;有错吗?short s1 = 1; s1 += 1;有错吗？</h5><p>对于<code>short s1 = 1; s1 = s1 + 1;</code>由于1是<code>int</code>类型，因此<code>s1+1</code>运算结果也是<code>int</code> 型，需要强制转换类型才能赋值给<code>short</code>型。而<code>short s1 = 1; s1 += 1;</code>可以正确编译，因为<code>s1+= 1;</code>相当于<code>s1 = (short)(s1 + 1);</code>其中有隐含的强制类型转换。</p>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;final-finalize-finally关键字&quot;&gt;&lt;a href=&quot;#final-finalize-finally关键字&quot; class=&quot;headerlink&quot; title=&quot;final,finalize,finally关键字&quot;&gt;&lt;/a&gt;final,finalize,finally关键字&lt;/h4&gt;&lt;h5 id=&quot;1-finalize和final关键字&quot;&gt;&lt;a href=&quot;#1-finalize和final关键字&quot; class=&quot;headerlink&quot; title=&quot;1.finalize和final关键字&quot;&gt;&lt;/a&gt;1.finalize和final关键字&lt;/h5&gt;&lt;p&gt;&lt;strong&gt;什么是finalize()方法？finalize()方法什么时候被调用？&lt;/strong&gt;&lt;br&gt;答： Java允许在类中定义一个名为&lt;code&gt;finalize()&lt;/code&gt;的方法，一旦垃圾回收器准备好释放对象占用的存储空间，将首先调用&lt;code&gt;finalize()&lt;/code&gt;方法，并且在下一次垃圾回收动作发生时，才会真正回收对象占用的内存。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;析构函数(finalization)的目的是什么&lt;/strong&gt;&lt;br&gt;析构函数目的是撤销对象前、完成一些清理工作，比如释放资源。释放了之后这些资源可以被回收，重新利用。&lt;br&gt;
    
    </summary>
    
      <category term="Java Basic" scheme="https://tisonkong.github.io/github.io/categories/Java-Basic/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="关键字，操作符" scheme="https://tisonkong.github.io/github.io/tags/%E5%85%B3%E9%94%AE%E5%AD%97%EF%BC%8C%E6%93%8D%E4%BD%9C%E7%AC%A6/"/>
    
  </entry>
  
  <entry>
    <title>Java面向对象的一些总结</title>
    <link href="https://tisonkong.github.io/github.io/2018/02/02/%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E7%BC%96%E7%A8%8B%EF%BC%88OOP%EF%BC%89%E7%9F%A5%E8%AF%86%E7%82%B9%E6%80%BB%E7%BB%93/"/>
    <id>https://tisonkong.github.io/github.io/2018/02/02/面向对象编程（OOP）知识点总结/</id>
    <published>2018-02-02T13:50:18.966Z</published>
    <updated>2018-02-28T01:40:21.414Z</updated>
    
    <content type="html"><![CDATA[<h4 id="一-面向对象的基本概念"><a href="#一-面向对象的基本概念" class="headerlink" title="一.面向对象的基本概念"></a>一.面向对象的基本概念</h4><h5 id="1-解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）"><a href="#1-解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）" class="headerlink" title="1.解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）"></a>1.解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）</h5><p><strong>抽象</strong>：抽象是将一类对象的共同特征总结出来构造类的过程，包括<strong>数据抽象</strong>和<strong>行为抽象</strong>两方面。抽象<strong>只关注对象有哪些属性和行为</strong>，并不关注这些行为的细节是什么。</p><p><strong>封装</strong>：通常认为<strong>封装是把数据和操作数据的方法绑定起来，对数据的访问只能通过已定义的接口</strong>。面向对象的本质就是将现实世界描绘成一系列完全自治、封闭的对象。我们在类中编写的方法就是对实现细节的一种封装；我们编写一个类就是对数据和数据操作的封装。可以说，封装就是隐藏一切可隐藏的东西，只向外界提供最简单的编程接口（可以想想普通洗衣机和全自动洗衣机的差别，明显全自动洗衣机封装更好因此操作起来更简单；我们现在使用的智能手机也是封装得足够好的，因为几个按键就搞定了所有的事情）。<br><a id="more"></a><br><strong>内聚</strong>：进行架构设计时的内聚高低是指，<strong>设计某个模块或者关注点时，模块或关注点内部的一系列相关功能的相关程度的高低</strong>。高内聚提供了更好的可维护性和可复用性。而低内聚的模块则表名模块直接的依赖程度高，那么一旦修改了该模块依赖的对象则无法使用该模块，必须也进行相应的修改才可以继续使用。</p><p><strong>耦合</strong>：简单地说，软件工程中对象之间的<strong>耦合度就是对象之间的依赖性</strong>。指导使用和维护对象的主要问题是对象之间的多重依赖性。对象之间的耦合越高，维护成本越高。因此对象的设计应使类和构件之间的耦合最小。<strong>耦合性是程序结构中各个模块之间相互关联的度量</strong>。它取决于各个模块之间的接口的复杂程度、调用模块的方式以及哪些信息通过接口。</p><p>耦合可以分为以下几种，它们之间的耦合度由高到低排列如下：</p><ul><li><strong>内容耦合</strong>。当一个模块直接修改或操作另一个模块的数据时，或一个模块不通过正常入口而转入另一个模块时，这样的耦合被称为内容耦合。内容耦合是最高程度的耦合，应该避免使用之。</li><li><strong>公共耦合</strong>。两个或两个以上的模块共同引用一个全局数据项，这种耦合被称为公共耦合。在具有大量公共耦合的结构中，确定究竟是哪个模块给全局变量赋了一个特定的值是十分困难的。</li><li><strong>外部耦合</strong> 。一组模块都访问同一全局简单变量而不是同一全局数据结构，而且不是通过参数表传递该全局变量的信息，则称之为外部耦合。<br>控制耦合 。一个模块通过接口向另一个模块传递一个控制信号，接受信号的模块根据信号值而进行适当的动作，这种耦合被称为控制耦合。<br>标记耦合 。若一个模块A通过接口向两个模块B和C传递一个公共参数，那么称模块B和C之间存在一个标记耦合。</li><li><strong>数据耦合</strong>。模块之间通过参数来传递数据，那么被称为数据耦合。数据耦合是最低的一种耦合形式，系统中一般都存在这种类型的耦合，因为为了完成一些有意义的功能，往往需要将某些模块的输出数据作为另一些模块的输入数据。</li><li><strong>非直接耦合</strong> 。两个模块之间没有直接关系，它们之间的联系完全是通过主模块的控制和调用来实现的。<br><img src="http://upload-images.jianshu.io/upload_images/8926909-d63352e73d06a55c.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="高内聚&amp;低耦合"><blockquote><p><a href="http://www.cnblogs.com/corvoh/p/5747856.html" target="_blank" rel="noopener">参考：《面向对象三大特性五大原则 + 低耦合高内聚》</a></p></blockquote></li></ul><h5 id="2-多态的用途和实现原理"><a href="#2-多态的用途和实现原理" class="headerlink" title="2.多态的用途和实现原理"></a>2.多态的用途和实现原理</h5><blockquote><p><strong>1、编译时多态（又称静态多态）<br>2、运行时多态（又称动态多态）</strong></p></blockquote><p><strong>重载（overload）</strong>就是<strong>编译时多态</strong>的一个例子，编译时多态在编译时就已经确定，运行时运行的时候调用的是确定的方法。<br>我们通常所说的<strong>多态指的都是运行时多态</strong>，也就是编译时不确定究竟调用哪个具体方法，一直延迟到运行时才能确定。这也是为什么有时候多态方法又被称为<strong>延迟方法</strong>的原因。</p><ul><li>多态通常有两种实现方法：<br>1、子类继承父类（extends）<br>2、子类实现接口（implements）</li><li><strong>多态最大的用途</strong><br>个人认为在于<strong>对设计和架构的复用</strong>，更进一步来说，《<a href="http://book.douban.com/subject/1052241/" target="_blank" rel="noopener">设计模式</a>》中提倡的针对接口编程而不是针对实现编程就是充分利用多态的典型例子。<strong>定义功能和组件时定义接口，实现可以留到之后的流程中。</strong>同时一个接口可以有多个实现，甚至于完全可以在一个设计中同时使用一个接口的多种实现。</li><li><strong>多态实现原理</strong><br>多态允许具体访问时实现方法的动态绑定。Java对于动态绑定的实现主要<strong>依赖于方法表</strong>，通过继承和接口的多态实现有所不同。<br><strong>继承</strong>：在执行某个方法时，在方法区中找到该类的方法表，再确认该方法在方法表中的偏移量，找到该方法后如果被重写则直接调用，否则认为没有重写父类该方法，这时会按照继承关系搜索父类的方法表中该偏移量对应的方法。<br><strong>接口</strong>：Java 允许一个类实现多个接口，从某种意义上来说相当于多继承，这样同一个接口的的方法在不同类方法表中的位置就可能不一样了。所以不能通过偏移量的方法，而是通过搜索完整的方法表。<br><em>tips：因为每次接口调用都要搜索方法表，所以从效率上来说，接口方法的调用总是慢于类方法的调用的。</em></li></ul><blockquote><p><a href="http://www.cnblogs.com/crane-practice/p/3671074.html" target="_blank" rel="noopener">《Java 多态的实现机制》</a><br><a href="http://blog.csdn.net/seu_calvin/article/details/52191321" target="_blank" rel="noopener">《Java技术——多态的实现原理》</a></p></blockquote><h5 id="3-对象封装的原则是什么"><a href="#3-对象封装的原则是什么" class="headerlink" title="3.对象封装的原则是什么?"></a>3.对象封装的原则是什么?</h5><p>在面向对象程式设计方法中，封装（英语：Encapsulation）是指一种将抽象性函式接口的实现细节部份包装、隐藏起来的方法。封装可以被认为是一个保护屏障，防止该类的代码和数据被外部类定义的代码随机访问。要访问该类的代码和数据，必须通过严格的接口控制。封装最主要的功能在于我们能修改自己的实现代码，而不用修改那些调用我们代码的程序片段。适当的封装可以让程式码更容易理解与维护，也加强了程式码的安全性。</p><ul><li>修改属性的可见性来限制对属性的访问（一般限制为private）；</li><li>对每个值属性提供对外的公共方法访问，也就是创建一对赋取值方法，用于对私有属性的访问；</li></ul><h5 id="4-获得一个类的类对象有哪些方式？"><a href="#4-获得一个类的类对象有哪些方式？" class="headerlink" title="4.获得一个类的类对象有哪些方式？"></a>4.获得一个类的类对象有哪些方式？</h5><ul><li>1.通过对象的getClass方法进行获取。这种方式需要具体的类和该类的对象，以及调用getClass方法。</li><li>2.任何数据类型(包括基本数据类型)都具备着一个静态的属性class，通过它可直接获取到该类型对应的Class对象。这种方式要使用具体的类，然后调用类中的静态属性class完成，无需调用方法，性能更好。</li><li>3.通过Class.forName()方法获取。这种方式仅需使用类名，就可以获取该类的Class对象，更有利于扩展。<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 演示获取Class c对象的三种方法</span></span><br><span class="line"><span class="comment"> *<span class="doctag">@fileName</span> ReflectGetClass.java</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ReflectGetClass</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 法1：通过对象---对象.getClass()来获取c(一个Class对象)</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">get1</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Person p=<span class="keyword">new</span> Person(<span class="string">"Jack"</span>, <span class="number">23</span>);</span><br><span class="line">        Class c=p.getClass();<span class="comment">//来自Object方法</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 法2：通过类(类型)---任何数据类型包括(基本数据类型)</span></span><br><span class="line"><span class="comment">     * 都有一个静态的属性class ，他就是c 一个Class对象</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">get2</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Class c=Person.class;</span><br><span class="line">        Class c2=<span class="keyword">int</span>.class;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 法3：通过字符串(类全名 )---能够实现解耦：Class.forName(str)</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">get3</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Class c=Class.forName(<span class="string">"cn.hncu.reflect.test.Person"</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (ClassNotFoundException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h5 id="5-重载（Overload）和重写（Override）的区别。重载的方法能否根据返回类型进行区分？"><a href="#5-重载（Overload）和重写（Override）的区别。重载的方法能否根据返回类型进行区分？" class="headerlink" title="5.重载（Overload）和重写（Override）的区别。重载的方法能否根据返回类型进行区分？"></a>5.重载（Overload）和重写（Override）的区别。重载的方法能否根据返回类型进行区分？</h5><p><strong>一、重写（override）</strong><br>override是重写（覆盖）了一个方法，以实现不同的功能。一般是用于子类在继承父类时，重写（重新实现）父类中的方法。重写（覆盖）的规则：</p><ul><li>1、重写方法的参数列表必须完全与被重写的方法的相同,否则不能称其为重写而是重载.</li><li>2、重写方法的访问修饰符一定要大于被重写方法的访问修饰符（public&gt;protected&gt;default&gt;private）。</li><li>3、重写的方法的返回值必须和被重写的方法的返回一致；</li><li>4、重写的方法所抛出的异常必须和被重写方法的所抛出的异常一致，或者是其子类；</li><li>5、被重写的方法不能为private，否则在其子类中只是新定义了一个方法，并没s有对其进行重写。</li><li>6、静态方法不能被重写为非静态的方法（会编译出错）。<br><strong>二、overload是重载</strong><br>一般是用于在一个类内实现若干重载的方法，这些方法的名称相同而参数形式不同。<br>重载的规则：</li><li>1、在使用重载时只能通过相同的方法名、不同的参数形式实现。不同的参数类型可以是不同的参数类型，不同的参数个数，不同的参数顺序（参数类型必须不一样）；</li><li>2、不能通过访问权限、返回类型、抛出的异常进行重载；</li><li>3、方法的异常类型和数目不会对重载造成影响；<br>多态的概念比较复杂，有多种意义的多态，一个有趣但不严谨的说法是：继承是子类使用父类的方法，而多态则是父类使用子类的方法。一般，我们使用多态是为了避免在父类里大量重载引起代码臃肿且难于维护。<br>《java编程思想》中很好的回答了<strong>不能以返回值来区分重载方法：</strong><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span></span>&#123; &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">()</span></span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br></pre></td></tr></table></figure></li></ul><p>假如有<code>int x=f()</code>,这里是可以区分重载方法，但有时候并不需要返回值，只是调用方法，那么像这样的<code>f()</code>就让人无法理解了。</p><h5 id="6-说出几条-Java-中方法重载的最佳实践？"><a href="#6-说出几条-Java-中方法重载的最佳实践？" class="headerlink" title="6.说出几条 Java 中方法重载的最佳实践？**"></a>6.说出几条 Java 中方法重载的最佳实践？**</h5><ul><li>a）不要重载这样的方法：一个方法接收 int 参数，而另个方法接收 Integer 参数。</li><li>b）不要重载参数数量一致，而只是参数顺序不同的方法。</li><li>c）如果重载的方法参数个数多于 5 个，采用<strong>可变参数</strong>。</li></ul><h4 id="二、抽象类和接口"><a href="#二、抽象类和接口" class="headerlink" title="二、抽象类和接口"></a>二、抽象类和接口</h4><h5 id="1-抽象类和接口的区别"><a href="#1-抽象类和接口的区别" class="headerlink" title="1.抽象类和接口的区别"></a>1.抽象类和接口的区别</h5><ul><li><strong>一、相似性</strong></li><li>接口和抽象类都不能被实例化，它们都位于继承树的顶端，用于被其他类实现和继承。</li><li>接口和抽象类都可以包含抽象方法，实现接口或继承抽象类的普通子类都必须实现这些抽象方法。<br><strong>二、接口和抽象类的区别</strong></li><li>接口里只能包含抽象方法，静态方法和默认方法，不能为普通方法提供方法实现，抽象类则完全可以包含普通方法。</li><li>接口里只能定义静态常量，不能定义普通成员变量，抽象类里则既可以定义普通成员变量，也可以定义静态常量。</li><li>接口不能包含构造器，抽象类可以包含构造器，抽象类里的构造器并不是用于创建对象，而是让其子类调用这些构造器来完成属于抽象类的初始化操作。</li><li>接口里不能包含初始化块，但抽象类里完全可以包含初始化块。</li><li>一个类最多只能有一个直接父类，包括抽象类，但一个类可以直接实现多个接口，通过实现多个接口可以弥补Java单继承不足。<blockquote><p>接口可以继承接口。<br>抽象类可以实现(implements)接口<br>抽象类可继承具体类。<br>抽象类中可以有静态的main方法。</p></blockquote></li></ul><p>备注：只要明白了接口和抽象类的本质和作用，这些问题都很好回答，你想想，如果你是java语言的设计者，你是否会提供这样的支持，如果不提供的话，有什么理由吗？如果你没有道理不提供，那答案就是肯定的了。只有记住抽象类与普通类的唯一区别就是不能创建实例对象和允许有abstract方法。</p><h5 id="2-java接口的基本概念，是否可继承，以及优点？"><a href="#2-java接口的基本概念，是否可继承，以及优点？" class="headerlink" title="2.java接口的基本概念，是否可继承，以及优点？"></a>2.java接口的基本概念，是否可继承，以及优点？</h5><p><strong>接口（Interface）</strong>，在JAVA编程语言中是一个抽象类型，是抽象方法的集合。接口通常以interface来声明。一个类通过继承接口的方式，从而来继承接口的抽象方法。如果一个类只由抽象方法和全局常量组成，那么这种情况下不会将其定义为一个抽象类。只会定义为一个接口，所以接口严格的来讲属于一个特殊的类，而这个类里面只有抽象方法和全局常量，就连构造方法也没有。</p><ul><li>一个接口可以继承多个接口.<br><code>interface C extends A, B {}</code>是可以的.</li><li>一个类可以实现多个接口:<br><code>class D implements A,B,C{}</code></li><li>但是一个类只能继承一个类,不能继承多个类<br><code>class B extends A{}</code></li><li>在继承类的同时,也可以继承接口:<br><code>class E extends D implements A,B,C{}</code><br>这也正是选择用接口而不是抽象类的原因</li></ul><h5 id="3、接口的优点或者说面向接口编程的思想是什么（这里要结合运行时多态更好理解）"><a href="#3、接口的优点或者说面向接口编程的思想是什么（这里要结合运行时多态更好理解）" class="headerlink" title="3、接口的优点或者说面向接口编程的思想是什么（这里要结合运行时多态更好理解）"></a>3、接口的优点或者说面向接口编程的思想是什么（这里要结合运行时多态更好理解）</h5><p>在系统分析和架构中，分清层次和依赖关系，每个层次不是直接向其上层提供服务（即不是直接实例化在上层中），而是<strong>通过定义一组接口，仅向上层暴露其接口功能，上层对于下层仅仅是接口依赖，而不依赖具体类</strong>。<br>好处：首先对系统灵活性大有好处。当下层需要改变时，只要接口及接口功能不变，则上层不用做任何修改。甚至可以在不改动上层代码时将下层整个替换掉。接口体现的是一种规范和实现分离的设计哲学，充分利用接口可以极好地降低程序各模块之间的耦合，从而提高系统的可扩展性和可维护性。基于这种原则，通常推荐“面向接口”编程，而不是面向实现类编程，希望通过面向接口编程来降低程序的耦合。<br>总的来说就是：<strong>降低程序耦合度，提高系统的可扩展性和维护性。</strong></p><h4 id="三、继承"><a href="#三、继承" class="headerlink" title="三、继承"></a>三、继承</h4><p><strong>1、继承（Inheritance）与聚合（Aggregation）的区别在哪里</strong><br><strong>2、继承和组合之间有什么不同</strong></p><ul><li>如果存在一种IS-A的关系（比如Bee“是一个”Insect），并且一个类需要向另一个类暴露所有的方法接口，那么更应该用继承的机制。</li><li>如果存在一种HAS-A的关系（比如Bee“有一个”attack功能），那么更应该运用组合。</li></ul><p><strong>3、为什么类只能单继承，接口可以多继承</strong><br>首先，类的多继承有缺点：<br>第一，如果一个类继承多个父类，如果父类中的方法名如果相同，那么就会产生歧义。<br>第二，如果父类中的方法同名，子类中没有覆盖，同样会产生上面的错误。<br>但是接口就设计成多继承，是因为接口可以避免上述问题：<br>首先，接口中的只有抽象方法和静态常量。对于一个类实现多个接口的情况和一个接口继承多个接口的情况，因为接口只有抽象方法，具体方法只能由实现接口的类实现（也是因为实现类一定会覆盖接口中的方法），在调用的时候始终只会调用实现类（也就是子类覆盖的方法）的方法（不存在歧义），因此不存在 多继承的第二个缺点；而又因为接口只有静态的常量，但是由于静态变量是在编译期决定调用关系的，即使存在一定的冲突也会在编译时提示出错；而引用静态变量一般直接使用类名或接口名，从而避免产生歧义，因此也不存在多继承的第一个缺点。<br><strong>4、存在两个类，C 继承 B，B 继承 A，能将 B 转换为 C 么？如 C = (C) B</strong><br>不能转换，测试代码报错：<br><code>Exception in thread &quot;main&quot; java.lang.ClassCastException: interfaceDemo.B cannot be cast to interfaceDemo.C</code></p><p><strong>5、如果类 a 继承类 b，实现接口c，而类 b 和接口 c 中定义了同名变量，请问会出现什么问题</strong></p><h4 id="四、泛型"><a href="#四、泛型" class="headerlink" title="四、泛型"></a>四、泛型</h4><h5 id="1、泛型的存在是用来解决什么问题？"><a href="#1、泛型的存在是用来解决什么问题？" class="headerlink" title="1、泛型的存在是用来解决什么问题？"></a>1、泛型的存在是用来解决什么问题？</h5><p>首先需要明确泛型的概念，<strong>泛型（Generics ）</strong>是把类型参数化，运用于类、接口、方法中，可以通过执行泛型类型调用 分配一个类型，将用分配的具体类型替换泛型类型。然后，所分配的类型将用于限制容器内使用的值，这样就无需进行类型转换，还可以在编译时提供更强的类型检查。<br>总结来说就是：<br>（1）消除显示的强制类型转换，提高代码复用<br>（2）提供更强的类型检查，避免运行时的<code>ClassCastException</code>。<br>这个问题产生的背景是针对容器中，基于继承的泛型实现会带来两个问题，请看代码：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ArrayList</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Object <span class="title">get</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123; ... &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(Object o)</span> </span>&#123; ... &#125;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">private</span> Object[] elementData;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p><p>基于继承的泛型实现会带来两个问题：第一个问题是有关<code>get()</code>方法的，我们每次调用<code>get()</code>方法都会返回一个<code>Object</code>对象，每一次都要强制类型转换为我们需要的类型，这样会显得很麻烦；第二个问题是有关add方法的，假如我们往聚合了<code>String</code>对象的<code>ArrayList</code>中加入一个<code>File</code>对象，编译器不会产生任何错误提示，而这不是我们想要的。所以，从Java 5开始，ArrayList在使用时可以加上一个类型参数（type parameter），这个类型参数用来指明ArrayList中的元素类型。类型参数的引入解决了以上提到的两个问题，如以下代码所示：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">ArrayList&lt;String&gt; s = <span class="keyword">new</span> ArrayList&lt;String&gt;();</span><br><span class="line">s.add(<span class="string">"abc"</span>);</span><br><span class="line">String s = s.get(<span class="number">0</span>); <span class="comment">//无需进行强制转换</span></span><br><span class="line">s.add(<span class="number">123</span>);  <span class="comment">//编译错误，只能向其中添加String对象</span></span><br></pre></td></tr></table></figure></p><h5 id="2、泛型的常用特点？"><a href="#2、泛型的常用特点？" class="headerlink" title="2、泛型的常用特点？"></a>2、泛型的常用特点？</h5><p>这里其实问的就是泛型在使用过程中遵循的相关规范。类型参数（又称类型变量）用作占位符，指示在运行时为类分配类型。根据需要，可能有一个或多个类型参数，并且可以用于整个类。根据惯例，类型参数是单个大写字母，该字母用于指示所定义的参数类型。下面列出每个用例的标准类型参数：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">E：元素</span><br><span class="line">K：键</span><br><span class="line">N：数字</span><br><span class="line">T：类型</span><br><span class="line">V：值</span><br><span class="line">S、U、V 等：多参数情况中的第 <span class="number">2</span>、<span class="number">3</span>、<span class="number">4</span> 个类型</span><br><span class="line">? 表示不确定的java类型（无限制通配符类型）</span><br></pre></td></tr></table></figure></p><blockquote><p><a href="http://www.importnew.com/27037.html" target="_blank" rel="noopener">《Java 泛型一览笔录》</a><br><a href="http://www.importnew.com/19740.html" target="_blank" rel="noopener">《深入理解Java之泛型》</a></p><h4 id="五、匿名内部类"><a href="#五、匿名内部类" class="headerlink" title="五、匿名内部类"></a>五、匿名内部类</h4><p><strong>内部类（nested classes）</strong>，面向对象程序设计中，可以在一个类的内部定义另一个类。嵌套类分为两种，即静态嵌套类和非静态嵌套类。静态嵌套类使用很少，最重要的是非静态嵌套类，也即是被称作为内部类(inner)。内部类是JAVA语言的主要附加部分。内部类几乎可以处于一个类内部任何位置，可以与实例变量处于同一级，或处于方法之内，甚至是一个<a href="http://baike.baidu.com/view/420676.htm" target="_blank" rel="noopener">表达式</a>的一部分。</p><h5 id="1、匿名内部类是否可以继承其它类？是否可以实现接口？"><a href="#1、匿名内部类是否可以继承其它类？是否可以实现接口？" class="headerlink" title="1、匿名内部类是否可以继承其它类？是否可以实现接口？"></a>1、匿名内部类是否可以继承其它类？是否可以实现接口？</h5><p>使用匿名内部类我们必须要继承一个父类或者实现一个接口，当然也仅能只继承一个父类或者实现一个接口。同时它也是没有class关键字，这是因为匿名内部类是直接使用new来生成一个对象的引用，当然这个引用是隐式的。不可以继承其它类和实现接口。</p></blockquote><h5 id="2、内部类分为几种？"><a href="#2、内部类分为几种？" class="headerlink" title="2、内部类分为几种？"></a>2、内部类分为几种？</h5><blockquote><ul><li>成员内部类，在一个类（外部类）中直接定义的内部类；</li><li>局部内部类，在一个方法（外部类的方法）中定义的内部类;</li><li>匿名内部类，</li></ul></blockquote><p><strong>1.成员内部类</strong><br>可以访问它的外部类的所有成员变量和方法，不管是静态的还是非静态的都可以。<br>在外部类里面创建成员内部类的实例：<code>this.new B()；</code><br>在外部类之外创建内部类的实例：<code>(new Test1()).new B().go();</code><br><strong>2.局部内部类</strong><br>定义在方法中，比方法的范围还小。是内部类中最少用到的一种类型。像局部变量一样，不能被<code>public</code>,<code>protected</code>, <code>private</code>和<code>static</code>修饰。只能访问方法中定义的final类型的局部变量。<br>方法内部类在方法中定义，所以只能在方法中使用，即只能在方法当中生成方法内部类的实例并且调用其方法。<br><strong>3.匿名内部类</strong><br>没有名字的局部内部类，不使用关键字class, extends, implements, 没有构造方法。什么情况下需要使用匿名内部类？如果满足下面的一些条件，使用匿名内部类是比较合适的：</p><ul><li>只用到类的一个实例。</li><li>类在定义后马上用到。</li><li>类非常小（SUN推荐是在4行代码以下）</li><li>给类命名并不会导致你的代码更容易被理解。</li></ul><p><strong>在使用匿名内部类时，要记住以下几个原则：</strong></p><ul><li>匿名内部类不能有构造方法。</li><li>匿名内部类不能定义任何静态成员、方法和类。</li><li>匿名内部类不能是public,protected,private,static。</li><li>只能创建匿名内部类的一个实例。</li><li>一个匿名内部类一定是在new的后面，用其隐含实现一个接口或实现一个类。</li><li>因匿名内部类为局部内部类，所以局部内部类的所有限制都对其生效。<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">//实例代码</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">innerclass</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        innerclass i = <span class="keyword">new</span> innerclass() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                System.out.println(<span class="string">"匿名内部类"</span>);</span><br><span class="line">                <span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        i.print();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><p>匿名内部类的高频使用场景是在多线程下(灵活使用箭头函数语法糖)：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Java 8之前：</span></span><br><span class="line"><span class="keyword">new</span> Thread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Before Java8, too much code for too little to do"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;).start();</span><br><span class="line"></span><br><span class="line"><span class="comment">//Java 8方式：</span></span><br><span class="line"><span class="keyword">new</span> Thread(() -&gt; System.out.println(<span class="string">"In Java8, Lambda expression!!"</span>) ).start();</span><br></pre></td></tr></table></figure></p><h5 id="3、内部类可以引用它的包含类（外部类）的成员吗？"><a href="#3、内部类可以引用它的包含类（外部类）的成员吗？" class="headerlink" title="3、内部类可以引用它的包含类（外部类）的成员吗？"></a>3、内部类可以引用它的包含类（外部类）的成员吗？</h5><p>内部类可以直接访问外部类的成员属性</p><h5 id="4、请说一下-Java-中为什么要引入内部类？还有匿名内部类？"><a href="#4、请说一下-Java-中为什么要引入内部类？还有匿名内部类？" class="headerlink" title="4、请说一下 Java 中为什么要引入内部类？还有匿名内部类？"></a>4、请说一下 Java 中为什么要引入内部类？还有匿名内部类？</h5><ul><li>内部类对象可以访问创建它的对象的实现，包括私有数据；</li><li>内部类不为同一包的其他类所见，具有很好的封装性；</li><li>使用内部类可以很方便的编写事件驱动程序；</li><li>匿名内部类可以方便的定义运行时回调；</li><li>内部类可以方便的定义</li></ul>]]></content>
    
    <summary type="html">
    
      &lt;h4 id=&quot;一-面向对象的基本概念&quot;&gt;&lt;a href=&quot;#一-面向对象的基本概念&quot; class=&quot;headerlink&quot; title=&quot;一.面向对象的基本概念&quot;&gt;&lt;/a&gt;一.面向对象的基本概念&lt;/h4&gt;&lt;h5 id=&quot;1-解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）&quot;&gt;&lt;a href=&quot;#1-解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）&quot; class=&quot;headerlink&quot; title=&quot;1.解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）&quot;&gt;&lt;/a&gt;1.解释下多态性（polymorphism），封装性（encapsulation），内聚（cohesion）以及耦合（coupling）&lt;/h5&gt;&lt;p&gt;&lt;strong&gt;抽象&lt;/strong&gt;：抽象是将一类对象的共同特征总结出来构造类的过程，包括&lt;strong&gt;数据抽象&lt;/strong&gt;和&lt;strong&gt;行为抽象&lt;/strong&gt;两方面。抽象&lt;strong&gt;只关注对象有哪些属性和行为&lt;/strong&gt;，并不关注这些行为的细节是什么。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;封装&lt;/strong&gt;：通常认为&lt;strong&gt;封装是把数据和操作数据的方法绑定起来，对数据的访问只能通过已定义的接口&lt;/strong&gt;。面向对象的本质就是将现实世界描绘成一系列完全自治、封闭的对象。我们在类中编写的方法就是对实现细节的一种封装；我们编写一个类就是对数据和数据操作的封装。可以说，封装就是隐藏一切可隐藏的东西，只向外界提供最简单的编程接口（可以想想普通洗衣机和全自动洗衣机的差别，明显全自动洗衣机封装更好因此操作起来更简单；我们现在使用的智能手机也是封装得足够好的，因为几个按键就搞定了所有的事情）。&lt;br&gt;
    
    </summary>
    
      <category term="Java Basic" scheme="https://tisonkong.github.io/github.io/categories/Java-Basic/"/>
    
    
      <category term="Interview" scheme="https://tisonkong.github.io/github.io/tags/Interview/"/>
    
      <category term="java" scheme="https://tisonkong.github.io/github.io/tags/java/"/>
    
      <category term="面向对象" scheme="https://tisonkong.github.io/github.io/tags/%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1/"/>
    
  </entry>
  
</feed>
